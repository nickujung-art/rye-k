---
phase: 02-portal-completion
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/components/portal/PublicPortal.jsx
autonomous: true
requirements:
  - POR-05
  - POR-06
  - POR-08

must_haves:
  truths:
    - "monthlyFee === 0인 학생이 pay 탭에 접근하면 '수납 정보 없음' empty state가 표시된다"
    - "sPay.length > 0이고 monthlyFee > 0인 정상 케이스에는 기존 수납 목록이 그대로 표시된다"
    - "sibling 전환 모달이 .mb / .modal / .modal-h / .modal-b / .modal-f 클래스를 사용한다"
    - "sibling modal 내 자녀 카드가 .tl-student-item 클래스를 사용한다"
    - "에러 메시지가 .form-err 클래스를 사용한다"
    - "window.confirm 및 window.alert가 코드에 없다"
  artifacts:
    - path: "src/components/portal/PublicPortal.jsx"
      provides: "pay 탭 monthlyFee=0 empty state + sibling modal CSS 클래스"
      contains: "수납 정보 없음"
  key_links:
    - from: "pay 탭 sPay 분기 (line ~1663)"
      to: "PortalEmptyState"
      via: "monthlyFee=0 조건 추가"
      pattern: "student\\.monthlyFee.*=== 0"
    - from: "showSiblingModal JSX"
      to: ".modal / .modal-b CSS 클래스"
      via: "className 교체"
      pattern: "className=\"mb\""
---

<objective>
PublicPortal.jsx의 pay 탭 empty state(POR-05), 학부모 뷰 일관성(POR-06), sibling 전환 모달 CSS 클래스 통일(POR-08)을 구현한다.

Purpose: 수납 데이터 없음 상태를 명확히 안내하고, 다자녀 전환 모달의 CSS 클래스를 기존 표준으로 통일하여 UI 일관성을 확보한다.
Output: PublicPortal.jsx 수정본 — pay 탭 3-way empty state + showSiblingModal 모달 CSS 클래스 표준화
</objective>

<execution_context>
@C:\Users\GIGABYTE\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GIGABYTE\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-CONTEXT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-RESEARCH.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-PATTERNS.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-02-SUMMARY.md

<interfaces>
<!-- PublicPortal.jsx 수정 지점 (RESEARCH.md + PATTERNS.md에서 추출) -->

POR-05 수정 지점 — pay 탭 empty state (line ~1663):
현재 코드:
```jsx
{sPay.length === 0
  ? <PortalEmptyState title="수납 기록이 없습니다" sub="수납 정보가 등록되면 이곳에서 확인하실 수 있어요." />
  : sPay.slice(0,24).map(...)
}
```

수정 후 3-way 분기:
```jsx
{(sPay.length === 0 && (student.monthlyFee || 0) === 0)
  ? <PortalEmptyState
      title="수납 정보 없음"
      sub={"이번 달 수강료가 등록되지 않았습니다.\n담당 선생님이나 관리자에게 문의해주세요."}
    />
  : sPay.length === 0
    ? <PortalEmptyState title="수납 기록이 없습니다" sub="수납 정보가 등록되면 이곳에서 확인하실 수 있어요." />
    : sPay.slice(0,24).map(...)
}
```

POR-08 수정 지점 — showSiblingModal 모달 (line ~1789-1823):
현재: 인라인 style 사용
수정 후: .mb / .modal / .modal-h / .modal-b / .modal-f 클래스 + .tl-student-item 카드

CSS 클래스 의미 (constants.jsx 기반):
- .mb — fixed backdrop overlay, z-index 500, align flex-end
- .modal — white panel, max-height 95vh, border-radius top, slideUp animation
- .modal-h — sticky header with title and close button
- .modal-b — scrollable body, padding 20px, padding-bottom 120px
- .tl-student-item — flex, padding 10px 14px, border-radius var(--radius-sm), cursor pointer
- .form-err — background var(--red-lt), color var(--red), border-left 3px solid var(--red)

POR-06 작업 범위:
- PublicParentView 기존 .parent-wrap / .parent-section 클래스 이미 존재함
- "통합 레이아웃 개선 중심" = 홈 탭 각 섹션의 marginBottom이 16px로 일관되게 적용되어 있는지 확인
- 추가 CSS 클래스 불필요. 기존 섹션 div들의 marginBottom 값 확인 후 필요한 경우만 수정.
- 신규 컴포넌트 생성 없음.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pay 탭 monthlyFee=0 empty state 추가 (POR-05)</name>
  <files>src/components/portal/PublicPortal.jsx</files>
  <read_first>
    - src/components/portal/PublicPortal.jsx (line 1655-1690 — pay 탭 sPay 분기 현황)
    - src/components/portal/PublicPortal.jsx (line 311-336 — PortalEmptyState 컴포넌트 정의, props 확인)
  </read_first>
  <action>
str_replace로 pay 탭의 empty state 분기를 3-way로 확장한다.

str_replace 대상 (sPay 분기 — 실제 라인을 read 후 정확히 매칭):
```jsx
{sPay.length === 0
  ? <PortalEmptyState title="수납 기록이 없습니다" sub="수납 정보가 등록되면 이곳에서 확인하실 수 있어요." />
  : sPay.slice(0,24).map(
```

교체 후:
```jsx
{(sPay.length === 0 && (student.monthlyFee || 0) === 0)
  ? <PortalEmptyState
      title="수납 정보 없음"
      sub={"이번 달 수강료가 등록되지 않았습니다.\n담당 선생님이나 관리자에게 문의해주세요."}
    />
  : sPay.length === 0
    ? <PortalEmptyState title="수납 기록이 없습니다" sub="수납 정보가 등록되면 이곳에서 확인하실 수 있어요." />
    : sPay.slice(0,24).map(
```

주의:
- PortalEmptyState의 `sub` prop이 문자열 또는 JSX를 받는지 line 311-336에서 먼저 확인.
- `\n` 지원 여부 확인 후, 지원 안 하면 두 줄을 `<>` 프래그먼트 또는 `<br/>` 분리로 전달.
- 기존 `sPay.slice(0,24).map(...)` 블록은 변경 없음 — 3번째 분기의 결과로만 연결.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "수납 정보 없음" src/components/portal/PublicPortal.jsx` → 1개 출력
    - `grep -n "monthlyFee" src/components/portal/PublicPortal.jsx` → pay 탭 조건문 줄 포함
    - `grep -n "수납 기록이 없습니다" src/components/portal/PublicPortal.jsx` → 기존 empty state 문구 여전히 존재 (삭제 안됨)
    - `npm run build` 0 exit code
  </acceptance_criteria>
  <done>
    pay 탭이 3가지 상태를 처리한다: (1) monthlyFee=0 + 기록 없음 → "수납 정보 없음", (2) 기록 없음 → "수납 기록이 없습니다", (3) 기록 있음 → 기존 목록. 빌드 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: sibling 전환 모달 CSS 클래스 표준화 (POR-08) + 홈 탭 섹션 gap 확인 (POR-06)</name>
  <files>src/components/portal/PublicPortal.jsx</files>
  <read_first>
    - src/components/portal/PublicPortal.jsx (line 1785-1830 — showSiblingModal 모달 전체 현황)
    - src/components/portal/PublicPortal.jsx (line 880-910 — childCandidates 모달 현황)
  </read_first>
  <action>
두 가지 작업을 수행한다.

**작업 1: showSiblingModal 모달 CSS 클래스 표준화 (POR-08)**

line 1789-1823의 showSiblingModal 모달을 읽어 인라인 style 기반 구조를 확인한 후, 아래 패턴으로 교체한다. 기능 변경 없음 — CSS 클래스 교체만.

교체 후 모달 구조:
```jsx
{showSiblingModal && (
  <div className="mb" onClick={() => setShowSiblingModal(false)}>
    <div className="modal" onClick={e => e.stopPropagation()}>
      <div className="modal-h">
        <span style={{fontFamily:"'Noto Serif KR',serif",fontWeight:600}}>자녀 변경</span>
        <button
          type="button"
          style={{background:"none",border:"none",cursor:"pointer",padding:8,color:"var(--ink-60)"}}
          onClick={() => setShowSiblingModal(false)}
        >✕</button>
      </div>
      <div className="modal-b">
        {switchErr && (
          <div className="form-err" style={{marginBottom:14,borderRadius:10,fontSize:13}}>⚠ {switchErr}</div>
        )}
        {siblings.map(sib => (
          <div
            key={sib.id}
            className="tl-student-item"
            onClick={() => handleSiblingSwitch(sib)}
          >
            <span style={{fontWeight:600,color:"var(--ink)"}}>{sib.name}</span>
            <span style={{fontSize:12,color:"var(--ink-60)",marginLeft:8}}>{sib.lessons?.[0]?.instrument || ""}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
)}
```

주의:
- 실제 파일의 siblings 변수명, handleSiblingSwitch 함수명, switchErr 상태명을 line 1785-1830에서 읽어 확인 후 정확히 매칭.
- 모달 기능(자녀 전환 로직)은 변경 없음. handleSiblingSwitch()는 기존 코드 그대로.
- window.confirm 없음. 탭 → 즉시 전환.

**작업 2: POR-06 홈 탭 섹션 gap 확인**

홈 탭(tab === "home") 내 각 주요 섹션 div의 `marginBottom` 또는 `marginBottom` style prop을 읽어 16px 미만인 섹션이 있으면 16px로 통일.

대상 섹션들 (이달 출석, 공지사항, 연습 가이드, 레슨 일정, 최근 레슨노트, 이번 달 수납, 기본 정보):
- 각 `<div style={{marginBottom:N}}>` 에서 N이 8 이하인 경우 16으로 수정.
- 이미 16 또는 그 이상인 섹션은 변경 없음.
- 신규 CSS 클래스 불필요 — 인라인 style 값 조정만.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "className=\"mb\"" src/components/portal/PublicPortal.jsx` → showSiblingModal 모달에서 1개 출력
    - `grep -n "className=\"modal\"" src/components/portal/PublicPortal.jsx` → showSiblingModal 모달에서 1개 출력
    - `grep -n "tl-student-item" src/components/portal/PublicPortal.jsx` → sibling 자녀 카드 className 1개 이상 출력
    - `grep -n "window.confirm\|window.alert" src/components/portal/PublicPortal.jsx` → 출력 없음
    - `npm run build` 0 exit code
  </acceptance_criteria>
  <done>
    showSiblingModal 모달이 .mb / .modal / .modal-h / .modal-b 클래스를 사용하고, 자녀 카드가 .tl-student-item을 사용한다. 기능 변경 없음. 홈 탭 섹션 gap이 16px로 일관된다. 빌드 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student.monthlyFee (Firestore) → React render | 수납 정보 없음 조건 계산에 사용 |
| siblings 배열 → handleSiblingSwitch | 자녀 전환 시 로그인 상태 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Spoofing | showSiblingModal handleSiblingSwitch | accept | 같은 보호자 연락처 기준으로 siblings 계산됨 — 타 가족 학생 접근 불가. Firestore 규칙(Phase 1)이 데이터 범위 통제. |
| T-02-03-02 | Information Disclosure | pay 탭 monthlyFee 표시 | accept | 본인 수납 정보만 표시. Firestore 규칙으로 타 학생 데이터 차단. |
| T-02-03-03 | Tampering | PortalEmptyState title/sub 문자열 | accept | 정적 한국어 문자열. XSS 위험 없음 — React JSX 자동 이스케이프. |
</threat_model>

<verification>
```bash
# 1. pay 탭 empty state
grep -n "수납 정보 없음" src/components/portal/PublicPortal.jsx
grep -n "monthlyFee.*=== 0\|=== 0.*monthlyFee" src/components/portal/PublicPortal.jsx

# 2. sibling modal CSS 클래스
grep -n "className=\"mb\"\|className=\"modal\"\|tl-student-item" src/components/portal/PublicPortal.jsx

# 3. window.confirm 없음
grep -n "window.confirm\|window.alert" src/components/portal/PublicPortal.jsx
# → 출력 없으면 PASS

# 4. 빌드
npm run build
```
</verification>

<success_criteria>
- pay 탭에서 monthlyFee=0 + 수납 기록 없는 학생 → "수납 정보 없음" empty state 표시
- pay 탭의 기존 "수납 기록이 없습니다" 케이스가 유지됨
- showSiblingModal 모달이 표준 CSS 클래스(.mb/.modal/.modal-h/.modal-b) 사용
- 자녀 카드가 .tl-student-item 클래스 사용
- 에러가 .form-err 클래스로 표시됨
- window.confirm/window.alert 없음
- npm run build 0 exit code
</success_criteria>

<output>
After completion, create `.planning/phases/02-portal-completion/02-03-SUMMARY.md`
</output>
