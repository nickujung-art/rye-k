---
phase: 11-discount-system
plan: 04
type: execute
wave: 3
depends_on:
  - 11-02
  - 11-03
files_modified:
  - src/components/payment/PaymentsView.jsx
  - src/components/dashboard/Dashboard.jsx
  - src/components/settlement/SettlementView.jsx
autonomous: true
requirements:
  - DIS-06
  - DIS-07
  - DIS-08

must_haves:
  truths:
    - "수납 리스트 행에서 할인 적용 학생은 원가 취소선 + 할인가 + 할인명 뱃지가 표시된다"
    - "수납 상세 모달에 할인 브레이크다운 섹션이 표시된다 (원가, 할인명, 할인가)"
    - "자동계산 수강료(p.amount 없음)에만 할인 표시 — 관리자 수동 입력 금액에는 표시 안 함"
    - "Dashboard 미납 금액 계산이 할인 적용 수강료 기준으로 동작한다"
    - "SettlementView 정산 계산이 할인 적용 수강료 기준으로 동작한다"
    - "autoFee(s)가 discountTypes를 반영한 total을 반환한다 (NaN 없음)"
  artifacts:
    - path: "src/components/payment/PaymentsView.jsx"
      provides: "autoFeeResult 함수 + 수납 리스트 할인 표시 + 모달 할인 브레이크다운"
      contains: "autoFeeResult"
    - path: "src/components/dashboard/Dashboard.jsx"
      provides: "calcTotalFee .total 접근 업데이트"
      contains: "discountTypes"
    - path: "src/components/settlement/SettlementView.jsx"
      provides: "calcTotalFee .total 접근 업데이트"
      contains: "discountTypes"
  key_links:
    - from: "PaymentsView autoFeeResult"
      to: "calcTotalFee(s, feePresets, discountTypes)"
      via: "discountTypes prop (Plan 01 배선)"
      pattern: "autoFeeResult"
    - from: "Dashboard calcTotalFee 호출"
      to: ".total"
      via: "discountTypes prop"
      pattern: "discountTypes\\.total"
---

<objective>
수납 화면(PaymentsView)에 할인 표시를 추가하고, Dashboard와 SettlementView의
calcTotalFee 호출부를 .total 접근으로 업데이트하여 NaN을 제거한다.

Purpose: 관리자가 수납 리스트와 모달에서 할인 정보를 바로 확인할 수 있어야 한다 (DIS-06, DIS-07).
         모든 calcTotalFee 호출부가 객체 반환에 맞게 업데이트되어야 한다 (DIS-08).
Output: 수납 할인 표시 UI + 전체 호출부 NaN 제거
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/GIGABYTE/Coding/rye-k/.planning/phases/11-discount-system/11-CONTEXT.md
@C:/Users/GIGABYTE/Coding/rye-k/.planning/phases/11-discount-system/11-01-SUMMARY.md
@C:/Users/GIGABYTE/Coding/rye-k/.planning/phases/11-discount-system/11-03-SUMMARY.md

CRITICAL:
- window.confirm / window.alert 절대 금지
- saveStudents 금지

Plan 01 결과: calcTotalFee(student, feePresets, discountTypes) → { total, original, discountAmount, discountName }
Plan 03 결과: PaymentsView props에 discountTypes, onSaveDiscountTypes 추가됨

PaymentsView 핵심 상태/함수 위치:
- 라인 92: `const autoFee = (s) => calcTotalFee(s, feePresets);` ← 이 Plan에서 수정
- 라인 96: `(p?.amount ?? autoFee(s))` — totalDue 계산
- 라인 120: `const autoBase = autoFee(s);` — openEdit 내부
- 라인 422: `const totalAmt = p?.amount ?? autoFee(s);`
- 라인 442: `{!isTeacher && <div className="pay-amount" ...>{fmtMoney(amt)}</div>}` ← 할인 표시 추가
- 라인 501: `autoFee={autoFee}` — UnmatchedPaymentsTab에 전달

수납 상세 모달 구조:
- 라인 636: `{editingId && (() => { const s = ...; ... })()`
- 라인 797-810: 합계 브레이크다운 (추가 청구 있을 때만)
- 라인 811+: 이달 입금 현황

할인 표시 기준 (D-08):
- `!p?.amount`: 관리자 수동 입력 없이 auto-calculated인 경우에만 할인 표시
- autoFeeResult(s).discountAmount > 0: 실제 할인이 적용된 경우

Dashboard:
- 라인 128 (함수 선언): { students, ..., feePresets = {}, instantCharges = [], onUpdateStudent = null }
  → discountTypes = [] 추가
- 라인 145: `return sum + (p?.amount ?? calcTotalFee(s, feePresets));` → .total 추가

SettlementView calcResult 함수 파라미터 (라인 28):
  `function calcResult({ teacher, month, allStudents, attendance, payments, institutions, instantCharges, feePresets })`
  → discountTypes 추가
- 라인 44: `const studentTotalFee = calcTotalFee(student, feePresets);` → .total
- 라인 79: `const gTotalFee = calcTotalFee(gs, feePresets);` → .total
SettlementView export default 함수의 props에도 discountTypes = [] 추가하고 calcResult 호출 시 전달
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaymentsView.jsx — autoFee 업데이트 + 수납 리스트 할인 표시 (DIS-06)</name>
  <files>src/components/payment/PaymentsView.jsx</files>

  <read_first>
    - src/components/payment/PaymentsView.jsx 라인 83~100 (autoFee 정의 + totalDue 계산)
    - src/components/payment/PaymentsView.jsx 라인 118~140 (openEdit 함수)
    - src/components/payment/PaymentsView.jsx 라인 420~488 (수납 리스트 행 렌더링)
    - src/components/payment/PaymentsView.jsx 라인 490~503 (UnmatchedPaymentsTab autoFee prop)
  </read_first>

  <action>
**[A] 라인 92: autoFee 정의 교체**

현재:
```
  const autoFee = (s) => calcTotalFee(s, feePresets);
```
변경 (두 줄로 교체):
```
  const autoFeeResult = (s) => calcTotalFee(s, feePresets, discountTypes);
  const autoFee = (s) => autoFeeResult(s).total;
```

이 변경으로:
- `autoFee(s)` — 여전히 숫자를 반환 (totalDue, openEdit, UnmatchedPaymentsTab 모두 정상 동작)
- `autoFeeResult(s)` — { total, original, discountAmount, discountName } 반환 (할인 표시용)

**[B] 라인 422~443 영역: 수납 리스트 행 할인 표시 추가 (DIS-06)**

현재 수납 행 렌더링 부분에서 `const totalAmt = p?.amount ?? autoFee(s);` 줄 다음에
`const feeResult = autoFeeResult(s);` 를 추가한다.

그리고 현재 금액 표시 부분:
```
              {!isTeacher && <div className="pay-amount" style={{color: isPaid ? "var(--green)" : isPartialPaid ? "var(--gold-dk)" : "var(--ink)"}}>{fmtMoney(amt)}</div>}
```
를 아래로 교체한다:
```
              {!isTeacher && (
                <div style={{ textAlign: "right" }}>
                  {!p?.amount && feeResult.discountAmount > 0 ? (
                    <>
                      <div style={{ fontSize: 11, color: "var(--ink-30)", textDecoration: "line-through", lineHeight: 1.3 }}>
                        {fmtMoney(feeResult.original)}
                      </div>
                      <div className="pay-amount" style={{ color: isPaid ? "var(--green)" : isPartialPaid ? "var(--gold-dk)" : "var(--ink)" }}>
                        {fmtMoney(amt)}
                      </div>
                      <div style={{ fontSize: 9.5, background: "var(--blue-lt,#EFF6FF)", color: "var(--blue,#3B82F6)", borderRadius: 4, padding: "1px 5px", fontWeight: 600, marginTop: 1, display: "inline-block", whiteSpace: "nowrap" }}>
                        {feeResult.discountName}
                      </div>
                    </>
                  ) : (
                    <div className="pay-amount" style={{ color: isPaid ? "var(--green)" : isPartialPaid ? "var(--gold-dk)" : "var(--ink)" }}>
                      {fmtMoney(amt)}
                    </div>
                  )}
                </div>
              )}
```

주의: `const feeResult = autoFeeResult(s);` 삽입 위치가 `const totalAmt = ...` 바로 다음,
`const isPaid = ...` 이전이어야 한다 (올바른 스코프).
  </action>

  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "autoFeeResult" src/components/payment/PaymentsView.jsx 결과 >= 3 (정의 + feeResult 할당 + 조건 사용)
    - grep -c "feeResult.discountAmount > 0" src/components/payment/PaymentsView.jsx 결과 >= 1
    - grep -c "textDecoration.*line-through" src/components/payment/PaymentsView.jsx 결과 >= 1 (취소선)
    - grep -c "feeResult.discountName" src/components/payment/PaymentsView.jsx 결과 >= 1 (할인명 뱃지)
    - grep -n "const autoFee" src/components/payment/PaymentsView.jsx 에서 ".total" 이 포함됨
    - npm run build 통과 (NaN 제거 — autoFee 이제 숫자 반환)
  </acceptance_criteria>

  <done>
    autoFee(s)가 .total을 반환하여 수납 금액 계산에서 NaN이 제거됨.
    수납 리스트 행에서 할인 적용 학생은 원가 취소선 + 할인가 + 할인명 뱃지가 표시됨.
    npm run build 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: PaymentsView 모달 할인 브레이크다운 + Dashboard + SettlementView 업데이트 (DIS-07, DIS-08)</name>
  <files>src/components/payment/PaymentsView.jsx, src/components/dashboard/Dashboard.jsx, src/components/settlement/SettlementView.jsx</files>

  <read_first>
    - src/components/payment/PaymentsView.jsx 라인 636~650 (editingId 모달 열기 + s 정의)
    - src/components/payment/PaymentsView.jsx 라인 795~815 (합계 브레이크다운 섹션 끝 + 이달 입금 현황 시작)
    - src/components/dashboard/Dashboard.jsx 라인 128 (함수 선언 props)
    - src/components/dashboard/Dashboard.jsx 라인 142~148 (unpaidAmount reduce — calcTotalFee 사용)
    - src/components/settlement/SettlementView.jsx 라인 28~50 (calcResult 함수 파라미터 + studentTotalFee)
    - src/components/settlement/SettlementView.jsx 라인 72~90 (gTotalFee 사용부)
    - src/components/settlement/SettlementView.jsx 라인 (export default SettlementView 함수 선언 — props)
    - src/components/settlement/SettlementView.jsx 라인 (calcResult 호출부 — discountTypes 전달 필요)
  </read_first>

  <action>
**[A] PaymentsView.jsx — 수납 상세 모달 할인 브레이크다운 섹션 추가 (DIS-07)**

모달 JSX 내 "합계 브레이크다운" 섹션 (`extraSum > 0 && (...)` 블록, 약 라인 797~810) 다음,
"이달 입금 현황" 섹션 시작 전에 아래 블록을 삽입한다:

```jsx
                {/* 할인 브레이크다운 — 자동계산 수강료 + 할인 적용 시만 표시 (DIS-07) */}
                {canManageAll(currentUser.role) && !isTeacher && !s?.isInstitution && (() => {
                  if (editForm.amount && getPayment(s?.id)?.amount) return null; // 수동 입력 금액인 경우 표시 안 함
                  const feeRes = autoFeeResult(s);
                  if (!feeRes || feeRes.discountAmount <= 0) return null;
                  return (
                    <div style={{ background: "var(--blue-lt,#EFF6FF)", border: "1px solid rgba(59,130,246,.2)", borderRadius: 10, padding: "10px 14px", marginBottom: 8, fontSize: 12.5 }}>
                      <div style={{ fontWeight: 700, fontSize: 11, color: "var(--blue,#3B82F6)", marginBottom: 6, letterSpacing: .3 }}>할인 적용</div>
                      <div style={{ display: "flex", justifyContent: "space-between", color: "var(--ink-60)", marginBottom: 3 }}>
                        <span>원가</span><span>{fmtMoney(feeRes.original)}</span>
                      </div>
                      <div style={{ display: "flex", justifyContent: "space-between", color: "var(--blue,#3B82F6)", marginBottom: 3 }}>
                        <span>{feeRes.discountName}</span><span>-{fmtMoney(feeRes.discountAmount)}</span>
                      </div>
                      <div style={{ borderTop: "1px dashed rgba(59,130,246,.25)", paddingTop: 6, display: "flex", justifyContent: "space-between", fontWeight: 700, color: "var(--ink)", fontSize: 13 }}>
                        <span>할인 적용가</span><span>{fmtMoney(feeRes.total)}</span>
                      </div>
                    </div>
                  );
                })()}
```

주의: `s`는 이미 모달 IIFE 스코프에서 `const s = visibleStudents.find(...) || students.find(...);`로 정의되어 있다.
`autoFeeResult`는 Task 1에서 정의했으므로 사용 가능하다.

**[B] Dashboard.jsx — discountTypes prop 추가 + calcTotalFee .total 접근**

1. 함수 선언부에서 props에 `discountTypes = []` 추가:
   현재: `export default function Dashboard({ students, teachers, currentUser, notices, categories, attendance, payments, pending, institutions, nav, onUnpaidCardClick, feePresets = {}, instantCharges = [], onUpdateStudent = null })`
   변경: 파라미터 목록 끝 `onUpdateStudent = null` 다음에 `, discountTypes = []` 추가

2. 라인 145 변경:
   현재: `return sum + (p?.amount ?? calcTotalFee(s, feePresets));`
   변경: `return sum + (p?.amount ?? calcTotalFee(s, feePresets, discountTypes).total);`

**[C] SettlementView.jsx — discountTypes 전달 + .total 접근**

1. `calcResult` 함수 파라미터(라인 28)에 `discountTypes = []` 추가:
   현재: `function calcResult({ teacher, month, allStudents, attendance, payments, institutions, instantCharges, feePresets })`
   변경: `feePresets` 다음에 `, discountTypes = []` 추가

2. 라인 44 변경:
   현재: `const studentTotalFee = calcTotalFee(student, feePresets);`
   변경: `const studentTotalFee = calcTotalFee(student, feePresets, discountTypes).total;`

3. 라인 79 변경:
   현재: `const gTotalFee = calcTotalFee(gs, feePresets);`
   변경: `const gTotalFee = calcTotalFee(gs, feePresets, discountTypes).total;`

4. `export default function SettlementView` 의 props에 `discountTypes = []` 추가:
   기존 props 목록(teachers, students, attendance, payments, institutions, instantCharges, feePresets, shopItems, currentUser)에 `, discountTypes = []` 추가

5. SettlementView 내에서 `calcResult({ ... })` 를 호출하는 곳을 찾아 `discountTypes` 를 전달:
   예: `calcResult({ teacher, month, allStudents: students, attendance, payments, institutions, instantCharges, feePresets })`
   → `calcResult({ teacher, month, allStudents: students, attendance, payments, institutions, instantCharges, feePresets, discountTypes })`
   (이 함수는 useMemo 또는 직접 호출로 쓰일 수 있음 — 파일 읽기 후 정확한 위치 확인)
  </action>

  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "할인 브레이크다운\|할인 적용가" src/components/payment/PaymentsView.jsx 결과 >= 1 (모달 섹션 존재)
    - grep -c "feeRes.discountAmount <= 0" src/components/payment/PaymentsView.jsx 결과 >= 1
    - grep -c "discountTypes" src/components/dashboard/Dashboard.jsx 결과 >= 2 (props + 호출)
    - grep -c "\.total" src/components/dashboard/Dashboard.jsx 결과 >= 1
    - grep -c "discountTypes" src/components/settlement/SettlementView.jsx 결과 >= 3 (calcResult 파라미터 + 함수 props + 호출부)
    - grep -c "\.total" src/components/settlement/SettlementView.jsx 결과 >= 2 (라인 44, 79)
    - grep -c "calcTotalFee(s, feePresets)" src/components/dashboard/Dashboard.jsx 결과 == 0 (구 패턴 완전 제거)
    - npm run build 통과
  </acceptance_criteria>

  <done>
    수납 상세 모달에 할인 브레이크다운 섹션(원가, 할인명, 할인가)이 추가됨.
    Dashboard와 SettlementView에서 calcTotalFee 호출 시 discountTypes를 전달하고 .total로 접근함.
    모든 calcTotalFee 호출부 업데이트 완료 (DIS-08).
    npm run build 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PaymentsView autoFeeResult | discountTypes는 App.jsx에서 Firestore 리스너로 수신 — 신뢰됨 |
| 모달 할인 표시 | autoFeeResult는 UI 표시용, 실제 결제 금액은 editForm.amount에서 관리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | 할인 적용 수납 금액 | mitigate | autoFeeResult는 표시용 — 실제 저장되는 payment.amount는 관리자가 직접 입력하거나 autoFee(s)로만 세팅됨. 할인이 적용된 autoFee가 payment.amount로 저장되므로 정확성 보장 |
| T-11-08 | Repudiation | 모달 할인 표시 | accept | 할인 표시는 현재 student.discount 기반 계산 — 이력 관리는 Phase 추후 |
</threat_model>

<verification>
```bash
cd C:/Users/GIGABYTE/Coding/rye-k

# PaymentsView 모달 할인 섹션 확인
grep -n "할인 브레이크다운\|할인 적용가\|feeRes.discountAmount" src/components/payment/PaymentsView.jsx

# Dashboard 업데이트 확인
grep -n "discountTypes\|\.total" src/components/dashboard/Dashboard.jsx

# SettlementView 업데이트 확인
grep -n "discountTypes\|\.total" src/components/settlement/SettlementView.jsx

# 구 패턴 잔존 여부 확인 (0이어야 함)
grep -c "calcTotalFee(s, feePresets)" src/components/dashboard/Dashboard.jsx
grep -c "calcTotalFee(student, feePresets)" src/components/settlement/SettlementView.jsx

# 최종 빌드
npm run build 2>&1 | tail -3
```
</verification>

<success_criteria>
- 수납 리스트 행: 할인 적용 학생은 원가 취소선 + 할인가 + 할인명 뱃지 표시
- 수납 상세 모달: 할인 브레이크다운 섹션 (원가/할인명/할인가) 표시
- Dashboard: calcTotalFee 호출 시 discountTypes 전달 + .total 접근
- SettlementView: calcTotalFee 두 호출부 모두 .total 접근
- npm run build 통과 (NaN 없음)
- Phase 11 전체 DIS-01 ~ DIS-08 요구사항 충족
</success_criteria>

<output>
완료 후 `.planning/phases/11-discount-system/11-04-SUMMARY.md` 생성:
- autoFeeResult 패턴 도입 내역
- 할인 표시 UI 삽입 위치
- Dashboard/SettlementView 업데이트 라인 번호
- Phase 11 전체 완료 상태 확인
</output>
