---
phase: 01-security-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - firestore.rules
autonomous: false
requirements:
  - SEC-06

must_haves:
  truths:
    - "익명 사용자가 /register 또는 /myryk 에서 rye-students, rye-teachers, rye-payments에 직접 읽기를 시도하면 PERMISSION_DENIED가 반환된다"
    - "email 로그인 강사(role=teacher)가 rye-students, rye-attendance를 읽을 수 있다"
    - "email 로그인 admin/manager가 rye-payments를 읽을 수 있다"
    - "익명 사용자가 rye-pending에 쓸 수 있다 (/register 폼)"
    - "/myryk 포털 학생 로그인이 더 이상 작동하지 않음을 Nick이 인지하고 Phase 2에서 수정될 예정임을 문서화했다"
  artifacts:
    - path: "firestore.rules"
      provides: "역할별 read/write 제어 규칙"
      contains: "hasRole"
  key_links:
    - from: "firestore.rules isStaff()"
      to: "request.auth.token.role"
      via: "Custom Claims (Plan 03에서 설정됨)"
      pattern: "request\\.auth\\.token\\.role"
---

<objective>
Firestore 보안 규칙을 역할 기반으로 재작성하여 익명 전체 읽기를 차단한다 (SEC-06).

Purpose: 현재 규칙 `allow read: if isAuthed()` 는 익명 Firebase 사용자(공개 /register, /myryk 방문자 포함)가 77명 학생 PII, 강사 연락처, 수납 기록 전체를 읽을 수 있는 PIPA 위반 상태이다. Custom Claims(Plan 03)가 배포·검증된 이후에만 이 plan을 실행한다.

Output: 재작성된 firestore.rules

CRITICAL ORDERING WARNING:
- Plan 03(set-role Worker) 배포 + 체크포인트 검증 완료 전에 이 plan을 시작하지 말 것
- 규칙 배포 후 /myryk 포털 학생 로그인이 중단된다 — Phase 2(POR-01)에서 Worker 기반 포털 조회로 수정 예정

PORTAL BREAKAGE NOTE:
이 규칙 배포 후 학생 포털(/myryk)의 studentCode 로그인이 실패한다. 이는 예상된 동작이다:
- Phase 1 목표: 운영 PII 데이터 보호 (우선순위)
- Phase 2 목표: 포털 Worker 기반 student lookup 구현 (POR-01)
Nick에게 배포 전 반드시 이 사항을 알려야 한다.
</objective>

<execution_context>
@C:\Users\GIGABYTE\Coding\rye-k\CLAUDE.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\01-security-foundation\01-03-SUMMARY.md

<interfaces>
<!-- firestore.rules 현재 구조 (전체) -->
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAuthed() { return request.auth != null; }
    function isEmailUser() {
      return request.auth != null &&
        request.auth.token.firebase.sign_in_provider == 'password';
    }
    match /appData/{document} {
      allow read:  if isAuthed();   // ← 익명 전체 읽기 허용 — 이게 문제
      allow write: if isEmailUser();
    }
    match /appData/rye-pending { allow read, write: if isAuthed(); }
    match /appData/rye-attendance { allow read, write: if isAuthed(); }
    match /appData/rye-student-notices { allow read, write: if isAuthed(); }
    match /appData/rye-trash { allow read: if isAuthed(); allow write: if isEmailUser(); }
    match /appData/rye-activity { allow read: if isAuthed(); allow write: if isEmailUser(); }
    match /{document=**} { allow read, write: if false; }
  }
}
```

<!-- 역할 체계 (constants.jsx + CLAUDE.md 기반) -->
<!-- admin: 전체 read/write -->
<!-- manager: 전체 read/write (수강료 포함) -->
<!-- teacher: 담당 회원만 (수납 숨김) -->
<!-- anonymous: rye-pending write만 (등록폼) + 일부 포털 컬렉션 read -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Firestore rules 배포 전 Nick에게 포털 중단 사항을 반드시 알린다. 이 체크포인트는 정보 전달 및 배포 승인용이다.</what-built>
  <how-to-verify>
**배포 전 Nick에게 아래 내용을 확인받는다:**

이 plan을 배포하면:
1. **보호됨**: 익명 방문자가 학생 77명 PII, 강사 연락처, 수납 기록을 더 이상 읽을 수 없다 (PIPA 준수)
2. **중단됨**: `/myryk` 학생 포털의 학생 코드 로그인이 작동하지 않는다
   - 이유: 포털이 익명 auth로 rye-students를 직접 읽는데, 새 규칙이 이를 차단
   - 수정 시기: Phase 2 (POR-01) — Cloudflare Worker 기반 포털 조회로 대체 예정
3. **정상 유지**: /register 신청 폼, 강사 앱, 관리자 기능 모두 정상 작동

배포를 진행해도 되면 resume signal을 입력한다.
  </how-to-verify>
  <resume-signal>"포털 중단 인지함. Firestore rules 배포 진행" 또는 "OK" (Nick이 Phase 2 전까지 포털 중단을 수용함을 확인)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: firestore.rules 재작성</name>
  <files>firestore.rules</files>
  <action>
firestore.rules 파일 전체를 아래 내용으로 교체한다. 기존 내용은 모두 제거한다.

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ── 헬퍼 함수 ─────────────────────────────────────────────────────────────

    // 이메일 로그인 사용자 (강사·관리자) — password provider 확인
    function isEmailUser() {
      return request.auth != null &&
        request.auth.token.firebase.sign_in_provider == 'password';
    }

    // 익명 로그인 사용자 (포털·등록폼)
    function isAnonymous() {
      return request.auth != null &&
        request.auth.token.firebase.sign_in_provider == 'anonymous';
    }

    // Custom Claims 기반 역할 확인
    function hasRole(r) {
      return isEmailUser() && request.auth.token.role == r;
    }

    function isAdmin()          { return hasRole('admin'); }
    function isManagerOrAbove() { return isEmailUser() && request.auth.token.role in ['admin', 'manager']; }
    function isStaff()          { return isEmailUser() && request.auth.token.role in ['admin', 'manager', 'teacher']; }

    // ── appData 컬렉션별 규칙 ─────────────────────────────────────────────────

    // 학생 PII — staff만 읽기, 관리자 이상만 쓰기
    match /appData/rye-students {
      allow read:  if isStaff();
      allow write: if isManagerOrAbove();
    }

    // 강사 정보 — staff만 읽기, 관리자 이상만 쓰기
    match /appData/rye-teachers {
      allow read:  if isStaff();
      allow write: if isManagerOrAbove();
    }

    // 수납 기록 — 관리자/매니저만 읽기·쓰기 (강사 숨김)
    match /appData/rye-payments {
      allow read:  if isManagerOrAbove();
      allow write: if isManagerOrAbove();
    }

    // 출석 기록 — staff + 익명(포털 댓글 읽기·쓰기)
    // NOTE(Phase 2): 포털 댓글은 Phase 2에서 서브컬렉션 분리 예정
    match /appData/rye-attendance {
      allow read:  if isStaff() || isAnonymous();
      allow write: if isStaff() || isAnonymous();
    }

    // 수강 등록 대기 — staff 읽기, 인증된 모든 사용자 쓰기 (/register 폼)
    match /appData/rye-pending {
      allow read:  if isStaff();
      allow write: if request.auth != null;
    }

    // 회원 공지 — staff + 익명(포털 읽음 처리)
    match /appData/rye-student-notices {
      allow read:  if isStaff() || isAnonymous();
      allow write: if isStaff() || isAnonymous();
    }

    // 기관(B2B) — staff 읽기, 관리자 이상 쓰기
    match /appData/rye-institutions {
      allow read:  if isStaff();
      allow write: if isManagerOrAbove();
    }

    // 카테고리 설정 — staff 읽기, 관리자 이상 쓰기
    match /appData/rye-categories {
      allow read:  if isStaff();
      allow write: if isManagerOrAbove();
    }

    // 수강료 프리셋 — staff 읽기, 관리자 이상 쓰기
    match /appData/rye-fee-presets {
      allow read:  if isStaff();
      allow write: if isManagerOrAbove();
    }

    // 스케줄 변경 — staff + 익명 읽기(포털 일정 확인), staff만 쓰기
    match /appData/rye-schedule-overrides {
      allow read:  if isStaff() || isAnonymous();
      allow write: if isStaff();
    }

    // 활동 로그 — admin만 읽기, staff 쓰기
    match /appData/rye-activity {
      allow read:  if isAdmin();
      allow write: if isStaff();
    }

    // 휴지통 — admin만 읽기·쓰기
    match /appData/rye-trash {
      allow read, write: if isAdmin();
    }

    // 공지사항 — staff + 익명 읽기, staff 쓰기
    match /appData/rye-notices {
      allow read:  if isStaff() || isAnonymous();
      allow write: if isStaff();
    }

    // 미지정 appData 문서 — 관리자 이상만 (신규 컬렉션 추가 시 기본 보호)
    match /appData/{document} {
      allow read, write: if isManagerOrAbove();
    }

    // 그 외 모든 경로 — 차단
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

주의사항:
- 더 구체적인 match 규칙이 더 일반적인 규칙보다 우선 적용됨 (Firestore rules 평가 방식)
- `match /appData/rye-students` 가 `match /appData/{document}` 보다 우선함
- 포털 학생 조회 관련 rye-students read 허용은 Phase 2까지 의도적으로 제외
  </action>
  <verify>
    <automated>grep -c "isAuthed" firestore.rules</automated>
  </verify>
  <done>
1. `grep -c "isAuthed" firestore.rules` → 0 (isAuthed 함수 완전 제거됨)
2. `grep -n "hasRole\|isStaff\|isManagerOrAbove" firestore.rules` → 각 함수 정의 1줄씩 포함됨
3. `grep -n "rye-students\|rye-teachers\|rye-payments" firestore.rules` → 각 컬렉션별 rule 존재
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>firestore.rules 파일이 재작성되었다. Firebase CLI 또는 Firebase Console을 통해 배포하고, 실제 동작을 검증한다.</what-built>
  <how-to-verify>
**Step 1: Firestore rules 배포**

방법 A (Firebase CLI):
```
npx firebase deploy --only firestore:rules
```

방법 B (Firebase Console 수동):
Firebase Console → Firestore Database → Rules 탭 → 에디터에 firestore.rules 내용 붙여넣기 → 게시

**Step 2: 익명 읽기 차단 확인**

브라우저에서 `/register` 또는 새 시크릿 창 열기:
1. DevTools → Console 열기
2. 아래 코드를 실행:
```javascript
// 이 코드는 테스트 목적 — 익명 read 시도
import { getFirestore, doc, getDoc } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";
// 또는 앱의 firebase.js를 통해 시도
```
실제로는: 앱에서 로그인 없이 Dashboard 접근 시도 → Firestore 읽기 에러 확인.

더 간단한 방법:
- 시크릿 창에서 앱 URL 열기
- `/register` 페이지 로드 (익명 auth 자동 시작)
- DevTools Network 탭에서 Firestore 요청 중 `rye-students` 관련 요청 → 403/PERMISSION_DENIED 확인

**Step 3: 강사 로그인 후 정상 접근 확인**

강사로 로그인 → 학생 목록이 정상 로드되면 isStaff() 규칙 작동 중.

**Step 4: 포털 중단 확인 (예상된 동작)**

`/myryk` 에서 학생코드로 로그인 시도 → "접근 거부" 또는 학생 정보 없음 오류 확인. 이는 예상된 동작 (Phase 2에서 수정 예정).
  </how-to-verify>
  <resume-signal>"Firestore rules 배포 완료. 익명 read 차단됨 ✓, 강사 로그인 정상 ✓, 포털 학생 로그인 중단 확인됨 ✓" 또는 발견된 오류</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 익명 Firebase 사용자 → Firestore | 공개 URL 방문자 + 포털 미인증 상태 — rye-students 등 PII 컬렉션 read 차단 |
| email 사용자(role claim 없음) → Firestore | Custom Claims 미설정 강사 — isStaff() false로 read 차단됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | firestore.rules / rye-students | mitigate | isStaff() 전용 — 익명 및 미인증 read 차단 |
| T-04-02 | Information Disclosure | firestore.rules / rye-payments | mitigate | isManagerOrAbove() 전용 — teacher role도 차단 |
| T-04-03 | Information Disclosure | firestore.rules / rye-teachers | mitigate | isStaff() 전용 — 강사 연락처 보호 |
| T-04-04 | Elevation of Privilege | firestore.rules / rye-pending | accept | 익명 write 허용 — /register 폼 필수 요구사항. 임시 등록 데이터만 포함, PII 최소화 |
| T-04-05 | Availability | Rules 배포 후 포털 중단 | accept | 의도된 트레이드오프 — PII 보호 > 포털 편의성. Phase 2에서 수정 예정 |
| T-04-06 | Elevation of Privilege | email user without claims calling isStaff() | mitigate | role claim 없으면 request.auth.token.role = null → 'teacher' 비교 실패 → deny. Plan 03 체크포인트 통과 후에만 배포 |
</threat_model>

<verification>
1. `grep -c "isAuthed" firestore.rules` → 0
2. `grep -n "isStaff\|isManagerOrAbove\|isAdmin" firestore.rules` → 함수 정의 + 사용처 포함
3. `grep -n "rye-students\|rye-payments\|rye-teachers" firestore.rules` → 각 3줄 이상
4. (배포 후) 시크릿 창에서 Firestore read 시도 → PERMISSION_DENIED 확인
5. (배포 후) 강사 로그인 후 학생 목록 정상 로드 확인
</verification>

<success_criteria>
- firestore.rules에 isAuthed() 없음 — 익명 전체 읽기 패턴 완전 제거
- isStaff(), isManagerOrAbove(), isAdmin() 함수 + Custom Claims role 기반 gate 적용
- rye-students, rye-teachers, rye-payments: isStaff() 또는 isManagerOrAbove() 전용
- rye-pending: 익명 write 허용 유지
- Firebase 배포 완료 + 실제 동작 검증됨 (체크포인트)
</success_criteria>

<output>
완료 후 `.planning/phases/01-security-foundation/01-04-SUMMARY.md` 생성

SUMMARY에 반드시 포함:
- "포털 학생 로그인 중단됨 — Phase 2 POR-01에서 Worker 기반 student lookup으로 수정 예정"
</output>
