---
phase: 09-schedule-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/App.jsx
autonomous: true
requirements:
  - SCH-01
  - SCH-04
  - SCH-05

must_haves:
  truths:
    - "학생 저장(신규/수정) 시 autoSyncStudentSlots가 호출되어 slotId 없는 레슨에 슬롯이 자동 생성 또는 연결된다"
    - "슬롯이 새로 생성된 경우 '슬롯 N개 생성됨' 토스트가 표시된다"
    - "기존 slotId와 스케줄이 불일치하면 slotId가 null로 초기화되고 새 슬롯이 생성된다"
    - "slotId 있고 스케줄 변경 없으면 autoSyncStudentSlots가 skip (idempotent)"
    - "onResumeStudent 호출 시 pauseHistory[] 배열에 { pausedAt, pausedReason, resumedAt, durationDays, slotIds } 항목이 append된다"
    - "onResumeStudent 호출 시 student.status가 'active', pausedAt/pausedReason이 null로 업데이트된다"
    - "view === 'pauseManagement'가 topTitle 맵과 뷰 라우팅에 추가되어 있다"
    - "onAddStudentToSlot(student, teacherId, day, time, instrument) 함수가 정의된다"
  artifacts:
    - path: "src/App.jsx"
      provides: "autoSyncStudentSlots, onResumeStudent, onAddStudentToSlot, pauseManagement routing"
      contains: "autoSyncStudentSlots"
  key_links:
    - from: "src/App.jsx (sForm handler line ~1430)"
      to: "autoSyncStudentSlots"
      via: "직접 호출 (await)"
      pattern: "autoSyncStudentSlots"
    - from: "autoSyncStudentSlots"
      to: "src/utils.js slotMatchesLesson"
      via: "import { slotMatchesLesson } from './utils.js'"
      pattern: "slotMatchesLesson"
    - from: "autoSyncStudentSlots"
      to: "addLessonSlot (firebase.js)"
      via: "직접 호출, docRef.id를 slotId로 사용"
      pattern: "addLessonSlot"
    - from: "onResumeStudent"
      to: "updateStudentDoc"
      via: "pauseHistory append 후 직접 호출"
      pattern: "updateStudentDoc"
---

<objective>
App.jsx에 슬롯 자동 생성 로직(`autoSyncStudentSlots`)과 복귀 처리 콜백(`onResumeStudent`)을 추가하고, 학생 저장 핸들러와 `pauseManagement` 뷰 라우팅을 연결한다.

Purpose: D-01(슬롯 자동생성), D-04(pauseHistory append), D-03(뷰 라우팅) 핵심 로직을 App.jsx 클로저로 구현한다. TimetableView와 PauseManagementView는 이 함수들을 prop으로 받아 사용한다.
Output: App.jsx에 autoSyncStudentSlots, onResumeStudent, onAddStudentToSlot 함수 + sForm 핸들러 수정 + pauseManagement 라우팅.
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-schedule-enhancement/09-CONTEXT.md
@.planning/phases/09-schedule-enhancement/09-RESEARCH.md
@.planning/phases/09-schedule-enhancement/09-01-SUMMARY.md

<interfaces>
<!-- 이 플랜이 사용하는 기존 함수 시그니처 (RESEARCH.md 확인) -->

From src/App.jsx (verified):
```js
// addStudentDoc — void 반환
const addStudentDoc = async (student) => { /* runTransaction */ };

// updateStudentDoc — void 반환, student.id 필수
const updateStudentDoc = async (student) => { /* runTransaction */ };
```

From src/firebase.js (verified):
```js
// addLessonSlot — DocumentReference 반환 (docRef.id = 새 슬롯 Firestore ID)
export async function addLessonSlot(data) {
  return addDoc(collection(db, "rye-lesson-slots"), { ...data, createdAt: Date.now() });
}
export async function updateLessonSlot(id, data) { ... }
```

From src/utils.js (Plan 01에서 추가됨):
```js
export function slotMatchesLesson(slot, lesson, studentTeacherId) { ... }
```

App.jsx sForm 핸들러 현재 코드 (line 1430~1434):
```js
if (data.id) {
  await updateStudentDoc({ ...data, studentCode: data.studentCode || students.find(s => s.id === data.id)?.studentCode });
} else {
  await addStudentDoc({ ...data, id: uid(), studentCode: generateStudentCode() });
}
addLog(`${data.name} 회원 ${isNew ? "등록" : "수정"}`);
setModal(null); showToast(isNew ? "회원이 등록되었습니다." : "회원 정보가 수정되었습니다.");
```

App.jsx onStatusChange 현재 코드 (line 1439 sDetail 내부):
```js
onStatusChange={async (newStatus, meta = {}) => {
  const upd = { ...selected, status: newStatus,
    ...(newStatus === "paused"
      ? { pausedAt: Date.now(), pausedReason: meta.reason || "", careLogs: selected.careLogs || [] }
      : { pausedAt: null, pausedReason: null })
  };
  try { await updateStudentDoc(upd); setSelected(upd); ... } catch { ... }
}}
```

App.jsx view 라우팅 — settlement 뷰 끝 (line ~1392):
```js
{view === "settlement" && canManageAll(user.role) && <SettlementView ... />}
{view === "more" && <MoreMenu ... />}
```

topTitle 맵 (line 1247):
```js
const topTitle = { dashboard: "RYE-K", ..., settlement: "정산 관리" }[view] || "RYE-K";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: autoSyncStudentSlots 클로저 추가 + sForm 핸들러 수정 (D-01)</name>
  <read_first>
    - src/App.jsx — line 1~30 (import 목록 확인)
    - src/App.jsx — line 538~614 (addStudentDoc, updateStudentDoc, runLessonSlotMigration 패턴)
    - src/App.jsx — line 1414~1438 (sForm 핸들러 전체)
    - CLAUDE.md — "saveStudents 절대 금지" 규칙 확인
  </read_first>
  <files>src/App.jsx</files>
  <action>
    **변경 1: utils.js에서 slotMatchesLesson import 추가**

    App.jsx 상단 import 블록에서 `src/utils.js`를 import하는 줄을 찾는다. 이미 `import { ..., canManageAll, ... } from './utils.js'` 형태가 있으면 그 목록에 `slotMatchesLesson`을 추가한다. 없으면 새 import 줄을 추가한다.

    ```js
    // 기존 utils import에 slotMatchesLesson 추가 (예시):
    import { uid, canManageAll, /* ...기존들... */, slotMatchesLesson } from './utils.js';
    ```

    **변경 2: autoSyncStudentSlots 클로저 추가**

    `runLessonSlotMigration` 함수 정의 바로 다음에 (또는 App.jsx 내 다른 클로저 함수들이 모여 있는 곳에) 다음 함수를 추가한다:

    ```js
    // Phase 9 — D-01: 학생 저장 시 슬롯 자동 생성·연결·detach
    // autoSyncStudentSlots는 반드시 addStudentDoc/updateStudentDoc 호출 이후에 실행한다.
    // addStudentDoc은 void 반환이므로 studentWithId를 인자로 직접 전달한다.
    const autoSyncStudentSlots = async (student) => {
      const lessons = student.lessons || [];
      if (lessons.length === 0) return 0;

      let newSlotCount = 0;
      const updatedLessons = [...lessons];

      for (let i = 0; i < lessons.length; i++) {
        const lesson = lessons[i];
        const tid = lesson.teacherId || student.teacherId;
        if (!tid || !lesson.instrument) continue; // 강사/악기 없는 레슨은 스킵

        // 기존 slotId 검증 (detach 판단)
        if (lesson.slotId) {
          const existingSlot = lessonSlots.find(s => s.id === lesson.slotId);
          if (existingSlot && slotMatchesLesson(existingSlot, lesson, tid)) {
            continue; // idempotent — 변경 없음
          }
          // 불일치 또는 슬롯 소멸 → detach
          updatedLessons[i] = { ...lesson, slotId: null };
        }

        // 완전 일치 슬롯 탐색 (closed 슬롯 제외)
        const matched = lessonSlots.find(s =>
          s.status !== "closed" &&
          slotMatchesLesson(s, { ...updatedLessons[i], slotId: null }, tid)
        );

        if (matched) {
          updatedLessons[i] = { ...updatedLessons[i], slotId: matched.id };
        } else {
          // 같은 셀에 다른 학생이 이미 있으면 그룹으로 판단
          const memberCount = students.filter(s =>
            s.id !== student.id &&
            (s.lessons || []).some(l =>
              slotMatchesLesson(
                { teacherId: tid, instrument: lesson.instrument, schedule: lesson.schedule },
                l,
                l.teacherId || s.teacherId
              )
            )
          ).length;
          const isGroup = memberCount >= 1;
          const docRef = await addLessonSlot({
            teacherId: tid,
            instrument: lesson.instrument,
            type: isGroup ? "group" : "individual",
            name: isGroup ? `${lesson.instrument} 그룹` : student.name,
            schedule: lesson.schedule || [],
            status: "active",
            notes: "",
          });
          updatedLessons[i] = { ...updatedLessons[i], slotId: docRef.id };
          newSlotCount++;
        }
      }

      // 변경된 lessons로 학생 문서 업데이트 (saveStudents 절대 금지 — per-op 트랜잭션)
      const finalStudent = { ...student, lessons: updatedLessons };
      await updateStudentDoc(finalStudent);
      return newSlotCount;
    };
    ```

    **변경 3: sForm 핸들러 수정 (line 1430~1434 구간 교체)**

    다음 기존 코드 블록을:
    ```js
    if (data.id) {
      await updateStudentDoc({ ...data, studentCode: data.studentCode || students.find(s => s.id === data.id)?.studentCode });
    } else {
      await addStudentDoc({ ...data, id: uid(), studentCode: generateStudentCode() });
    }
    addLog(`${data.name} 회원 ${isNew ? "등록" : "수정"}`);
    setModal(null); showToast(isNew ? "회원이 등록되었습니다." : "회원 정보가 수정되었습니다.");
    ```

    아래로 교체한다:
    ```js
    // addStudentDoc은 void 반환 → studentWithId를 미리 구성 후 autoSyncStudentSlots에 전달
    const studentWithId = data.id
      ? { ...data, studentCode: data.studentCode || students.find(s => s.id === data.id)?.studentCode }
      : { ...data, id: uid(), studentCode: generateStudentCode() };

    if (studentWithId.id && students.some(s => s.id === studentWithId.id)) {
      await updateStudentDoc(studentWithId);
    } else {
      await addStudentDoc(studentWithId);
    }
    const newSlots = await autoSyncStudentSlots(studentWithId).catch(() => 0);
    addLog(`${data.name} 회원 ${isNew ? "등록" : "수정"}`);
    setModal(null);
    showToast(isNew ? "회원이 등록되었습니다." : "회원 정보가 수정되었습니다.");
    if (newSlots > 0) setTimeout(() => showToast(`슬롯 ${newSlots}개 생성됨`), 800);
    ```

    주의: `addStudentDoc`과 `updateStudentDoc`은 변경하지 않는다. 기존 per-op 트랜잭션 함수 그대로 유지. `saveStudents([...])` 절대 금지.
  </action>
  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && grep -n "autoSyncStudentSlots" src/App.jsx && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "autoSyncStudentSlots" src/App.jsx` 결과가 3 이상 (정의 1, sForm 호출 1, 내부 참조 1+)
    - `grep -c "slotMatchesLesson" src/App.jsx` 결과가 2 이상 (import + 사용)
    - `grep -c "saveStudents" src/App.jsx` 결과가 0 (saveStudents 호출 없음 확인)
    - `grep -c "addLessonSlot" src/App.jsx` 결과가 1 이상 (autoSyncStudentSlots 내부)
    - sForm 핸들러에 `const studentWithId =` 패턴이 존재: `grep -c "studentWithId" src/App.jsx` >= 2
    - `npm run build` 통과
  </acceptance_criteria>
  <done>autoSyncStudentSlots가 App.jsx에 정의되고, sForm 핸들러가 studentWithId 패턴으로 교체되어 npm run build 통과</done>
</task>

<task type="auto">
  <name>Task 2: onResumeStudent + onAddStudentToSlot + pauseManagement 라우팅 (D-03, D-04)</name>
  <read_first>
    - src/App.jsx — line 1240~1260 (topTitle 맵)
    - src/App.jsx — line 1360~1400 (view 라우팅 끝 부분 — settlement, more)
    - src/App.jsx — line 1439~1445 (sDetail onStatusChange 핸들러)
    - src/App.jsx — line 1362 (ScheduleView 렌더 — onUpdateSlot prop 위치)
  </read_first>
  <files>src/App.jsx</files>
  <action>
    **변경 1: onResumeStudent 함수 추가**

    `autoSyncStudentSlots` 정의 바로 다음에 추가:

    ```js
    // Phase 9 — D-04: 복귀 처리 콜백 (paused → active + pauseHistory append)
    // 기존 pausedAt/pausedReason 에서 pauseHistory entry 자동 구성
    const onResumeStudent = async (student) => {
      const resumedAt = Date.now();
      const newEntry = {
        pausedAt: student.pausedAt || null,
        pausedReason: student.pausedReason || "",
        resumedAt,
        durationDays: student.pausedAt
          ? Math.floor((resumedAt - student.pausedAt) / 86400000)
          : null,
        slotIds: (student.lessons || []).map(l => l.slotId).filter(Boolean),
      };
      const upd = {
        ...student,
        status: "active",
        pausedAt: null,
        pausedReason: null,
        pauseHistory: [...(student.pauseHistory || []), newEntry],
      };
      await updateStudentDoc(upd);
      showToast(`${student.name} 복귀 처리되었습니다.`);
    };
    ```

    **변경 2: onAddStudentToSlot 함수 추가**

    `onResumeStudent` 정의 바로 다음에 추가:

    ```js
    // Phase 9 — D-02: TimetableView "+" 클릭 후 학생 배정 콜백
    // instrument는 TimetableView에서 학생의 기존 레슨 또는 선택 드롭다운으로 결정되어 전달
    const onAddStudentToSlot = async (student, teacherId, day, time, instrument) => {
      if (!instrument) { showToast("악기를 선택해주세요.", true); return; }
      // 이미 같은 강사/요일/시간 레슨이 있으면 중복 추가 방지
      const alreadyHas = (student.lessons || []).some(l =>
        (l.teacherId || student.teacherId) === teacherId &&
        (l.schedule || []).some(sch => sch.day === day && sch.time === time)
      );
      if (alreadyHas) { showToast("이미 해당 시간에 레슨이 있습니다.", true); return; }
      const newLesson = { instrument, teacherId, schedule: [{ day, time }], slotId: null };
      const updStudent = { ...student, lessons: [...(student.lessons || []), newLesson] };
      await updateStudentDoc(updStudent);
      const created = await autoSyncStudentSlots(updStudent).catch(() => 0);
      showToast(`${student.name} 배정${created > 0 ? ` (슬롯 ${created}개 생성됨)` : ""}`);
    };
    ```

    **변경 3: topTitle 맵에 pauseManagement 추가**

    line ~1247의 topTitle 객체에 `pauseManagement: "휴회 관리"` 항목을 추가한다:
    ```js
    const topTitle = { dashboard: "RYE-K", ..., settlement: "정산 관리", pauseManagement: "휴회 관리" }[view] || "RYE-K";
    ```

    **변경 4: ScheduleView 렌더에 onAddStudentToSlot prop 추가**

    line ~1362의 ScheduleView JSX에 prop 추가:
    ```js
    {view === "schedule" && <ScheduleView
      students={allMembers} teachers={teachers} currentUser={user}
      attendance={attendance}
      onSaveAttendance={async(upd)=>{await saveAttendance(upd);}}
      onSaveScheduleOverride={saveScheduleOverrides}
      scheduleOverrides={scheduleOverrides}
      notices={notices}
      lessonSlots={lessonSlots}
      onUpdateSlot={async (id, data) => updateLessonSlot(id, data)}
      onAddStudentToSlot={onAddStudentToSlot}
    />}
    ```

    **변경 5: pauseManagement 뷰 라우팅 추가**

    settlement 뷰 다음(`{view === "more" && <MoreMenu .../>}` 바로 전)에 추가:
    ```jsx
    {view === "pauseManagement" && (canManageAll(user.role) || user.role === "teacher") && (
      <PauseManagementView
        students={visible}
        teachers={teachers}
        currentUser={user}
        lessonSlots={lessonSlots}
        onUpdateStudent={async (s) => { await updateStudentDoc(s); }}
        onResumeStudent={onResumeStudent}
        showToast={showToast}
      />
    )}
    ```

    PauseManagementView import를 상단에 추가 (다른 component import 근처):
    ```js
    import PauseManagementView from './components/student/PauseManagementView.jsx';
    ```

    **변경 6: sDetail onStatusChange 핸들러 업데이트 (paused→active 시 pauseHistory append)**

    line ~1439의 onStatusChange에서 `paused → active` 분기를 onResumeStudent를 활용하도록 업데이트한다. 현재:
    ```js
    const upd = { ...selected, status: newStatus,
      ...(newStatus === "paused"
        ? { pausedAt: Date.now(), pausedReason: meta.reason || "", careLogs: selected.careLogs || [] }
        : { pausedAt: null, pausedReason: null })
    };
    try { await updateStudentDoc(upd); setSelected(upd); showToast(...); } catch { ... }
    ```

    교체:
    ```js
    if (newStatus === "active" && selected.status === "paused") {
      // pauseHistory append는 onResumeStudent가 처리
      try {
        await onResumeStudent(selected);
        // onResumeStudent 내부에서 showToast 호출함
        // setSelected 업데이트: visible 목록에서 최신 상태 찾음
        setSelected(prev => ({ ...prev, status: "active", pausedAt: null, pausedReason: null }));
      } catch {
        showToast("저장에 실패했습니다. 네트워크를 확인 후 다시 시도해주세요.", true);
      }
    } else {
      const upd = { ...selected, status: newStatus,
        ...(newStatus === "paused"
          ? { pausedAt: Date.now(), pausedReason: meta.reason || "", careLogs: selected.careLogs || [] }
          : { pausedAt: null, pausedReason: null })
      };
      try { await updateStudentDoc(upd); setSelected(upd); showToast(newStatus === "paused" ? "휴원 처리되었습니다." : "재원 복귀되었습니다."); } catch { showToast("저장에 실패했습니다. 네트워크를 확인 후 다시 시도해주세요.", true); }
    }
    ```

    주의: PauseManagementView는 Plan 04에서 생성되므로, import 후 빌드가 실패하면 Plan 04 실행 전까지는 import 줄을 주석 처리하고 빌드 통과 확인. 단, Plan 04와 동시 실행 시 주석 불필요.
  </action>
  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && grep -n "onResumeStudent\|onAddStudentToSlot\|pauseManagement" src/App.jsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const onResumeStudent" src/App.jsx` 결과가 1
    - `grep -c "const onAddStudentToSlot" src/App.jsx` 결과가 1
    - `grep -c "pauseManagement.*휴회 관리" src/App.jsx` 결과가 1 (topTitle)
    - `grep -c "view.*pauseManagement.*PauseManagementView" src/App.jsx` 결과가 1 (라우팅)
    - `grep -c "onAddStudentToSlot.*onAddStudentToSlot" src/App.jsx` 결과가 1 (ScheduleView prop)
    - `grep -c "pauseHistory" src/App.jsx` 결과가 2 이상 (onResumeStudent + onStatusChange)
    - `npm run build` 통과 (PauseManagementView import가 아직 없으면 import 줄 임시 주석 처리 후 빌드)
  </acceptance_criteria>
  <done>onResumeStudent, onAddStudentToSlot 정의, pauseManagement 라우팅 추가, sDetail onStatusChange 업데이트, npm run build 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sForm data → addStudentDoc/updateStudentDoc | StudentFormModal에서 온 data — 내부 React state, 외부 입력 아님 |
| onAddStudentToSlot instrument 파라미터 | TimetableView에서 전달. 값이 없으면 showToast("악기를 선택해주세요.", true) 처리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | autoSyncStudentSlots — student.id 없음 | mitigate | `updateStudentDoc`에 `if (!student?.id) throw` 검증 이미 존재 |
| T-09-02-02 | Denial of Service | autoSyncStudentSlots 반복 호출 | accept | 이미 slotId 있고 일치하면 skip(idempotent). 연속 저장 시 중복 슬롯 생성 방지됨 |
| T-09-02-03 | Elevation of Privilege | pauseManagement 뷰 — 강사가 타 강사 학생 복귀 | mitigate | `visible = students.filter(강사 역할 필터)` — App.jsx line ~1196. PauseManagementView에 `visible` 전달 |
</threat_model>

<verification>
```bash
cd C:/Users/GIGABYTE/Coding/rye-k

# autoSyncStudentSlots 정의 확인
grep -n "const autoSyncStudentSlots" src/App.jsx

# onResumeStudent + onAddStudentToSlot 확인
grep -n "const onResumeStudent\|const onAddStudentToSlot" src/App.jsx

# pauseManagement topTitle + 라우팅 확인
grep -n "pauseManagement" src/App.jsx

# saveStudents 호출 없음 확인 (CRITICAL)
grep -n "saveStudents\[" src/App.jsx || echo "OK: saveStudents 배열 호출 없음"

# pauseHistory append 확인
grep -n "pauseHistory" src/App.jsx

# 빌드
npm run build
```
</verification>

<success_criteria>
- App.jsx에 `autoSyncStudentSlots` (슬롯 자동생성 클로저) 정의됨
- App.jsx에 `onResumeStudent` (pauseHistory append) 정의됨
- App.jsx에 `onAddStudentToSlot` (TimetableView 배정 콜백) 정의됨
- sForm 핸들러에서 `studentWithId` 패턴으로 저장 후 `autoSyncStudentSlots` 호출
- `topTitle` 맵에 `pauseManagement: "휴회 관리"` 추가
- ScheduleView 렌더에 `onAddStudentToSlot` prop 추가
- `pauseManagement` 뷰 라우팅 추가 (강사 포함)
- `npm run build` 에러 없이 통과
</success_criteria>

<output>
완료 후 `.planning/phases/09-schedule-enhancement/09-02-SUMMARY.md` 생성.
</output>
