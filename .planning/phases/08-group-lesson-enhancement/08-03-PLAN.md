---
phase: 08-group-lesson-enhancement
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/components/ScheduleView.jsx
autonomous: true
requirements:
  - GRP-03
must_haves:
  truths:
    - "ScheduleView 그룹 레슨 헤더에서 lessonSlots의 slot.name이 표시된다"
    - "slotId가 없으면 기존 '그룹 레슨' 텍스트로 fallback된다"
    - "연필 아이콘 클릭 시 인라인 input이 나타나고 Enter/blur 시 Firestore에 저장된다"
    - "그룹 헤더 클릭(접힘/펼침) 동작은 이름 편집과 독립적으로 동작한다"
  artifacts:
    - path: src/components/ScheduleView.jsx
      provides: "lessonSlots prop 수신 + slotId 엔트리 포함 + 인라인 이름 편집 UI"
      contains: "lessonSlots"
  key_links:
    - from: ScheduleView entry.slotId
      to: lessonSlots array
      via: "lessonSlots.find(s => s.id === entry.slotId)?.name"
      pattern: "entry\\.slotId"
    - from: inline edit input
      to: Firestore rye-lesson-slots
      via: "onUpdateSlot(entry.slotId, { name })"
      pattern: "onUpdateSlot"
---

<objective>
ScheduleView에서 그룹 레슨 헤더의 "그룹 레슨" 하드코딩을 slotId 기반 슬롯 이름으로 교체하고, 관리자/매니저가 그룹 이름을 인라인으로 편집할 수 있도록 한다.

Purpose: slotId 마이그레이션 이후 이름이 의미 있게 표시되고, 운영 중 이름을 쉽게 변경할 수 있다.
Output: ScheduleView에 lessonSlots 기반 이름 표시 + 인라인 편집 UI
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-group-lesson-enhancement/08-CONTEXT.md
@.planning/phases/08-group-lesson-enhancement/08-01-SUMMARY.md

<interfaces>
<!-- ScheduleView 현재 시그니처 (line 14) -->
```js
function ScheduleView({ students, teachers, currentUser, attendance,
  onSaveAttendance, onSaveScheduleOverride, scheduleOverrides, notices })
```
<!-- 08-01 이후 props 추가됨: lessonSlots, onUpdateSlot -->

<!-- 엔트리 객체 구조 (line 72-80) — non-institution 학생용 -->
```js
scheduleByDay[sch.day].push({
  studentId: s.id, studentName: s.name, instrument: lesson.instrument,
  time: sch.time || "", teacherId: lessonTid,
  teacherName: teacher ? teacher.name : "미배정",
  color: getTeacherColor(lessonTid, teachers), isMakeup: false,
});
// → slotId: lesson.slotId || null 추가 필요
```

<!-- 그룹 맵 구성 (line 85-107) -->
```js
// groupMap[gKey] = { ...entry, members: [...] }
// entry에 slotId가 포함되므로 groupMap[gKey].slotId 자동 전파됨
// gKey = `${entry.teacherId}|${entry.time}|${entry.instrument}`
```

<!-- 현재 그룹 헤더 렌더 (line 218) — 변경 대상 -->
```jsx
{entry.isGroupLesson ? "그룹 레슨" : entry.studentName}
// → slotId 기반 slot.name으로 교체 + 편집 UI 추가
```

<!-- D-04: 편집 규칙 -->
<!--
  - 그룹 헤더 클릭: 접힘/펼침 유지 (기존 로직 손대지 않음)
  - 그룹 이름 텍스트 옆 연필(✏) 아이콘 → 클릭 시 인라인 input 전환
  - Enter/blur 시 onUpdateSlot(slotId, { name: value }) 호출 후 input 닫기
  - window.confirm / window.alert 절대 금지
  - 이름 편집은 canSeeAll(관리자/매니저)만 가능
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScheduleView — slotId 엔트리 전파 + props 수신</name>
  <read_first>
    - src/components/ScheduleView.jsx (line 1-115 전체 — 함수 시그니처, scheduleByDay 구성, groupMap 구성 로직)
  </read_first>
  <files>src/components/ScheduleView.jsx</files>
  <action>
1. 함수 시그니처에 `lessonSlots`, `onUpdateSlot` prop 추가:
   ```js
   function ScheduleView({ students, teachers, currentUser, attendance,
     onSaveAttendance, onSaveScheduleOverride, scheduleOverrides, notices,
     lessonSlots, onUpdateSlot })
   ```

2. 상태 선언부(alimSend, alimToast 아래)에 인라인 편집 상태 추가:
   ```js
   const [editingSlotId, setEditingSlotId] = useState(null);
   const [editingSlotName, setEditingSlotName] = useState("");
   ```

3. scheduleByDay 구성 루프 (line ~72-80, non-institution 분기)에서 slotId 추가:
   ```js
   scheduleByDay[sch.day].push({
     studentId: s.id, studentName: s.name, instrument: lesson.instrument,
     time: sch.time || "", teacherId: lessonTid,
     teacherName: teacher ? teacher.name : "미배정",
     color: getTeacherColor(lessonTid, teachers), isMakeup: false,
     slotId: lesson.slotId || null,   // ← 추가
   });
   ```
   주의: institution 분기(line ~62-70)는 건드리지 말 것.

4. groupMap에 slotId가 자동 전파되는지 확인:
   - `groupMap[gKey] = { ...entry, members: [...] }` 패턴에서 `...entry`가 slotId를 포함하므로 별도 수정 불필요.
  </action>
  <verify>
    <automated>grep -n "slotId" src/components/ScheduleView.jsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "slotId" src/components/ScheduleView.jsx` → 엔트리 추가 줄 + 사용 줄 포함 2줄 이상
    - `grep "lessonSlots" src/components/ScheduleView.jsx` → 시그니처 줄 출력
    - `grep "editingSlotId\|editingSlotName" src/components/ScheduleView.jsx` → useState 선언 2줄
  </acceptance_criteria>
  <done>ScheduleView가 lessonSlots/onUpdateSlot props를 받고 각 엔트리에 slotId가 포함된다</done>
</task>

<task type="auto">
  <name>Task 2: ScheduleView — 그룹 이름 표시 + 인라인 편집 UI</name>
  <read_first>
    - src/components/ScheduleView.jsx (line 200-245 — 그룹 헤더 렌더 영역, "그룹 레슨" 하드코딩 위치)
  </read_first>
  <files>src/components/ScheduleView.jsx</files>
  <action>
그룹 헤더 이름 표시 부분(현재 line ~218)을 다음으로 교체한다:

```jsx
{/* 변경 전: entry.isGroupLesson ? "그룹 레슨" : entry.studentName */}
{entry.isGroupLesson ? (() => {
  const slot = entry.slotId ? (lessonSlots || []).find(s => s.id === entry.slotId) : null;
  const slotName = slot?.name || "그룹 레슨";
  const isEditing = editingSlotId === entry.slotId && entry.slotId;
  return (
    <span style={{ display: "inline-flex", alignItems: "center", gap: 4 }}>
      {isEditing ? (
        <input
          className="inp"
          style={{ fontSize: 13, fontWeight: 600, padding: "2px 6px", height: 26, width: 140 }}
          value={editingSlotName}
          autoFocus
          onChange={e => setEditingSlotName(e.target.value)}
          onKeyDown={e => {
            if (e.key === "Enter") {
              e.preventDefault();
              if (editingSlotName.trim() && onUpdateSlot) {
                onUpdateSlot(entry.slotId, { name: editingSlotName.trim() });
              }
              setEditingSlotId(null);
            }
            if (e.key === "Escape") setEditingSlotId(null);
          }}
          onBlur={() => {
            if (editingSlotName.trim() && onUpdateSlot) {
              onUpdateSlot(entry.slotId, { name: editingSlotName.trim() });
            }
            setEditingSlotId(null);
          }}
          onClick={e => e.stopPropagation()}
        />
      ) : (
        <>
          {slotName}
          {canSeeAll && entry.slotId && onUpdateSlot && (
            <span
              style={{ fontSize: 11, cursor: "pointer", color: "var(--ink-30)", padding: "2px 3px" }}
              title="이름 편집"
              onClick={e => {
                e.stopPropagation();
                setEditingSlotId(entry.slotId);
                setEditingSlotName(slotName);
              }}
            >
              ✏
            </span>
          )}
        </>
      )}
    </span>
  );
})() : entry.studentName}
```

주의사항:
- onClick의 e.stopPropagation() — 그룹 헤더 클릭(접힘/펼침)과 이름 편집 클릭이 독립 동작하도록
- `canSeeAll` 변수는 파일 상단에 이미 선언되어 있음 (line 26)
- onUpdateSlot이 undefined인 경우(prop 미전달) 연필 아이콘 숨김
- window.confirm / window.alert 사용 없음
  </action>
  <verify>
    <automated>grep -c "editingSlotId\|editingSlotName\|onUpdateSlot\|slotName" src/components/ScheduleView.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "그룹 레슨" src/components/ScheduleView.jsx` → 0줄 또는 fallback 문자열 값으로만 존재 (하드코딩된 JSX 텍스트 노드로는 없어야 함)
    - `grep "slot?.name\|slot\?.name" src/components/ScheduleView.jsx` → slot 이름 조회 줄 출력
    - `grep "onUpdateSlot.*slotId\|slotId.*onUpdateSlot" src/components/ScheduleView.jsx` → 저장 호출 줄 출력
    - `grep "stopPropagation" src/components/ScheduleView.jsx` → 편집 클릭 이벤트에서 출력
    - npm run build 통과
  </acceptance_criteria>
  <done>그룹 헤더가 슬롯 이름을 표시하고, 관리자/매니저가 연필 아이콘으로 이름을 인라인 편집할 수 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 input → onUpdateSlot → Firestore | 슬롯 이름 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Elevation of Privilege | 연필 아이콘 노출 | mitigate | `canSeeAll && entry.slotId && onUpdateSlot` 조건에서만 렌더. teacher 역할은 canSeeAll=false이므로 편집 UI 미노출 |
| T-08-03-02 | Tampering | 슬롯 이름 input | accept | 슬롯 이름은 PII 아님. 빈 문자열 제출은 trim() 체크로 방지 |
</threat_model>

<verification>
1. npm run build 통과
2. ScheduleView.jsx에 "그룹 레슨" 하드코딩이 JSX 렌더 텍스트로 없음
3. slotId 엔트리 전파 확인: `grep "slotId: lesson.slotId" src/components/ScheduleView.jsx`
4. 인라인 편집 UI: `grep "editingSlotId" src/components/ScheduleView.jsx` → useState + 렌더 영역 모두 포함
</verification>

<success_criteria>
- 그룹 헤더가 lessonSlots[slotId].name 표시 (fallback: "그룹 레슨")
- 관리자/매니저: 연필 아이콘 → input → Enter/blur → Firestore 저장
- 접힘/펼침 독립 동작 유지
- window.confirm/alert 없음
</success_criteria>

<output>
완료 후 `.planning/phases/08-group-lesson-enhancement/08-03-SUMMARY.md` 생성
</output>
