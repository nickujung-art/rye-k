---
phase: 08-group-lesson-enhancement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/firebase.js
  - src/App.jsx
autonomous: true
requirements:
  - GRP-01
must_haves:
  truths:
    - "App.jsx에 lessonSlots 상태가 존재하고 rye-lesson-slots 컬렉션 변경 시 실시간 반영된다"
    - "firebase.js에서 addLessonSlot / updateLessonSlot / deleteLessonSlot을 named import할 수 있다"
    - "ScheduleView가 lessonSlots와 onUpdateSlot prop을 받는다"
    - "AttendanceView가 lessonSlots prop을 받는다"
  artifacts:
    - path: src/firebase.js
      provides: "레슨 슬롯 CRUD 3종"
      exports: ["addLessonSlot", "updateLessonSlot", "deleteLessonSlot"]
    - path: src/App.jsx
      provides: "lessonSlots 상태 + rye-lesson-slots onSnapshot 리스너"
      contains: "rye-lesson-slots"
  key_links:
    - from: src/App.jsx
      to: "Firestore collection('rye-lesson-slots')"
      via: "onSnapshot 리스너 (rye-instant-charges 패턴 동일)"
      pattern: "rye-lesson-slots"
    - from: src/App.jsx
      to: src/components/ScheduleView.jsx
      via: "lessonSlots={lessonSlots} onUpdateSlot prop"
      pattern: "lessonSlots=\\{lessonSlots\\}"
---

<objective>
rye-lesson-slots 독립 Firestore 컬렉션을 위한 CRUD 함수를 firebase.js에 추가하고, App.jsx에 실시간 리스너와 lessonSlots 상태를 세팅한다. ScheduleView와 AttendanceView에 lessonSlots prop을 연결하는 최소 배선(wiring) 작업만 수행한다.

Purpose: 이후 Wave 2 플랜(03, 04)이 lessonSlots 데이터를 즉시 사용할 수 있도록 기반을 마련한다.
Output: firebase.js CRUD 함수 3종 + App.jsx lessonSlots 상태 + 리스너 + prop 전달
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-group-lesson-enhancement/08-CONTEXT.md

<interfaces>
<!-- firebase.js 현재 imports (line 2) — deleteDoc 추가 필요 -->
```js
import { getFirestore, doc, setDoc, onSnapshot, runTransaction, collection, addDoc, updateDoc, getDoc } from "firebase/firestore";
// deleteDoc 은 현재 import 없음 → 추가 필요
```

<!-- App.jsx 현재 firebase import (line 2) — 3개 함수 추가 필요 -->
```js
import { db, auth, doc, setDoc, onSnapshot, runTransaction, collection, getDoc,
  addInstantCharge, updateInstantCharge,
  firebaseSignIn, firebaseSignInAnon, firebaseLogout, onAuthStateChanged } from "./firebase.js";
// addLessonSlot, updateLessonSlot, deleteLessonSlot 추가 필요
```

<!-- App.jsx rye-instant-charges 리스너 패턴 (line 467-478) — 동일 패턴으로 rye-lesson-slots 추가 -->
```js
const chargesUnsub = onSnapshot(
  collection(db, "rye-instant-charges"),
  (snap) => { setInstantCharges(snap.docs.map(d => ({ id: d.id, ...d.data() }))); },
  (err) => { console.error("Firestore listener error: rye-instant-charges", err); setInstantCharges([]); }
);
unsubscribes.push(chargesUnsub);
```

<!-- App.jsx ScheduleView 렌더 (line 1288) — lessonSlots + onUpdateSlot 추가 -->
```jsx
{view === "schedule" && <ScheduleView
  students={allMembers} teachers={teachers} currentUser={user}
  attendance={attendance} onSaveAttendance={...}
  onSaveScheduleOverride={saveScheduleOverrides}
  scheduleOverrides={scheduleOverrides} notices={notices}
/>}
// → lessonSlots={lessonSlots} onUpdateSlot={async (id, data) => updateLessonSlot(id, data)} 추가

<!-- App.jsx AttendanceView 렌더 (line 1201) — lessonSlots 추가 -->
```jsx
{view === "attendance" && <AttendanceView
  students={allMembers} teachers={teachers} currentUser={user}
  attendance={attendance} onSaveAttendance={...}
  categories={categories} scheduleOverrides={scheduleOverrides}
  onSaveScheduleOverride={saveScheduleOverrides}
  onUpdateStudent={...} showToast={showToast}
/>}
// → lessonSlots={lessonSlots} 추가
```

<!-- D-01: 슬롯 스키마 -->
```js
{
  id,                   // Firestore auto-id (docRef.id)
  teacherId: string,
  instrument: string,
  type: "individual" | "group",
  name: string,
  capacity: number,
  schedule: [{ day: string, time: string }],
  status: "active" | "paused" | "closed",
  notes: string,
  createdAt: number,    // Date.now()
}
```

<!-- D-09: rye-lesson-slots는 appData 밖 독립 컬렉션 -->
<!-- rye-instant-charges와 완전히 동일한 방식 — collection(db, 'rye-lesson-slots') -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: firebase.js — addLessonSlot / updateLessonSlot / deleteLessonSlot 추가</name>
  <read_first>
    - src/firebase.js (전체 — 현재 imports와 기존 함수 패턴 확인 필수)
  </read_first>
  <files>src/firebase.js</files>
  <action>
1. firebase/firestore import 줄에 `deleteDoc` 추가:
   ```js
   import { getFirestore, doc, setDoc, onSnapshot, runTransaction, collection, addDoc, updateDoc, getDoc, deleteDoc } from "firebase/firestore";
   ```

2. `updateInstantCharge` 함수 아래에 다음 3개 함수 추가:
   ```js
   // ── rye-lesson-slots 독립 컬렉션 CRUD ─────────────────────────────────────
   export async function addLessonSlot(data) {
     return addDoc(collection(db, "rye-lesson-slots"), {
       ...data,
       createdAt: Date.now(),
     });
   }
   export async function updateLessonSlot(id, data) {
     if (!id) throw new Error("updateLessonSlot: id 없음");
     return updateDoc(doc(db, "rye-lesson-slots", id), { ...data, updatedAt: Date.now() });
   }
   export async function deleteLessonSlot(id) {
     if (!id) throw new Error("deleteLessonSlot: id 없음");
     return deleteDoc(doc(db, "rye-lesson-slots", id));
   }
   ```

3. 파일 마지막 export 줄에 세 함수를 추가 — 기존 `export { db, auth, doc, ... }` 줄은 그대로 두고, 세 함수는 이미 `export async function`으로 선언되어 있으므로 별도 추가 불필요.

규칙: addLessonSlot은 docRef를 return해야 한다 (호출자가 .id로 슬롯 ID를 얻음). runTransaction 불필요 — 슬롯은 독립 문서 (D-09).
  </action>
  <verify>
    <automated>grep -c "addLessonSlot\|updateLessonSlot\|deleteLessonSlot" src/firebase.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function addLessonSlot" src/firebase.js` → 1줄 출력
    - `grep "export async function updateLessonSlot" src/firebase.js` → 1줄 출력
    - `grep "export async function deleteLessonSlot" src/firebase.js` → 1줄 출력
    - `grep "deleteDoc" src/firebase.js` → import 줄과 사용 줄 모두 출력
    - npm run build 통과
  </acceptance_criteria>
  <done>firebase.js에서 세 함수를 named import로 사용할 수 있고 빌드가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: App.jsx — lessonSlots 상태 + rye-lesson-slots 리스너 + prop 전달</name>
  <read_first>
    - src/App.jsx (line 1-10 imports, line 240-250 useState 선언부, line 360-480 setupListeners 함수, line 1285-1295 view 렌더 부분)
  </read_first>
  <files>src/App.jsx</files>
  <action>
1. firebase import 줄(line 2)에 `addLessonSlot, updateLessonSlot, deleteLessonSlot` 추가:
   ```js
   import { db, auth, doc, setDoc, onSnapshot, runTransaction, collection, getDoc,
     addInstantCharge, updateInstantCharge,
     addLessonSlot, updateLessonSlot, deleteLessonSlot,
     firebaseSignIn, firebaseSignInAnon, firebaseLogout, onAuthStateChanged } from "./firebase.js";
   ```

2. useState 선언부(shopItems useState 바로 아래)에 추가:
   ```js
   const [lessonSlots, setLessonSlots] = useState([]);
   ```

3. setupListeners 함수 내부, `chargesUnsub` 블록 바로 아래에 다음 리스너 추가 (rye-instant-charges 패턴 동일):
   ```js
   // rye-lesson-slots — 독립 컬렉션 (appData 아님)
   const slotsUnsub = onSnapshot(
     collection(db, "rye-lesson-slots"),
     (snap) => {
       setLessonSlots(snap.docs.map(d => ({ id: d.id, ...d.data() })));
     },
     (err) => {
       console.error("Firestore listener error: rye-lesson-slots", err);
       setLessonSlots([]);
     }
   );
   unsubscribes.push(slotsUnsub);
   ```

4. ScheduleView 렌더 줄(view === "schedule")에 두 prop 추가:
   - `lessonSlots={lessonSlots}`
   - `onUpdateSlot={async (id, data) => updateLessonSlot(id, data)}`

5. AttendanceView 렌더 줄(view === "attendance")에 prop 추가:
   - `lessonSlots={lessonSlots}`

주의: KEYS 배열에 rye-lesson-slots를 추가하지 말 것 — 독립 컬렉션이므로 별도 onSnapshot 패턴 사용 (rye-instant-charges와 동일).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "lessonSlots" src/App.jsx` → useState 선언, slotsUnsub 리스너, ScheduleView prop, AttendanceView prop 포함 4줄 이상 출력
    - `grep "rye-lesson-slots" src/App.jsx` → 리스너 줄 출력
    - `grep "onUpdateSlot" src/App.jsx` → ScheduleView 렌더 줄에서 출력
    - npm run build 오류 없이 통과
  </acceptance_criteria>
  <done>App.jsx에서 lessonSlots 상태가 선언되고, rye-lesson-slots 실시간 리스너가 등록되며, ScheduleView/AttendanceView에 prop이 전달된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore | 브라우저에서 rye-lesson-slots 컬렉션 직접 읽기/쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | rye-lesson-slots onSnapshot | accept | 슬롯 데이터는 PII 아님 (강사 ID/악기/시간표). Firestore 규칙은 Phase 1에서 별도 강화 |
| T-08-01-02 | Tampering | addLessonSlot / updateLessonSlot | mitigate | Wave 2 AdminTools에서 admin role check로 미이그레이션 버튼 가드. 이 plan은 함수만 export |
</threat_model>

<verification>
1. `npm run build` 오류 없이 통과
2. `grep "rye-lesson-slots" src/App.jsx` → slotsUnsub 리스너 줄 확인
3. `grep "lessonSlots={lessonSlots}" src/App.jsx` → ScheduleView 렌더 줄 확인
4. `grep "addLessonSlot\|updateLessonSlot\|deleteLessonSlot" src/firebase.js` → 3줄 이상
</verification>

<success_criteria>
- firebase.js: addLessonSlot, updateLessonSlot, deleteLessonSlot 3개 함수 export
- App.jsx: lessonSlots useState + rye-lesson-slots onSnapshot 리스너 + ScheduleView에 lessonSlots/onUpdateSlot prop + AttendanceView에 lessonSlots prop
- 빌드 통과
</success_criteria>

<output>
완료 후 `.planning/phases/08-group-lesson-enhancement/08-01-SUMMARY.md` 생성
</output>
