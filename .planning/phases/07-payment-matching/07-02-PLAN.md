---
wave: 2
depends_on:
  - 07-01-PLAN.md
files_modified:
  - functions/api/payments/kakaobank-webhook.js
autonomous: true
requirements:
  - PAY-10
---

# Plan 07-02: fuzzyMatchStudent 알고리즘 고도화

## Objective

보호자 이름 매칭, 공백 포함 2명 이름 split, 금액 기반 보조 신호를 추가하여
현재 대부분 미매칭으로 빠지는 입금 알림을 정확히 처리한다.

**전제**: 07-01-PLAN.md 완료 후 students_cache에 `guardianName`, `monthlyFee` 포함.

**현재 매칭 흐름**:
1. `exact` — `s.name === inputName`
2. `fuzzy_1` — Levenshtein distance ≤ 1
3. `no_match` → `splitNameCandidates` (공백 없이 붙은 이름만)
4. 그래도 없으면 `pending:unmatched`

**추가할 흐름**:
- `guardian_exact` / `guardian_fuzzy` — guardianName으로 매칭
- 공백 포함 이름("홍길동 김개똥") 토큰 split 처리
- `amount_match` — 이름 no_match이지만 amount === monthlyFee 유일 학생 → suggestedStudentId 포함 unmatched

---

## Tasks

### Task 1: fuzzyMatchStudent — guardianName 매칭 추가

<read_first>
- functions/api/payments/kakaobank-webhook.js
  - `fuzzyMatchStudent` 함수 (line ~305~323)
  - `levenshtein` 함수 (line ~278~290)
  - POST 처리 section (line ~144~196): confidence에 따른 KV 저장 로직
    - `exact` 또는 `fuzzy_1` → `pending:matched`
    - 그 외 → `pending:unmatched`
</read_first>

<action>
`fuzzyMatchStudent` 함수 전체 재작성 (현재 24줄 → 확장):

```js
function fuzzyMatchStudent(inputName, students) {
  const active = students.filter(s => !s.isInstitution && (s.status || "active") === "active");

  // 1. Exact match on student name
  const exact = active.filter(s => s.name === inputName);
  if (exact.length === 1) return { match: exact[0], confidence: "exact" };
  if (exact.length > 1)   return { match: null,     confidence: "duplicate_exact" };

  // 2. Levenshtein ≤ 1 on student name
  const close = active
    .map(s => ({ s, dist: levenshtein(inputName, s.name) }))
    .filter(({ dist }) => dist <= 1)
    .sort((a, b) => a.dist - b.dist);
  if (close.length === 1) return { match: close[0].s, confidence: "fuzzy_1" };
  if (close.length > 1)   return { match: null,       confidence: "duplicate_fuzzy" };

  // 3. Exact match on guardianName
  const guardianExact = active.filter(s => s.guardianName && s.guardianName === inputName);
  if (guardianExact.length === 1) return { match: guardianExact[0], confidence: "guardian_exact" };
  if (guardianExact.length > 1)   return { match: null,             confidence: "duplicate_guardian" };

  // 4. Levenshtein ≤ 1 on guardianName
  const guardianClose = active
    .filter(s => s.guardianName && s.guardianName.length >= 2)
    .map(s => ({ s, dist: levenshtein(inputName, s.guardianName) }))
    .filter(({ dist }) => dist <= 1)
    .sort((a, b) => a.dist - b.dist);
  if (guardianClose.length === 1) return { match: guardianClose[0].s, confidence: "guardian_fuzzy" };
  if (guardianClose.length > 1)   return { match: null,               confidence: "duplicate_guardian_fuzzy" };

  return { match: null, confidence: "no_match" };
}
```

POST 처리 section에서 auto-match 조건도 업데이트:
```js
// 기존 (line ~144)
if (match && (confidence === "exact" || confidence === "fuzzy_1")) {
// 변경 후 — guardian 매칭도 자동 처리
if (match && (confidence === "exact" || confidence === "fuzzy_1" || confidence === "guardian_exact" || confidence === "guardian_fuzzy")) {
```
</action>

<acceptance_criteria>
- `fuzzyMatchStudent`에 guardianName exact 매칭 로직 포함
- `fuzzyMatchStudent`에 guardianName Levenshtein ≤ 1 매칭 로직 포함
- confidence 값으로 `"guardian_exact"`, `"guardian_fuzzy"`, `"duplicate_guardian"`, `"duplicate_guardian_fuzzy"` 사용
- POST 처리 auto-match 조건에 `guardian_exact`, `guardian_fuzzy` 포함
- 기존 exact/fuzzy_1 로직 변경 없음
</acceptance_criteria>

---

### Task 2: 공백 포함 이름 split 처리 추가

<read_first>
- functions/api/payments/kakaobank-webhook.js
  - `splitNameCandidates` 함수 (line ~296~303): 공백 없는 연결 이름만 처리
  - POST no_match 처리 블록 (line ~155~187): splitNameCandidates 호출 부분
  - `parseRawText` 함수 (line ~332~361): 이름 파싱
</read_first>

<action>
POST no_match 처리 블록에서 splitNameCandidates 시도 전, 공백 포함 이름을 먼저 처리:

```js
// 기존 no_match 처리 시작 부분
if (confidence === "no_match" && name.length >= 4) {
  const candidates = splitNameCandidates(name);
  // ...
}
```

앞에 공백 split 처리 삽입:
```js
// 공백으로 구분된 이름 처리 (예: "홍길동 김개똥")
if (confidence === "no_match" && name.includes(" ")) {
  const tokens = name.split(/\s+/).filter(t => t.length >= 2);
  if (tokens.length === 2) {
    const m1 = fuzzyMatchStudent(tokens[0], students);
    const m2 = fuzzyMatchStudent(tokens[1], students);
    const ok1 = m1.match && (m1.confidence === "exact" || m1.confidence === "fuzzy_1" || m1.confidence === "guardian_exact" || m1.confidence === "guardian_fuzzy");
    const ok2 = m2.match && (m2.confidence === "exact" || m2.confidence === "fuzzy_1" || m2.confidence === "guardian_exact" || m2.confidence === "guardian_fuzzy");
    if (ok1 && ok2) {
      for (const [nm, mt] of [[tokens[0], m1], [tokens[1], m2]]) {
        const rid = crypto.randomUUID();
        const splitRec = {
          id: rid,
          senderName: nm,
          amount: mt.match.monthlyFee || 0,
          timestamp: ts,
          source: "kakaobank",
          rawText,
          createdAt: now,
          matchedAt: now,
          matchedStudentId: mt.match.id,
          confidence: "split_space",
        };
        await env.RATE_LIMIT_KV.put(`pending:matched:${rid}`, JSON.stringify(splitRec), { expirationTtl: 604800 });
      }
      return json({ ok: true, matched: true, split: true, names: tokens });
    }
  }
}

// 기존 splitNameCandidates (공백 없이 붙은 이름)
if (confidence === "no_match" && name.length >= 4) {
  // ... (기존 코드 유지)
}
```
</action>

<acceptance_criteria>
- `name.includes(" ")` 조건으로 공백 포함 이름 split 처리
- confidence `"split_space"` 사용
- 2토큰 각각 fuzzyMatchStudent 호출 (guardian 매칭 포함)
- 기존 `splitNameCandidates` (공백 없는 연결 이름) 로직 유지
- `pending:matched` KV 저장 형식 동일
</acceptance_criteria>

---

### Task 3: monthlyFee 기반 amount_match 보조 신호

<read_first>
- functions/api/payments/kakaobank-webhook.js
  - POST no_match 최종 unmatched 저장 블록 (line ~189~196)
  - record 구조 (line ~131~142)
</read_first>

<action>
모든 split 시도 후에도 no_match인 경우, amount 기반 단서 탐색:

```js
// 기존 unmatched 저장 블록 앞에 삽입
// amount === monthlyFee인 학생이 정확히 1명인 경우 suggestedStudentId 추가
let suggestedStudentId = null;
if (amount > 0) {
  const activeStudents = students.filter(s => !s.isInstitution && (s.status || "active") === "active");
  const feeMatches = activeStudents.filter(s => s.monthlyFee > 0 && s.monthlyFee === amount);
  if (feeMatches.length === 1) {
    suggestedStudentId = feeMatches[0].id;
    record.confidence = "amount_match";
  }
}
if (suggestedStudentId) record.suggestedStudentId = suggestedStudentId;

// 기존
await env.RATE_LIMIT_KV.put(`pending:unmatched:${id}`, JSON.stringify(record), { expirationTtl: 604800 });
return json({ ok: true, matched: false, confidence: record.confidence });
```

`amount_match`는 자동 매칭으로 처리하지 않고 unmatched 큐에 저장한다.
브라우저 UI에서 추천 학생으로 표시 (07-03-PLAN.md에서 처리).
</action>

<acceptance_criteria>
- amount > 0이고 monthlyFee 일치 학생이 1명이면 `record.suggestedStudentId` 설정
- confidence `"amount_match"` 사용
- `amount_match`는 `pending:unmatched`로 저장 (자동 처리 안 함)
- amount 0이거나 일치 학생이 0명 또는 2명 이상이면 기존 confidence 그대로
- 기존 unmatched 저장 경로 변경 없음
</acceptance_criteria>

---

## Verification Criteria

```bash
# guardianName 매칭 로직 확인
grep -n "guardian_exact\|guardian_fuzzy\|guardianName" functions/api/payments/kakaobank-webhook.js

# 공백 split 로직 확인
grep -n "split_space\|includes.*\" \"" functions/api/payments/kakaobank-webhook.js

# amount_match 로직 확인
grep -n "amount_match\|suggestedStudentId\|monthlyFee" functions/api/payments/kakaobank-webhook.js

# auto-match 조건에 guardian 포함 확인
grep -n "guardian_exact\|guardian_fuzzy" functions/api/payments/kakaobank-webhook.js | grep "confidence ==="

# 빌드 통과 (Worker는 JS라 빌드 별도 없음, wrangler pages deploy로 검증)
npm run build
```

## must_haves

- truth: 보호자 이름으로 입금 시 `guardian_exact` 또는 `guardian_fuzzy` confidence로 `pending:matched` KV에 저장된다
- truth: "홍길동 김개똥" 형태 입금자명이 두 토큰으로 분리되어 각각 매칭된다 (confidence: split_space)
- truth: 이름 매칭 실패 + 금액 일치 학생 1명이면 `suggestedStudentId` 포함 unmatched로 저장된다
- truth: 기존 exact/fuzzy_1 매칭 동작 변경 없음
