---
phase: 10-accounting-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rye-k-accounting/package.json
  - rye-k-accounting/vite.config.ts
  - rye-k-accounting/tsconfig.json
  - rye-k-accounting/tsconfig.node.json
  - rye-k-accounting/index.html
  - rye-k-accounting/src/main.tsx
  - rye-k-accounting/src/App.tsx
  - rye-k-accounting/src/index.css
  - rye-k-accounting/src/supabase.ts
  - rye-k-accounting/src/types/database.ts
  - rye-k-accounting/tailwind.config.js
  - rye-k-accounting/postcss.config.js
  - rye-k-accounting/.env.local
  - rye-k-accounting/.gitignore
  - rye-k-accounting/public/_redirects
  - rye-k-accounting/supabase/config.toml
autonomous: true
requirements:
  - ACC-01
  - ACC-15

user_setup:
  - service: supabase
    why: "Database + Auth 호스팅 (PostgreSQL + Supabase Auth)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "supabase.com → Account → Access Tokens → Generate new token"
      - name: VITE_SUPABASE_URL
        source: "supabase.com → Project → Settings → API → Project URL"
      - name: VITE_SUPABASE_ANON_KEY
        source: "supabase.com → Project → Settings → API → anon public key"
      - name: SUPABASE_PROJECT_REF
        source: "supabase.com → Project → Settings → General → Reference ID (예: abcdefghijklmnop)"
    dashboard_config:
      - task: "새 Supabase 프로젝트 생성 (지역: ap-northeast-2 Seoul 또는 ap-northeast-1 Tokyo)"
        location: "supabase.com → New Project → 이름: rye-k-accounting"
  - service: github
    why: "새 리포지토리 호스팅 + Cloudflare Pages 연결"
    dashboard_config:
      - task: "새 GitHub 리포지토리 생성 (private)"
        location: "github.com → New repository → 이름: rye-k-accounting (private)"

must_haves:
  truths:
    - "rye-k-accounting/ 디렉토리가 존재하고 npm run build 가 dist/ 생성 후 0 exit"
    - "src/supabase.ts 가 VITE_SUPABASE_URL/ANON_KEY 미설정 시 명확한 에러를 throw"
    - "supabase/config.toml 이 존재해 supabase CLI 명령이 동작"
    - ".env.local 에 6개 환경변수 이름(템플릿)이 모두 존재"
    - "public/_redirects 로 SPA 라우팅이 Cloudflare Pages 에서 동작"
  artifacts:
    - path: "rye-k-accounting/package.json"
      provides: "의존성 선언"
      contains: "@supabase/supabase-js"
    - path: "rye-k-accounting/src/supabase.ts"
      provides: "Supabase 클라이언트 singleton"
      exports: ["supabase"]
    - path: "rye-k-accounting/src/App.tsx"
      provides: "react-router-dom 라우팅 골격 (7개 경로)"
    - path: "rye-k-accounting/supabase/config.toml"
      provides: "Supabase CLI 프로젝트 설정"
  key_links:
    - from: "rye-k-accounting/src/main.tsx"
      to: "rye-k-accounting/src/App.tsx"
      via: "ReactDOM.createRoot render"
    - from: "rye-k-accounting/src/App.tsx"
      to: "rye-k-accounting/src/supabase.ts"
      via: "import { supabase }"
---

<objective>
새 rye-k-accounting Git 리포지토리를 생성하고 React 18 + Vite 5 + TypeScript + Tailwind CSS + Supabase 프로젝트 골격을 구축한다. Cloudflare Pages SPA 라우팅 설정도 포함한다.

Purpose: Phase 10의 모든 후속 플랜이 이 골격 위에서 구현된다. 이 플랜 없이는 어떤 플랜도 실행 불가.
Output: 빌드 통과하는 빈 React+TS 앱 + Supabase CLI 초기화 + 환경변수 템플릿
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-accounting-app/10-CONTEXT.md

NOTE: 이 플랜은 rye-k/ 형제 디렉토리인 rye-k-accounting/ 를 신규 생성한다.
모든 파일 경로는 rye-k-accounting/ 루트 기준이다.
실행 전 Nick 이 user_setup 의 GitHub 리포와 Supabase 프로젝트를 생성해야 한다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 신규 프로젝트 초기화 + 의존성 설치</name>
  <read_first>
    - .planning/phases/10-accounting-app/10-CONTEXT.md (D-01 기술스택, D-10 UI원칙, specifics 폴더구조)
  </read_first>
  <files>
    rye-k-accounting/package.json,
    rye-k-accounting/vite.config.ts,
    rye-k-accounting/tsconfig.json,
    rye-k-accounting/tsconfig.node.json,
    rye-k-accounting/index.html,
    rye-k-accounting/src/main.tsx,
    rye-k-accounting/src/index.css,
    rye-k-accounting/tailwind.config.js,
    rye-k-accounting/postcss.config.js,
    rye-k-accounting/.gitignore,
    rye-k-accounting/public/_redirects
  </files>
  <action>
아래 순서로 실행한다. rye-k/ 의 부모 디렉토리(C:/Users/GIGABYTE/Coding/)에서 실행:

```bash
cd /c/Users/GIGABYTE/Coding
npm create vite@latest rye-k-accounting -- --template react-ts
cd rye-k-accounting
npm install @supabase/supabase-js react-router-dom recharts jspdf jspdf-autotable date-fns
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

그런 다음 아래 파일을 정확히 이 내용으로 작성/덮어쓴다:

**tailwind.config.js:**
```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        income: '#2563eb',
        expense: '#6b7280',
        ar: '#f97316',
        matched: '#16a34a',
        error: '#dc2626',
      },
    },
  },
  plugins: [],
}
```

**src/index.css** (전체 내용, 기존 내용 삭제 후 교체):
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 한국식 금액 포맷 helper class */
.amount-income { @apply text-income font-semibold; }
.amount-expense { @apply text-expense; }
.amount-ar { @apply text-ar font-semibold; }
```

**vite.config.ts** (기존 덮어쓰기):
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: { port: 3001 },
})
```

**public/_redirects** (Cloudflare Pages SPA 라우팅):
```
/*    /index.html    200
```

**.gitignore** (기존 내용에 추가):
```
.env.local
.env.*.local
supabase/.temp/
```

**index.html** title 변경:
```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>RYE-K 회계</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**src/main.tsx** (기존 덮어쓰기):
```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```
  </action>
  <acceptance_criteria>
    - rye-k-accounting/tailwind.config.js 에 `income`, `expense`, `ar`, `matched`, `error` 색상이 정의됨
    - rye-k-accounting/src/index.css 첫 줄이 `@tailwind base;`
    - rye-k-accounting/public/_redirects 파일이 존재하고 `/*    /index.html    200` 포함
    - rye-k-accounting/package.json 에 `@supabase/supabase-js`, `recharts`, `jspdf`, `react-router-dom` 모두 존재
  </acceptance_criteria>
  <verify>
    <automated>
cd /c/Users/GIGABYTE/Coding/rye-k-accounting && grep -c "supabase/supabase-js" package.json && grep -c "_redirects" public/_redirects
    </automated>
  </verify>
  <done>npm install 완료, tailwind 설정, _redirects 파일 존재</done>
</task>

<task type="auto">
  <name>Task 2: Supabase CLI 초기화 + 클라이언트 + App.tsx 라우팅 골격 + .env.local 템플릿</name>
  <read_first>
    - .planning/phases/10-accounting-app/10-CONTEXT.md (D-01, D-03 스키마 목록, specifics 폴더구조)
  </read_first>
  <files>
    rye-k-accounting/src/supabase.ts,
    rye-k-accounting/src/types/database.ts,
    rye-k-accounting/src/App.tsx,
    rye-k-accounting/.env.local,
    rye-k-accounting/supabase/config.toml
  </files>
  <action>
**1. Supabase CLI 초기화** (rye-k-accounting/ 디렉토리에서):
```bash
cd /c/Users/GIGABYTE/Coding/rye-k-accounting
npx supabase init
```
이 명령이 supabase/config.toml 을 생성한다.

그런 다음 Nick 이 제공한 PROJECT_REF 로 링크:
```bash
npx supabase link --project-ref $SUPABASE_PROJECT_REF
```
(SUPABASE_ACCESS_TOKEN 환경변수가 설정되어 있어야 함)

**2. src/types/database.ts** (stub — 10-02 플랜에서 실제 타입으로 교체됨):
```typescript
// 이 파일은 10-02 플랜에서 supabase gen types typescript 로 자동 생성됨
// 지금은 stub 으로만 존재
export type Database = {
  public: {
    Tables: {
      tenants: { Row: { id: string; name: string; created_at: string } }
      tenant_members: { Row: { id: string; tenant_id: string; user_id: string; role: string } }
      accounts: { Row: { id: string; tenant_id: string; code: string; name: string; type: string; is_system: boolean; is_active: boolean } }
      journal_entries: { Row: { id: string; tenant_id: string; entry_date: string; description: string; status: string; source_type: string | null; source_id: string | null } }
      journal_lines: { Row: { id: string; journal_entry_id: string; account_id: string; side: string; amount: number; memo: string | null } }
      bank_imports: { Row: { id: string; tenant_id: string; bank_name: string; filename: string } }
      bank_transactions: { Row: { id: string; bank_import_id: string; tenant_id: string; transaction_date: string; description: string | null; amount: number; status: string } }
      bank_rules: { Row: { id: string; tenant_id: string; name: string; conditions: unknown; priority: number } }
      payroll_records: { Row: { id: string; tenant_id: string; teacher_name: string; income_type: string; gross_amount: number; withholding_tax: number; net_amount: number; pay_date: string } }
      invoices: { Row: { id: string; tenant_id: string; institution_name: string; invoice_date: string; supply_amount: number; hometax_issued: boolean } }
      recurring_templates: { Row: { id: string; tenant_id: string; name: string; day_of_month: number; amount: number } }
      ryek_sync_log: { Row: { id: string; tenant_id: string; synced_at: string; payment_count: number; new_entries: number; status: string } }
      tax_exports: { Row: { id: string; tenant_id: string; period_year: number; period_month: number; status: string } }
    }
    Functions: {
      my_tenant_id: { Returns: string }
      seed_default_accounts: { Args: { p_tenant_id: string }; Returns: void }
    }
  }
}
```

**3. src/supabase.ts:**
```typescript
import { createClient } from '@supabase/supabase-js'
import type { Database } from './types/database'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL as string
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY as string

if (!supabaseUrl || !supabaseAnonKey) {
  throw new Error(
    'Supabase 환경변수 미설정: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY 를 .env.local 에 입력하세요'
  )
}

export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey, {
  auth: {
    persistSession: true,
    autoRefreshToken: true,
  },
})
```

**4. src/App.tsx** (라우팅 골격 — 10-03 플랜에서 실제 컴포넌트로 교체됨):
```typescript
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'

// 10-03 플랜이 이 컴포넌트들을 실제 구현으로 교체한다
const Placeholder = ({ name }: { name: string }) => (
  <div className="p-4 text-gray-500">{name} (구현 예정)</div>
)

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<Placeholder name="LoginView" />} />
        <Route path="/dashboard" element={<Placeholder name="DashboardView" />} />
        <Route path="/journal" element={<Placeholder name="JournalListView" />} />
        <Route path="/journal/new" element={<Placeholder name="QuickExpenseView" />} />
        <Route path="/bank" element={<Placeholder name="BankImportView" />} />
        <Route path="/payroll" element={<Placeholder name="PayrollView" />} />
        <Route path="/invoices" element={<Placeholder name="InvoiceView" />} />
        <Route path="/reports/pl" element={<Placeholder name="ProfitLossView" />} />
        <Route path="/reports/ledger" element={<Placeholder name="LedgerView" />} />
        <Route path="/reports/ar-aging" element={<Placeholder name="ArAgingView" />} />
        <Route path="/recurring" element={<Placeholder name="RecurringView" />} />
        <Route path="*" element={<Navigate to="/dashboard" replace />} />
      </Routes>
    </BrowserRouter>
  )
}
```

**5. .env.local** (템플릿 — 실제 값은 Nick 이 입력):
```
# Supabase
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# RYE-K Firebase (Edge Function 에서만 사용 — 브라우저 미노출)
# 아래는 Supabase Edge Function Secrets 에 설정 (supabase secrets set)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
# RYEK_TENANT_ID=uuid-of-ryek-tenant

# Cloudflare Pages 빌드 설정 (Dashboard 에서 설정)
# Build command: npm run build
# Build output directory: dist
# Node version: 20
```
  </action>
  <acceptance_criteria>
    - rye-k-accounting/supabase/config.toml 파일이 존재
    - rye-k-accounting/src/supabase.ts 가 `createClient<Database>` 호출을 포함
    - rye-k-accounting/src/App.tsx 에 `/login`, `/dashboard`, `/journal`, `/bank`, `/payroll`, `/invoices`, `/reports/pl` 경로가 모두 존재
    - rye-k-accounting/.env.local 에 `VITE_SUPABASE_URL` 과 `VITE_SUPABASE_ANON_KEY` 변수명이 존재
    - `cd rye-k-accounting && npm run build` 가 exit 0 으로 완료됨
  </acceptance_criteria>
  <verify>
    <automated>
cd /c/Users/GIGABYTE/Coding/rye-k-accounting && npm run build && echo "BUILD OK" && grep -c "VITE_SUPABASE_URL" .env.local && ls supabase/config.toml
    </automated>
  </verify>
  <done>
    npm run build 통과, supabase/config.toml 존재, .env.local 템플릿 작성,
    src/supabase.ts singleton 존재, App.tsx 라우팅 골격 완성
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local → 빌드 번들 | VITE_ 접두사 변수만 번들에 포함됨 |
| 브라우저 → Supabase | anon key 는 브라우저 노출 허용 (RLS 로 제한) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | .env.local | mitigate | .gitignore 에 .env.local 포함 필수. FIREBASE_SERVICE_ACCOUNT_JSON 는 절대 .env.local 에 두지 않고 Supabase Secrets 에만 저장 |
| T-10-02 | Information Disclosure | VITE_SUPABASE_ANON_KEY | accept | anon key 브라우저 노출은 Supabase 설계 의도. RLS 정책이 실제 격리를 담당 |
| T-10-03 | Tampering | public/_redirects | accept | 정적 파일이므로 위험 없음. Cloudflare Pages CDN 이 서빙 |
</threat_model>

<verification>
1. `cd /c/Users/GIGABYTE/Coding/rye-k-accounting && npm run build` → exit 0, dist/ 생성
2. `ls supabase/config.toml` → 파일 존재
3. `grep "VITE_SUPABASE_URL" .env.local` → 변수명 존재
4. `cat public/_redirects` → `/*    /index.html    200` 확인
5. `grep "createClient" src/supabase.ts` → import 확인
</verification>

<success_criteria>
- rye-k-accounting/ 디렉토리가 C:/Users/GIGABYTE/Coding/ 에 존재
- npm run build 가 오류 없이 dist/ 를 생성
- Supabase CLI 가 프로젝트에 링크된 상태 (supabase status 명령 응답)
- 모든 환경변수 이름이 .env.local 에 문서화됨
</success_criteria>

<output>
완료 후 `.planning/phases/10-accounting-app/10-01-SUMMARY.md` 를 생성하라.
summary.md 템플릿: @$HOME/.claude/get-shit-done/templates/summary.md
</output>
