---
phase: 02-portal-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants.jsx
autonomous: true
requirements:
  - POR-01
  - POR-02

must_haves:
  truths:
    - "constants.jsx CSS 문자열 내 .portal-expiry-banner 클래스가 존재한다"
    - "constants.jsx CSS 문자열 내 .portal-next-lesson 클래스가 존재한다"
    - "constants.jsx CSS 문자열 내 .portal-week-chip 클래스가 존재한다"
    - "모든 새 CSS 클래스가 CSS 커스텀 프로퍼티 var(--*)만 사용하고 하드코딩 헥스값이 없다"
  artifacts:
    - path: "src/constants.jsx"
      provides: "포털 세션 배너 + 시간표 위젯 CSS 클래스 10개"
      contains: ".portal-expiry-banner"
  key_links:
    - from: "src/constants.jsx"
      to: "src/components/portal/PublicPortal.jsx"
      via: "CSS 클래스명 string 참조"
      pattern: "portal-expiry-banner|portal-next-lesson|portal-week-chip"
---

<objective>
포털 세션 배너(POR-01)와 시간표 위젯(POR-02)에 필요한 CSS 클래스 10개를 `src/constants.jsx`의 CSS 문자열에 추가한다.

Purpose: Wave 2 실행자가 PublicPortal.jsx에 JSX를 추가할 때 CSS 클래스가 이미 존재하도록 선행 처리. 프로젝트 아키텍처 규칙(외부 CSS 파일 없음)을 준수.
Output: constants.jsx CSS 문자열에 두 개의 신규 섹션 블록 추가 — Portal Schedule Widget + Portal Expiry Banner
</objective>

<execution_context>
@C:\Users\GIGABYTE\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GIGABYTE\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-CONTEXT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-UI-SPEC.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-PATTERNS.md

<interfaces>
<!-- constants.jsx CSS 문자열 구조 -->
<!-- 현재 마지막 CSS 클래스는 line 739: .ai-stats-row strong{...} -->
<!-- line 740: `;` (백틱 닫힘) -->
<!-- 모든 새 CSS는 line 739 끝 다음, 백틱 닫힘(line 740 `;`) 앞에 삽입 -->

기존 섹션 블록 패턴 (constants.jsx line 606, 617):
```
/* ── Portal Schedule Widget ─────────────────────────────── */
/* ── Portal Expiry Banner ────────────────────────────────── */
```
블록 주석 형식: `/* ──` + 공백 + 섹션명 + 공백 + `─` 반복 + 공백 + `*/`

사용 가능 CSS 변수 (이미 :root에 선언됨):
- 배경: var(--hanji), var(--gold-lt), var(--blue-lt), var(--paper), var(--bg)
- 테두리: var(--border), var(--gold)
- 텍스트: var(--ink), var(--ink-60), var(--blue), var(--red)
- 반경: var(--radius-lg), var(--radius-sm), var(--radius)
- 그림자: var(--shadow)
- 폰트: 'Noto Serif KR',serif (이미 @import됨)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: constants.jsx CSS 문자열에 포털 CSS 클래스 10개 추가</name>
  <files>src/constants.jsx</files>
  <read_first>
    - src/constants.jsx (line 730-741 — 현재 CSS 문자열 끝부분과 닫힘 위치 확인)
    - .planning/phases/02-portal-completion/02-PATTERNS.md (CSS 섹션 패턴 및 정확한 클래스 정의 확인)
  </read_first>
  <action>
Edit/str_replace를 사용하여 `src/constants.jsx`의 CSS 문자열에 두 섹션을 추가한다.

삽입 위치: line 739의 `.ai-stats-row strong{color:var(--ink);font-weight:700}` 다음, 백틱+세미콜론(`;`) 바로 앞.

str_replace 대상 (`.ai-stats-row strong` 줄 끝에서 백틱 닫힘까지):
```
.ai-stats-row strong{color:var(--ink);font-weight:700}
`;
```

교체 후:
```
.ai-stats-row strong{color:var(--ink);font-weight:700}

/* ── Portal Schedule Widget ─────────────────────────────── */
.portal-next-lesson{background:var(--hanji);border:1px solid var(--border);border-radius:var(--radius-lg);padding:16px;box-shadow:var(--shadow)}
.portal-next-lesson-inst{font-family:'Noto Serif KR',serif;font-size:14px;font-weight:600;color:var(--blue);margin-bottom:4px}
.portal-next-lesson-time{font-size:20px;font-family:'Noto Serif KR',serif;font-weight:600;color:var(--ink);font-variant-numeric:tabular-nums;line-height:1.2}
.portal-next-lesson-teacher{font-size:12px;color:var(--ink-60);margin-top:8px}
.portal-week-chips{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}
.portal-week-chip{background:var(--blue-lt);color:var(--blue);font-size:12px;padding:4px 12px;border-radius:8px}

/* ── Portal Expiry Banner ────────────────────────────────── */
.portal-expiry-banner{background:var(--gold-lt);border-bottom:2px solid var(--gold);padding:12px 16px;display:flex;align-items:center;gap:8px;flex-wrap:wrap;position:sticky;top:0;z-index:10}
.portal-expiry-text{font-size:14px;color:var(--ink);flex:1;line-height:1.5}
.portal-expiry-extend{background:var(--blue);color:#fff;font-size:12px;font-weight:600;border-radius:var(--radius-sm);padding:8px 16px;border:none;cursor:pointer;min-height:44px;font-family:inherit}
.portal-expiry-logout{background:none;border:none;color:var(--red);font-size:12px;font-weight:600;cursor:pointer;font-family:inherit;padding:8px;min-height:44px}
`;
```

규칙: 새 CSS 클래스에 하드코딩 헥스값 절대 없음 — 모두 var(--*) 커스텀 프로퍼티만 사용. 예외: `.portal-expiry-extend`의 `color:#fff` 는 흰색 고정값으로 허용 (다크모드에서도 흰색이어야 함).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "portal-expiry-banner" src/constants.jsx` → 1 이상
    - `grep -c "portal-next-lesson" src/constants.jsx` → 1 이상
    - `grep -c "portal-week-chip" src/constants.jsx` → 1 이상
    - `grep -c "portal-expiry-extend" src/constants.jsx` → 1 이상
    - `grep -c "portal-expiry-logout" src/constants.jsx` → 1 이상
    - `npm run build` 0 exit code
    - 새 CSS 클래스 중 `#[0-9a-fA-F]{3,6}` 패턴이 `#fff` 외에 없음 (grep으로 확인)
  </acceptance_criteria>
  <done>
    constants.jsx에 Portal Schedule Widget 섹션(6클래스)과 Portal Expiry Banner 섹션(4클래스)이 추가되어 있고, npm run build가 통과한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS string → browser render | constants.jsx CSS 문자열은 빌드 시 정적으로 주입됨 — 런타임 위협 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-CSS-01 | Tampering | constants.jsx CSS string | accept | 정적 빌드 아티팩트, 런타임 주입 없음. CI 빌드 검증으로 충분. |
| T-02-CSS-02 | Information Disclosure | CSS 변수명 노출 | accept | CSS 변수는 DevTools에서 이미 노출되는 디자인 토큰. 민감 정보 없음. |
</threat_model>

<verification>
```bash
# 1. 신규 CSS 클래스 존재 확인
grep -c "portal-expiry-banner" src/constants.jsx
grep -c "portal-next-lesson{" src/constants.jsx
grep -c "portal-week-chip{" src/constants.jsx

# 2. 하드코딩 헥스 없음 확인 (fff 제외)
grep "portal-expiry\|portal-next\|portal-week" src/constants.jsx | grep -v "#fff" | grep "#[0-9a-fA-F]"
# → 출력 없으면 PASS

# 3. 빌드 통과
npm run build
```
</verification>

<success_criteria>
- constants.jsx에 10개 신규 CSS 클래스 존재 (portal-next-lesson, portal-next-lesson-inst, portal-next-lesson-time, portal-next-lesson-teacher, portal-week-chips, portal-week-chip, portal-expiry-banner, portal-expiry-text, portal-expiry-extend, portal-expiry-logout)
- 모든 신규 클래스가 CSS 커스텀 프로퍼티 var(--*) 사용 (color:#fff 단독 예외)
- npm run build 0 exit code
</success_criteria>

<output>
After completion, create `.planning/phases/02-portal-completion/02-01-SUMMARY.md`
</output>
