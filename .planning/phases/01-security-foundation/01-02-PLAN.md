---
phase: 01-security-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wrangler.toml
  - functions/api/ai/_utils/ratelimit.js
autonomous: false
requirements:
  - SEC-04

must_haves:
  truths:
    - "강사가 AI 기능을 사용할 때 rate limiter가 KV namespace를 통해 실제로 동작한다"
    - "KV namespace가 바인딩되지 않은 상태에서 AI 엔드포인트 호출 시 500 오류가 반환된다 (기존: 모든 요청 허용)"
  artifacts:
    - path: "wrangler.toml"
      provides: "RATE_LIMIT_KV 바인딩 설정"
      contains: "RATE_LIMIT_KV"
    - path: "functions/api/ai/_utils/ratelimit.js"
      provides: "KV null 시 throw하는 checkRateLimit"
      contains: "throw new Error"
  key_links:
    - from: "functions/api/ai/_middleware.js"
      to: "functions/api/ai/_utils/ratelimit.js"
      via: "checkRateLimit(env.RATE_LIMIT_KV, userId)"
      pattern: "checkRateLimit"
    - from: "wrangler.toml"
      to: "env.RATE_LIMIT_KV"
      via: "[[kv_namespaces]] binding"
      pattern: "RATE_LIMIT_KV"

user_setup:
  - service: cloudflare-kv
    why: "Rate limiter KV namespace — Cloudflare 계정에서 직접 생성해야 함"
    steps:
      - task: "KV namespace 생성"
        command: "npx wrangler kv namespace create RATE_LIMIT_KV"
        location: "터미널 (프로젝트 루트에서 실행)"
        note: "출력에서 'id = \"...\"' 값을 복사해 wrangler.toml의 id 필드에 입력"
      - task: "Preview KV namespace 생성 (로컬 개발용)"
        command: "npx wrangler kv namespace create RATE_LIMIT_KV --preview"
        location: "터미널"
        note: "출력에서 'id = \"...\"' 값을 복사해 wrangler.toml의 preview_id 필드에 입력"
---

<objective>
Cloudflare KV namespace를 wrangler.toml에 바인딩하고 ratelimit.js의 null guard를 강화하여 rate limiter를 실제로 활성화한다 (SEC-04).

Purpose: 현재 `if (!kv) return true` 패턴은 KV가 바인딩되지 않으면 모든 AI 요청을 무제한 허용한다. KV namespace를 생성·바인딩하고 null 시 throw하도록 변경해 rate limiting이 항상 작동하도록 보장한다.

Output: 수정된 wrangler.toml + ratelimit.js
</objective>

<execution_context>
@C:\Users\GIGABYTE\Coding\rye-k\CLAUDE.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md

<interfaces>
<!-- wrangler.toml 현재 내용 (전체) -->
```toml
name = "rye-k-center"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"
```
<!-- KV 바인딩이 전혀 없음 -->

<!-- functions/api/ai/_utils/ratelimit.js 현재 코드 -->
```javascript
// KV key: rl:{uid}:{minute_bucket} — TTL 2 minutes
export async function checkRateLimit(kv, userId, limit = 20) {
  if (!kv) return true; // KV not bound (local dev) — allow  ← 이 줄 교체
  const bucket = Math.floor(Date.now() / 60000);
  const key = `rl:${userId}:${bucket}`;
  const val = await kv.get(key);
  const count = val ? parseInt(val, 10) : 0;
  if (count >= limit) return false;
  await kv.put(key, String(count + 1), { expirationTtl: 120 });
  return true;
}
```

<!-- functions/api/ai/_middleware.js — checkRateLimit 호출 방식 참조용 -->
<!-- checkRateLimit(env.RATE_LIMIT_KV, userId) 형태로 호출됨 -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>이 체크포인트는 Claude가 실행할 수 없는 Cloudflare 대시보드 작업이다. KV namespace ID를 얻어야 wrangler.toml을 올바르게 작성할 수 있다.</what-built>
  <how-to-verify>
프로젝트 루트 디렉터리(C:\Users\GIGABYTE\Coding\rye-k)에서 아래 두 명령을 순서대로 실행하고 각 명령의 출력에서 ID를 메모한다:

**Step 1 — Production KV namespace 생성:**
```
npx wrangler kv namespace create RATE_LIMIT_KV
```
출력 예시:
```
Add the following to your configuration file in your kv_namespaces array:
{ binding = "RATE_LIMIT_KV", id = "abcdef1234567890abcdef1234567890" }
```
→ `id` 값 복사

**Step 2 — Preview KV namespace 생성 (로컬 개발용):**
```
npx wrangler kv namespace create RATE_LIMIT_KV --preview
```
출력 예시:
```
{ binding = "RATE_LIMIT_KV", id = "preview_abcdef1234567890" }
```
→ `id` 값 복사 (이것이 preview_id)

두 ID를 준비했으면 아래 resume signal에 입력한다.
  </how-to-verify>
  <resume-signal>
"KV 생성 완료. production_id=xxxxx preview_id=yyyyy" 형식으로 입력하거나, 아이디 두 개를 텍스트로 제공하면 다음 Task에서 wrangler.toml을 작성한다.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: wrangler.toml — KV 바인딩 추가</name>
  <files>wrangler.toml</files>
  <action>
체크포인트에서 Nick이 제공한 KV namespace ID 두 개를 사용해 wrangler.toml에 [[kv_namespaces]] 섹션을 추가한다.

최종 wrangler.toml 내용:
```toml
name = "rye-k-center"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"

[[kv_namespaces]]
binding = "RATE_LIMIT_KV"
id = "<Nick이 제공한 production_id>"
preview_id = "<Nick이 제공한 preview_id>"
```

주의: `[[kv_namespaces]]` 섹션은 파일 끝에 추가하고 기존 3줄은 그대로 유지한다.
  </action>
  <verify>
    <automated>grep -n "RATE_LIMIT_KV" wrangler.toml</automated>
  </verify>
  <done>grep 결과에 binding = "RATE_LIMIT_KV", id, preview_id 3줄이 포함됨</done>
</task>

<task type="auto">
  <name>Task 2: ratelimit.js — null guard → throw 변경</name>
  <files>functions/api/ai/_utils/ratelimit.js</files>
  <action>
`if (!kv) return true;` 줄을 `if (!kv) throw new Error(...)` 로 교체한다.

현재 (line 3):
```javascript
  if (!kv) return true; // KV not bound (local dev) — allow
```

변경 후:
```javascript
  if (!kv) throw new Error("RATE_LIMIT_KV 바인딩이 없습니다 — wrangler.toml의 [[kv_namespaces]] 설정을 확인하세요");
```

이렇게 하면 KV가 바인딩되지 않은 환경에서 AI 엔드포인트를 호출할 경우 Worker가 500 오류를 반환한다. 로컬 개발 시(`wrangler dev`)에도 preview_id가 설정되어 있으면 정상 동작한다.
  </action>
  <verify>
    <automated>grep -n "return true" functions/api/ai/_utils/ratelimit.js</automated>
  </verify>
  <done>grep 결과 0줄 — `return true` (null guard 허용 패턴) 없음. `throw new Error` 가 포함된 줄이 존재.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/ai/* Worker | AI 엔드포인트 호출 — rate limiter가 없으면 무제한 요청 가능 |
| wrangler.toml → Cloudflare runtime | KV namespace 바인딩이 없으면 runtime에서 env.RATE_LIMIT_KV가 undefined |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | /api/ai/* Worker (rate limiter) | mitigate | KV null 시 throw: 바인딩 없으면 AI 요청 자체가 실패하므로 무제한 허용 경로 차단 |
| T-02-02 | Elevation of Privilege | wrangler.toml KV ID | accept | KV namespace ID는 공개 레포에 커밋되어도 무방 — 읽기/쓰기에는 Cloudflare API 토큰 필요 |
</threat_model>

<verification>
1. `grep -n "RATE_LIMIT_KV" wrangler.toml` → binding, id, preview_id 3줄 출력
2. `grep -n "return true" functions/api/ai/_utils/ratelimit.js` → 0줄
3. `grep -n "throw new Error" functions/api/ai/_utils/ratelimit.js` → 1줄 출력
4. `npm run build` → 통과 (ratelimit.js는 Worker 코드이므로 Vite 빌드에는 포함되지 않음)
</verification>

<success_criteria>
- wrangler.toml에 [[kv_namespaces]] 섹션 존재, binding="RATE_LIMIT_KV", id, preview_id 설정됨
- ratelimit.js의 null guard가 `throw new Error`로 변경됨
- `return true` (silent allow) 패턴 없음
</success_criteria>

<output>
완료 후 `.planning/phases/01-security-foundation/01-02-SUMMARY.md` 생성
</output>
