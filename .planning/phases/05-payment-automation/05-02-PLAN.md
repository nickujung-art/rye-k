---
phase: 05-payment-automation
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/payment/PaymentsView.jsx
autonomous: true
requirements:
  - PAY-01
  - PAY-03
  - PAY-06
  - ALM-07

must_haves:
  truths:
    - "관리자가 수납 화면 회원 행의 수강료 셀을 클릭하면 인라인 input이 활성화된다"
    - "Tab/Enter 키로 다음 행 인라인 input으로 포커스가 이동한다"
    - "onBlur 시 updateStudentDoc (per-op 트랜잭션) 으로 저장되며 saveStudents는 절대 호출되지 않는다"
    - "수납 화면 상단에 '수납 관리' / '미매칭 입금' 탭이 존재하며 미매칭 개수 배지가 표시된다"
    - "미매칭 탭에서 입금자명+금액 확인 후 학생 선택 → '수납 처리' 버튼으로 수동 매칭 완료된다"
    - "미납 회원 행에 💬 버튼이 존재하고 클릭 시 alimtalkModal을 열되 onSend는 no-op stub이다"
    - "initFilterUnpaid=true 로 PaymentsView가 마운트되면 filterUnpaid가 true로 초기화된다"
  artifacts:
    - path: "src/components/payment/PaymentsView.jsx"
      provides: "인라인 수강료 편집, 미매칭 탭, ALM-07 stub, initFilterUnpaid 초기화"
      exports: ["default function PaymentsView"]
  key_links:
    - from: "PaymentsView fee input onBlur"
      to: "onSaveStudents callback"
      via: "onSaveStudents([{ ...s, monthlyFee: feeEdits[s.id] }]) — routes to batchStudentDocs"
      pattern: "onSaveStudents"
    - from: "UnmatchedPaymentsTab match button"
      to: "onSaveUnmatched"
      via: "매칭 완료 후 matchedAt/matchedStudentId 채운 배열을 onSaveUnmatched에 전달"
      pattern: "onSaveUnmatched"
    - from: "💬 stub button"
      to: "setAlimtalkModal"
      via: "setAlimtalkModal('unpaid_reminder') — existing state"
      pattern: "unpaid_reminder"
---

<objective>
Add all three React-side payment features to PaymentsView.jsx:
- PAY-01: 인라인 수강료 편집 (스프레드시트 방식, Tab/Enter 이동, updateStudentDoc 저장)
- PAY-03: initFilterUnpaid prop 으로 Dashboard → PaymentsView 미납 필터 초기화
- PAY-06: '미매칭 입금' 탭 + UnmatchedPaymentsTab 서브 컴포넌트 (수동 매칭 UI)
- ALM-07: 미납 행 💬 버튼 스텁 (onSend = no-op, Phase 4 후 활성화)

Purpose: 관리자가 수납 화면 하나에서 수강료 입력, 미매칭 입금 처리, 미납 리마인더
발송(스텁)을 모두 처리할 수 있다.

Output: 수정된 PaymentsView.jsx — 기존 기능 100% 보존, 신규 기능 추가.
</objective>

<execution_context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\05-payment-automation\execute-plan.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\05-payment-automation\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\PROJECT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\STATE.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\05-payment-automation\05-01-SUMMARY.md

<interfaces>
<!-- Critical: read PaymentsView.jsx before editing. Key contracts: -->

<!-- Props signature (line 9): -->
```javascript
export default function PaymentsView({
  students, teachers, currentUser, payments, onSavePayments, onLog,
  attendance = [], onSaveStudents
}) {
```
<!-- Plan 01 added: unmatchedPayments, onSaveUnmatched, initFilterUnpaid, onMountFilterConsumed -->

<!-- Existing state block (lines 10-28): -->
```javascript
const [month, setMonth] = useState(THIS_MONTH);
const [filterUnpaid, setFilterUnpaid] = useState(false);
// ... (alimtalkModal, quickPayingId, etc.)
const [alimtalkModal, setAlimtalkModal] = useState(null); // null | "monthly_fee" | "unpaid_reminder"
```

<!-- visibleStudents (line 39-42) — used for Tab/Enter navigation index lookup: -->
```javascript
const visibleStudents = (filterTeacher === "all" ? students : ...)
  .filter(s => (s.status || "active") === "active")
  .filter(s => !searchQuery.trim() || s.name.includes(searchQuery.trim()))
  .filter(s => !filterUnpaid || !getPayment(s.id)?.paid);
```

<!-- pay-row render (lines 310-348) — fee input and ALM-07 stub inserted here: -->
```javascript
<div key={s.id} className="pay-row" onClick={() => openEdit(s)} style={...}>
  <Av ... />
  <div style={{flex:1,minWidth:0}}> ... name, pay-status ... </div>
  {/* fee input cell goes HERE (after status div) */}
  {!isTeacher && <div className="pay-amount" ...>{fmtMoney(amt)}</div>}
  {canManageAll(currentUser.role) && !isPaid && (
    <button /* quick-pay button */> ... </button>
  )}
  {/* ALM-07 button goes AFTER quick-pay button */}
</div>
```

<!-- AlimtalkModal render (lines 570-582): -->
```javascript
{alimtalkModal && (
  <AlimtalkModal
    type={alimtalkModal}
    students={visibleStudents}
    month={month}
    getPayment={getPayment}
    onClose={() => setAlimtalkModal(null)}
    onSend={async (type, targets) => {
      await sendAligoMessage(type, targets);
    }}
  />
)}
```

<!-- ftabs CSS already exists in constants.jsx (lines 178-182): .ftabs, .ftab, .ftab.active -->
<!-- fee-inp-cell and unmatched-card CSS added by Plan 01 -->

<!-- BANNED: saveStudents([...]) — throws at runtime. NEVER call this. -->
<!-- BANNED: window.confirm, window.alert — use inline UI. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline fee editor (PAY-01) and initFilterUnpaid (PAY-03) to PaymentsView.jsx</name>
  <files>src/components/payment/PaymentsView.jsx</files>
  <read_first>
    - src/components/payment/PaymentsView.jsx (full file — 767 lines, must read before editing)
  </read_first>
  <action>
Make the following targeted edits to PaymentsView.jsx.

**Edit 1 — Extend props destructuring** (line 9):

Replace:
```javascript
export default function PaymentsView({ students, teachers, currentUser, payments, onSavePayments, onLog, attendance = [], onSaveStudents }) {
```

With:
```javascript
export default function PaymentsView({
  students, teachers, currentUser, payments, onSavePayments, onLog,
  attendance = [], onSaveStudents,
  unmatchedPayments = [],
  onSaveUnmatched,
  initFilterUnpaid = false,
  onMountFilterConsumed,
}) {
```

**Edit 2 — filterUnpaid initial state (PAY-03)** (line 14 — change initial value):

Replace:
```javascript
const [filterUnpaid, setFilterUnpaid] = useState(false);
```

With:
```javascript
const [filterUnpaid, setFilterUnpaid] = useState(initFilterUnpaid);
```

Add useEffect immediately after (line 15 area, inside the function body, before pendingRequestStudents):
```javascript
// PAY-03: consume initFilterUnpaid on mount — reset App.jsx flag so re-entry is clean
useEffect(() => {
  if (initFilterUnpaid && onMountFilterConsumed) onMountFilterConsumed();
}, []); // eslint-disable-line react-hooks/exhaustive-deps
```

Note: `useEffect` must be imported. Check if it's already in the React import on line 1:
`import { useState } from "react";` — change to `import { useState, useEffect } from "react";`

**Edit 3 — Fee edit state variables** (add after line 28, after the last useState in the block):
```javascript
const [feeEdits, setFeeEdits] = useState({});      // { studentId: newFeeValue }
const [savingFeeId, setSavingFeeId] = useState(null); // optimistic loading indicator
const [activeTab, setActiveTab] = useState("payments"); // "payments" | "unmatched"
```

**Edit 4 — Insert tabs UI** (inside the return, after the month-selector `</div>` block but BEFORE the search input `<div className="srch-wrap">`):

```javascript
{/* ── PAY-06: 수납 관리 / 미매칭 입금 탭 ── */}
{canManageAll(currentUser.role) && (
  <div className="ftabs" style={{marginBottom:8}}>
    <button className={`ftab${activeTab==="payments"?" active":""}`}
      onClick={() => setActiveTab("payments")}>수납 관리</button>
    <button className={`ftab${activeTab==="unmatched"?" active":""}`}
      onClick={() => setActiveTab("unmatched")}>
      미매칭 입금
      {unmatchedPayments.filter(u => !u.matchedAt).length > 0 && (
        <span className="unmatched-badge">
          {unmatchedPayments.filter(u => !u.matchedAt).length}
        </span>
      )}
    </button>
  </div>
)}
```

**Edit 5 — Wrap existing payments UI with tab conditional** (wrap everything from the `<div className="srch-wrap"` through the end of the `visibleStudents.map(...)` section with):
```javascript
{activeTab === "payments" && <>
  {/* ... existing search, summary cards, progress bar, student list ... */}
</>}
{activeTab === "unmatched" && canManageAll(currentUser.role) && (
  <UnmatchedPaymentsTab
    unmatchedPayments={unmatchedPayments}
    students={students}
    payments={payments}
    month={month}
    onSavePayments={onSavePayments}
    onSaveUnmatched={onSaveUnmatched}
    onLog={onLog}
    uid={uid}
    TODAY_STR={TODAY_STR}
    autoFee={autoFee}
  />
)}
```

**Edit 6 — Add inline fee input cell inside pay-row** (inside the `visibleStudents.map(s => { ... return (...) })` block, after the `{!isTeacher && <div className="pay-amount"...>}` element):

```javascript
{/* PAY-01: 인라인 수강료 편집 — 관리자/매니저만, 기관 가상회원 제외 */}
{canManageAll(currentUser.role) && !isInst && (
  <div className="fee-inp-cell" onClick={e => e.stopPropagation()}>
    <input
      className="inp"
      inputMode="numeric"
      value={feeEdits[s.id] !== undefined
        ? (feeEdits[s.id] === 0 ? "" : feeEdits[s.id].toLocaleString("ko-KR"))
        : (s.monthlyFee ? s.monthlyFee.toLocaleString("ko-KR") : "")}
      onChange={e => setFeeEdits(f => ({
        ...f,
        [s.id]: parseInt(e.target.value.replace(/[^\d]/g, "")) || 0,
      }))}
      onBlur={async () => {
        if (feeEdits[s.id] === undefined || feeEdits[s.id] === s.monthlyFee) return;
        setSavingFeeId(s.id);
        // CORRECT: per-op via onSaveStudents (routes to batchStudentDocs). NEVER saveStudents.
        await onSaveStudents([{ ...s, monthlyFee: feeEdits[s.id] }]);
        setFeeEdits(f => { const n = { ...f }; delete n[s.id]; return n; });
        setSavingFeeId(null);
      }}
      onKeyDown={e => {
        if (e.key === "Tab" || e.key === "Enter") {
          e.preventDefault();
          const idx = visibleStudents.findIndex(st => st.id === s.id);
          const nextId = visibleStudents[idx + 1]?.id;
          if (nextId) document.querySelector(`[data-fee-input="${nextId}"]`)?.focus();
        }
      }}
      data-fee-input={s.id}
      style={{
        width: 90, height: 28, padding: "3px 7px", fontSize: 12,
        textAlign: "right", opacity: savingFeeId === s.id ? 0.5 : 1,
      }}
    />
    <span style={{ fontSize: 11, color: "var(--ink-30)", flexShrink: 0 }}>원</span>
    {savingFeeId === s.id && <span style={{ fontSize: 10, color: "var(--ink-30)" }}>…</span>}
  </div>
)}
```

**Edit 7 — ALM-07 stub button** (inside the `visibleStudents.map` block, AFTER the existing quick-pay `</button>`, still inside the pay-row):

```javascript
{/* ALM-07: 미납 리마인더 stub — Phase 4 AlimTalk API 연동 후 활성화 */}
{canManageAll(currentUser.role) && !isPaid && !isInst && (
  <button
    onClick={e => {
      e.stopPropagation();
      setAlimtalkModal("unpaid_reminder");
    }}
    style={{
      background: "var(--blue-lt)", color: "var(--blue)",
      border: "1px solid var(--blue)", borderRadius: 8,
      padding: "5px 9px", fontSize: 11, fontWeight: 700,
      flexShrink: 0, cursor: "pointer", fontFamily: "inherit",
      whiteSpace: "nowrap",
    }}
    title="ALM-07: Phase 4 AlimTalk 연동 후 활성화"
  >💬</button>
)}
```

**Edit 8 — Patch AlimtalkModal onSend to stub** (replace the onSend prop at lines 578-580):

Replace:
```javascript
onSend={async (type, targets) => {
  await sendAligoMessage(type, targets);
}}
```

With:
```javascript
onSend={async (type, targets) => {
  if (type === "unpaid_reminder") {
    // ALM-07 stub — Phase 4 AlimTalk API 심사 전. 실제 발송 없음.
    onLog("ALM-07 stub: Phase 4 AlimTalk 연동 후 활성화됩니다");
    setAlimtalkModal(null);
    return;
  }
  await sendAligoMessage(type, targets);
}}
```

CRITICAL rules:
- NEVER use saveStudents([...]) — use onSaveStudents callback which routes to batchStudentDocs
- NEVER use window.confirm / window.alert
- e.stopPropagation() is REQUIRED on the fee input wrapper div (Pitfall 3: row click opens modal)
- isInst check is REQUIRED on fee editor (institution virtual members are read-only)
  </action>
  <verify>
    <automated>cd /c/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "feeEdits" src/components/payment/PaymentsView.jsx shows at least 4 matches (useState, setFeeEdits in onChange, onBlur check, delete)
    - grep -n "data-fee-input" src/components/payment/PaymentsView.jsx shows exactly 2 matches (onKeyDown querySelector call and attribute on input)
    - grep -n "activeTab" src/components/payment/PaymentsView.jsx shows at least 4 matches
    - grep -n "unmatched" src/components/payment/PaymentsView.jsx shows at least 3 matches (tab button, badge, tab conditional)
    - grep -n "unpaid_reminder" src/components/payment/PaymentsView.jsx shows at least 2 matches (button onClick and stub onSend)
    - grep -n "saveStudents" src/components/payment/PaymentsView.jsx should return 0 matches (banned)
    - grep -n "initFilterUnpaid" src/components/payment/PaymentsView.jsx shows at least 2 matches
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    - Inline fee editor active in pay-row for admin/manager, not institution members
    - Tab/Enter keyboard navigation functional
    - Tabs UI renders with unmatched badge
    - ALM-07 💬 button present on unpaid rows
    - initFilterUnpaid initializes filterUnpaid correctly
    - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add UnmatchedPaymentsTab sub-component to PaymentsView.jsx (PAY-06)</name>
  <files>src/components/payment/PaymentsView.jsx</files>
  <read_first>
    - src/components/payment/PaymentsView.jsx (read lines 1-10 for imports; read around line 765 for the closing brace to find the insertion point for the sub-component)
  </read_first>
  <action>
Add the `UnmatchedPaymentsTab` function component to PaymentsView.jsx BEFORE the
`export default function PaymentsView(...)` declaration (at the top of the file, after
the imports, OR after the closing brace of PaymentsView — either location works since
it's referenced inside PaymentsView's render return).

Recommended placement: Add AFTER the closing `}` of `PaymentsView` (the very last `}` in
the file), but BEFORE any `export` — though PaymentsView is already the default export, so
add it as a plain function declaration at the bottom of the file.

```javascript
// ── UnmatchedPaymentsTab ─────────────────────────────────────────────────────
function UnmatchedPaymentsTab({
  unmatchedPayments, students, payments, month,
  onSavePayments, onSaveUnmatched, onLog, uid, TODAY_STR, autoFee,
}) {
  const [matchingId, setMatchingId] = useState(null);   // which unmatched item is being matched
  const [selectedStudentId, setSelectedStudentId] = useState({});  // { unmatchedId: studentId }
  const pending = unmatchedPayments.filter(u => !u.matchedAt);
  const matched = unmatchedPayments.filter(u => u.matchedAt);

  if (pending.length === 0 && matched.length === 0) {
    return (
      <div className="empty" style={{paddingTop:40}}>
        <div className="empty-txt">미매칭 입금 내역이 없습니다.</div>
        <div style={{fontSize:12,color:"var(--ink-30)",marginTop:6}}>
          카카오뱅크 Webhook이 자동 연결되면 미매칭 입금이 여기에 표시됩니다.
        </div>
      </div>
    );
  }

  const handleMatch = async (unmatched) => {
    const sid = selectedStudentId[unmatched.id];
    if (!sid) return;
    const s = students.find(st => st.id === sid);
    if (!s) return;

    setMatchingId(unmatched.id);
    try {
      // 1. 수납 레코드 생성
      const existing = payments.find(p => p.studentId === sid && p.month === month);
      const amount = unmatched.amount || (autoFee ? autoFee(s) : (s.monthlyFee || 0));
      const record = {
        ...(existing || {}),
        id: existing?.id || uid(),
        studentId: sid,
        month,
        amount,
        paid: true,
        paidAmount: unmatched.amount || amount,
        paidDate: TODAY_STR,
        method: "transfer",
        note: `미매칭 수동 매칭 — 입금자: ${unmatched.senderName}`,
        extraCharges: existing?.extraCharges || [],
        createdAt: existing?.createdAt || Date.now(),
        updatedAt: Date.now(),
      };
      const updPayments = existing
        ? payments.map(p => p.id === existing.id ? record : p)
        : [...payments, record];
      await onSavePayments(updPayments);

      // 2. 미매칭 레코드 matchedAt 기록
      const updUnmatched = unmatchedPayments.map(u =>
        u.id === unmatched.id
          ? { ...u, matchedAt: Date.now(), matchedStudentId: sid }
          : u
      );
      await onSaveUnmatched(updUnmatched);

      onLog(`미매칭 입금 수동 매칭 완료 — ${unmatched.senderName} → ${s.name} ${(unmatched.amount || 0).toLocaleString()}원`);
      setSelectedStudentId(prev => { const n = { ...prev }; delete n[unmatched.id]; return n; });
    } catch (e) {
      onLog("미매칭 매칭 실패: " + e.message);
    } finally {
      setMatchingId(null);
    }
  };

  return (
    <div>
      {pending.length > 0 && (
        <>
          <div style={{fontSize:12,color:"var(--ink-60)",marginBottom:8,fontWeight:600}}>
            미처리 입금 {pending.length}건
          </div>
          {pending.map(u => (
            <div key={u.id} className="unmatched-card">
              <div style={{flex:1,minWidth:0}}>
                <div style={{fontSize:13.5,fontWeight:700}}>{u.senderName || "알 수 없음"}</div>
                <div style={{fontSize:12,color:"var(--green)",fontWeight:600}}>
                  {(u.amount || 0).toLocaleString()}원
                </div>
                <div style={{fontSize:11,color:"var(--ink-30)"}}>
                  {u.timestamp ? new Date(u.timestamp).toLocaleString("ko-KR", {month:"numeric",day:"numeric",hour:"2-digit",minute:"2-digit"}) : ""}
                  {u.rawText ? ` · ${u.rawText.slice(0, 30)}` : ""}
                </div>
              </div>
              <div style={{display:"flex",flexDirection:"column",gap:6,alignItems:"flex-end"}}>
                <select
                  className="sel"
                  style={{fontSize:12,height:30,minWidth:120}}
                  value={selectedStudentId[u.id] || ""}
                  onChange={e => setSelectedStudentId(prev => ({ ...prev, [u.id]: e.target.value }))}
                >
                  <option value="">학생 선택</option>
                  {students
                    .filter(s => !s.isInstitution && (s.status || "active") === "active")
                    .sort((a, b) => a.name.localeCompare(b.name, "ko"))
                    .map(s => (
                      <option key={s.id} value={s.id}>{s.name}</option>
                    ))
                  }
                </select>
                <button
                  className="btn btn-sm"
                  style={{
                    background: selectedStudentId[u.id] ? "var(--green)" : "var(--ink-10)",
                    color: selectedStudentId[u.id] ? "#fff" : "var(--ink-30)",
                    border: "none", opacity: matchingId === u.id ? 0.5 : 1,
                  }}
                  disabled={!selectedStudentId[u.id] || !!matchingId}
                  onClick={() => handleMatch(u)}
                >
                  {matchingId === u.id ? "처리 중…" : "✓ 수납 처리"}
                </button>
              </div>
            </div>
          ))}
        </>
      )}
      {matched.length > 0 && (
        <>
          <div style={{fontSize:12,color:"var(--ink-30)",margin:"16px 0 8px",fontWeight:600}}>
            처리 완료 {matched.length}건
          </div>
          {matched.map(u => {
            const s = students.find(st => st.id === u.matchedStudentId);
            return (
              <div key={u.id} className="unmatched-card" style={{opacity:0.6}}>
                <div style={{flex:1,minWidth:0}}>
                  <div style={{fontSize:13,fontWeight:600}}>{u.senderName || "알 수 없음"}</div>
                  <div style={{fontSize:11,color:"var(--ink-60)"}}>
                    {(u.amount || 0).toLocaleString()}원
                    {s ? ` → ${s.name}` : ""}
                  </div>
                </div>
                <span style={{fontSize:11,color:"var(--green)",fontWeight:700}}>✓ 매칭 완료</span>
              </div>
            );
          })}
        </>
      )}
    </div>
  );
}
```

CRITICAL: This function uses `useState` — it is declared inside the same file as PaymentsView
which already imports `useState`. The function is a plain function declaration (not exported),
so it can reference `useState` from the file-level import.

NEVER use window.alert or window.confirm in this component. The error feedback goes through
`onLog` (activity log).
  </action>
  <verify>
    <automated>cd /c/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "UnmatchedPaymentsTab" src/components/payment/PaymentsView.jsx shows at least 2 matches (function declaration + usage in PaymentsView render)
    - grep -n "handleMatch" src/components/payment/PaymentsView.jsx shows at least 2 matches
    - grep -n "matchedAt" src/components/payment/PaymentsView.jsx shows at least 2 matches
    - grep -n "window.alert\|window.confirm" src/components/payment/PaymentsView.jsx returns 0 matches
    - grep -n "saveStudents" src/components/payment/PaymentsView.jsx returns 0 matches (banned)
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    - UnmatchedPaymentsTab renders pending unmatched items with student selector and match button
    - Matched items show in "처리 완료" section
    - Empty state displayed when no unmatched payments exist
    - Build passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → onSaveStudents | Per-op fee update crosses the App.jsx batchStudentDocs boundary |
| Browser → onSaveUnmatched | Unmatched payment matching writes to Firestore via App.jsx saveUnmatchedPayments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing | onSaveStudents fee update | accept | Runs under existing Firebase Auth session — same trust as all student updates |
| T-05-02-02 | Tampering | Inline fee input — numeric validation | mitigate | `parseInt(e.target.value.replace(/[^\d]/g, "")) \|\| 0` strips non-numeric input at onChange |
| T-05-02-03 | Tampering | ALM-07 stub onSend bypass | accept | Stub explicitly blocks sendAligoMessage when type==="unpaid_reminder". Phase 4 re-enables. |
| T-05-02-04 | Denial of Service | UnmatchedPaymentsTab matchingId guard | mitigate | `!!matchingId` disables button during in-flight request — prevents duplicate submissions |
| T-05-02-05 | Information Disclosure | unmatched-card rawText display | mitigate | rawText sliced to 30 chars — limits notification text exposure in UI |
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` exits 0
2. Browser: open 수납 관리 — '수납 관리' / '미매칭 입금' tabs visible (admin)
3. Browser: click a fee cell in pay-row — inline input activates, blur saves
4. Browser: Tab key moves to next row's fee input
5. Browser: unpaid row has 💬 button — clicking opens alimtalkModal in stub mode
6. Browser: navigate from Dashboard 미납 카드 — PaymentsView opens with filterUnpaid=true
</verification>

<success_criteria>
- 수납 화면 수강료 셀 인라인 편집 동작
- Tab/Enter 다음 행 이동 동작
- updateStudentDoc(per-op) 저장 — saveStudents 호출 없음 (검증: grep 0건)
- '미매칭 입금' 탭 전환 및 UnmatchedPaymentsTab 렌더링
- 미납 행 💬 버튼 → alimtalkModal("unpaid_reminder") 열림, onSend는 no-op
- initFilterUnpaid=true 마운트 시 미납 필터 활성화
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-payment-automation/05-02-SUMMARY.md`
</output>
