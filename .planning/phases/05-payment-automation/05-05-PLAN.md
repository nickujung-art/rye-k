---
phase: 05-payment-automation
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-04"]
files_modified:
  - functions/api/payments/sync-students.js
  - src/App.jsx
autonomous: true
requirements: [PAY-04, PAY-05]
gap_closure: true

must_haves:
  truths:
    - "카카오뱅크 입금 알림이 도착하면 students_cache에서 학생 이름을 조회해 자동 매칭된다"
    - "PaymentsView를 열 때마다 활성 학생 목록이 KV에 동기화된다"
    - "teacher 역할은 students_cache를 갱신할 수 없다 (403)"
    - "KV sync 실패는 drain 동작을 막지 않는다 (silent fail)"
  artifacts:
    - path: "functions/api/payments/sync-students.js"
      provides: "POST /api/payments/sync-students — KV students_cache 갱신 엔드포인트"
      exports: ["onRequest"]
    - path: "src/App.jsx"
      provides: "drainPending 내 sync-students 선행 호출"
      contains: "sync-students"
  key_links:
    - from: "src/App.jsx (drainPending)"
      to: "/api/payments/sync-students"
      via: "POST fetch — drain 직전에 실행"
      pattern: "sync-students"
    - from: "functions/api/payments/kakaobank-webhook.js"
      to: "env.RATE_LIMIT_KV.get(\"students_cache\")"
      via: "sync-students.js가 PUT한 KV 값"
      pattern: "students_cache"
---

<objective>
students_cache KV 갭 해소: kakaobank-webhook POST 핸들러가 fuzzy match에 사용하는 `students_cache` KV 키를 실제로 채우는 경로를 구현한다.

Purpose: 현재 students는 항상 [] → 모든 입금이 unmatched로 기록됨. 이 갭을 닫으면 PAY-04/05 자동 매칭이 실제로 동작한다.
Output: functions/api/payments/sync-students.js (신규) + src/App.jsx drainPending 수정
</objective>

<execution_context>
@C:\Users\GIGABYTE\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GIGABYTE\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\CLAUDE.md
@C:\Users\GIGABYTE\Coding\rye-k\functions\api\payments\kakaobank-webhook.js
@C:\Users\GIGABYTE\Coding\rye-k\functions\api\ai\_utils\auth.js
@C:\Users\GIGABYTE\Coding\rye-k\src\App.jsx

<interfaces>
<!-- 아래 인터페이스는 실행 전 파악한 실제 코드 기준. 별도 탐색 불필요. -->

/* functions/api/ai/_utils/auth.js */
export async function verifyToken(request): Promise<JWTPayload | null>
// Authorization: Bearer {idToken} 헤더 검증
// 실패 시 null 반환. payload.sub 있어야 유효.
// payload에 role 커스텀 클레임 없음 — 역할은 payload.email 또는 payload.firebase.sign_in_provider로 판단 불가.
// 역할은 body.role로 클라이언트가 전달, 또는 별도 Firestore 조회 필요.
// → 이 엔드포인트에서는 역할 체크를 body.role 필드로 받는다 (App.jsx가 user.role을 전송).

/* kakaobank-webhook.js json helper (재사용 패턴) */
function json(data, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { "content-type": "application/json", "Access-Control-Allow-Origin": "*" },
  });
}

/* App.jsx — drainPending useEffect (line 492–547) */
useEffect(() => {
  if (view !== "payments") return;
  const drainPending = async () => {
    try {
      const currentUser = auth.currentUser;
      if (!currentUser) return;
      const token = await currentUser.getIdToken();
      // ... GET /api/payments/kakaobank-webhook ...
    } catch { /* silent */ }
  };
  drainPending();
}, [view]);

/* App.jsx — students 상태 (line 224) */
const [students, setStudents] = useState([]);
// 타입: { id, name, status, isInstitution?, ... }[]

/* App.jsx — user 상태 */
// user.role: "admin" | "manager" | "teacher"
// canManageAll(user.role) → admin|manager만 true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: functions/api/payments/sync-students.js 신규 생성</name>
  <files>functions/api/payments/sync-students.js</files>

  <read_first>
    - functions/api/payments/kakaobank-webhook.js — json() 헬퍼 패턴, CORS preflight 패턴, verifyToken import 방식 확인
    - functions/api/ai/_utils/auth.js — verifyToken 시그니처 확인
  </read_first>

  <action>
아래 스펙을 그대로 구현한다. 패턴은 kakaobank-webhook.js와 동일하게 유지.

**파일 경로:** `functions/api/payments/sync-students.js`

**구현 내용:**

```js
// functions/api/payments/sync-students.js
// POST /api/payments/sync-students
// 브라우저 앱 → Worker → KV students_cache 갱신
// Auth: Firebase JWT Bearer + body.role ("admin"|"manager"만 허용)

import { verifyToken } from "../ai/_utils/auth.js";

export async function onRequest(context) {
  const { request, env } = context;
  const method = request.method;

  // CORS preflight
  if (method === "OPTIONS") {
    return new Response(null, {
      status: 204,
      headers: {
        "Access-Control-Allow-Origin": "*",
        "Access-Control-Allow-Methods": "POST, OPTIONS",
        "Access-Control-Allow-Headers": "Content-Type, Authorization",
      },
    });
  }

  if (method !== "POST") return new Response("Method Not Allowed", { status: 405 });

  // 1. Firebase JWT 인증
  const payload = await verifyToken(request);
  if (!payload) return json({ error: "Unauthorized" }, 401);

  // 2. Body 파싱
  let body;
  try { body = await request.json(); }
  catch { return json({ error: "Bad Request" }, 400); }

  // 3. 역할 체크 — admin/manager만 허용
  const role = String(body.role || "").toLowerCase();
  if (role !== "admin" && role !== "manager") {
    return json({ error: "Forbidden" }, 403);
  }

  // 4. 학생 목록 수신 및 필터
  const raw = Array.isArray(body.students) ? body.students : [];
  const filtered = raw
    .filter(s => s && typeof s.id === "string" && typeof s.name === "string")
    .filter(s => !s.isInstitution && (s.status || "active") === "active")
    .map(s => ({ id: s.id, name: s.name, status: s.status || "active" }));

  // 5. KV 저장 — TTL 24h (webhook이 같은 TTL 사용)
  try {
    await env.RATE_LIMIT_KV.put(
      "students_cache",
      JSON.stringify(filtered),
      { expirationTtl: 86400 }
    );
  } catch (e) {
    return json({ error: "KV write failed", detail: e.message }, 500);
  }

  return json({ ok: true, count: filtered.length });
}

function json(data, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: {
      "content-type": "application/json",
      "Access-Control-Allow-Origin": "*",
    },
  });
}
```

**주의사항:**
- `isInstitution` 필드는 반드시 필터로 제거 (가상회원 캐시 오염 방지)
- KV에 저장하는 객체는 `{ id, name, status }` 3개 필드만 (PII 최소화 — phone, birthDate 등 제외)
- verifyToken이 null 반환 시 즉시 401 (anonymous 포함)
- teacher 역할(body.role === "teacher")은 403 반환
  </action>

  <verify>
    <automated>cd C:\Users\GIGABYTE\Coding\rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - 파일 `functions/api/payments/sync-students.js`가 존재한다
    - `grep -c "students_cache" functions/api/payments/sync-students.js` 출력 >= 1
    - `grep -c "expirationTtl: 86400" functions/api/payments/sync-students.js` 출력 >= 1
    - `grep -c "Forbidden" functions/api/payments/sync-students.js` 출력 >= 1 (teacher 403 처리)
    - `grep -c "isInstitution" functions/api/payments/sync-students.js` 출력 >= 1 (기관 필터)
    - `npm run build` 빌드 오류 없음
  </acceptance_criteria>

  <done>sync-students.js 신규 파일 생성 완료, 빌드 통과</done>
</task>

<task type="auto">
  <name>Task 2: App.jsx drainPending — sync-students 선행 호출 추가</name>
  <files>src/App.jsx</files>

  <read_first>
    - src/App.jsx line 488–547 — 기존 drainPending useEffect 전체 구조 확인
    - src/App.jsx line 224 — students 상태 변수명(`students`) 확인
    - src/App.jsx line 258–260 — canManageAll import 확인
  </read_first>

  <action>
drainPending async 함수 내부 **맨 앞**에 sync-students POST 호출을 삽입한다.

**수정 위치:** `src/App.jsx` line 494–498 사이 (drainPending 함수 시작 직후, 기존 token 취득 전)

**기존 코드 (변경하지 않음):**
```js
const drainPending = async () => {
  try {
    const currentUser = auth.currentUser;
    if (!currentUser) return;
    const token = await currentUser.getIdToken();
    const res = await fetch("/api/payments/kakaobank-webhook", {
```

**변경 후 (삽입 부분만 표시):**
```js
const drainPending = async () => {
  try {
    const currentUser = auth.currentUser;
    if (!currentUser) return;
    const token = await currentUser.getIdToken();

    // PAY-04/05 gap: students_cache KV 갱신 — drain 직전에 실행
    // 실패해도 drain을 막지 않는다 (best-effort).
    if (canManageAll(user?.role)) {
      try {
        await fetch("/api/payments/sync-students", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${token}`,
          },
          body: JSON.stringify({
            role: user.role,
            students: students.map(s => ({
              id: s.id,
              name: s.name,
              status: s.status || "active",
              isInstitution: s.isInstitution || false,
            })),
          }),
        });
      } catch { /* silent — cache sync failure must not block drain */ }
    }

    const res = await fetch("/api/payments/kakaobank-webhook", {
```

**주의사항:**
- `canManageAll(user?.role)` 조건으로 teacher는 호출 자체를 건너뜀
- fetch는 await하되, 전체를 별도 try-catch로 감싸 silent fail 보장
- students 상태 변수는 현재 스코프에서 직접 참조 (App.jsx 클로저 내)
- user?.role은 옵셔널 체이닝 사용 (user가 null일 수 있는 경우 방어)
- 기존 drain GET 로직은 한 글자도 수정하지 않는다
- 이 블록 삽입 외 App.jsx의 다른 코드는 일절 건드리지 않는다
  </action>

  <verify>
    <automated>cd C:\Users\GIGABYTE\Coding\rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "sync-students" src/App.jsx` 출력 >= 1
    - `grep -c "canManageAll" src/App.jsx` 출력 >= 2 (기존 2개 + 삽입 1개 = 최소 3이어도 무방)
    - `grep -n "sync-students" src/App.jsx` 출력이 drainPending 함수 내부 라인을 가리킨다
    - `grep -c "students_cache" src/App.jsx` 출력 == 0 (App.jsx는 KV를 직접 쓰지 않는다 — Worker가 담당)
    - `npm run build` 빌드 오류 없음
  </acceptance_criteria>

  <done>drainPending 내 sync-students POST 호출 삽입 완료, 빌드 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → sync-students Worker | Firebase JWT 인증 필수. teacher 역할 차단 |
| sync-students body.students[] | 배열 길이·필드 타입 검증. isInstitution 필터로 가상회원 차단 |
| KV students_cache → webhook fuzzy match | 캐시 오염 시 오매칭 발생. TTL 24h로 stale 데이터 자동 만료 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Spoofing | POST /api/payments/sync-students | mitigate | verifyToken으로 Firebase JWT 필수 검증 — anonymous 포함 null 반환 시 401 |
| T-05-05-02 | Tampering | body.students[] — 오염된 학생 목록 삽입 | mitigate | isInstitution 필터 + status==="active" 필터 + id/name 문자열 타입 검증 |
| T-05-05-03 | Elevation of Privilege | teacher 역할이 students_cache 갱신 시도 | mitigate | body.role !== "admin" && !== "manager" → 403 Forbidden |
| T-05-05-04 | Information Disclosure | KV에 PII(phone, birthDate) 저장 | mitigate | map()으로 { id, name, status } 3개 필드만 추출하여 저장 |
| T-05-05-05 | Denial of Service | 거대한 students 배열로 KV 부하 | accept | 실운영 학생 수 최대 수백명 수준 — KV value 크기 제한(25MB)에 여유. rate limit 미추가(브라우저 앱 호출만) |
</threat_model>

<verification>
## 통합 동작 검증 (빌드 후 로컬 dev에서 확인)

1. `npm run dev` 실행
2. admin 계정으로 로그인 → PaymentsView 이동
3. 브라우저 DevTools Network 탭에서 `/api/payments/sync-students` POST 요청 확인
   - Request body: `{ role: "admin", students: [...] }`
   - Response: `{ ok: true, count: N }` (N > 0이면 실제 활성 학생 수)
4. 이후 Automate(Android) 앱에서 테스트 입금 알림 전송 → webhook POST 호출
5. KV `students_cache` 존재 시 fuzzy match 결과가 `confidence: "exact"` 또는 `"fuzzy_1"`으로 반환되는 것 확인
</verification>

<success_criteria>
- `functions/api/payments/sync-students.js` 파일 존재
- POST 엔드포인트가 admin/manager JWT로 200 응답, teacher JWT로 403 응답
- App.jsx drainPending이 PaymentsView 진입 시 sync-students를 drain GET 직전에 호출
- 빌드(`npm run build`) 오류 없음
- kakaobank-webhook.js의 students_cache 읽기 코드는 변경하지 않음 (기존 코드 보존)
</success_criteria>

<output>
완료 후 `.planning/phases/05-payment-automation/05-05-SUMMARY.md` 생성:
- 구현한 파일 2개 목록
- students_cache gap 해소 확인 여부
- 빌드 통과 여부
</output>
