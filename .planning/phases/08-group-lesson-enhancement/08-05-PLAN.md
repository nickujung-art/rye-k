---
phase: 08-group-lesson-enhancement
plan: "05"
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-03"
files_modified:
  - src/components/TimetableView.jsx
  - src/components/ScheduleView.jsx
  - src/constants.jsx
autonomous: true
requirements:
  - GRP-05
  - GRP-06
  - GRP-07
must_haves:
  truths:
    - "ScheduleView 상단에 '시간표' 탭이 생겨 기존 주간/월간 탭과 나란히 표시된다"
    - "강사 로그인 시 TimetableView에서 자신의 09:00~21:00 주간 시간표를 격자로 볼 수 있다"
    - "관리자/매니저는 강사 카드 목록을 보고 클릭해 해당 강사의 시간표를 확인할 수 있다"
    - "각 슬롯 카드는 강사 컬러, 슬롯 이름, 인원 수를 표시한다"
    - "슬롯이 없는 시간 셀은 빈 상태로 표시된다"
  artifacts:
    - path: src/components/TimetableView.jsx
      provides: "TimetableView 컴포넌트 (default export)"
      exports: ["default"]
    - path: src/components/ScheduleView.jsx
      provides: "'시간표' 탭 + TimetableView import + viewMode=timetable 렌더"
      contains: "timetable"
    - path: src/constants.jsx
      provides: "timetable CSS 클래스들"
      contains: ".timetable-grid"
  key_links:
    - from: TimetableView
      to: lessonSlots prop
      via: "slot.schedule[].day + slot.schedule[].time → grid position"
      pattern: "lessonSlots"
    - from: TimetableView
      to: students prop
      via: "students.filter(s => s.lessons.some(l => l.slotId === slot.id)).length"
      pattern: "slotId.*slot\\.id"
---

<objective>
TimetableView 컴포넌트를 신규 생성하고 ScheduleView에 '시간표' 탭으로 통합한다. 09:00~21:00 × 요일 CSS 격자에 레슨 슬롯 카드를 배치하고, 강사는 자신의 시간표를, 관리자/매니저는 강사 카드 선택 후 해당 강사의 시간표를 확인한다.

Purpose: 공강/수업 현황을 한눈에 파악해 레슨 배정 효율을 높인다.
Output: TimetableView.jsx 신규 + ScheduleView '시간표' 탭 + 관련 CSS
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-group-lesson-enhancement/08-CONTEXT.md
@.planning/phases/08-group-lesson-enhancement/08-01-SUMMARY.md
@.planning/phases/08-group-lesson-enhancement/08-03-SUMMARY.md

<interfaces>
<!-- D-06: TimetableView 레이아웃 -->
<!--
  시간축: 09:00~21:00, 30분 단위 = 25행
  요일축: 월화수목금토일 = 7열
  CSS grid: 60px(시간 라벨) + repeat(7, 1fr) = 8열
            40px(요일 헤더) + repeat(25, 48px) = 26행

  슬롯 카드 위치 계산:
    DAYS = ["월","화","수","목","금","토","일"]
    dayIdx = DAYS.indexOf(schedule.day)  → 0~6
    timeparts = schedule.time.split(":")  → [h, m]
    timeIdx = (h - 9) * 2 + (m >= 30 ? 1 : 0)  → 0~24
    gridColumn = dayIdx + 2  (1=시간라벨, 2~8=요일)
    gridRow = timeIdx + 2    (1=요일헤더, 2~26=시간행)
  
  범위 밖 슬롯(시간이 09:00 미만 또는 21:00 이상): 렌더 스킵
-->

<!-- D-06: 슬롯 카드 내용 -->
<!--
  배경색: getTeacherColor(slot.teacherId, teachers) + 투명도 0.15 (배경), 불투명 (좌측 border)
  슬롯 이름: slot.name (굵게)
  인원 수: students.filter(s => s.status === "active" && !s.isInstitution &&
                  (s.lessons||[]).some(l => l.slotId === slot.id)).length
-->

<!-- D-07: TimetableView 진입점 -->
<!--
  강사(teacher role): selectedTeacherId = currentUser.id 고정, 강사 선택 UI 없음
  관리자/매니저(canSeeAll): 초기 state null → 강사 카드 그리드 표시
                            카드 클릭 → selectedTeacherId 설정 → 격자 표시
                            격자 상단에 "← {teacherName}" 뒤로가기 버튼
-->

<!-- ScheduleView 현재 탭 (line 165-167) -->
```jsx
<button className={"sched-mode-btn " + (viewMode==="week"?"active":"")} onClick={() => setViewMode("week")}>주간</button>
<button className={"sched-mode-btn " + (viewMode==="month"?"active":"")} onClick={() => setViewMode("month")}>월간</button>
// → "시간표" 탭 추가
```

<!-- getTeacherColor (ScheduleView.jsx line 7-11) — TimetableView.jsx에 동일하게 복사 -->
```js
const TEACHER_COLORS = ["var(--blue)","var(--red)","var(--green)","#7C3AED","var(--gold-dk)","var(--blue-md)","var(--gold)","var(--red-dk)"];
function getTeacherColor(id, teachersList) {
  if (!id) return "var(--ink-30)";
  const idx = teachersList.findIndex(t => t.id === id);
  return TEACHER_COLORS[Math.abs(idx) % TEACHER_COLORS.length] || "var(--ink-30)";
}
```

<!-- canManageAll import — utils.js에서 이미 export됨 -->
```js
import { canManageAll } from "../utils.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimetableView.jsx 신규 생성</name>
  <read_first>
    - src/components/ScheduleView.jsx (line 1-12 — import 패턴, getTeacherColor 함수)
    - src/constants.jsx (line 1-60 — CSS 변수 이름 확인: --blue, --ink-30 등)
  </read_first>
  <files>src/components/TimetableView.jsx</files>
  <action>
새 파일 `src/components/TimetableView.jsx` 생성:

```jsx
import { useState } from "react";
import { canManageAll } from "../utils.js";
import { Av } from "./shared/CommonUI.jsx";

const DAYS = ["월","화","수","목","금","토","일"];
const TIME_START = 9;   // 09:00
const TIME_END = 21;    // 21:00 (마지막 행)
const ROWS = (TIME_END - TIME_START) * 2; // 25 (09:00~20:30, 표시는 09:00~21:00)

const TEACHER_COLORS = ["var(--blue)","var(--red)","var(--green)","#7C3AED","var(--gold-dk)","var(--blue-md)","var(--gold)","var(--red-dk)"];
function getTeacherColor(id, teachersList) {
  if (!id) return "var(--ink-30)";
  const idx = teachersList.findIndex(t => t.id === id);
  return TEACHER_COLORS[Math.abs(idx) % TEACHER_COLORS.length] || "var(--ink-30)";
}

function formatTimeLabel(rowIdx) {
  const totalMins = (TIME_START + Math.floor(rowIdx / 2)) * 60 + (rowIdx % 2) * 30;
  const h = Math.floor(totalMins / 60);
  const m = totalMins % 60;
  return `${String(h).padStart(2,"0")}:${String(m).padStart(2,"0")}`;
}

function TimetableGrid({ lessonSlots, students, teachers, teacherId }) {
  const color = getTeacherColor(teacherId, teachers);

  // 슬롯을 그리드 위치로 매핑
  // slotCell: { dayIdx, rowIdx, slot, memberCount }[]
  const slotCells = [];
  (lessonSlots || [])
    .filter(s => s.teacherId === teacherId && s.status !== "closed")
    .forEach(slot => {
      (slot.schedule || []).forEach(sc => {
        const dayIdx = DAYS.indexOf(sc.day);
        if (dayIdx < 0) return;
        const [h, mStr] = (sc.time || "09:00").split(":").map(Number);
        const m = mStr || 0;
        const rowIdx = (h - TIME_START) * 2 + (m >= 30 ? 1 : 0);
        if (rowIdx < 0 || rowIdx >= ROWS) return; // 범위 밖 스킵
        const memberCount = (students || []).filter(s =>
          s.status === "active" && !s.isInstitution &&
          (s.lessons || []).some(l => l.slotId === slot.id)
        ).length;
        slotCells.push({ dayIdx, rowIdx, slot, memberCount });
      });
    });

  // 셀 점유 맵 (그리드 위치 중복 감지용)
  const occupied = new Set(slotCells.map(c => `${c.dayIdx},${c.rowIdx}`));

  return (
    <div className="timetable-wrap">
      <div className="timetable-grid">
        {/* 헤더 행 — 첫 번째 셀: 빈 코너 */}
        <div className="timetable-header" />
        {DAYS.map(d => (
          <div key={d} className="timetable-header">{d}</div>
        ))}

        {/* 시간 행 × 요일 열 */}
        {Array.from({ length: ROWS }, (_, rowIdx) => {
          const timeLabel = formatTimeLabel(rowIdx);
          return [
            <div key={`t-${rowIdx}`} className="timetable-time">{timeLabel}</div>,
            ...DAYS.map((d, dayIdx) => {
              const cell = slotCells.find(c => c.dayIdx === dayIdx && c.rowIdx === rowIdx);
              if (cell) {
                const bg = color;
                return (
                  <div key={`${dayIdx}-${rowIdx}`} className="timetable-cell">
                    <div className="timetable-slot" style={{
                      background: bg + "26",  // ~15% 투명도
                      borderLeft: `3px solid ${bg}`,
                    }}>
                      <div className="timetable-slot-name">{cell.slot.name}</div>
                      {cell.memberCount > 0 && (
                        <div className="timetable-slot-sub">{cell.memberCount}명</div>
                      )}
                    </div>
                  </div>
                );
              }
              return <div key={`${dayIdx}-${rowIdx}`} className="timetable-cell" />;
            }),
          ];
        })}
      </div>
    </div>
  );
}

function TeacherSelectGrid({ teachers, onSelect }) {
  return (
    <div>
      <div style={{ fontSize: 13, color: "var(--ink-60)", marginBottom: 16 }}>
        시간표를 확인할 강사를 선택하세요.
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(140px, 1fr))", gap: 10 }}>
        {(teachers || []).map(t => (
          <div
            key={t.id}
            className="card"
            style={{ padding: "14px 12px", cursor: "pointer", textAlign: "center", borderLeft: `4px solid ${getTeacherColor(t.id, teachers)}` }}
            onClick={() => onSelect(t.id)}
          >
            <Av name={t.name} size="av-sm" />
            <div style={{ fontSize: 13, fontWeight: 600, marginTop: 8 }}>{t.name}</div>
            <div style={{ fontSize: 11, color: "var(--ink-30)" }}>{(t.instruments || []).join(", ")}</div>
          </div>
        ))}
      </div>
    </div>
  );
}

export default function TimetableView({ lessonSlots, students, teachers, currentUser }) {
  const isTeacher = currentUser.role === "teacher";
  const canSeeAll = canManageAll(currentUser.role);
  const [selectedTeacherId, setSelectedTeacherId] = useState(isTeacher ? currentUser.id : null);

  const selectedTeacher = selectedTeacherId ? teachers.find(t => t.id === selectedTeacherId) : null;

  return (
    <div>
      <div className="ph">
        <div>
          <h1>시간표</h1>
          <div className="ph-sub">09:00 ~ 21:00 주간 레슨 현황</div>
        </div>
      </div>

      {canSeeAll && selectedTeacherId && (
        <div style={{ marginBottom: 12 }}>
          <button
            className="btn btn-secondary btn-sm"
            onClick={() => setSelectedTeacherId(null)}
            style={{ fontSize: 12 }}
          >
            ← 강사 목록
          </button>
          {selectedTeacher && (
            <span style={{ marginLeft: 12, fontSize: 14, fontWeight: 600 }}>
              {selectedTeacher.name} 강사 시간표
            </span>
          )}
        </div>
      )}

      {canSeeAll && !selectedTeacherId ? (
        <TeacherSelectGrid teachers={teachers} onSelect={setSelectedTeacherId} />
      ) : selectedTeacherId ? (
        <TimetableGrid
          lessonSlots={lessonSlots}
          students={students}
          teachers={teachers}
          teacherId={selectedTeacherId}
        />
      ) : (
        <div className="empty">
          <div className="empty-txt">시간표를 불러올 수 없습니다.</div>
        </div>
      )}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>grep -c "export default function TimetableView\|TimetableGrid\|TeacherSelectGrid" src/components/TimetableView.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export default function TimetableView" src/components/TimetableView.jsx` → 1줄
    - `grep "timetable-grid\|timetable-slot\|timetable-cell" src/components/TimetableView.jsx` → 3줄 이상
    - `grep "window.confirm\|window.alert" src/components/TimetableView.jsx` → 0줄
    - `grep "canManageAll" src/components/TimetableView.jsx` → import + 사용 줄
  </acceptance_criteria>
  <done>TimetableView.jsx가 존재하고 default export로 컴포넌트를 제공한다</done>
</task>

<task type="auto">
  <name>Task 2: ScheduleView.jsx '시간표' 탭 + TimetableView 통합 + CSS 추가</name>
  <read_first>
    - src/components/ScheduleView.jsx (line 1-30 imports/state, line 154-180 viewMode 분기 + 탭 버튼)
    - src/constants.jsx (line 50-65 CSS 선두 — 기존 CSS 변수와 마지막 클래스 위치 확인)
  </read_first>
  <files>src/components/ScheduleView.jsx, src/constants.jsx</files>
  <action>
### constants.jsx 변경 — CSS 추가
CSS 문자열 끝(닫는 백틱 `` ` `` 바로 앞)에 다음 CSS 블록 추가:

```css
/* ── TimetableView ─────────────────────────────────────────────────── */
.timetable-wrap{overflow-x:auto;padding-bottom:16px;-webkit-overflow-scrolling:touch;}
.timetable-grid{display:grid;grid-template-columns:52px repeat(7,minmax(72px,1fr));grid-template-rows:36px repeat(25,48px);border:1px solid var(--border);border-radius:var(--radius-sm);overflow:hidden;min-width:580px;}
.timetable-header{background:var(--bg);border-right:1px solid var(--border);border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:600;color:var(--ink-60);position:sticky;top:0;z-index:1;}
.timetable-time{background:var(--bg);border-right:1px solid var(--border);border-bottom:1px solid var(--border);display:flex;align-items:flex-start;justify-content:center;padding-top:4px;font-size:10px;color:var(--ink-30);}
.timetable-cell{border-right:1px solid var(--border);border-bottom:1px solid var(--border);position:relative;background:var(--paper);}
.timetable-slot{position:absolute;inset:2px;border-radius:4px;padding:3px 5px;display:flex;flex-direction:column;justify-content:flex-start;gap:1px;overflow:hidden;}
.timetable-slot-name{font-size:11px;font-weight:600;line-height:1.3;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.timetable-slot-sub{font-size:10px;opacity:.65;white-space:nowrap;}
```

### ScheduleView.jsx 변경

변경 1: imports에 TimetableView 추가 (파일 상단, 기존 imports 아래):
```js
import TimetableView from "./TimetableView.jsx";
```

변경 2: 탭 버튼 영역 (line ~165-167)에 "시간표" 버튼 추가:
```jsx
<button className={"sched-mode-btn " + (viewMode==="week"?"active":"")} onClick={() => setViewMode("week")}>주간</button>
<button className={"sched-mode-btn " + (viewMode==="month"?"active":"")} onClick={() => setViewMode("month")}>월간</button>
<button className={"sched-mode-btn " + (viewMode==="timetable"?"active":"")} onClick={() => setViewMode("timetable")}>시간표</button>
```
주의: 탭 버튼이 여러 위치(주간 뷰 툴바, 월간 뷰 툴바)에 중복 정의되어 있다면 각각에 추가.

변경 3: viewMode 분기 최상단에 timetable 분기 추가 (if (viewMode === "week") { ... } 분기 위):
```jsx
if (viewMode === "timetable") {
  return (
    <div className="sched-wrap">
      {alimToast && <div style={{position:"fixed",top:72,left:"50%",transform:"translateX(-50%)",zIndex:9999,background:"var(--paper)",border:"1px solid var(--border)",borderRadius:10,padding:"10px 20px",boxShadow:"0 4px 16px rgba(0,0,0,.14)",fontSize:13,fontWeight:600,whiteSpace:"nowrap"}}>{alimToast}</div>}
      <div className="sched-toolbar">
        <button className={"sched-mode-btn " + (viewMode==="week"?"active":"")} onClick={() => setViewMode("week")}>주간</button>
        <button className={"sched-mode-btn " + (viewMode==="month"?"active":"")} onClick={() => setViewMode("month")}>월간</button>
        <button className={"sched-mode-btn active"}>시간표</button>
      </div>
      <TimetableView
        lessonSlots={lessonSlots}
        students={students}
        teachers={teachers}
        currentUser={currentUser}
      />
    </div>
  );
}
```

주의:
- `students`는 ScheduleView의 prop (institution 가상회원 포함 allMembers). TimetableView 내부에서 isInstitution 필터링함.
- `lessonSlots`, `onUpdateSlot`은 08-01에서 App.jsx가 이미 전달하므로 ScheduleView prop에 존재.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "timetable" src/components/ScheduleView.jsx` → import + viewMode 분기 + 탭 버튼 3줄 이상
    - `grep "TimetableView" src/components/ScheduleView.jsx` → import 줄 + 렌더 줄
    - `grep "timetable-grid" src/constants.jsx` → CSS 클래스 정의 줄 출력
    - `grep ".timetable-slot" src/constants.jsx` → CSS 클래스 정의 줄 출력
    - npm run build 통과
  </acceptance_criteria>
  <done>ScheduleView에 '시간표' 탭이 추가되어 클릭 시 TimetableView가 렌더된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render | TimetableView가 강사 스케줄 데이터를 화면에 표시 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information Disclosure | TeacherSelectGrid (관리자 뷰) | mitigate | TimetableView는 ScheduleView 내부에서만 렌더되며, ScheduleView 자체가 로그인 후 view="schedule" 라우트에서만 접근 가능. 추가 role guard 불필요 |
| T-08-05-02 | Information Disclosure | 강사 본인 뷰 (isTeacher=true) | mitigate | selectedTeacherId를 currentUser.id로 고정. 타 강사 ID로 변경 불가 (UI에 강사 선택 없음) |
</threat_model>

<verification>
1. npm run build 통과
2. `grep "import TimetableView" src/components/ScheduleView.jsx` → 출력
3. `grep "viewMode.*timetable\|timetable.*viewMode" src/components/ScheduleView.jsx` → 탭 버튼 + 분기
4. `grep ".timetable-grid" src/constants.jsx` → CSS 정의 출력
5. TimetableView.jsx 파일 존재: `ls src/components/TimetableView.jsx`
</verification>

<success_criteria>
- TimetableView.jsx 신규 파일 존재
- ScheduleView에 '시간표' 탭 추가
- 강사: 자신의 슬롯만 표시
- 관리자/매니저: 강사 카드 선택 → 해당 강사 시간표
- CSS 클래스 8종 constants.jsx에 추가
- 빌드 통과
</success_criteria>

<output>
완료 후 `.planning/phases/08-group-lesson-enhancement/08-05-SUMMARY.md` 생성
</output>
