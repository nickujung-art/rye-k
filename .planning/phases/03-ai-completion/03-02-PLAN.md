---
phase: 03-ai-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - functions/api/ai/care-message.js
  - src/aiClient.js
  - src/components/dashboard/ChurnWidget.jsx
  - src/components/dashboard/Dashboard.jsx
  - src/components/aireports/MonthlyReportsView.jsx
autonomous: true
requirements:
  - AI-02
  - AI-03

must_haves:
  truths:
    - "이탈 위험 회원 행에 '케어 메시지' 버튼이 표시된다"
    - "버튼 클릭 시 AI가 초안을 생성하고 행 아래에 텍스트가 표시된다"
    - "초안 옆에 '복사' 버튼이 있어 클립보드 복사가 가능하다"
    - "월별 리포트 공개 상태 카드에 '발송 준비' 버튼이 표시된다"
    - "에러 발생 시 인라인 에러 메시지가 표시된다 (window.alert 없음)"
  artifacts:
    - path: "functions/api/ai/care-message.js"
      provides: "단건 케어 메시지 생성 Worker (SEC-08 익명화 포함)"
      contains: "callGemini"
    - path: "src/aiClient.js"
      provides: "aiChurnCare() 함수"
      contains: "aiChurnCare"
    - path: "src/components/dashboard/ChurnWidget.jsx"
      provides: "케어 메시지 버튼 + 인라인 초안 표시"
      contains: "careResults"
    - path: "src/components/aireports/MonthlyReportsView.jsx"
      provides: "발송 준비 버튼 (Phase 4 stub)"
      contains: "발송 준비"
  key_links:
    - from: "src/components/dashboard/ChurnWidget.jsx"
      to: "src/aiClient.js"
      via: "aiChurnCare({ name, consecutive, rate, score, teacherName })"
      pattern: "aiChurnCare"
    - from: "src/aiClient.js"
      to: "functions/api/ai/care-message.js"
      via: "callAi('care-message', payload)"
      pattern: "care-message"
    - from: "functions/api/ai/care-message.js"
      to: "functions/api/ai/_utils/gemini.js"
      via: "callGemini(env.GEMINI_API_KEY, ...)"
      pattern: "callGemini"
---

<objective>
AI-02: 월별 리포트 published 카드에 "발송 준비" 버튼 추가 (Phase 4 AlimTalk stub)
AI-03: ChurnWidget에 케어 메시지 버튼 + care-message.js Worker 신규 생성 + aiClient 함수 추가

Purpose: 이탈 위험 회원에게 원클릭으로 케어 메시지 초안을 생성하고 복사하는 흐름을 완성한다.
월별 리포트는 발송 준비 상태로 전환하는 UI를 Phase 4 연결을 위해 미리 준비한다.

Output:
- `functions/api/ai/care-message.js` — 단건 케어 메시지 생성 Worker (익명화 포함)
- `src/aiClient.js` — `aiChurnCare()` 함수 추가
- `src/components/dashboard/ChurnWidget.jsx` — 케어 메시지 버튼 + 인라인 초안 표시
- `src/components/aireports/MonthlyReportsView.jsx` — "발송 준비" 버튼 stub 추가
</objective>

<execution_context>
@C:\Users\GIGABYTE\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GIGABYTE\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-CONTEXT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-RESEARCH.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-PATTERNS.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-01-SUMMARY.md

<interfaces>
<!-- Wave 1에서 생성·수정된 파일 기반 인터페이스 -->

src/aiClient.js 현재 callAi 패턴:
```javascript
export async function callAi(endpoint, payload) {
  // Firebase ID Token 획득 + POST /api/ai/{endpoint}
  // 401 → "server_401", 429 → "rate_limited", 503 → "ai_disabled"
  return resp.json();
}

// 기존 aiChurnAnalysis — aiChurnCare의 레퍼런스
export async function aiChurnAnalysis(students) {
  if (!students?.length) return { comments: [] };
  const payload = students.slice(0, 5).map(s => ({
    name: s.name, consecutive: s.consecutive, rate: s.rate, score: s.score
  }));
  return callAi("churn", { students: payload });
}
// aiChurnCare는 이 아래에 추가 (단건, teacherName 포함)
```

functions/api/ai/churn.js (care-message.js의 구조 레퍼런스, Wave 1 완료 후 상태):
```javascript
import { callGemini } from "./_utils/gemini.js";
import { stripPii } from "./_utils/pii-guard.js";
import { buildNameMap, deanonymize } from "./_utils/anonymize.js";

export async function onRequest(context) {
  const { request, env } = context;
  let body;
  try { body = await request.json(); } catch { return err(400, "Bad Request"); }
  body = stripPii(body);
  // ... 비즈니스 로직 ...
}

const json = (d, s=200) => new Response(JSON.stringify(d), { status:s, headers:{"content-type":"application/json"} });
const err  = (s, m)     => new Response(JSON.stringify({ error: m }), { status:s, headers:{"content-type":"application/json"} });
```

src/components/dashboard/ChurnWidget.jsx 현재 구조:
```javascript
export default function ChurnWidget({ students, attendance }) {
  const [expanded, setExpanded] = useState(false);
  // atRisk 계산 로직 (변경 없음)
  // 렌더: shown.map(s => <div key={s.id}> ... </div>)
  // 행 마지막: <span 위험/주의 배지>
}
// props에 teachers 추가 필요 (teacherName을 케어 메시지에 전달하기 위해)
```

src/components/aireports/MonthlyReportsView.jsx published 버튼 영역 (line 466-473):
```jsx
<div style={{display:"flex",gap:8,flexWrap:"wrap"}}>
  <button className="btn btn-sm" onClick={() => handleEditPublished(report)}
    style={{background:"var(--ink-10)",color:"var(--ink)",fontSize:12}}>
    ✏️ 수정
  </button>
  <button className="btn btn-sm" onClick={() => handleArchive(report)}
    style={{background:"transparent",color:"var(--ink-30)",border:"1px solid var(--border)",fontSize:11}}>
    보관 처리
  </button>
  {/* 발송 준비 버튼을 이 div 안에 추가 */}
</div>
```

src/components/aireports/MonthlyReportsView.jsx의 기존 에러 처리 패턴 (line 132):
```javascript
const msg = e.message === "rate_limited"
  ? "잠시 후 다시 시도해주세요 (분당 제한)"
  : e.message === "auth_required" ? "로그인이 필요합니다."
  : "AI 오류가 발생했습니다.";
```

App.jsx에서 ChurnWidget 호출 위치 (Dashboard.jsx를 통해 간접 사용):
```javascript
// src/components/dashboard/Dashboard.jsx 내에서 ChurnWidget 사용
// ChurnWidget에 teachers prop 추가 시 Dashboard.jsx에서도 전달 필요
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: care-message.js Worker 신규 생성 + aiClient aiChurnCare 추가</name>
  <files>
    functions/api/ai/care-message.js,
    src/aiClient.js
  </files>
  <read_first>
    - functions/api/ai/churn.js — 완성된 구조 레퍼런스 (Wave 1 완료 후 callGemini 사용)
    - functions/api/ai/_utils/anonymize.js — buildNameMap, deanonymize 시그니처
    - functions/api/ai/_utils/pii-guard.js — stripPii 사용법 확인
    - src/aiClient.js — aiChurnAnalysis 패턴 (aiChurnCare 추가 위치 및 스타일)
  </read_first>
  <action>
**care-message.js 신규 생성** (`functions/api/ai/care-message.js`):

```javascript
// care-message.js — 이탈 위험 회원 단건 케어 메시지 초안 생성 (AI-03)
import { callGemini } from "./_utils/gemini.js";
import { stripPii } from "./_utils/pii-guard.js";
import { buildNameMap, deanonymize } from "./_utils/anonymize.js";

export async function onRequest(context) {
  const { request, env } = context;
  let body;
  try { body = await request.json(); } catch { return err(400, "Bad Request"); }
  body = stripPii(body);

  const { name, consecutive, rate, score, teacherName } = body;
  if (!name) return err(400, "name required");

  // SEC-08: 이름 익명화
  const nameMap = buildNameMap([name]);
  const anonName = nameMap[name] || name;

  const level = score >= 50 ? "위험" : "주의";
  const system = "당신은 국악 교육기관 RYE-K의 학원 관리 AI 비서입니다. 이탈 위험 회원의 학부모에게 보낼 따뜻하고 진심 어린 케어 메시지 초안을 한국어로 작성합니다. 300자 이내로 간결하게 작성하세요.";
  const lines = [
    `회원: ${anonName}`,
    consecutive >= 2 ? `연속 결석 ${consecutive}회` : null,
    rate != null ? `최근 4주 출석률 ${rate}%` : null,
    `위험도: ${level}`,
    teacherName ? `담당 강사: ${teacherName}` : null,
    "\n학부모에게 보낼 케어 메시지 초안을 1개만 작성하세요. 인사말부터 마무리까지 완성된 형태로 작성하세요.",
  ].filter(Boolean);

  try {
    const raw = await callGemini(env.GEMINI_API_KEY, {
      system,
      user: lines.join("\n"),
      max_tokens: 400,
      temperature: 0.5,
    });
    // SEC-08: 익명 이름 복원
    const result = Object.keys(nameMap).length ? deanonymize(raw, nameMap) : raw;
    return json({ result });
  } catch (e) {
    console.error("care-message AI error:", e);
    if (e.status === 429) return new Response("Too Many Requests", { status: 429 });
    return err(500, e.message);
  }
}

const json = (d, s=200) => new Response(JSON.stringify(d), { status:s, headers:{"content-type":"application/json"} });
const err  = (s, m)     => new Response(JSON.stringify({ error: m }), { status:s, headers:{"content-type":"application/json"} });
```

**src/aiClient.js 수정** — 파일 끝에 aiChurnCare 함수 추가:
```javascript
// aiChurnAnalysis 함수 바로 아래에 추가
export async function aiChurnCare({ name, consecutive, rate, score, teacherName }) {
  return callAi("care-message", { name, consecutive, rate, score, teacherName });
}
```

aiChurnAnalysis의 위치를 기준으로 파일 끝부분에 삽입. callAi 반환값은 `{ result: string }` 형태.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `functions/api/ai/care-message.js` 파일 존재
    - `grep -c "callGemini" functions/api/ai/care-message.js` → 1
    - `grep -c "buildNameMap\|deanonymize" functions/api/ai/care-message.js` → 2
    - `grep -c "aiChurnCare" src/aiClient.js` → 1 이상
    - `grep "care-message" src/aiClient.js` → "callAi('care-message'" 패턴 포함
    - `npm run build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>
    care-message.js 존재 + callGemini + buildNameMap + deanonymize + stripPii 사용.
    aiClient.js에 aiChurnCare 함수 추가.
    빌드 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: ChurnWidget 케어 메시지 버튼 + MonthlyReportsView 발송 준비 버튼</name>
  <files>
    src/components/dashboard/ChurnWidget.jsx,
    src/components/aireports/MonthlyReportsView.jsx
  </files>
  <read_first>
    - src/components/dashboard/ChurnWidget.jsx — 전체 파일 (props, state, 렌더 구조 파악)
    - src/components/aireports/MonthlyReportsView.jsx — published 상태 버튼 영역 (line 458-475)
    - src/aiClient.js — aiChurnCare import 경로 확인 (Task 1 완료 후)
  </read_first>
  <action>
**ChurnWidget.jsx 수정:**

1. import 추가 (파일 최상단):
   ```javascript
   import { aiChurnCare } from "../../aiClient.js";
   ```

2. props 서명 변경:
   ```javascript
   // 기존
   export default function ChurnWidget({ students, attendance })
   // 변경
   export default function ChurnWidget({ students, attendance, teachers })
   ```

3. useState 추가 (기존 `const [expanded, setExpanded] = useState(false);` 아래):
   ```javascript
   const [generating, setGenerating] = useState(new Set());
   const [careResults, setCareResults] = useState({});   // { [studentId]: string }
   const [careErrors, setCareErrors] = useState({});     // { [studentId]: string }
   ```

4. 핸들러 함수 추가 (atRisk 계산 코드 아래, `if (atRisk.length === 0) return null;` 위):
   ```javascript
   const handleCareMessage = async (student) => {
     setGenerating(prev => new Set([...prev, student.id]));
     setCareErrors(prev => ({ ...prev, [student.id]: null }));
     try {
       const teacher = teachers?.find(t => t.id === student.teacherId);
       const { result } = await aiChurnCare({
         name: student.name,
         consecutive: student.consecutive,
         rate: student.rate,
         score: student.score,
         teacherName: teacher?.name,
       });
       setCareResults(prev => ({ ...prev, [student.id]: result }));
     } catch (e) {
       const msg = e.message === "rate_limited"
         ? "잠시 후 다시 시도해주세요 (분당 제한)"
         : "AI 오류가 발생했습니다.";
       setCareErrors(prev => ({ ...prev, [student.id]: msg }));
     } finally {
       setGenerating(prev => { const n = new Set(prev); n.delete(student.id); return n; });
     }
   };
   ```

5. 각 회원 행(shown.map 내부) 수정 — 위험도 배지(`<span ...위험/주의...>`) 옆에 버튼 추가:
   위험도 배지 `<span style={{fontSize:11,fontWeight:700,...}}>` 다음에 삽입:
   ```jsx
   {generating.has(s.id) ? (
     <span style={{fontSize:11,color:"var(--blue)",flexShrink:0}}>생성 중…</span>
   ) : careResults[s.id] ? (
     <button
       onClick={() => navigator.clipboard.writeText(careResults[s.id])}
       style={{fontSize:10,color:"var(--blue)",background:"none",border:"1px solid var(--blue)",
         borderRadius:4,padding:"2px 7px",cursor:"pointer",fontFamily:"inherit",flexShrink:0}}
     >
       복사
     </button>
   ) : (
     <button
       onClick={() => handleCareMessage(s)}
       style={{fontSize:10,color:"var(--ink-50)",background:"none",border:"1px solid var(--border)",
         borderRadius:4,padding:"2px 7px",cursor:"pointer",fontFamily:"inherit",flexShrink:0}}
     >
       케어 메시지
     </button>
   )}
   ```

6. 위험도 배지 div 아래(각 행 div 닫기 전)에 초안·에러 표시 영역 추가:
   ```jsx
   {careErrors[s.id] && (
     <div style={{fontSize:11,color:"var(--red)",marginTop:6,paddingLeft:2}}>
       {careErrors[s.id]}
     </div>
   )}
   {careResults[s.id] && (
     <div style={{fontSize:12,color:"var(--ink-60)",lineHeight:1.6,marginTop:6,
       padding:"8px 10px",background:"var(--hanji)",borderRadius:6,border:"1px solid var(--border)"}}>
       {careResults[s.id]}
     </div>
   )}
   ```
   주의: 에러·초안 영역은 현재 row의 flex 컨테이너 밖, 별도 div로 감싸야 할 수 있음.
   기존 row div 구조를 확인해 flex 행 안에서 아래 줄로 흘러내리도록 배치.
   flex-direction: column 래퍼가 없다면 row div 자체를 column으로 변경 또는 새 div 추가.

7. Dashboard.jsx 확인 및 teachers prop 전달:
   ChurnWidget을 사용하는 Dashboard.jsx를 찾아 teachers prop을 추가.
   Dashboard.jsx가 이미 teachers를 prop으로 받고 있는지 확인 후 ChurnWidget에 전달.
   Dashboard.jsx에서 `<ChurnWidget students=... attendance=...` → `<ChurnWidget students=... attendance=... teachers={teachers}` 추가.

**CLAUDE.md 규칙 준수:**
- window.confirm/window.alert 사용 금지 — 모든 확인은 인라인 (케어 메시지 생성 확인 없이 바로 실행)
- CSS-in-JS 인라인 스타일 사용 (외부 CSS 파일 금지)

**MonthlyReportsView.jsx 수정 — 발송 준비 버튼:**

published 상태 카드의 버튼 영역 (line 466-473):
기존 버튼 div에 발송 준비 버튼 추가:
```jsx
{/* 기존: ✏️ 수정 + 보관 처리 버튼 */}
{/* 추가: 발송 준비 버튼 (Phase 4 AlimTalk stub) */}
<button
  className="btn btn-sm"
  onClick={() => {/* Phase 4: AlimTalk Worker 연결 예정 */}}
  style={{background:"var(--gold-lt)",color:"var(--gold-dk)",
    border:"1px solid #FCD34D",fontSize:11,whiteSpace:"nowrap"}}
>
  📨 발송 준비
</button>
```
보관 처리 버튼 앞에 삽입하거나 뒤에 추가 (기존 버튼 순서 변경 없음 권장).

주의: Phase 4에서 실제 AlimTalk 발송으로 대체될 stub. onClick은 빈 함수 또는 주석으로 표시.
window.confirm 사용 금지 — onClick이 비어있어도 무방.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aiChurnCare" src/components/dashboard/ChurnWidget.jsx` → 1
    - `grep -c "careResults" src/components/dashboard/ChurnWidget.jsx` → 2 이상 (state 선언 + 사용)
    - `grep -c "케어 메시지" src/components/dashboard/ChurnWidget.jsx` → 1 이상
    - `grep -c "발송 준비" src/components/aireports/MonthlyReportsView.jsx` → 1
    - `grep "window.confirm\|window.alert" src/components/dashboard/ChurnWidget.jsx` → 결과 없음
    - `grep "teachers" src/components/dashboard/Dashboard.jsx` → 1건 이상 (ChurnWidget에 prop 전달)
    - `npm run build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>
    ChurnWidget: 각 이탈 위험 행에 케어 메시지/생성 중/복사 버튼 표시 + 초안·에러 인라인 표시.
    MonthlyReportsView: published 카드 버튼 영역에 발송 준비 버튼 추가.
    빌드 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Cloudflare Worker | Firebase ID Token 검증 (_middleware.js auth.js) |
| Worker 내부 프롬프트 | teacherName은 PII이므로 stripPii 후 body에서 접근 — 단, teacherName은 stripPii 대상 아님(phone/email만 제거). 강사 이름은 공개 정보로 간주 |
| ClipBoard API | navigator.clipboard.writeText는 HTTPS에서만 동작 — Cloudflare Pages는 HTTPS 보장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | care-message.js | mitigate | SEC-08 anonymize 파이프라인 적용 (Task 1). teacherName은 공개 정보이므로 익명화 대상 아님 |
| T-03-05 | Denial of Service | care-message.js | accept | _middleware.js RATE_LIMIT_KV 20 req/min/user 상속 — 개별 버튼 남용 방지 충분 |
| T-03-06 | Information Disclosure | ChurnWidget careResults state | accept | careResults는 메모리에만 존재, Firestore 미저장. 페이지 이탈 시 소멸 |
</threat_model>

<verification>
Wave 2 완료 조건:
1. `functions/api/ai/care-message.js` 파일 존재
2. `grep "callGemini" functions/api/ai/care-message.js` → 1건
3. `grep "deanonymize" functions/api/ai/care-message.js` → 1건
4. `grep "aiChurnCare" src/aiClient.js` → 1건
5. `grep "케어 메시지" src/components/dashboard/ChurnWidget.jsx` → 1건 이상
6. `grep "발송 준비" src/components/aireports/MonthlyReportsView.jsx` → 1건
7. `grep "teachers" src/components/dashboard/Dashboard.jsx` → 1건 이상
8. `npm run build` exit code 0
</verification>

<success_criteria>
- ChurnWidget 이탈 위험 행마다 "케어 메시지" 버튼이 표시된다
- 버튼 클릭 시 "생성 중..." 표시 후 AI 초안이 행 아래에 인라인 표시된다
- 초안 옆 "복사" 버튼으로 클립보드 복사 가능
- MonthlyReportsView published 카드에 "발송 준비" 버튼이 표시된다
- 빌드가 통과한다
</success_criteria>

<output>
완료 후 `.planning/phases/03-ai-completion/03-02-SUMMARY.md` 생성
</output>
