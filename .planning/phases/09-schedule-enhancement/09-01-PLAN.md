---
phase: 09-schedule-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils.js
  - src/constants.jsx
autonomous: true
requirements:
  - SCH-01
  - SCH-02
  - SCH-03
  - SCH-04

must_haves:
  truths:
    - "slotMatchesLesson(slot, lesson, tid) returns false when teacherId differs"
    - "slotMatchesLesson returns false when instrument differs"
    - "slotMatchesLesson returns true even if schedule elements are in different array order"
    - "IC.pause SVG exists in constants.jsx IC object"
    - "CSS class .tt-cell-add is defined in the CSS string for the empty-cell '+' button"
    - "CSS class .pm-card is defined in the CSS string for the PauseManagementView card"
  artifacts:
    - path: "src/utils.js"
      provides: "slotMatchesLesson() exported pure function"
      contains: "export function slotMatchesLesson"
    - path: "src/constants.jsx"
      provides: "IC.pause SVG icon + CSS classes for Phase 9 UI"
      contains: "pause:"
  key_links:
    - from: "src/App.jsx (autoSyncStudentSlots, Plan 02)"
      to: "src/utils.js"
      via: "import { slotMatchesLesson } from './utils.js'"
      pattern: "slotMatchesLesson"
    - from: "src/components/TimetableView.jsx (Plan 03)"
      to: "src/constants.jsx"
      via: "className='tt-cell-add'"
      pattern: "tt-cell-add"
    - from: "src/components/student/PauseManagementView.jsx (Plan 04)"
      to: "src/constants.jsx"
      via: "className='pm-card'"
      pattern: "pm-card"
---

<objective>
슬롯 매칭 순수 함수와 Phase 9 전체에서 공유하는 CSS/아이콘 기반을 구축한다.

Purpose: Plan 02~04가 의존하는 `slotMatchesLesson` 유틸 함수와 `.tt-cell-add`, `.pm-card` 등 CSS 클래스를 미리 확보해 다른 플랜이 이를 사용할 수 있게 한다.
Output: utils.js에 `slotMatchesLesson` export, constants.jsx에 IC.pause SVG + Phase 9 CSS 클래스 추가.
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-schedule-enhancement/09-CONTEXT.md
@.planning/phases/09-schedule-enhancement/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: slotMatchesLesson 순수 함수 추가 (utils.js)</name>
  <read_first>
    - src/utils.js — 파일 전체 읽기 (기존 export 목록 파악, 함수 추가 위치 결정)
  </read_first>
  <files>src/utils.js</files>
  <action>
    src/utils.js 파일 맨 끝에 다음 함수를 추가한다. 기존 코드를 수정하거나 삭제하지 않는다.

    ```js
    // Phase 9 — 슬롯-레슨 완전 일치 판단 (teacherId + instrument + schedule 배열 정렬 비교)
    // schedule 배열 순서는 정렬 후 비교 (입력 순서 무관)
    export function slotMatchesLesson(slot, lesson, studentTeacherId) {
      const tid = lesson.teacherId || studentTeacherId;
      if (slot.teacherId !== tid) return false;
      if (slot.instrument !== lesson.instrument) return false;
      const sched = [...(lesson.schedule || [])].sort((a, b) =>
        `${a.day}${a.time}`.localeCompare(`${b.day}${b.time}`)
      );
      const slotSched = [...(slot.schedule || [])].sort((a, b) =>
        `${a.day}${a.time}`.localeCompare(`${b.day}${b.time}`)
      );
      return JSON.stringify(sched) === JSON.stringify(slotSched);
    }
    ```

    주의: `slot.schedule`은 슬롯의 요일·시간 배열이고 `lesson.schedule`은 학생 레슨의 배열이다. 두 배열 모두 `{ day: string, time: string }` 요소로 구성된다. 정렬 키는 `"${a.day}${a.time}"` 문자열 결합이다.
  </action>
  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && grep -n "export function slotMatchesLesson" src/utils.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function slotMatchesLesson" src/utils.js` 결과가 1
    - `grep -c "JSON.stringify(sched)" src/utils.js` 결과가 1 (정렬 후 JSON 비교 확인)
    - `grep -c "localeCompare" src/utils.js` 결과가 2 (sched와 slotSched 각각)
    - `npm run build` 통과 (utils.js export 오류 없음)
  </acceptance_criteria>
  <done>utils.js에 slotMatchesLesson이 export되고 npm run build 통과</done>
</task>

<task type="auto">
  <name>Task 2: IC.pause 아이콘 + Phase 9 CSS 클래스 추가 (constants.jsx)</name>
  <read_first>
    - src/constants.jsx — 파일 전체 읽기 (IC 객체 위치, CSS 문자열 끝 위치 파악)
    - CLAUDE.md "UI" 섹션 — CSS 패턴 규칙 확인
  </read_first>
  <files>src/constants.jsx</files>
  <action>
    두 가지 변경을 순서대로 수행한다.

    **변경 1: IC.pause 아이콘 추가**

    constants.jsx의 IC 객체(현재 `robot:` 등이 있는 부분) 마지막 항목 뒤에 `,` 쉼표를 확인하고, 이미 있다면 그 다음에, 없다면 마지막 항목 뒤에 쉼표와 함께 다음을 추가한다:

    ```jsx
    pause: <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="6" y="4" width="4" height="16"/><rect x="14" y="4" width="4" height="16"/></svg>,
    ```

    **변경 2: Phase 9 CSS 클래스 추가**

    기존 CSS 문자열(backtick 템플릿 리터럴, 변수명 CSS 또는 유사)의 마지막 닫는 backtick 직전에 다음 CSS 블록을 추가한다. 이미 있는 클래스와 중복되지 않도록 한다.

    ```css
    /* === Phase 9: TimetableView 배정 UI === */
    .tt-cell-add {
      display: flex; align-items: center; justify-content: center;
      width: 24px; height: 24px; border-radius: 6px;
      border: 1.5px dashed var(--border); background: transparent;
      color: var(--ink-30); font-size: 16px; cursor: pointer;
      transition: border-color .15s, color .15s; font-family: inherit;
      margin: auto;
    }
    .tt-cell-add:hover { border-color: var(--blue); color: var(--blue); background: var(--blue-lt); }
    .tt-member-add-btn {
      display: block; width: 100%; margin-top: 6px;
      padding: 5px 10px; border-radius: 6px;
      border: 1px dashed var(--blue); background: var(--blue-lt);
      color: var(--blue); font-size: 12px; cursor: pointer; font-family: inherit;
      text-align: center;
    }
    .tt-member-add-btn:hover { background: var(--blue); color: #fff; }

    /* === Phase 9: PauseManagementView === */
    .pm-view { padding: 0 0 40px; }
    .pm-empty { text-align: center; color: var(--ink-30); padding: 48px 0; font-size: 14px; }
    .pm-card {
      background: var(--paper); border: 1px solid var(--border);
      border-radius: var(--radius); padding: 14px 16px;
      margin-bottom: 10px; position: relative;
    }
    .pm-card-header {
      display: flex; align-items: flex-start; gap: 10px;
      margin-bottom: 8px;
    }
    .pm-card-name { font-size: 15px; font-weight: 600; flex: 1; }
    .pm-card-meta { font-size: 12px; color: var(--ink-30); margin-bottom: 6px; }
    .pm-badge-row { display: flex; gap: 6px; flex-wrap: wrap; margin-bottom: 8px; }
    .pm-badge {
      font-size: 11px; padding: 2px 8px; border-radius: 12px;
      background: var(--paper-2); color: var(--ink-60); border: 1px solid var(--border);
    }
    .pm-badge--urgent { background: #fff0f0; color: var(--red); border-color: var(--red); }
    .pm-badge--due { background: #fffbe6; color: var(--gold); border-color: var(--gold); }
    .pm-slot-tag {
      font-size: 11px; padding: 2px 8px; border-radius: 12px;
      background: var(--blue-lt); color: var(--blue); margin: 2px 0;
      display: inline-block;
    }
    .pm-accordion-toggle {
      font-size: 11px; color: var(--blue); cursor: pointer;
      background: none; border: none; padding: 0; font-family: inherit;
      text-decoration: underline;
    }
    .pm-care-label { font-size: 12px; font-weight: 600; color: var(--ink-60); margin: 8px 0 4px; }
    .pm-care-log-row {
      display: flex; align-items: center; gap: 8px;
      padding: 4px 0; border-bottom: 1px solid var(--border); font-size: 12px;
    }
    .pm-care-log-row:last-child { border-bottom: none; }
    .pm-care-type { padding: 1px 6px; border-radius: 4px; background: var(--paper-2); font-size: 11px; }
    .pm-confirm-row { display: flex; align-items: center; gap: 6px; margin-top: 8px; }
    .pm-confirm-text { font-size: 11px; color: var(--ink-60); }
    .pm-resume-btn {
      font-size: 12px; padding: 4px 12px; border-radius: 6px;
      border: 1px solid var(--blue); background: var(--blue-lt);
      color: var(--blue); cursor: pointer; font-family: inherit;
    }
    .pm-resume-btn:hover { background: var(--blue); color: #fff; }
    .pm-confirm-yes {
      font-size: 11px; padding: 3px 10px; border-radius: 6px;
      border: none; background: var(--green); color: #fff; cursor: pointer; font-family: inherit;
    }
    .pm-confirm-no {
      font-size: 11px; padding: 3px 10px; border-radius: 6px;
      border: 1px solid var(--border); background: var(--bg); cursor: pointer; font-family: inherit;
    }
    .pm-carelog-input-row { display: flex; gap: 6px; align-items: center; margin-top: 6px; flex-wrap: wrap; }
    .pm-carelog-note { flex: 1; min-width: 0; }
    .pm-link-banner {
      background: var(--blue-lt); border: 1px solid var(--blue);
      border-radius: var(--radius); padding: 12px 16px; cursor: pointer;
      display: flex; align-items: center; gap: 10px; color: var(--blue);
      font-size: 13px; font-weight: 600; margin-bottom: 12px;
    }
    .pm-link-banner:hover { background: var(--blue); color: #fff; }
    ```
  </action>
  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && grep -c "pause:" src/constants.jsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pause:" src/constants.jsx` 결과가 1 이상 (IC.pause 추가됨)
    - `grep -c "tt-cell-add" src/constants.jsx` 결과가 1 이상
    - `grep -c "pm-card" src/constants.jsx` 결과가 1 이상
    - `grep -c "pm-resume-btn" src/constants.jsx` 결과가 1 이상
    - `npm run build` 통과 (0 errors)
  </acceptance_criteria>
  <done>IC.pause SVG와 Phase 9 CSS 클래스 전체가 constants.jsx에 추가되고 npm run build 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| utils.js export | 순수 함수, 외부 입력 없음 — trust boundary 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | slotMatchesLesson schedule sort | accept | 정렬 후 JSON 비교로 순서 불일치 방지. 입력은 내부 React state에서만 옴 |
</threat_model>

<verification>
```bash
cd C:/Users/GIGABYTE/Coding/rye-k
# 1) slotMatchesLesson export 확인
grep -n "export function slotMatchesLesson" src/utils.js

# 2) IC.pause 확인
grep -n "pause:" src/constants.jsx

# 3) 핵심 CSS 클래스 확인
grep -n "tt-cell-add\|pm-card\|pm-resume-btn\|pm-link-banner" src/constants.jsx

# 4) 빌드 통과
npm run build
```
</verification>

<success_criteria>
- src/utils.js에 `export function slotMatchesLesson` 정의됨
- src/constants.jsx IC 객체에 `pause:` SVG 추가됨
- src/constants.jsx CSS에 `.tt-cell-add`, `.tt-member-add-btn`, `.pm-card`, `.pm-resume-btn`, `.pm-link-banner` 등 Phase 9 클래스 정의됨
- `npm run build` 에러 없이 통과
</success_criteria>

<output>
완료 후 `.planning/phases/09-schedule-enhancement/09-01-SUMMARY.md` 생성.
</output>
