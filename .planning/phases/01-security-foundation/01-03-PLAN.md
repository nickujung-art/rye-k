---
phase: 01-security-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - functions/api/auth/set-role.js
  - src/App.jsx
autonomous: false
requirements:
  - SEC-05

must_haves:
  truths:
    - "강사가 로그인하면 POST /api/auth/set-role 이 호출되고 Firebase Auth Custom Claims에 {role, teacherId}가 설정된다"
    - "claims 설정 후 getIdToken(true) 강제 갱신이 호출되어 Firestore 읽기 시 새 token이 사용된다"
    - "admin 계정은 rye-teachers Firestore 조회 없이 role=admin claim을 받는다"
    - "비밀번호 provider가 아닌 사용자(anonymous)가 set-role을 호출하면 401이 반환된다"
  artifacts:
    - path: "functions/api/auth/set-role.js"
      provides: "Custom Claims Worker — Firebase Auth REST API 호출"
      exports: ["onRequest"]
    - path: "src/App.jsx"
      provides: "로그인 성공 후 set-role 호출 + getIdToken(true) 갱신"
      contains: "/api/auth/set-role"
  key_links:
    - from: "src/App.jsx handleLogin()"
      to: "functions/api/auth/set-role.js"
      via: "fetch('/api/auth/set-role', { method: 'POST', headers: { Authorization: Bearer token }})"
      pattern: "api/auth/set-role"
    - from: "functions/api/auth/set-role.js"
      to: "identitytoolkit.googleapis.com"
      via: "fetch with Google OAuth2 access token"
      pattern: "identitytoolkit"

user_setup:
  - service: firebase-service-account
    why: "Custom Claims Worker가 Firebase Admin REST API를 호출하려면 서비스 계정 JWT가 필요"
    steps:
      - task: "Firebase 서비스 계정 JSON 생성"
        location: "Firebase Console → 프로젝트 설정 → 서비스 계정 → 새 비공개 키 생성"
        note: "다운로드된 JSON 파일을 열어 내용 전체를 복사"
      - task: "Cloudflare Secret 등록"
        command: "npx wrangler secret put FIREBASE_SERVICE_ACCOUNT_JSON"
        note: "명령 실행 후 프롬프트에 JSON 내용 전체를 붙여넣는다"
      - task: "Firebase API Key 확인"
        location: "Firebase Console → 프로젝트 설정 → 일반 → 웹 API 키"
        note: "이미 firebase.js에 하드코딩된 apiKey와 동일: AIzaSyDViGzxa0o1tqqX6fGr46Sfiews-ieGmks. 별도 secret 불필요."
---

<objective>
Firebase Custom Claims Worker(`functions/api/auth/set-role.js`)를 구현하고 App.jsx 로그인 흐름에서 호출한다 (SEC-05).

Purpose: 현재 모든 email user token에 role claim이 없어 Phase 1에서 배포될 Firestore rules의 `hasRole()` 체크가 전원 실패한다. 이 Worker를 먼저 배포하고 검증한 뒤 Plan 04에서 Firestore rules를 적용한다.

Output: functions/api/auth/set-role.js (신규), src/App.jsx (handleLogin 수정)

CRITICAL ORDERING NOTE: 이 plan(Wave 2)이 완료되고 배포·검증된 후에만 Plan 04(Wave 3 — Firestore rules)를 실행한다. 순서가 역전되면 모든 강사가 즉시 접근 불가 상태가 된다.
</objective>

<execution_context>
@C:\Users\GIGABYTE\Coding\rye-k\CLAUDE.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\01-security-foundation\01-01-SUMMARY.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\01-security-foundation\01-02-SUMMARY.md

<interfaces>
<!-- functions/api/ai/_utils/auth.js — 재사용할 verifyToken 시그니처 -->
```javascript
// PROJECT_ID = "rye-k-center"
export async function verifyToken(request) {
  // Authorization: Bearer <firebase-id-token> 헤더 파싱
  // jwtVerify with Google JWKS
  // Returns: JWT payload (sub, email, firebase.sign_in_provider 포함) or null
}
```

<!-- firebase.js에서 확인한 이메일 형식 -->
```javascript
const toAuthEmail = (username) => `${username}@ryek.app`;
// 예: teacher "김선생" → username 필드값@ryek.app
```

<!-- src/App.jsx handleLogin 현재 흐름 (line 776~783) -->
```javascript
const fbUser = await firebaseSignIn(username, password);
if (!fbUser) {
  // console.warn 제거됨 (Plan 01에서)
}
setUserPersist(appUser);
return true;
```
<!-- fbUser 취득 후, setUserPersist 전에 set-role 호출을 삽입해야 함 -->

<!-- firebase.js exports -->
```javascript
export { db, auth, doc, setDoc, onSnapshot, runTransaction,
         firebaseSignIn, firebaseSignInAnon, firebaseLogout, onAuthStateChanged };
// getIdToken은 firebase/auth 에서 import
import { getIdToken } from "firebase/auth";
```

<!-- ADMIN 계정 (constants.jsx에서 — username 확인 필요) -->
<!-- ADMIN.username 혹은 ADMIN.id로 admin 여부 판단 -->
<!-- set-role Worker에서 admin special-case: email이 "admin@ryek.app"이면 role=admin -->

<!-- Cloudflare Pages Functions 라우팅 -->
<!-- functions/api/auth/set-role.js → POST /api/auth/set-role 로 자동 매핑됨 -->
<!-- onRequest(context) export 형식 사용 (기존 ai/ Worker들과 동일) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>set-role Worker를 구현하기 전에 Firebase 서비스 계정 JSON을 Cloudflare Secret으로 등록해야 한다. 이 작업은 Claude가 수행할 수 없다.</what-built>
  <how-to-verify>
아래 순서를 따른다:

**Step 1: Firebase 서비스 계정 JSON 생성**
1. Firebase Console(console.firebase.google.com) → 프로젝트 `rye-k-center` → 프로젝트 설정(톱니 아이콘)
2. 서비스 계정(Service accounts) 탭 → "새 비공개 키 생성(Generate new private key)" 클릭
3. 다운로드된 JSON 파일을 텍스트 에디터로 열어 내용 전체를 확인

**Step 2: Cloudflare Secret 등록**
프로젝트 루트에서 실행:
```
npx wrangler secret put FIREBASE_SERVICE_ACCOUNT_JSON
```
프롬프트가 나타나면 JSON 파일의 전체 내용을 붙여넣고 Enter. 한 줄로 붙여넣거나 그대로 붙여넣어도 됨.

**Step 3: 확인**
```
npx wrangler secret list
```
출력에 `FIREBASE_SERVICE_ACCOUNT_JSON` 이 보이면 성공.

완료되면 아래 resume signal을 입력한다.
  </how-to-verify>
  <resume-signal>"서비스 계정 secret 등록 완료" 또는 "wrangler secret list에서 FIREBASE_SERVICE_ACCOUNT_JSON 확인됨"</resume-signal>
</task>

<task type="auto">
  <name>Task 1: set-role Worker 구현</name>
  <files>functions/api/auth/set-role.js</files>
  <action>
`functions/api/auth/set-role.js` 신규 파일을 생성한다. `functions/api/auth/` 디렉토리가 없으면 함께 생성한다.

아래 코드를 정확히 작성한다. 주석은 한국어로 유지한다:

```javascript
import { verifyToken } from "../ai/_utils/auth.js";
import { SignJWT, importPKCS8 } from "jose";

const FIREBASE_PROJECT_ID = "rye-k-center";
const FIREBASE_API_KEY = "AIzaSyDViGzxa0o1tqqX6fGr46Sfiews-ieGmks";

// Google OAuth2 Access Token 취득 (서비스 계정 JWT → Bearer token)
async function getGoogleAccessToken(env) {
  const sa = JSON.parse(env.FIREBASE_SERVICE_ACCOUNT_JSON);
  const privateKey = await importPKCS8(sa.private_key, "RS256");
  const now = Math.floor(Date.now() / 1000);
  const jwt = await new SignJWT({
    iss: sa.client_email,
    sub: sa.client_email,
    aud: "https://oauth2.googleapis.com/token",
    iat: now,
    exp: now + 3600,
    scope: [
      "https://www.googleapis.com/auth/identitytoolkit",
      "https://www.googleapis.com/auth/datastore",
    ].join(" "),
  })
    .setProtectedHeader({ alg: "RS256" })
    .sign(privateKey);

  const resp = await fetch("https://oauth2.googleapis.com/token", {
    method: "POST",
    headers: { "Content-Type": "application/x-www-form-urlencoded" },
    body: new URLSearchParams({
      grant_type: "urn:ietf:params:oauth2:grant-type:jwt-bearer",
      assertion: jwt,
    }),
  });
  const { access_token, error } = await resp.json();
  if (error) throw new Error(`Google OAuth 오류: ${error}`);
  return access_token;
}

// Firestore REST API로 rye-teachers 조회
async function fetchTeachers(accessToken) {
  const url = `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT_ID}/databases/(default)/documents/appData/rye-teachers`;
  const resp = await fetch(url, {
    headers: { Authorization: `Bearer ${accessToken}` },
  });
  if (!resp.ok) throw new Error(`Firestore 조회 실패: ${resp.status}`);
  const data = await resp.json();
  // rye-teachers 문서는 { value: Teacher[] } 구조로 저장됨
  const values = data.fields?.value?.arrayValue?.values || [];
  return values.map((v) => v.mapValue?.fields || {});
}

// Firestore 필드 값 추출 헬퍼
function strField(f) {
  return f?.stringValue || "";
}

// Firebase Auth REST — Custom Claims 설정
async function setCustomClaims(uid, claims, accessToken) {
  const url = `https://identitytoolkit.googleapis.com/v1/accounts:update?key=${FIREBASE_API_KEY}`;
  const resp = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${accessToken}`,
    },
    body: JSON.stringify({
      localId: uid,
      customAttributes: JSON.stringify(claims),
    }),
  });
  if (!resp.ok) {
    const err = await resp.json().catch(() => ({}));
    throw new Error(`Custom Claims 설정 실패: ${resp.status} ${JSON.stringify(err)}`);
  }
}

export async function onRequest(context) {
  const { request, env } = context;

  if (request.method !== "POST") {
    return new Response("Method Not Allowed", { status: 405 });
  }

  // 1. Firebase ID Token 검증 (email 사용자만 허용)
  const payload = await verifyToken(request);
  if (!payload) {
    return new Response("Unauthorized", { status: 401 });
  }
  // 익명 사용자 차단
  if (payload.firebase?.sign_in_provider !== "password") {
    return new Response("Unauthorized: email 로그인 전용", { status: 401 });
  }

  const uid = payload.sub;
  const email = payload.email || "";

  try {
    const accessToken = await getGoogleAccessToken(env);

    // 2. admin 특수 처리 — rye-teachers 조회 없이 바로 admin claim 부여
    //    admin 계정은 username="admin" → email="admin@ryek.app"
    if (email === "admin@ryek.app") {
      await setCustomClaims(uid, { role: "admin", teacherId: "admin" }, accessToken);
      return new Response(JSON.stringify({ ok: true, role: "admin" }), {
        headers: { "Content-Type": "application/json" },
      });
    }

    // 3. rye-teachers에서 email의 username 부분으로 강사 조회
    //    Firebase email = "${username}@ryek.app" → username = email.split("@")[0]
    const username = email.split("@")[0];
    const teachers = await fetchTeachers(accessToken);
    const teacher = teachers.find((f) => strField(f.username) === username);

    if (!teacher) {
      // 강사 레코드 없음 — 기본 role=teacher 부여 (계정은 존재하나 레코드 미생성 케이스)
      await setCustomClaims(uid, { role: "teacher", teacherId: "" }, accessToken);
      return new Response(JSON.stringify({ ok: true, role: "teacher", note: "teacher record not found" }), {
        headers: { "Content-Type": "application/json" },
      });
    }

    const role = strField(teacher.role) || "teacher";
    const teacherId = strField(teacher.id) || "";

    await setCustomClaims(uid, { role, teacherId }, accessToken);

    return new Response(JSON.stringify({ ok: true, role, teacherId }), {
      headers: { "Content-Type": "application/json" },
    });
  } catch (e) {
    console.error("[set-role] 오류:", e.message);
    return new Response(JSON.stringify({ error: e.message }), {
      status: 500,
      headers: { "Content-Type": "application/json" },
    });
  }
}
```

구현 완료 후 파일 라인 수를 확인: 100줄 이내여야 한다.
  </action>
  <verify>
    <automated>grep -n "onRequest\|verifyToken\|setCustomClaims\|getGoogleAccessToken" functions/api/auth/set-role.js</automated>
  </verify>
  <done>4개 함수/심볼 모두 파일 내 존재. npm run build 통과.</done>
</task>

<task type="auto">
  <name>Task 2: App.jsx — 로그인 후 set-role 호출 + token 강제 갱신</name>
  <files>src/App.jsx</files>
  <action>
`handleLogin` 함수에서 `fbUser` 취득 성공 후, `setUserPersist(appUser)` 호출 전에 set-role Worker 호출과 token 강제 갱신 로직을 삽입한다.

현재 코드 (Plan 01에서 수정 후):
```javascript
const fbUser = await firebaseSignIn(username, password);
setUserPersist(appUser);
return true;
```

수정 후:
```javascript
const fbUser = await firebaseSignIn(username, password);

// SEC-05: Firebase Custom Claims 설정 (role, teacherId)
// fbUser가 있어야만 실행 — Firebase Auth 실패 시 건너뜀
if (fbUser) {
  try {
    const idToken = await getIdToken(fbUser);
    await fetch("/api/auth/set-role", {
      method: "POST",
      headers: { Authorization: `Bearer ${idToken}` },
    });
    // Claims가 설정된 후 token 강제 갱신 — Firestore rules에서 role claim 즉시 인식
    await getIdToken(fbUser, /* forceRefresh= */ true);
  } catch (e) {
    console.error("[auth] set-role 호출 실패:", e);
    // set-role 실패 시에도 로컬 로그인은 계속 진행
  }
}

setUserPersist(appUser);
return true;
```

`getIdToken`은 App.jsx 상단의 import에 추가해야 한다. 현재 import 확인:
- Line 1: `import { useState, useEffect, lazy, Suspense } from "react";`
- Line 2: `import { db, auth, doc, setDoc, onSnapshot, runTransaction, firebaseSignIn, firebaseSignInAnon, firebaseLogout, onAuthStateChanged } from "./firebase.js";`

`getIdToken`은 `firebase/auth` 패키지에서 import한다. App.jsx 상단에 아래 라인을 추가한다 (기존 import 블록 직후):
```javascript
import { getIdToken } from "firebase/auth";
```

주의: `firebase/auth`는 이미 프로젝트에 설치되어 있으므로 추가 패키지 설치 불필요.
  </action>
  <verify>
    <automated>grep -n "set-role\|getIdToken\|forceRefresh" src/App.jsx</automated>
  </verify>
  <done>
1. `grep` 결과에 `/api/auth/set-role`, `getIdToken`, `forceRefresh` 모두 포함
2. `npm run build` 통과
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>set-role Worker가 배포되었고 App.jsx 로그인 흐름에서 호출된다. 실제 동작을 검증해야 한다.</what-built>
  <how-to-verify>
**전제: Cloudflare Pages에 배포가 완료되어야 한다.**
Nick이 `git push`로 Cloudflare Pages 자동 배포를 트리거한 후 아래를 확인한다.

**Step 1: 강사 계정으로 로그인**
1. 앱에서 강사 계정(admin 외 일반 강사)으로 로그인
2. 브라우저 DevTools → Network 탭 → `/api/auth/set-role` 요청 확인
3. 응답이 `{"ok":true,"role":"teacher","teacherId":"..."}` 형태여야 함

**Step 2: Firebase Auth Custom Claims 확인**
1. Firebase Console → Authentication → Users → 해당 강사 계정 클릭
2. "Custom claims" 필드에 `{"role":"teacher","teacherId":"t2"}` 형태가 표시되면 성공

**Step 3: admin 계정 확인**
1. admin 계정으로 로그인
2. `/api/auth/set-role` 응답이 `{"ok":true,"role":"admin"}` 이어야 함
3. Firebase Console에서 admin UID의 Custom claims에 `{"role":"admin"}` 확인

세 단계 모두 통과하면 Plan 04(Firestore rules) 진행 가능.
  </how-to-verify>
  <resume-signal>"set-role 검증 완료. 강사 claim: teacher ✓, admin claim: admin ✓" 또는 발견된 오류 내용</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client App.jsx → /api/auth/set-role | Firebase ID Token이 Bearer로 전달됨 — verifyToken으로 검증 |
| set-role Worker → Firebase Auth REST | Google OAuth2 서비스 계정 JWT 사용 — FIREBASE_SERVICE_ACCOUNT_JSON은 Cloudflare Secret |
| set-role Worker → Firestore REST | 동일 access token 사용 — rye-teachers 읽기 전용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | set-role Worker 입력 토큰 | mitigate | verifyToken(request): Google JWKS로 Firebase ID Token 검증 — 위조 토큰 차단 |
| T-03-02 | Elevation of Privilege | set-role role 결정 로직 | mitigate | role은 Firestore rye-teachers에서 읽음 — 클라이언트가 전달하는 값 아님 |
| T-03-03 | Information Disclosure | FIREBASE_SERVICE_ACCOUNT_JSON | mitigate | Cloudflare Secret에 저장 — 소스 코드/wrangler.toml에 포함되지 않음 |
| T-03-04 | Spoofing | anonymous user calling set-role | mitigate | sign_in_provider !== "password" 체크로 401 반환 |
| T-03-05 | Elevation of Privilege | admin email 특수 처리 | accept | admin@ryek.app 이메일 소유자는 Firebase Auth에서 별도 관리 — 비밀번호 탈취 위험은 VITE_AUTH_SALT로 완화 |
</threat_model>

<verification>
1. `grep -n "onRequest\|verifyToken\|setCustomClaims" functions/api/auth/set-role.js` → 3개 심볼 확인
2. `grep -n "set-role\|getIdToken" src/App.jsx` → 2개 패턴 확인
3. `grep -n "FIREBASE_SERVICE_ACCOUNT_JSON" functions/api/auth/set-role.js` → 1줄 확인
4. `npm run build` 통과
5. (배포 후) Firebase Console Custom Claims 확인 (체크포인트)
</verification>

<success_criteria>
- functions/api/auth/set-role.js 파일 존재
- POST /api/auth/set-role 에 email 사용자 token 전송 시 200 + {ok:true, role} 반환
- anonymous token 전송 시 401 반환
- App.jsx 로그인 후 set-role 호출 + getIdToken(true) 강제 갱신 코드 존재
- Firebase Console에서 강사/admin 계정의 Custom Claims 설정 확인됨 (체크포인트)
</success_criteria>

<output>
완료 후 `.planning/phases/01-security-foundation/01-03-SUMMARY.md` 생성
</output>
