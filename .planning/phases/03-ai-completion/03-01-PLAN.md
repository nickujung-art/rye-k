---
phase: 03-ai-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/api/ai/_utils/gemini.js
  - functions/api/ai/monthly-report.js
  - functions/api/ai/churn.js
  - functions/api/ai/lesson-note.js
  - functions/api/ai/reply-suggest.js
  - functions/api/ai/payment-tone.js
  - functions/api/ai/practice-guide.js
  - functions/api/ai/punctuate.js
  - functions/api/ai/query.js
autonomous: true
requirements:
  - AI-01
  - SEC-08

must_haves:
  truths:
    - "gemini.js 파일이 존재하고 callGemini 함수를 export한다"
    - "anthropic.js 파일이 삭제되거나 더 이상 import되지 않는다"
    - "모든 AI Worker가 callGemini를 import하여 정상 동작한다"
    - "monthly-report.js와 churn.js에서 Gemini로 전송되는 프롬프트에 실제 학생 이름이 없다"
    - "응답 결과에서 학생A/학생B 레이블이 실제 이름으로 복원된다"
  artifacts:
    - path: "functions/api/ai/_utils/gemini.js"
      provides: "callGemini, callGeminiTools export"
      exports: ["callGemini", "callGeminiTools"]
    - path: "functions/api/ai/monthly-report.js"
      provides: "월별 리포트 생성 Worker (익명화 적용)"
      contains: "callGemini"
    - path: "functions/api/ai/churn.js"
      provides: "이탈 위험 분석 Worker (익명화 적용)"
      contains: "callGemini"
  key_links:
    - from: "functions/api/ai/*.js (8개)"
      to: "functions/api/ai/_utils/gemini.js"
      via: "import { callGemini } from './_utils/gemini.js'"
      pattern: "callGemini"
    - from: "functions/api/ai/monthly-report.js"
      to: "functions/api/ai/_utils/anonymize.js"
      via: "buildNameMap + deanonymize"
      pattern: "deanonymize"
    - from: "functions/api/ai/churn.js"
      to: "functions/api/ai/_utils/anonymize.js"
      via: "buildNameMap + deanonymize"
      pattern: "deanonymize"
---

<objective>
AI-01: `anthropic.js` → `gemini.js` 파일명·함수명 통일, 8개 Worker import 경로 일괄 수정
SEC-08: `monthly-report.js`와 `churn.js`에서 studentName을 Gemini 전송 전 `anonymize.js`로 익명화

Purpose: Worker 파일명이 실제 호출 대상(Gemini)을 반영하지 않아 생기는 혼란을 제거하고,
학생 PII(이름)가 외부 AI API로 전송되는 보안 리스크를 차단한다.

Output:
- `functions/api/ai/_utils/gemini.js` (callAnthropic → callGemini 이름 변경)
- 8개 Worker 파일: import 경로 `anthropic` → `gemini` + 함수명 `callAnthropic` → `callGemini`
- `monthly-report.js`: SEC-08 익명화 파이프라인 추가
- `churn.js`: SEC-08 익명화 파이프라인 추가
</objective>

<execution_context>
@C:\Users\GIGABYTE\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GIGABYTE\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-CONTEXT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-RESEARCH.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-PATTERNS.md

<interfaces>
<!-- 핵심 유틸리티 — 실행자가 변경 없이 재사용할 코드 -->

functions/api/ai/_utils/anonymize.js (변경 없음 — 그대로 사용):
```javascript
const LABELS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

export function buildNameMap(names) {  // 이름 배열 → {실명: "학생A"} 맵
  const map = {};
  names.forEach((name, i) => {
    if (name && i < LABELS.length) map[name] = `학생${LABELS[i]}`;
  });
  return map;
}

export function anonymize(text, nameMap) { ... }   // text에서 실명을 레이블로 치환
export function deanonymize(text, nameMap) { ... } // text에서 레이블을 실명으로 복원
```

functions/api/ai/lesson-note.js (SEC-08 익명화의 검증된 레퍼런스):
```javascript
import { callAnthropic } from "./_utils/anthropic.js";  // → callGemini, gemini.js 로 바꾸는 대상
import { buildNameMap, anonymize, deanonymize } from "./_utils/anonymize.js";

// 단건 익명화 패턴
const nameMap = buildNameMap([studentName]);
const anonName = nameMap[studentName] || studentName;
// ... Gemini 호출 시 anonName 사용 ...
if (Object.keys(nameMap).length) result = deanonymize(result, nameMap);
```

functions/api/ai/churn.js 현재 구조 (수정 전):
```javascript
import { callAnthropic } from "./_utils/anthropic.js";
import { stripPii } from "./_utils/pii-guard.js";
// 현재: students[].name을 raw로 Gemini 전송 — SEC-08 위반
const studentList = top.map(s => `- ${s.name}: ...`).join("\n");
```

functions/api/ai/monthly-report.js 현재 구조 (수정 전):
```javascript
import { callAnthropic } from "./_utils/anthropic.js";
// 현재: studentName을 raw로 Gemini 전송 (line 69) — SEC-08 위반
const lines = [`회원: ${studentName}`, ...];
const result = await callAnthropic(env.GEMINI_API_KEY, { ... });
return json({ result });  // deanonymize 없음
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: anthropic.js → gemini.js 생성 및 8개 Worker import 일괄 수정 (AI-01)</name>
  <files>
    functions/api/ai/_utils/gemini.js,
    functions/api/ai/monthly-report.js,
    functions/api/ai/churn.js,
    functions/api/ai/lesson-note.js,
    functions/api/ai/reply-suggest.js,
    functions/api/ai/payment-tone.js,
    functions/api/ai/practice-guide.js,
    functions/api/ai/punctuate.js,
    functions/api/ai/query.js
  </files>
  <read_first>
    - functions/api/ai/_utils/anthropic.js — 이 파일 전체가 gemini.js의 기반. 내부 로직 그대로 유지, 이름만 바꿈.
    - functions/api/ai/lesson-note.js — import 패턴 참조 (callAnthropic → callGemini)
    - functions/api/ai/reply-suggest.js — import 라인 확인 (callAnthropic 사용 여부)
    - functions/api/ai/payment-tone.js — import 라인 확인
    - functions/api/ai/practice-guide.js — import 라인 확인
    - functions/api/ai/punctuate.js — import 라인 확인
    - functions/api/ai/query.js — import 라인 확인 (callGeminiTools 사용하는지도 확인)
  </read_first>
  <action>
1. `functions/api/ai/_utils/gemini.js` 신규 생성 — anthropic.js를 복사해 함수명만 변경:
   - `callAnthropic` → `callGemini` (export 이름 포함, 함수 시그니처 동일)
   - `callGeminiTools` — 이미 올바른 이름, 그대로 유지
   - 내부 로직(fetch URL, JSON 구조, error handling) 변경 없음
   - 파일 상단에 주석: `// gemini.js — anthropic.js에서 이름 정리 (Phase 3 AI-01)`

2. 8개 Worker 파일에서 import 경로·함수명 수정:
   - `import { callAnthropic } from "./_utils/anthropic.js"` → `import { callGemini } from "./_utils/gemini.js"`
   - 파일 내 모든 `callAnthropic(` → `callGemini(`
   - `callGeminiTools`를 사용하는 파일(query.js 등)은 import 경로만 anthropic → gemini로 변경, 함수명 유지
   - 대상 8개: monthly-report.js, churn.js, lesson-note.js, reply-suggest.js, payment-tone.js, practice-guide.js, punctuate.js, query.js

3. anthropic.js 파일은 삭제하지 않고 유지 (Cloudflare Pages 빌드에서 미사용 파일은 무해함).
   단, 어떤 Worker도 더 이상 anthropic.js를 import하지 않아야 한다.

4. 수정 전 grep으로 실제 `callAnthropic` 사용 확인 후 편집:
   ```
   grep -n "callAnthropic\|anthropic" functions/api/ai/*.js
   ```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "callAnthropic" functions/api/ai/` → 결과 없음 (0건)
    - `grep -r "from.*anthropic" functions/api/ai/` → 결과 없음 (0건)
    - `grep -c "callGemini" functions/api/ai/_utils/gemini.js` → 2 이상 (export 선언 포함)
    - `grep -l "callGemini" functions/api/ai/` → 8개 Worker 파일 + gemini.js 포함
    - `npm run build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>gemini.js 파일 존재 + callGemini export + 8개 Worker 모두 gemini.js import + 빌드 통과</done>
</task>

<task type="auto">
  <name>Task 2: monthly-report.js + churn.js SEC-08 studentName 익명화 파이프라인 적용</name>
  <files>
    functions/api/ai/monthly-report.js,
    functions/api/ai/churn.js
  </files>
  <read_first>
    - functions/api/ai/monthly-report.js — Task 1 완료 후의 파일 상태 확인 (callGemini로 변경된 상태)
    - functions/api/ai/churn.js — Task 1 완료 후의 파일 상태 확인
    - functions/api/ai/_utils/anonymize.js — buildNameMap/anonymize/deanonymize 함수 시그니처 확인
    - functions/api/ai/lesson-note.js — SEC-08 익명화 완성 레퍼런스 (단건 패턴)
  </read_first>
  <action>
**monthly-report.js 수정 (단건 익명화):**

1. import 최상단에 추가:
   ```javascript
   import { buildNameMap, deanonymize } from "./_utils/anonymize.js";
   ```

2. `body = stripPii(body);` 다음 줄 (studentName 구조분해 직전)에 추가:
   ```javascript
   const { studentName, instruments, audience, month, attendanceSummary, conditionTrend, noteSummaries, commentCount } = body;
   ```
   이 구조분해는 기존 그대로 두고, 그 아래에 익명화 코드 삽입:
   ```javascript
   // SEC-08: Gemini로 전송 전 학생 실명 익명화
   const nameMap = studentName ? buildNameMap([studentName]) : {};
   const anonName = nameMap[studentName] || studentName;
   ```

3. lines 배열에서 `studentName` 사용 부분을 `anonName`으로 교체 (line 69):
   ```javascript
   // 변경 전: `회원: ${studentName}`
   // 변경 후:
   `회원: ${anonName}`,
   ```

4. `return json({ result });` 직전에 deanonymize 적용:
   ```javascript
   // SEC-08: 응답에서 학생A → 실명 복원
   const finalResult = Object.keys(nameMap).length ? deanonymize(result, nameMap) : result;
   return json({ result: finalResult });
   ```

**churn.js 수정 (다건 익명화):**

1. import에 추가:
   ```javascript
   import { buildNameMap, deanonymize } from "./_utils/anonymize.js";
   ```

2. `const top = students.slice(0, 5);` 다음 줄에 추가:
   ```javascript
   // SEC-08: 배열 전체 이름 익명화 맵 생성 (buildNameMap 최대 26명 — top은 5명 이므로 안전)
   const nameMap = buildNameMap(top.map(s => s.name));
   ```

3. `studentList` 생성 시 s.name → 익명화된 이름으로 교체:
   ```javascript
   const studentList = top.map(s => {
     const anonName = nameMap[s.name] || s.name;
     const parts = [];
     if (s.consecutive >= 2) parts.push(`연속 결석 ${s.consecutive}회`);
     if (s.rate != null) parts.push(`최근 4주 출석률 ${s.rate}%`);
     const level = s.score >= 50 ? "위험" : "주의";
     return `- ${anonName}: ${parts.join(", ")} (위험도: ${level})`;
   }).join("\n");
   ```

4. JSON parse 결과에서 name 필드 복원:
   ```javascript
   // 기존:
   // if (match) comments = JSON.parse(match[0]);
   // 변경: name 필드를 deanonymize
   if (match) {
     const raw = JSON.parse(match[0]);
     comments = raw.map(c => ({ ...c, name: deanonymize(c.name, nameMap) }));
   }
   ```

**주의사항:**
- `anonymize()` 함수는 text 전체 치환용이므로 사용하지 않음. buildNameMap으로 맵 생성 후 직접 `nameMap[name] || name`으로 단건 조회
- deanonymize는 학생A 같은 레이블이 결과 텍스트에 포함된 경우 전체 치환 처리
- monthly-report.js의 `systemPrompt`에는 studentName이 없으므로 systemPrompt 수정 불필요
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "buildNameMap\|deanonymize" functions/api/ai/monthly-report.js` → 2건 이상 매치
    - `grep -n "buildNameMap\|deanonymize" functions/api/ai/churn.js` → 2건 이상 매치
    - `grep -n "studentName\b" functions/api/ai/monthly-report.js` → `anonName`이 lines 배열 내 `회원:` 라인에서 사용됨 (직접 studentName이 Gemini 전송 라인에 없음)
    - `grep "회원: \${studentName}" functions/api/ai/monthly-report.js` → 결과 없음 (raw studentName이 라인에 없음, anonName 사용)
    - `npm run build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>
    monthly-report.js: buildNameMap + deanonymize 적용, lines 배열의 `회원:` 라인이 anonName 사용.
    churn.js: buildNameMap + studentList에 anonName 적용, JSON 파싱 후 name 필드 deanonymize.
    빌드 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Cloudflare Worker | Firebase ID Token 검증 (_middleware.js auth.js) |
| Worker → Gemini API | API Key 인증 (env.GEMINI_API_KEY) |
| Worker 내 프롬프트 구성 | studentName이 익명화 없이 포함되는 것이 위협 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | monthly-report.js, churn.js | mitigate | SEC-08: buildNameMap → anonName → deanonymize 파이프라인 적용 (Task 2) |
| T-03-02 | Denial of Service | 모든 AI Worker | accept | 기존 RATE_LIMIT_KV (20 req/min/user) 유지 — 추가 조치 불필요 |
| T-03-03 | Elevation of Privilege | _middleware.js Firebase JWT | accept | 기존 auth.js + jose 검증 유지 — 이번 계획 범위 외 |
</threat_model>

<verification>
Phase 3 Wave 1 완료 조건:
1. `grep -r "callAnthropic" functions/api/ai/` → 0건
2. `grep -r "from.*anthropic" functions/api/ai/` → 0건
3. `functions/api/ai/_utils/gemini.js` 파일 존재
4. `grep "deanonymize" functions/api/ai/monthly-report.js` → 1건 이상
5. `grep "deanonymize" functions/api/ai/churn.js` → 1건 이상
6. `npm run build` exit code 0
</verification>

<success_criteria>
- gemini.js가 존재하고 callGemini를 export한다
- 8개 Worker 파일이 모두 gemini.js에서 callGemini를 import한다
- monthly-report.js 프롬프트의 `회원:` 라인에 실제 studentName 대신 anonName이 들어간다
- churn.js studentList에 실제 학생 이름 대신 학생A, 학생B 형태가 들어간다
- 빌드가 통과한다
</success_criteria>

<output>
완료 후 `.planning/phases/03-ai-completion/03-01-SUMMARY.md` 생성
</output>
