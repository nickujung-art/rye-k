---
phase: 08-group-lesson-enhancement
plan: "04"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/components/attendance/Attendance.jsx
autonomous: true
requirements:
  - GRP-04
must_haves:
  truths:
    - "Attendance 그룹 헤더(2명 이상)에 슬롯 이름이 표시된다"
    - "slotId 없으면 기존 instrument·time·teacher 형식 유지된다"
    - "detectLessonGroups 함수가 slotId를 그룹 객체에 포함한다"
  artifacts:
    - path: src/components/attendance/Attendance.jsx
      provides: "lessonSlots prop 수신 + group.slotId + 헤더 슬롯 이름 표시"
      contains: "lessonSlots"
  key_links:
    - from: "detectLessonGroups 반환값 group.slotId"
      to: lessonSlots array
      via: "lessonSlots.find(s => s.id === group.slotId)?.name"
      pattern: "group\\.slotId"
---

<objective>
Attendance.jsx의 그룹 감지 함수가 slotId를 포함하도록 확장하고, 그룹 헤더에서 lessonSlots 데이터를 이용해 슬롯 이름을 표시한다.

Purpose: 출석 체크 화면에서 그룹 레슨 이름이 의미 있게 표시되어 강사와 관리자가 어떤 반인지 즉시 인식할 수 있다.
Output: Attendance 그룹 헤더에 slot.name 표시 (fallback: 기존 instrument 기반 텍스트)
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-group-lesson-enhancement/08-CONTEXT.md
@.planning/phases/08-group-lesson-enhancement/08-01-SUMMARY.md

<interfaces>
<!-- detectLessonGroups 현재 구현 (line 347-367) -->
```js
function detectLessonGroups(students, dayName, filterTeacher) {
  const grouped = {};
  students.forEach(s => {
    (s.lessons || []).forEach(l => {
      if (filterTeacher !== "all" && (l.teacherId || s.teacherId) !== filterTeacher) return;
      (l.schedule || []).forEach(sc => {
        if (sc.day !== dayName) return;
        const instPrefix = s.isInstitution ? `inst_${s.id}__` : "";
        const key = `${instPrefix}${l.teacherId || s.teacherId}__${sc.time || ""}__${l.instrument}`;
        if (!grouped[key]) grouped[key] = {
          teacherId: l.teacherId || s.teacherId,
          time: sc.time || "",
          instrument: l.instrument,
          students: []
        };
        if (!grouped[key].students.find(x => x.id === s.id)) grouped[key].students.push(s);
      });
    });
  });
  return Object.entries(grouped).map(([key, g]) => ({ ...g, key, isGroup: g.students.length > 1 }))
    .sort((a, b) => (a.time || "").localeCompare(b.time || ""));
}
```
<!-- grouped[key] 초기화 시 slotId: l.slotId || null 추가 필요 -->

<!-- 그룹 헤더 렌더 (line 587-592) — 변경 대상 -->
```jsx
{group.isGroup && (
  <div ... onClick={() => toggleGroupCollapse(group.key)}>
    <span style={{fontSize:11,color:"var(--blue)",fontWeight:700,flex:1}}>
      {group.instrument} · {group.time && `${group.time} · `}{teacher?.name || ""} 강사 · {group.students.length}명
    </span>
    ...
  </div>
)}
```

<!-- AttendanceView 현재 시그니처 (line 369) -->
```js
function AttendanceView({ students, teachers, currentUser, attendance,
  onSaveAttendance, categories, scheduleOverrides, onSaveScheduleOverride,
  onUpdateStudent, showToast })
// 08-01 이후 lessonSlots prop 추가됨
```

<!-- D-05: Attendance 슬롯 이름 조회 규칙 -->
<!--
  - 그룹 key: teacherId|day|time|instrument (기존 로직 유지)
  - 슬롯 이름 조회: group.slotId → lessonSlots.find(s => s.id === group.slotId)?.name
  - fallback: slotId 없거나 lessonSlots에서 찾지 못하면 기존 표시 유지
  - 편집 UI 없음 (Attendance는 읽기 전용 표시만)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attendance.jsx — detectLessonGroups slotId + 그룹 헤더 슬롯 이름</name>
  <read_first>
    - src/components/attendance/Attendance.jsx (line 345-370 detectLessonGroups 함수, line 369 AttendanceView 시그니처, line 575-600 그룹 헤더 렌더 영역)
  </read_first>
  <files>src/components/attendance/Attendance.jsx</files>
  <action>
변경 1: AttendanceView 함수 시그니처에 lessonSlots 추가
```js
function AttendanceView({ students, teachers, currentUser, attendance,
  onSaveAttendance, categories, scheduleOverrides, onSaveScheduleOverride,
  onUpdateStudent, showToast, lessonSlots })
```

변경 2: detectLessonGroups 함수 내 grouped[key] 초기화 부분에 slotId 추가
현재:
```js
if (!grouped[key]) grouped[key] = {
  teacherId: l.teacherId || s.teacherId,
  time: sc.time || "",
  instrument: l.instrument,
  students: []
};
```
변경 후:
```js
if (!grouped[key]) grouped[key] = {
  teacherId: l.teacherId || s.teacherId,
  time: sc.time || "",
  instrument: l.instrument,
  slotId: l.slotId || null,   // ← 추가
  students: []
};
```
주의: slotId는 첫 번째 학생의 lesson에서만 가져옴. 같은 그룹의 학생들은 동일 슬롯이므로 첫 번째 값이면 충분.

변경 3: 그룹 헤더 렌더 영역에서 슬롯 이름 표시 추가
현재 span 내용:
```jsx
{group.instrument} · {group.time && `${group.time} · `}{teacher?.name || ""} 강사 · {group.students.length}명
```
변경 후 (span 내용 교체):
```jsx
{(() => {
  const slot = group.slotId ? (lessonSlots || []).find(s => s.id === group.slotId) : null;
  const slotName = slot?.name;
  return slotName ? (
    <>
      <strong>{slotName}</strong>
      {" · "}{group.instrument} · {group.time && `${group.time} · `}{teacher?.name || ""} 강사 · {group.students.length}명
    </>
  ) : (
    <>{group.instrument} · {group.time && `${group.time} · `}{teacher?.name || ""} 강사 · {group.students.length}명</>
  );
})()}
```

규칙:
- detectLessonGroups 함수 시그니처는 변경하지 말 것 — lessonSlots를 인자로 받지 않음 (AttendanceView 내부에서 참조)
- 편집 UI 없음 (Attendance는 읽기 전용)
- window.confirm / window.alert 없음
  </action>
  <verify>
    <automated>grep -n "lessonSlots\|slotId\|slotName" src/components/attendance/Attendance.jsx | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "lessonSlots" src/components/attendance/Attendance.jsx` → AttendanceView 시그니처 + 헤더 렌더 2줄 이상
    - `grep "slotId" src/components/attendance/Attendance.jsx` → detectLessonGroups 초기화 줄 + 헤더 조회 줄 2줄 이상
    - `grep "slot?.name\|slot\?.name" src/components/attendance/Attendance.jsx` → 조회 줄 출력
    - npm run build 통과
  </acceptance_criteria>
  <done>Attendance 그룹 헤더에서 slotId 기반 슬롯 이름이 표시되고, 없으면 기존 instrument/time/teacher 형식이 유지된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client read | Attendance.jsx가 lessonSlots prop으로 슬롯 이름 읽기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | 슬롯 이름 표시 | accept | 슬롯 이름은 PII 아님(악기+반 명칭). 이미 강사/관리자 로그인 상태에서만 Attendance 접근 가능 |
</threat_model>

<verification>
1. npm run build 통과
2. `grep "slotId: l.slotId" src/components/attendance/Attendance.jsx` → detectLessonGroups 초기화 줄 출력
3. `grep "lessonSlots" src/components/attendance/Attendance.jsx` → 시그니처 + 렌더 줄
4. 빌드 후 Attendance 뷰에서 그룹 헤더가 정상 렌더 (크래시 없음)
</verification>

<success_criteria>
- detectLessonGroups 반환값에 slotId 포함
- AttendanceView가 lessonSlots prop 수신
- 그룹 헤더: slotId → slot.name 표시, fallback: instrument 기반 표시 유지
- 편집 UI 없음 (읽기 전용)
</success_criteria>

<output>
완료 후 `.planning/phases/08-group-lesson-enhancement/08-04-SUMMARY.md` 생성
</output>
