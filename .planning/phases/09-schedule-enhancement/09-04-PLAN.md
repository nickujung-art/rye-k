---
phase: 09-schedule-enhancement
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - src/components/student/PauseManagementView.jsx
  - src/components/layout/NavLayout.jsx
  - src/components/dashboard/Dashboard.jsx
  - src/components/admin/AdminTools.jsx
autonomous: true
requirements:
  - SCH-04
  - SCH-05

must_haves:
  truths:
    - "사이드바(데스크톱)에 '휴회 관리' 항목이 schedule 다음에 표시된다 (teacher·manager·admin)"
    - "모바일 더보기 메뉴에 '휴회 관리' 항목이 표시된다"
    - "BottomNav '더보기' 탭 활성화 범위에 pauseManagement가 포함된다"
    - "PauseManagementView에 휴회 학생 카드가 표시된다 (이름, 강사, 악기, 휴회기간, 경과일, 슬롯 이력)"
    - "각 카드에 케어로그 입력 UI가 있다 (기존 Dashboard 케어로그 기능 동일)"
    - "'복귀 처리' 버튼 클릭 시 window.confirm 없이 인라인 [확인][취소] 버튼이 표시된다"
    - "[확인] 클릭 시 onResumeStudent가 호출된다"
    - "Dashboard '휴회 케어 관리' 섹션이 '휴회 관리 바로가기' 링크 배너로 교체된다"
    - "AdminTools LessonSlotsView에 '폐강' 버튼이 추가된다 (admin 전용)"
  artifacts:
    - path: "src/components/student/PauseManagementView.jsx"
      provides: "PauseManagementView + PauseCard + CareLogModal 컴포넌트"
      contains: "export default function PauseManagementView"
    - path: "src/components/layout/NavLayout.jsx"
      provides: "사이드바 + MoreMenu + BottomNav에 pauseManagement 메뉴 추가"
      contains: "pauseManagement"
    - path: "src/components/dashboard/Dashboard.jsx"
      provides: "휴회 케어 관리 섹션 → 링크 배너 교체"
      contains: "pm-link-banner"
    - path: "src/components/admin/AdminTools.jsx"
      provides: "LessonSlotsView 폐강 버튼 (D-05)"
      contains: "closed"
  key_links:
    - from: "NavLayout Sidebar/MoreMenu (pauseManagement)"
      to: "App.jsx view 라우팅 (view === 'pauseManagement')"
      via: "setView('pauseManagement') 호출"
      pattern: "pauseManagement"
    - from: "PauseCard '복귀 처리' 확인"
      to: "onResumeStudent (App.jsx)"
      via: "props.onResumeStudent(student) 호출"
      pattern: "onResumeStudent"
    - from: "Dashboard 링크 배너"
      to: "nav('pauseManagement') 또는 setView"
      via: "onClick"
      pattern: "pm-link-banner"
---

<objective>
PauseManagementView 신규 컴포넌트를 생성하고, NavLayout에 '휴회 관리' 메뉴를 추가하며, Dashboard의 인라인 케어로그 섹션을 링크 배너로 교체한다. AdminTools에 D-05 폐강 버튼을 추가한다.

Purpose: D-03(PauseManagementView 독립 뷰), D-05(폐강 최소 구현). 케어 기능이 전용 뷰에 집중되고 대시보드가 간소화된다.
Output: PauseManagementView.jsx (신규), NavLayout.jsx / Dashboard.jsx / AdminTools.jsx (수정).
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-schedule-enhancement/09-CONTEXT.md
@.planning/phases/09-schedule-enhancement/09-RESEARCH.md
@.planning/phases/09-schedule-enhancement/09-02-SUMMARY.md

<interfaces>
<!-- PauseManagementView props (RESEARCH.md Pattern 6) -->
```jsx
export default function PauseManagementView({
  students,        // visible (역할 필터 완료된 배열)
  teachers,
  currentUser,
  lessonSlots,     // 슬롯 이력 역조회용
  onUpdateStudent, // async (student) => void — updateStudentDoc 래퍼
  onResumeStudent, // async (student) => void — pauseHistory append + status active
  showToast,
})
```

<!-- Dashboard 현재 케어로그 데이터 구조 (RESEARCH.md line 506~523) -->
```js
// careLogs 항목:
{ id: string, createdAt: number, authorId: string, authorName: string,
  careType: "전화"|"문자"|"알림톡"|"기타",
  responseStatus: "응답없음"|"복귀 의향 있음"|"복귀 의향 없음"|"추후 연락",
  note: string }

// stage 판단 로직 (Dashboard.jsx line 456~534에서 이전):
const days = s.pausedAt ? Math.floor((Date.now() - s.pausedAt) / 86400000) : null;
const daysSinceCare = lastLog ? Math.floor((Date.now() - lastLog.createdAt) / 86400000) : null;
const needsCare = days >= 14 && (daysSinceCare === null || daysSinceCare >= 14);
const stage = !needsCare ? "ok" : days >= 30 ? "urgent" : "due";
```

<!-- NavLayout 현재 Sidebar nav 배열 (verified: line 72~95) -->
schedule 항목 (line ~79): `{ id: "schedule", label: "강사 스케줄", icon: "◫" }`
payments 항목 (line ~80): `{ id: "payments", label: "수납 관리", icon: "₩" }`

<!-- NavLayout MoreMenu items 배열 (verified: line 153~170) -->
schedule 항목 (line ~154): `{ id: "schedule", label: "강사 스케줄", desc: "주간/월간 시간표", icon: IC.schedule }`

<!-- BottomNav "더보기" catch 목록 (verified: line 18) -->
현재: `["teachers","notices","categories","profile","activity","pending","trash","studentNotices","analytics","lessonNotes","schedule","institutions","systemNews","monthlyReports","aiSettings","shop","lessonSlots","settlement"]`
추가: `"pauseManagement"`

<!-- AdminTools LessonSlotsView 위치 -->
AdminTools.jsx에 LessonSlotsView 또는 lessonSlots 관련 UI가 있음.
updateLessonSlot(id, { status: "closed" }) 호출로 폐강 처리.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PauseManagementView.jsx 신규 생성 (D-03)</name>
  <read_first>
    - src/components/dashboard/Dashboard.jsx — line 44~126 (CareLogModal 정의), line 456~534 (휴회 케어 관리 섹션 전체)
    - src/constants.jsx — .pm-card, .pm-resume-btn, .pm-confirm-row 등 Phase 9 CSS 클래스 확인 (Plan 01에서 추가됨)
    - CLAUDE.md — window.confirm 금지, per-op 트랜잭션 규칙
  </read_first>
  <files>src/components/student/PauseManagementView.jsx</files>
  <action>
    `src/components/student/PauseManagementView.jsx` 를 신규 생성한다. 이 파일은 Dashboard.jsx의 CareLogModal과 휴회 케어 관리 UI를 포함하며, 독립적으로 동작한다.

    파일 전체 내용:

    ```jsx
    import { useState } from "react";
    import { IC } from "../../constants.jsx";

    // ── CARE LOG MODAL (기존 Dashboard CareLogModal에서 이전) ─────────────────────
    function CareLogModal({ student, currentUser, onSave, onClose }) {
      const [careType, setCareType] = useState("전화");
      const [responseStatus, setResponseStatus] = useState("응답없음");
      const [note, setNote] = useState("");
      const [saving, setSaving] = useState(false);

      const handleSave = async () => {
        if (saving) return;
        setSaving(true);
        const newLog = {
          id: `care_${Date.now()}`,
          createdAt: Date.now(),
          authorId: currentUser.id,
          authorName: currentUser.name,
          careType,
          responseStatus,
          note: note.trim(),
        };
        const updCareLogs = [...(student.careLogs || []), newLog];
        await onSave({ ...student, careLogs: updCareLogs });
        setSaving(false);
        onClose();
      };

      return (
        <>
          <div style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,.4)", zIndex: 9000 }} onClick={onClose} />
          <div style={{
            position: "fixed", left: "50%", top: "50%",
            transform: "translate(-50%,-50%)",
            background: "var(--paper)", borderRadius: "var(--radius)",
            padding: 20, width: "min(90vw, 340px)", zIndex: 9001,
            boxShadow: "0 8px 32px rgba(0,0,0,.18)",
          }}>
            <div style={{ fontWeight: 600, fontSize: 15, marginBottom: 14 }}>
              케어로그 입력 — {student.name}
            </div>
            <div style={{ marginBottom: 10 }}>
              <div style={{ fontSize: 12, color: "var(--ink-30)", marginBottom: 4 }}>연락 방법</div>
              <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                {["전화","문자","알림톡","기타"].map(t => (
                  <button key={t}
                    onClick={() => setCareType(t)}
                    style={{
                      padding: "4px 10px", borderRadius: 6, fontSize: 12, cursor: "pointer",
                      fontFamily: "inherit",
                      border: careType === t ? "1px solid var(--blue)" : "1px solid var(--border)",
                      background: careType === t ? "var(--blue-lt)" : "var(--bg)",
                      color: careType === t ? "var(--blue)" : undefined,
                    }}
                  >{t}</button>
                ))}
              </div>
            </div>
            <div style={{ marginBottom: 10 }}>
              <div style={{ fontSize: 12, color: "var(--ink-30)", marginBottom: 4 }}>응답 여부</div>
              <select className="inp" value={responseStatus} onChange={e => setResponseStatus(e.target.value)} style={{ fontSize: 13 }}>
                {["응답없음","복귀 의향 있음","복귀 의향 없음","추후 연락"].map(s => <option key={s} value={s}>{s}</option>)}
              </select>
            </div>
            <div style={{ marginBottom: 14 }}>
              <div style={{ fontSize: 12, color: "var(--ink-30)", marginBottom: 4 }}>메모</div>
              <textarea
                className="inp"
                value={note}
                onChange={e => setNote(e.target.value)}
                placeholder="연락 내용, 특이사항..."
                rows={3}
                style={{ resize: "vertical", fontSize: 13 }}
              />
            </div>
            <div style={{ display: "flex", gap: 8, justifyContent: "flex-end" }}>
              <button onClick={onClose} style={{ padding: "7px 14px", borderRadius: 6, border: "1px solid var(--border)", background: "var(--bg)", cursor: "pointer", fontSize: 13, fontFamily: "inherit" }}>취소</button>
              <button onClick={handleSave} disabled={saving} style={{ padding: "7px 14px", borderRadius: 6, border: "none", background: "var(--blue)", color: "#fff", cursor: "pointer", fontSize: 13, fontFamily: "inherit" }}>
                {saving ? "저장 중..." : "저장"}
              </button>
            </div>
          </div>
        </>
      );
    }

    // ── PAUSE CARD ─────────────────────────────────────────────────────────────────
    function PauseCard({ student, teachers, lessonSlots, currentUser, onResume, onUpdateStudent, showToast }) {
      const [confirmingResume, setConfirmingResume] = useState(false);
      const [careModal, setCareModal] = useState(false);
      const [showHistory, setShowHistory] = useState(false);

      const teacher = teachers.find(t => t.id === (student.teacherId || (student.lessons||[])[0]?.teacherId));
      const mainInstrument = (student.lessons||[])[0]?.instrument || "";
      const days = student.pausedAt ? Math.floor((Date.now() - student.pausedAt) / 86400000) : null;
      const careLogs = student.careLogs || [];
      const lastLog = careLogs.length > 0 ? careLogs[careLogs.length - 1] : null;
      const daysSinceCare = lastLog ? Math.floor((Date.now() - lastLog.createdAt) / 86400000) : null;
      const needsCare = days >= 14 && (daysSinceCare === null || daysSinceCare >= 14);
      const stage = !needsCare ? "ok" : days >= 30 ? "urgent" : "due";

      // 슬롯 이력: student.lessons[].slotId → lessonSlots에서 name 조회
      const slotNames = (student.lessons || [])
        .map(l => l.slotId ? (lessonSlots || []).find(s => s.id === l.slotId)?.name : null)
        .filter(Boolean);

      // pauseHistory (이전 기록)
      const history = student.pauseHistory || [];

      return (
        <div className={`pm-card${stage === "urgent" ? " pm-card--urgent" : stage === "due" ? " pm-card--due" : ""}`}>
          <div className="pm-card-header">
            <div style={{ flex: 1 }}>
              <div className="pm-card-name">{student.name}</div>
              <div className="pm-card-meta">
                {teacher?.name || "강사 미배정"}{mainInstrument ? ` · ${mainInstrument}` : ""}
              </div>
            </div>
            <div className="pm-badge-row">
              {days !== null && (
                <span className={`pm-badge${stage === "urgent" ? " pm-badge--urgent" : stage === "due" ? " pm-badge--due" : ""}`}>
                  {days}일 경과
                </span>
              )}
              {stage === "urgent" && <span className="pm-badge pm-badge--urgent">케어 필요</span>}
              {stage === "due" && <span className="pm-badge pm-badge--due">케어 예정</span>}
            </div>
          </div>

          {student.pausedAt && (
            <div style={{ fontSize: 12, color: "var(--ink-30)", marginBottom: 6 }}>
              휴회 시작: {new Date(student.pausedAt).toLocaleDateString("ko-KR")}
              {student.pausedReason ? ` · 사유: ${student.pausedReason}` : ""}
            </div>
          )}

          {slotNames.length > 0 && (
            <div style={{ marginBottom: 6 }}>
              {slotNames.map((name, i) => <span key={i} className="pm-slot-tag">{name}</span>)}
            </div>
          )}

          {/* 케어로그 최근 1건 표시 */}
          {lastLog && (
            <div className="pm-care-log-row" style={{ marginBottom: 4 }}>
              <span className="pm-care-type">{lastLog.careType}</span>
              <span style={{ color: "var(--ink-60)" }}>{lastLog.responseStatus}</span>
              {lastLog.note && <span style={{ color: "var(--ink-30)", fontSize: 11 }}>{lastLog.note}</span>}
              <span style={{ marginLeft: "auto", fontSize: 10, color: "var(--ink-30)" }}>
                {daysSinceCare !== null ? `${daysSinceCare}일 전` : ""}
              </span>
            </div>
          )}

          {/* pauseHistory 이전 기록 accordion */}
          {history.length > 0 && (
            <div style={{ marginBottom: 6 }}>
              <button className="pm-accordion-toggle" onClick={() => setShowHistory(v => !v)}>
                이전 휴회 기록 {history.length}건 {showHistory ? "▲" : "▼"}
              </button>
              {showHistory && history.map((h, i) => (
                <div key={i} style={{ fontSize: 11, color: "var(--ink-30)", padding: "2px 0", borderBottom: "1px solid var(--border)" }}>
                  {h.pausedAt ? new Date(h.pausedAt).toLocaleDateString("ko-KR") : "날짜 없음"}
                  {h.durationDays != null ? ` (${h.durationDays}일)` : ""}
                  {h.pausedReason ? ` · ${h.pausedReason}` : ""}
                </div>
              ))}
            </div>
          )}

          {/* 액션 버튼 */}
          <div style={{ display: "flex", gap: 8, marginTop: 8, flexWrap: "wrap", alignItems: "center" }}>
            <button className="pm-resume-btn" onClick={() => setCareModal(true)}>
              케어로그 입력
            </button>
            {/* 복귀 처리 — window.confirm 절대 금지, 인라인 confirm UI */}
            {!confirmingResume ? (
              <button className="pm-resume-btn" onClick={() => setConfirmingResume(true)}
                style={{ borderColor: "var(--green)", background: "#f0fff4", color: "var(--green)" }}>
                복귀 처리
              </button>
            ) : (
              <div className="pm-confirm-row">
                <span className="pm-confirm-text">복귀 처리할까요?</span>
                <button className="pm-confirm-yes" onClick={async () => { setConfirmingResume(false); await onResume(student); }}>
                  확인
                </button>
                <button className="pm-confirm-no" onClick={() => setConfirmingResume(false)}>
                  취소
                </button>
              </div>
            )}
          </div>

          {careModal && (
            <CareLogModal
              student={student}
              currentUser={currentUser}
              onSave={async upd => { await onUpdateStudent(upd); showToast("케어로그가 저장되었습니다."); }}
              onClose={() => setCareModal(false)}
            />
          )}
        </div>
      );
    }

    // ── PAUSE MANAGEMENT VIEW ──────────────────────────────────────────────────────
    export default function PauseManagementView({
      students, teachers, currentUser, lessonSlots,
      onUpdateStudent, onResumeStudent, showToast,
    }) {
      const paused = (students || []).filter(s => s.status === "paused" && !s.isInstitution);

      // 케어 필요 순 정렬: urgent → due → ok, 그 안에서 경과일 내림차순
      const sorted = [...paused].sort((a, b) => {
        const stageOrder = { urgent: 0, due: 1, ok: 2 };
        const getStage = s => {
          const days = s.pausedAt ? Math.floor((Date.now() - s.pausedAt) / 86400000) : 0;
          const cl = s.careLogs || [];
          const last = cl.length > 0 ? cl[cl.length - 1] : null;
          const dsc = last ? Math.floor((Date.now() - last.createdAt) / 86400000) : null;
          const nc = days >= 14 && (dsc === null || dsc >= 14);
          return !nc ? "ok" : days >= 30 ? "urgent" : "due";
        };
        const sa = stageOrder[getStage(a)];
        const sb = stageOrder[getStage(b)];
        if (sa !== sb) return sa - sb;
        return (b.pausedAt || 0) - (a.pausedAt || 0);
      });

      return (
        <div className="pm-view">
          <div className="ph">
            <div>
              <h1>휴회 관리</h1>
              <p>휴회 학생 {paused.length}명</p>
            </div>
          </div>
          {sorted.length === 0 ? (
            <div className="pm-empty">현재 휴회 중인 회원이 없습니다.</div>
          ) : (
            sorted.map(s => (
              <PauseCard
                key={s.id}
                student={s}
                teachers={teachers}
                lessonSlots={lessonSlots}
                currentUser={currentUser}
                onResume={onResumeStudent}
                onUpdateStudent={onUpdateStudent}
                showToast={showToast}
              />
            ))
          )}
        </div>
      );
    }
    ```

    주의:
    - `window.confirm` 절대 사용 금지 — PauseCard의 `confirmingResume` state로 인라인 [확인][취소] 구현.
    - CareLogModal 내 onSave는 `onUpdateStudent(upd)` 호출 — `saveStudents([...])` 절대 금지.
    - PauseCard의 onResume prop은 App.jsx의 `onResumeStudent` 함수 — 내부에서 `updateStudentDoc` 호출.
  </action>
  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && ls src/components/student/PauseManagementView.jsx && grep -c "export default function PauseManagementView\|confirmingResume\|CareLogModal\|pm-confirm-yes" src/components/student/PauseManagementView.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/student/PauseManagementView.jsx` 파일 존재
    - `grep -c "export default function PauseManagementView" src/components/student/PauseManagementView.jsx` 결과가 1
    - `grep -c "window.confirm\|window.alert" src/components/student/PauseManagementView.jsx` 결과가 0 (금지 패턴 없음)
    - `grep -c "confirmingResume" src/components/student/PauseManagementView.jsx` 결과가 2 이상 (state + 렌더 분기)
    - `grep -c "pm-confirm-yes\|pm-confirm-no" src/components/student/PauseManagementView.jsx` 각 1 이상
    - `grep -c "saveStudents" src/components/student/PauseManagementView.jsx` 결과가 0
    - `npm run build` 통과 (App.jsx에서 import 추가된 상태)
  </acceptance_criteria>
  <done>PauseManagementView.jsx 생성 완료. 케어로그 입력, 복귀 처리 인라인 confirm, pauseHistory 이전 기록 accordion, 슬롯 이력 표시 모두 구현. npm run build 통과.</done>
</task>

<task type="auto">
  <name>Task 2: NavLayout + Dashboard + AdminTools 통합 (D-03, D-05)</name>
  <read_first>
    - src/components/layout/NavLayout.jsx — line 7~30 (import), line 72~95 (Sidebar nav 배열), line 153~170 (MoreMenu items), line 18 (BottomNav "더보기" catch 목록)
    - src/components/dashboard/Dashboard.jsx — line 44~126 (CareLogModal 및 휴회 섹션 초반), line 456~540 (휴회 케어 관리 섹션 전체 범위)
    - src/components/admin/AdminTools.jsx — LessonSlotsView 또는 레슨 슬롯 관련 UI 위치 확인
    - src/constants.jsx — IC.pause 추가 확인 (Plan 01), .pm-link-banner CSS 확인
  </read_first>
  <files>src/components/layout/NavLayout.jsx, src/components/dashboard/Dashboard.jsx, src/components/admin/AdminTools.jsx</files>
  <action>
    **NavLayout.jsx 변경 1: Sidebar nav 배열에 '휴회 관리' 추가**

    Sidebar 함수(line ~71) 내 nav 배열에서 `{ id: "schedule", ... }` 항목 다음에 추가:
    ```js
    ...(canManageAll(user.role) || user.role === "teacher"
      ? [{ id: "pauseManagement", label: "휴회 관리", icon: "⏸" }]
      : []),
    ```

    **NavLayout.jsx 변경 2: MoreMenu items 배열에 '휴회 관리' 추가**

    MoreMenu 함수(line ~147) 내 items 배열에서 schedule 항목 다음에 추가:
    ```js
    ...(canManageAll(user.role) || user.role === "teacher"
      ? [{ id: "pauseManagement", label: "휴회 관리", desc: "휴회 학생 케어 · 복귀 처리", icon: IC.pause }]
      : []),
    ```

    NavLayout.jsx 상단에 IC import가 이미 있으면 IC.pause를 사용한다. MoreMenu에서 icon은 IC 객체 참조로 처리됨.

    **NavLayout.jsx 변경 3: BottomNav '더보기' 활성화 catch 목록에 pauseManagement 추가**

    BottomNav 함수(line ~7) 내 `tabs` 배열에서 "more" 탭의 className 조건 (line ~18):
    현재:
    ```js
    view === t.id || (t.id === "more" && ["teachers","notices",...,"settlement"].includes(view))
    ```
    의 배열 끝에 `"pauseManagement"` 추가:
    ```js
    [...기존 배열..., "pauseManagement"]
    ```

    ---

    **Dashboard.jsx 변경: '휴회 케어 관리' 섹션 → 링크 배너 교체**

    Dashboard.jsx를 읽어 휴회 케어 관리 섹션(line ~456~534 주변)의 정확한 범위를 확인한다. 해당 섹션의 시작(`{/* 휴회 케어 관리 */}` 또는 유사 주석)부터 끝까지 다음 링크 배너로 교체한다:

    ```jsx
    {/* 휴회 관리 → PauseManagementView 링크 배너 (Phase 9) */}
    {pausedStudents.length > 0 && (
      <div className="pm-link-banner" onClick={() => nav("pauseManagement")}>
        <span style={{ fontSize: 20 }}>⏸</span>
        <div style={{ flex: 1 }}>
          <div>휴회 관리</div>
          <div style={{ fontSize: 11, opacity: 0.8 }}>휴회 {pausedStudents.length}명 · 케어로그 · 복귀 처리</div>
        </div>
        <span style={{ fontSize: 16 }}>›</span>
      </div>
    )}
    ```

    `pausedStudents` 변수는 Dashboard 내에서 이미 사용 중이거나, 없으면 섹션 직전에 한 줄 추가:
    ```js
    const pausedStudents = (students || []).filter(s => s.status === "paused" && !s.isInstitution);
    ```

    Dashboard에서 CareLogModal이 해당 섹션 외부에서도 사용되는지 확인한다. 만약 해당 섹션에서만 사용되고 있으면, CareLogModal 정의도 삭제 가능하다(PauseManagementView.jsx로 이전했으므로). 단, Dashboard.jsx에서 CareLogModal을 import하거나 사용하는 다른 곳이 있으면 삭제하지 않는다. 빌드 에러가 발생하지 않도록 주의한다.

    ---

    **AdminTools.jsx 변경: LessonSlotsView 폐강 버튼 추가 (D-05 최소 구현)**

    AdminTools.jsx를 읽어 `LessonSlotsView` 컴포넌트 또는 레슨 슬롯 목록을 렌더하는 곳을 찾는다. 각 슬롯 행 또는 카드에 다음 "폐강" 버튼을 추가한다 (admin 전용, 이미 `view === "lessonSlots" && user.role === "admin"` 조건 아래 있으므로 추가 역할 체크 불필요):

    ```jsx
    {slot.status !== "closed" ? (
      <button
        onClick={async () => {
          // 폐강: status → "closed", TimetableView에서 자동 제거됨 (기존 필터)
          // 학생 lessons[].slotId는 DB 보존 (이력 조회용)
          await onUpdateSlot(slot.id, { status: "closed" });
          showToast(`${slot.name || slot.instrument} 슬롯이 폐강되었습니다.`);
        }}
        style={{
          fontSize: 11, padding: "2px 8px", borderRadius: 4,
          border: "1px solid var(--red, #e53e3e)", background: "#fff0f0",
          color: "var(--red, #e53e3e)", cursor: "pointer", fontFamily: "inherit",
        }}
      >
        폐강
      </button>
    ) : (
      <span style={{ fontSize: 11, color: "var(--ink-30)" }}>폐강됨</span>
    )}
    ```

    AdminTools의 onUpdateSlot 또는 updateLessonSlot 접근 방식 확인 후 정확한 함수명 사용. AdminTools가 App.jsx로부터 prop으로 함수를 받는지, 또는 firebase.js에서 직접 import하는지 확인한다.

    모든 변경 후 `npm run build`를 실행해 에러가 없는지 확인한다.
  </action>
  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && grep -n "pauseManagement" src/components/layout/NavLayout.jsx && grep -n "pm-link-banner" src/components/dashboard/Dashboard.jsx && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pauseManagement" src/components/layout/NavLayout.jsx` 결과가 3 이상 (Sidebar + MoreMenu + BottomNav catch)
    - `grep -c "IC.pause" src/components/layout/NavLayout.jsx` 결과가 1 이상 (MoreMenu icon)
    - `grep -c "pm-link-banner" src/components/dashboard/Dashboard.jsx` 결과가 1
    - `grep -c "pauseManagement" src/components/dashboard/Dashboard.jsx` 결과가 1 이상 (nav("pauseManagement") 호출)
    - `grep -c "closed" src/components/admin/AdminTools.jsx` 결과가 1 이상 (폐강 버튼)
    - `grep -c "window.confirm\|window.alert" src/components/layout/NavLayout.jsx` 결과가 0
    - `npm run build` 통과
  </acceptance_criteria>
  <done>NavLayout에 '휴회 관리' 메뉴 추가(사이드바+모바일), Dashboard 인라인 케어 섹션이 링크 배너로 교체, AdminTools에 폐강 버튼 추가, npm run build 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PauseCard onResume → onResumeStudent | App.jsx 함수 호출. student는 visible (역할 필터됨) 배열에서 옴 |
| AdminTools 폐강 버튼 | admin 전용 뷰 (`user.role === "admin"` 조건으로 렌더됨) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Elevation of Privilege | PauseManagementView — 강사가 타 강사 학생 복귀 처리 | mitigate | App.jsx에서 `visible` (역할 필터됨) 전달. 강사는 담당 학생만 포함됨 |
| T-09-04-02 | Elevation of Privilege | AdminTools 폐강 버튼 — teacher/manager 접근 | mitigate | `view === "lessonSlots" && user.role === "admin"` 조건이 App.jsx 라우팅에 이미 있음 (line ~1373) |
| T-09-04-03 | Tampering | CareLogModal onSave — 임의 careLogs 배열 교체 | accept | onUpdateStudent = updateStudentDoc (per-op 트랜잭션). student 전체를 전달하므로 다른 필드는 유지됨 |
</threat_model>

<verification>
```bash
cd C:/Users/GIGABYTE/Coding/rye-k

# PauseManagementView 생성 확인
ls src/components/student/PauseManagementView.jsx
grep -c "window.confirm\|window.alert" src/components/student/PauseManagementView.jsx

# NavLayout 메뉴 확인
grep -n "pauseManagement" src/components/layout/NavLayout.jsx

# Dashboard 링크 배너 확인
grep -n "pm-link-banner\|pauseManagement" src/components/dashboard/Dashboard.jsx

# AdminTools 폐강 버튼 확인
grep -n "closed" src/components/admin/AdminTools.jsx

# 최종 빌드
npm run build
```
</verification>

<success_criteria>
- `src/components/student/PauseManagementView.jsx` 신규 생성됨
- PauseManagementView에 CareLogModal, PauseCard, 인라인 복귀 confirm UI 포함
- `window.confirm` / `window.alert` 사용 없음 (전 파일)
- NavLayout Sidebar nav, MoreMenu items, BottomNav catch 배열에 `pauseManagement` 추가됨
- Dashboard `휴회 케어 관리` 섹션이 `.pm-link-banner` 배너로 교체됨
- AdminTools LessonSlotsView에 `status: "closed"` 폐강 버튼 추가됨
- `npm run build` 에러 없이 통과
</success_criteria>

<output>
완료 후 `.planning/phases/09-schedule-enhancement/09-04-SUMMARY.md` 생성.
</output>
