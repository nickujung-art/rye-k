---
phase: 05-payment-automation
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/dashboard/Dashboard.jsx
autonomous: true
requirements:
  - PAY-02

must_haves:
  truths:
    - "Dashboard 이번달 수납 현황 카드가 클릭 가능하고 클릭 시 미납 필터 활성화 상태로 PaymentsView로 이동한다"
    - "카드에 미납 금액 합계(fmtMoney)가 표시된다"
    - "카드에 수납률(%)이 납부율 X% 형태로 표시된다"
    - "미납 금액 계산에 isInstitution 가상회원이 포함되지 않는다"
  artifacts:
    - path: "src/components/dashboard/Dashboard.jsx"
      provides: "클릭 가능한 미납 현황 카드, 미납 금액 합계, 수납률(%), onUnpaidCardClick prop 연결"
  key_links:
    - from: "Dashboard 수납 현황 카드 onClick"
      to: "onUnpaidCardClick prop"
      via: "onClick={() => onUnpaidCardClick && onUnpaidCardClick()}"
      pattern: "onUnpaidCardClick"
    - from: "unpaidAmount 계산"
      to: "students prop"
      via: "students.filter(s => !s.isInstitution).reduce(...)"
      pattern: "isInstitution"
    - from: "payRate 계산"
      to: "activeStudents (non-institution) length"
      via: "Math.round(paidCount / activeStudents.length * 100)"
      pattern: "payRate"
---

<objective>
Extend Dashboard.jsx to implement PAY-02: the 이번달 수납 현황 dash-card becomes
clickable (navigates to PaymentsView with unpaid filter active) and shows the total
unpaid amount in Korean Won plus 수납률(%) as 납부율 X%.

Purpose: 관리자가 대시보드에서 미납 금액과 수납률을 한눈에 확인하고 클릭 한 번으로 미납
목록으로 이동할 수 있어야 한다.

Output: 수정된 Dashboard.jsx — 기존 stat-card와 dash-card 동작 100% 보존,
신규 onClick + 미납 금액 표시 + 수납률 표시 추가.
</objective>

<execution_context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\05-payment-automation\execute-plan.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\05-payment-automation\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\PROJECT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\STATE.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\05-payment-automation\05-01-SUMMARY.md

<interfaces>
<!-- Extracted from src/components/dashboard/Dashboard.jsx -->

<!-- Props signature (line 44): -->
```javascript
export default function Dashboard({ students, teachers, currentUser, notices, categories,
  attendance, payments, pending, institutions, nav }) {
```
<!-- Plan 01 added: onUnpaidCardClick prop via App.jsx Dashboard render -->

<!-- Existing relevant computations (lines 53-54): -->
```javascript
const monthPayments = payments.filter(p => p.month === THIS_MONTH);
const unpaidThisMonth = students.filter(s => !monthPayments.find(p => p.studentId === s.id && p.paid)).length;
```

<!-- Existing "이번달 수납 현황" dash-card (lines 171-194): -->
```javascript
{canManageAll(currentUser.role) && students.length > 0 && (() => {
  const paidCount = students.filter(s => monthPayments.find(p => p.studentId === s.id && p.paid)).length;
  const unpaidCount = students.length - paidCount;
  return (
    <div className="dash-card" style={{marginBottom:12}}>
      <div className="dash-card-title">이번달 수납 현황 <span ...>{monthLabel(THIS_MONTH)}</span></div>
      <div style={{display:"flex",alignItems:"center",gap:16}}>
        <DonutChart paid={paidCount} total={students.length} />
        <div style={{flex:1}}>
          <div style={{...}}><div .../> <span ...>납부 완료</span> <span ...>{paidCount}명</span></div>
          <div style={{...}}><div .../> <span ...>미납</span>     <span ...>{unpaidCount}명</span></div>
        </div>
      </div>
    </div>
  );
})()}
```

<!-- Existing clickable stat-card pattern (line 166): -->
```javascript
{canManageAll(currentUser.role) && <div className="stat-card"
  onClick={() => nav("payments")} style={{cursor:"pointer"}}>
  <div className="stat-num" style={{color: unpaidThisMonth > 0 ? "var(--red)" : "var(--green)"}}>
    {unpaidThisMonth}
  </div>
  <div className="stat-label">이번달 미납</div>
  <div className="stat-sub">{monthLabel(THIS_MONTH)}</div>
</div>}
```

<!-- fmtMoney import (line 3): already imported from utils.js -->
```javascript
import { canManageAll, fmtDateTime, fmtDateShort, isMinor, monthLabel, ... } from "../../utils.js";
```

<!-- BANNED: window.confirm, window.alert -->
<!-- students prop = visible (App.jsx line 859) — isInstitution already excluded by expandInstitutionsToMembers logic, but do NOT assume — filter explicitly for safety -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Dashboard.jsx — add onUnpaidCardClick prop and make 이번달 수납 현황 dash-card clickable with unpaid amount and payRate</name>
  <files>src/components/dashboard/Dashboard.jsx</files>
  <read_first>
    - src/components/dashboard/Dashboard.jsx (read lines 44-200 to see props signature, computation section, and the full 이번달 수납 현황 card)
  </read_first>
  <action>
Make the following targeted edits to Dashboard.jsx.

**Edit 1 — Add onUnpaidCardClick to props** (line 44):

Replace:
```javascript
export default function Dashboard({ students, teachers, currentUser, notices, categories, attendance, payments, pending, institutions, nav }) {
```

With:
```javascript
export default function Dashboard({ students, teachers, currentUser, notices, categories,
  attendance, payments, pending, institutions, nav, onUnpaidCardClick }) {
```

**Edit 2 — Add unpaidAmount and payRate computations** (after line 54 `const unpaidThisMonth = ...`):

```javascript
// PAY-02: 미납 금액 합계 + 수납률 — 기관(isInstitution) 가상회원 제외하여 집계
const activeStudents = students.filter(s => !s.isInstitution);
const unpaidAmount = activeStudents.reduce((sum, s) => {
  const p = monthPayments.find(mp => mp.studentId === s.id);
  if (p?.paid) return sum;
  return sum + (p?.amount ?? ((s.monthlyFee || 0) + (s.instrumentRental ? (s.rentalFee || 0) : 0)));
}, 0);
const paidActiveCount = activeStudents.filter(s => monthPayments.find(p => p.studentId === s.id && p.paid)).length;
const payRate = activeStudents.length > 0 ? Math.round(paidActiveCount / activeStudents.length * 100) : 0;
```

**Edit 3 — Make stat-card at line 166 call onUnpaidCardClick** (replace existing stat-card onClick):

Replace the existing `이번달 미납` stat-card:
```javascript
{canManageAll(currentUser.role) && <div className="stat-card" onClick={() => nav("payments")} style={{cursor:"pointer"}}><div className="stat-num" style={{color: unpaidThisMonth > 0 ? "var(--red)" : "var(--green)"}}>{unpaidThisMonth}</div><div className="stat-label">이번달 미납</div><div className="stat-sub">{monthLabel(THIS_MONTH)}</div></div>}
```

With:
```javascript
{canManageAll(currentUser.role) && (
  <div className="stat-card"
    onClick={() => onUnpaidCardClick ? onUnpaidCardClick() : nav("payments")}
    style={{cursor:"pointer"}}>
    <div className="stat-num" style={{color: unpaidThisMonth > 0 ? "var(--red)" : "var(--green)"}}>
      {unpaidThisMonth}
    </div>
    <div className="stat-label">이번달 미납</div>
    <div className="stat-sub">{unpaidAmount > 0 ? fmtMoney(unpaidAmount) : monthLabel(THIS_MONTH)}</div>
  </div>
)}
```

**Edit 4 — Make 이번달 수납 현황 dash-card clickable and add payRate + unpaid amount display** (extend the dash-card block at lines 171-194):

The IIFE block wraps a `<div className="dash-card">`. Add `onClick` and `cursor:pointer` to that div:

Replace:
```javascript
<div className="dash-card" style={{marginBottom:12}}>
  <div className="dash-card-title">이번달 수납 현황 <span ...>...
```

With:
```javascript
<div className="dash-card"
  style={{marginBottom:12, cursor:"pointer"}}
  onClick={() => onUnpaidCardClick ? onUnpaidCardClick() : nav("payments")}>
  <div className="dash-card-title">이번달 수납 현황 <span ...>...
```

Also add `납부율` and unpaid amount display inside the IIFE. After the `납부 완료` row (which shows `{paidCount}명`), add a 납부율 line:
```javascript
<div style={{fontSize:12,color:"var(--text2)",marginTop:4}}>
  납부율 <span style={{fontWeight:600,color: payRate >= 80 ? "var(--green)" : "var(--red)"}}>{payRate}%</span>
</div>
```

After the `{unpaidCount}명` span in the 미납 row, also add:
```javascript
{unpaidAmount > 0 && (
  <span style={{fontSize:12,color:"var(--red)",fontFamily:"'Noto Serif KR',serif",fontVariantNumeric:"tabular-nums",marginLeft:4}}>
    {fmtMoney(unpaidAmount)}
  </span>
)}
```

NOTES:
- `fmtMoney` is already imported from utils.js — no new import needed
- `payRate` uses `activeStudents` (non-institution) as denominator — consistent with `unpaidAmount`
- The `onUnpaidCardClick` fallback to `nav("payments")` ensures backward compatibility if
  the prop is not passed (e.g., in tests or future refactors)
- Do NOT change the Notification section (lines 63-140) — it already has `onClick: () => nav("payments")`
  for the 미납 경고 notification. That notification does not need filter activation.
  </action>
  <verify>
    <automated>cd /c/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "onUnpaidCardClick" src/components/dashboard/Dashboard.jsx shows at least 3 matches (prop destructure, stat-card onClick, dash-card onClick)
    - grep -n "unpaidAmount" src/components/dashboard/Dashboard.jsx shows at least 3 matches (const declaration, stat-sub, dash-card span)
    - grep -n "payRate" src/components/dashboard/Dashboard.jsx shows at least 2 matches (const declaration, JSX display)
    - grep -n "isInstitution" src/components/dashboard/Dashboard.jsx shows at least 1 match (in activeStudents filter)
    - grep -n "fmtMoney" src/components/dashboard/Dashboard.jsx shows at least 2 matches
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    - 이번달 미납 stat-card shows fmtMoney(unpaidAmount) in sub-label when unpaid > 0
    - 이번달 수납 현황 dash-card shows 납부율 X% (green when ≥80%, red otherwise)
    - 이번달 수납 현황 dash-card is clickable and calls onUnpaidCardClick
    - Clicking either card navigates to PaymentsView with filterUnpaid=true (via App.jsx)
    - Build passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser render | Dashboard is read-only — it computes derived state from props, no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | unpaidAmount display | accept | Payment amounts are admin-only (canManageAll guard already on the card). No PII disclosed. |
| T-05-03-02 | Tampering | isInstitution filter | mitigate | Explicit `s => !s.isInstitution` filter prevents virtual institution members from inflating unpaid total or skewing payRate |
| T-05-03-03 | Spoofing | onUnpaidCardClick fallback | accept | Falls back to `nav("payments")` if prop absent — worst case is navigation without filter flag, not a security issue |
</threat_model>

<verification>
After task complete:
1. `npm run build` exits 0
2. Browser: Dashboard 이번달 미납 stat-card shows 미납 금액(원) when unpaid > 0
3. Browser: Dashboard 이번달 수납 현황 dash-card shows 납부율 X% label
4. Browser: click the stat-card — PaymentsView opens with 미납 필터 active (미납 카운트 표시됨)
5. Browser: 이번달 수납 현황 dash-card also clickable — same behavior
</verification>

<success_criteria>
- Dashboard 이번달 미납 stat-card sub-label shows fmtMoney(unpaidAmount) when amount > 0
- Dashboard 이번달 수납 현황 dash-card shows 납부율 X% (payRate computed from non-institution students)
- Dashboard 이번달 수납 현황 dash-card has cursor:pointer and onClick → onUnpaidCardClick
- onUnpaidCardClick in App.jsx sets paymentsInitFilter=true before navigate("payments")
- PaymentsView initializes filterUnpaid=true when initFilterUnpaid prop is true
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-payment-automation/05-03-SUMMARY.md`
</output>
