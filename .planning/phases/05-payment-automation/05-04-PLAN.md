---
phase: 05-payment-automation
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/api/payments/kakaobank-webhook.js
  - docs/operations/kakaobank-webhook-setup.md
autonomous: false
requirements:
  - PAY-04
  - PAY-05

user_setup:
  - service: cloudflare-pages
    why: "카카오뱅크 Webhook Worker가 RYE_WEBHOOK_SECRET 환경 변수를 필요로 합니다"
    env_vars:
      - name: RYE_WEBHOOK_SECRET
        source: "임의의 강력한 문자열 생성 (예: openssl rand -hex 32) — Cloudflare Pages 대시보드 → Settings → Environment variables → Add secret, 또는 wrangler CLI: wrangler pages secret put RYE_WEBHOOK_SECRET"
    dashboard_config:
      - task: "RYE_WEBHOOK_SECRET secret 등록"
        location: "Cloudflare Pages 대시보드 → rye-k project → Settings → Environment variables → Production secrets"
      - task: "Tasker + AutoNotification 플러그인 설치 및 Profile 설정"
        location: "Android 업무폰 — docs/operations/kakaobank-webhook-setup.md 참조"

must_haves:
  truths:
    - "Webhook Worker가 잘못된 X-RYE-Secret 헤더로 POST 시 401을 반환한다"
    - "Webhook Worker가 ±5분 초과 timestamp로 POST 시 400을 반환한다 (replay 방지)"
    - "올바른 secret + 유효 timestamp + 정상 body로 POST 시 200 ok:true를 반환한다"
    - "입금자명이 학생 이름과 완전 일치(단일) 시 수납 레코드가 KV에 pending으로 기록된다"
    - "입금자명이 Levenshtein 거리 1 이내 단일 학생과 일치 시 매칭 확정으로 KV에 기록된다"
    - "동명이인 또는 거리 초과 시 unmatched로 KV에 기록된다"
    - "IP당 분당 10회 이상 호출 시 429를 반환한다 (DoS 방지)"
    - "docs/operations/kakaobank-webhook-setup.md 파일이 존재하고 Tasker 설정 가이드를 포함한다"
    - "자동 매칭된 입금자명의 학생이 PaymentsView에서 납부 완료로 표시된다 (App.jsx polling이 KV를 드레인하여 rye-payments Firestore 레코드를 생성함)"
  artifacts:
    - path: "functions/api/payments/kakaobank-webhook.js"
      provides: "Cloudflare Pages Function — webhook POST handler with timingSafeEqual, replay protection, fuzzy match, KV buffer; GET handler with Firebase JWT auth for browser polling drain"
    - path: "docs/operations/kakaobank-webhook-setup.md"
      provides: "Tasker + AutoNotification Android 설정 가이드"
  key_links:
    - from: "Tasker HTTP POST"
      to: "kakaobank-webhook.js onRequest"
      via: "X-RYE-Secret header + JSON body {name, amount, timestamp}"
      pattern: "X-RYE-Secret"
    - from: "kakaobank-webhook.js fuzzyMatchStudent"
      to: "RATE_LIMIT_KV pending:{id} key"
      via: "kv.put('pending:{uid}', JSON.stringify(record), {expirationTtl: 86400})"
      pattern: "pending:"
    - from: "RATE_LIMIT_KV"
      to: "App.jsx usePendingPaymentsDrain (05-01 Task 3)"
      via: "GET /api/payments/kakaobank-webhook with Firebase JWT Authorization header"
      pattern: "pending:matched:"
---

<objective>
Implement the Cloudflare Pages Function that receives KakaoBank deposit notifications
from Android Tasker and stores matched/unmatched payments in the KV buffer for the
browser to drain.

Purpose: 은행 입금이 자동으로 감지되고 학생 이름 fuzzy 매칭을 통해 수납 처리되어야
한다. Worker는 KV 버퍼 방식으로 Firestore 동시성 문제를 피하면서 입금 기록을 저장한다.

Output: functions/api/payments/kakaobank-webhook.js (POST + GET handlers),
docs/operations/kakaobank-webhook-setup.md (Tasker 설정 가이드).

Security note: RYE_WEBHOOK_SECRET은 wrangler.toml에 절대 기록하지 않는다.
Cloudflare Pages 대시보드 또는 wrangler CLI secret으로만 등록한다.
</objective>

<execution_context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\05-payment-automation\execute-plan.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\05-payment-automation\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\PROJECT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\STATE.md

<interfaces>
<!-- Existing Worker pattern to replicate — functions/api/ai/lesson-note.js: -->
```javascript
export async function onRequest(context) {
  const { request, env } = context;
  let body;
  try { body = await request.json(); } catch { return new Response("Bad Request", { status: 400 }); }
  // ... logic ...
  return json({ result });
}

function json(data, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { "content-type": "application/json" },
  });
}
```

<!-- Firebase JWT auth utility — functions/api/ai/_utils/auth.js: -->
```javascript
import { jwtVerify, createRemoteJWKSet } from "jose";
const PROJECT_ID = "rye-k-center";
const JWKS = createRemoteJWKSet(
  new URL("https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com")
);
// Returns verified JWT payload (includes payload.role from custom claims), or null
export async function verifyToken(request) {
  const auth = request.headers.get("Authorization") || "";
  if (!auth.startsWith("Bearer ")) return null;
  const token = auth.slice(7);
  try {
    const { payload } = await jwtVerify(token, JWKS, {
      issuer: `https://securetoken.google.com/${PROJECT_ID}`,
      audience: PROJECT_ID,
    });
    if (!payload?.sub) return null;
    return payload;
  } catch { return null; }
}
```
<!-- payload.role is set by functions/api/auth/set-role.js via Firebase custom claims -->
<!-- values: "admin" | "manager" | "teacher" -->

<!-- Existing rate limit utility — functions/api/ai/_utils/ratelimit.js: -->
```javascript
export async function checkRateLimit(kv, userId, limit = 20) {
  if (!kv) throw new Error("RATE_LIMIT_KV 바인딩이 없습니다 ...");
  const bucket = Math.floor(Date.now() / 60000);
  const key = `rl:${userId}:${bucket}`;
  const val = await kv.get(key);
  const count = val ? parseInt(val, 10) : 0;
  if (count >= limit) return false;
  await kv.put(key, String(count + 1), { expirationTtl: 120 });
  return true;
}
```

<!-- wrangler.toml (DO NOT MODIFY): -->
```toml
name = "rye-k-center"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"

[[kv_namespaces]]
binding = "RATE_LIMIT_KV"
id = "59662ff0f284432db0e024e07fe9219e"
preview_id = "28d7163b7766455e9b4d4904c78318d1"
```
<!-- RYE_WEBHOOK_SECRET must NOT be added to wrangler.toml -->

<!-- rye-unmatched-payments Firestore record shape (from RESEARCH.md): -->
```javascript
{
  id: uid(),
  senderName: "홍길동",
  amount: 150000,
  timestamp: Date.now(),
  source: "kakaobank",
  rawText: "[카카오뱅크] 홍길동 150,000원 입금",
  createdAt: Date.now(),
  matchedAt: null,
  matchedStudentId: null,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kakaobank-webhook.js Cloudflare Pages Function</name>
  <files>functions/api/payments/kakaobank-webhook.js</files>
  <read_first>
    - functions/api/ai/lesson-note.js (full file — copy the onRequest structure and json() helper)
    - functions/api/ai/_utils/ratelimit.js (full file — copy checkRateLimit inline or import)
    - functions/api/ai/_utils/auth.js (full file — import verifyToken for GET handler)
    - wrangler.toml (verify RATE_LIMIT_KV binding name before referencing env.RATE_LIMIT_KV)
  </read_first>
  <action>
Create the NEW file `functions/api/payments/kakaobank-webhook.js`. This directory does not
exist yet — the file creation will create it.

IMPORTANT auth split:
- POST handler: uses X-RYE-Secret header (Tasker Android cannot send Firebase tokens)
- GET handler: uses Firebase JWT Bearer token (browser App.jsx sends getIdToken()) + role check

```javascript
// functions/api/payments/kakaobank-webhook.js
// KakaoBank Tasker Webhook — PAY-04/05
// POST: Android Tasker → webhook → fuzzy match → KV buffer
// GET:  Browser polling → drain KV buffer → return pending records (Firebase JWT auth)

import { verifyToken } from "../ai/_utils/auth.js";

export async function onRequest(context) {
  const { request, env } = context;
  const method = request.method;

  // ── CORS preflight ────────────────────────────────────────────────────
  if (method === "OPTIONS") {
    return new Response(null, {
      status: 204,
      headers: {
        "Access-Control-Allow-Origin": "*",
        "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
        "Access-Control-Allow-Headers": "Content-Type, X-RYE-Secret, Authorization",
      },
    });
  }

  if (method === "POST") return handlePost(request, env);
  if (method === "GET")  return handleGet(request, env);
  return new Response("Method Not Allowed", { status: 405 });
}

// ── POST: Tasker sends deposit notification ───────────────────────────────
async function handlePost(request, env) {
  // 1. IP rate limit — max 10 calls/min per IP (DoS protection, ASVS)
  const ip = request.headers.get("CF-Connecting-IP") || "unknown";
  const allowed = await checkRateLimit(env.RATE_LIMIT_KV, `wh:${ip}`, 10);
  if (!allowed) return new Response("Too Many Requests", { status: 429 });

  // 2. Parse body early (needed for timestamp check)
  let body;
  try { body = await request.json(); }
  catch { return new Response("Bad Request", { status: 400 }); }

  // 3. Secret header validation — timingSafeEqual (ASVS: timing attack prevention)
  const secret = request.headers.get("X-RYE-Secret") || "";
  const expected = env.RYE_WEBHOOK_SECRET || "";
  if (!expected || !(await timingSafeEqual(secret, expected))) {
    return json({ error: "Unauthorized" }, 401);
  }

  // 4. Replay protection — reject timestamp older than ±5 minutes (ASVS)
  const ts = typeof body.timestamp === "number" ? body.timestamp : parseInt(body.timestamp || "0");
  const now = Date.now();
  if (!ts || Math.abs(now - ts) > 5 * 60 * 1000) {
    return json({ error: "Request expired or timestamp missing" }, 400);
  }

  // 5. Input validation — name and amount (ASVS: input sanitization)
  const rawName = String(body.name || "").trim();
  // Allow Korean chars, spaces, alphanumeric only — reject XSS/injection attempts
  const name = rawName.replace(/[^가-힣ᄀ-ᇿ㄰-㆏ a-zA-Z0-9]/g, "").trim();
  const amount = parseInt(String(body.amount || "0").replace(/[^\d]/g, "")) || 0;
  const rawText = String(body.rawText || "").slice(0, 200); // cap raw notification text

  if (!name || name.length < 2 || name.length > 20) {
    return json({ error: "Invalid name" }, 400);
  }
  if (amount <= 0 || amount > 10_000_000) {
    return json({ error: "Invalid amount" }, 400);
  }

  // 6. Load student list from KV cache (set by browser app — see notes below)
  //    If no student cache in KV, store as unmatched directly.
  let students = [];
  try {
    const cached = await env.RATE_LIMIT_KV.get("students_cache");
    if (cached) students = JSON.parse(cached);
  } catch { /* no cache — proceed as unmatched */ }

  // 7. Fuzzy match
  const { match, confidence } = fuzzyMatchStudent(name, students);

  // 8. Build record
  const id = crypto.randomUUID();
  const record = {
    id,
    senderName: name,
    amount,
    timestamp: ts,
    source: "kakaobank",
    rawText,
    createdAt: now,
    matchedAt: null,
    matchedStudentId: null,
    confidence,
  };

  if (match && (confidence === "exact" || confidence === "fuzzy_1")) {
    // Auto-matched — store in KV as pending with studentId for browser to confirm
    record.matchedStudentId = match.id;
    record.matchedAt = now;
    await env.RATE_LIMIT_KV.put(
      `pending:matched:${id}`,
      JSON.stringify(record),
      { expirationTtl: 86400 }  // 24h TTL
    );
    return json({ ok: true, matched: true, studentId: match.id, confidence });
  } else {
    // Unmatched — store for manual review
    await env.RATE_LIMIT_KV.put(
      `pending:unmatched:${id}`,
      JSON.stringify(record),
      { expirationTtl: 86400 }
    );
    return json({ ok: true, matched: false, confidence });
  }
}

// ── GET: Browser polls to drain KV pending queue ─────────────────────────
// Auth: Firebase JWT Bearer token (Authorization: Bearer {idToken})
// Required role: "admin" or "manager" (from Firebase custom claims)
async function handleGet(request, env) {
  // 1. Verify Firebase JWT — uses jose JWKS, same as AI workers
  const payload = await verifyToken(request);
  if (!payload) {
    return json({ error: "Unauthorized" }, 401);
  }

  // 2. Role check — only admin/manager may drain the payment queue
  const role = payload.role;
  if (role !== "admin" && role !== "manager") {
    return json({ error: "Forbidden" }, 403);
  }

  // 3. Drain all pending:* keys from KV
  const matched   = [];
  const unmatched = [];

  try {
    // Cloudflare KV list — prefix filter
    const mList = await env.RATE_LIMIT_KV.list({ prefix: "pending:matched:" });
    for (const key of mList.keys) {
      const val = await env.RATE_LIMIT_KV.get(key.name);
      if (val) {
        try { matched.push(JSON.parse(val)); } catch {}
        await env.RATE_LIMIT_KV.delete(key.name);
      }
    }

    const uList = await env.RATE_LIMIT_KV.list({ prefix: "pending:unmatched:" });
    for (const key of uList.keys) {
      const val = await env.RATE_LIMIT_KV.get(key.name);
      if (val) {
        try { unmatched.push(JSON.parse(val)); } catch {}
        await env.RATE_LIMIT_KV.delete(key.name);
      }
    }
  } catch (e) {
    return json({ error: e.message }, 500);
  }

  return json({ ok: true, matched, unmatched });
}

// ── Helpers ──────────────────────────────────────────────────────────────

// Constant-time comparison to prevent timing attacks (ASVS V2, Pitfall 5)
async function timingSafeEqual(a, b) {
  const enc = new TextEncoder();
  const aB = enc.encode(a);
  const bB = enc.encode(b);
  if (aB.length !== bB.length) return false;
  const key = await crypto.subtle.importKey(
    "raw", aB, { name: "HMAC", hash: "SHA-256" }, false, ["sign"]
  );
  const [s1, s2] = await Promise.all([
    crypto.subtle.sign("HMAC", key, aB),
    crypto.subtle.sign("HMAC", key, bB),
  ]);
  const h = (buf) => Array.from(new Uint8Array(buf)).map(x => x.toString(16).padStart(2, "0")).join("");
  return h(s1) === h(s2);
}

// Inline rate limiter (mirrors _utils/ratelimit.js — avoids cross-function import)
async function checkRateLimit(kv, userId, limit) {
  if (!kv) return true; // fail open if KV not bound
  const bucket = Math.floor(Date.now() / 60000);
  const key = `rl:${userId}:${bucket}`;
  const val = await kv.get(key);
  const count = val ? parseInt(val, 10) : 0;
  if (count >= limit) return false;
  await kv.put(key, String(count + 1), { expirationTtl: 120 });
  return true;
}

// Levenshtein distance — inline, no npm, handles Korean chars correctly
function levenshtein(a, b) {
  const m = a.length, n = b.length;
  const dp = Array.from({ length: m + 1 }, (_, i) => [i, ...Array(n).fill(0)]);
  for (let j = 0; j <= n; j++) dp[0][j] = j;
  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      dp[i][j] = a[i - 1] === b[j - 1]
        ? dp[i - 1][j - 1]
        : 1 + Math.min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]);
    }
  }
  return dp[m][n];
}

// Fuzzy match student — returns { match, confidence }
// confidence: "exact" | "fuzzy_1" | "duplicate_exact" | "duplicate_fuzzy" | "no_match"
function fuzzyMatchStudent(inputName, students) {
  const active = students.filter(s => !s.isInstitution && (s.status || "active") === "active");

  // 1. Exact match
  const exact = active.filter(s => s.name === inputName);
  if (exact.length === 1) return { match: exact[0], confidence: "exact" };
  if (exact.length > 1)   return { match: null,     confidence: "duplicate_exact" };

  // 2. Levenshtein 1 — single closest match
  const close = active
    .map(s => ({ s, dist: levenshtein(inputName, s.name) }))
    .filter(({ dist }) => dist <= 1)
    .sort((a, b) => a.dist - b.dist);

  if (close.length === 1) return { match: close[0].s, confidence: "fuzzy_1" };
  if (close.length > 1)   return { match: null,       confidence: "duplicate_fuzzy" };

  return { match: null, confidence: "no_match" };
}

function json(data, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: {
      "content-type": "application/json",
      "Access-Control-Allow-Origin": "*",
    },
  });
}
```

SECURITY notes:
- RYE_WEBHOOK_SECRET must be set in Cloudflare Pages dashboard as a SECRET (encrypted).
  It must NOT appear in wrangler.toml. wrangler.toml must NOT be modified by this plan.
- POST uses timingSafeEqual (HMAC-SHA256) constant-time comparison (ASVS V2)
- GET uses Firebase JWT via verifyToken() — same jose JWKS pattern as AI workers
- GET role check: payload.role must be "admin" or "manager" (from Firebase custom claims)
- Replay protection: ±5 minutes timestamp window on POST (ASVS V2)
- Input sanitization: Korean/alpha chars only for name, integer for amount (ASVS V5)
- Rate limit: 10 req/min per IP using existing RATE_LIMIT_KV on POST only (ASVS DoS)

KV keys used:
- `rl:wh:{ip}:{bucket}` — rate limit buckets (TTL 120s, reuses existing namespace)
- `pending:matched:{uuid}` — auto-matched records (TTL 86400s)
- `pending:unmatched:{uuid}` — unmatched records for manual review (TTL 86400s)
- `students_cache` — student name list cached by browser (optional optimization)
  </action>
  <verify>
    <automated>cd /c/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "timingSafeEqual" functions/api/payments/kakaobank-webhook.js shows at least 2 matches (function definition + call in handlePost)
    - grep -n "timestamp" functions/api/payments/kakaobank-webhook.js shows at least 3 matches (body parse, range check, record field)
    - grep -n "levenshtein" functions/api/payments/kakaobank-webhook.js shows at least 2 matches (function definition + call)
    - grep -n "fuzzyMatchStudent" functions/api/payments/kakaobank-webhook.js shows at least 2 matches (function definition + call)
    - grep -n "RATE_LIMIT_KV" functions/api/payments/kakaobank-webhook.js shows at least 3 matches
    - grep -n "verifyToken" functions/api/payments/kakaobank-webhook.js shows at least 2 matches (import + call in handleGet)
    - grep -n "payload.role" functions/api/payments/kakaobank-webhook.js shows at least 1 match
    - grep -n "RYE_WEBHOOK_SECRET" wrangler.toml returns 0 matches (secret NOT in config file)
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    - Worker file exists at functions/api/payments/kakaobank-webhook.js
    - POST with wrong secret returns 401
    - POST with stale timestamp (>5min) returns 400
    - POST with valid params stores record in KV
    - Fuzzy match logic inline
    - GET endpoint with valid Firebase JWT + admin/manager role drains KV and returns { matched, unmatched }
    - GET without valid JWT returns 401
    - GET with teacher role returns 403
    - wrangler.toml unchanged
    - Build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Cloudflare Pages Function at functions/api/payments/kakaobank-webhook.js was created.
    The worker handles:
    - POST: X-RYE-Secret auth (timingSafeEqual), ±5min replay protection, fuzzy match, KV store
    - GET: Firebase JWT auth (verifyToken), role check (admin/manager), KV drain returning { matched, unmatched }

    Additionally, before this checkpoint, create docs/operations/kakaobank-webhook-setup.md
    with the Tasker setup guide (Task 2 is embedded in this checkpoint because the doc
    requires no code — it is pure configuration documentation).

    docs/operations/kakaobank-webhook-setup.md must contain:
    1. Prerequisites (Tasker + AutoNotification 플러그인, 업무폰 Android)
    2. Cloudflare secret 등록 방법 (wrangler CLI + 대시보드 방법 양쪽)
    3. Tasker Profile 설정 (트리거: KakaoTalk 알림 텍스트 "입금" 포함)
    4. Tasker Task JavaScript 코드 (정규식 파싱 + HTTP POST)
    5. 테스트 방법 (curl 예시)

    Tasker Task JavaScript template:
    ```javascript
    var text = "%antext";
    var patterns = [
      /(\S+)\s+([\d,]+)원\s+입금/,
      /\[카카오뱅크\]\s+(\S+)\s+([\d,]+)원/
    ];
    var name = "", amount = 0;
    for (var i = 0; i < patterns.length; i++) {
      var m = text.match(patterns[i]);
      if (m) { name = m[1]; amount = parseInt(m[2].replace(/,/g, "")); break; }
    }
    if (!name) { flash("파싱 실패: " + text); return; }

    var xhr = new XMLHttpRequest();
    xhr.open("POST", "https://rye-k.pages.dev/api/payments/kakaobank-webhook", false);
    xhr.setRequestHeader("Content-Type", "application/json");
    xhr.setRequestHeader("X-RYE-Secret", "YOUR_SECRET_HERE");
    xhr.send(JSON.stringify({
      name: name,
      amount: amount,
      rawText: text,
      timestamp: Date.now()
    }));
    flash("Webhook 전송: " + name + " " + amount + "원");
    ```

    CREATE docs/operations/kakaobank-webhook-setup.md using the Write tool now.
  </what-built>
  <how-to-verify>
    Step 1 — Verify build still passes after creating the docs file:
      Run: `npm run build` — must exit 0

    Step 2 — Test webhook endpoint with curl (after deployment):
      Wrong secret test:
        `curl -X POST https://rye-k.pages.dev/api/payments/kakaobank-webhook -H "Content-Type: application/json" -H "X-RYE-Secret: wrong" -d '{"name":"테스트","amount":100000,"timestamp":...}'`
        Expected: 401

      Replay protection test (old timestamp):
        `curl -X POST https://rye-k.pages.dev/api/payments/kakaobank-webhook -H "Content-Type: application/json" -H "X-RYE-Secret: YOUR_SECRET" -d '{"name":"테스트","amount":100000,"timestamp":1000}'`
        Expected: 400

      Valid request test:
        `curl -X POST https://rye-k.pages.dev/api/payments/kakaobank-webhook -H "Content-Type: application/json" -H "X-RYE-Secret: YOUR_SECRET" -d "{\"name\":\"테스트\",\"amount\":100000,\"timestamp\":$(date +%s000)}"`
        Expected: {"ok":true,...}

      GET without auth test:
        `curl -X GET https://rye-k.pages.dev/api/payments/kakaobank-webhook`
        Expected: 401

    Step 3 — Register RYE_WEBHOOK_SECRET in Cloudflare Pages:
      Run: `wrangler pages secret put RYE_WEBHOOK_SECRET`
      OR: Cloudflare Pages 대시보드 → rye-k → Settings → Environment variables → Add secret

    Step 4 — Confirm wrangler.toml has NOT been modified:
      Run: `grep RYE_WEBHOOK_SECRET wrangler.toml`
      Expected: no output (secret NOT in config file)
  </how-to-verify>
  <resume-signal>
    "approved" if all tests pass, or describe any issues found.
    Note: Tasker Android 설정은 Nick이 업무폰에서 직접 진행 (docs/operations/kakaobank-webhook-setup.md 참조).
    RYE_WEBHOOK_SECRET 등록 후 Tasker에 동일 값 입력 필요.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android Tasker → Worker POST | Untrusted HTTP POST from Android device — must be authenticated |
| Worker → RATE_LIMIT_KV | Cloudflare internal KV write — trusted |
| Browser → Worker GET | Browser drain request — Firebase JWT required (admin/manager only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | X-RYE-Secret header validation | mitigate | timingSafeEqual (HMAC-SHA256 constant-time comparison) prevents timing oracle attacks |
| T-05-04-02 | Spoofing | Replay attack — stale timestamp | mitigate | Reject requests with `\|now - timestamp\| > 5 minutes` (line: Math.abs(now - ts) > 5 * 60 * 1000) |
| T-05-04-03 | Denial of Service | Webhook flood from single IP | mitigate | RATE_LIMIT_KV reuse — 10 requests/minute per CF-Connecting-IP (KV key: `rl:wh:{ip}:{bucket}`) |
| T-05-04-04 | Tampering | Malicious name field (XSS/injection) | mitigate | `/[^가-힣ᄀ-ᇿ㄰-㆏ a-zA-Z0-9]/g` — Korean + alphanumeric only |
| T-05-04-05 | Information Disclosure | RYE_WEBHOOK_SECRET in wrangler.toml | mitigate | Secret registered via Cloudflare dashboard encrypted secret ONLY. wrangler.toml must NOT contain it. Verified by grep gate. |
| T-05-04-06 | Information Disclosure | rawText notification text | mitigate | rawText sliced to 200 chars at input, 30 chars at UI display (Plan 02) |
| T-05-04-07 | Elevation of Privilege | GET endpoint accessing KV drain | mitigate | GET requires Firebase JWT (verifyToken) + role === "admin" \|\| "manager" check — teacher role returns 403 |
</threat_model>

<verification>
After tasks complete:
1. `npm run build` exits 0
2. `grep -c "RYE_WEBHOOK_SECRET" wrangler.toml` returns 0
3. `grep -c "timingSafeEqual" functions/api/payments/kakaobank-webhook.js` returns >= 2
4. `grep -c "5 \* 60 \* 1000" functions/api/payments/kakaobank-webhook.js` returns >= 1
5. `grep -c "verifyToken" functions/api/payments/kakaobank-webhook.js` returns >= 2
6. `grep -c "payload.role" functions/api/payments/kakaobank-webhook.js` returns >= 1
7. docs/operations/kakaobank-webhook-setup.md exists and contains "Tasker" and "X-RYE-Secret"
8. Post-deployment: curl tests pass (401 on wrong secret, 400 on old timestamp, 200 on valid POST; 401 on GET without token)
9. Nick registers RYE_WEBHOOK_SECRET as Cloudflare Pages secret
</verification>

<success_criteria>
- functions/api/payments/kakaobank-webhook.js exists with POST + GET handlers
- POST: timingSafeEqual used for secret comparison, replay protection, rate limit, fuzzy match
- GET: Firebase JWT auth via verifyToken(), role === "admin" || "manager" required
- KV buffer approach: no Firestore writes from Worker
- docs/operations/kakaobank-webhook-setup.md has Tasker JavaScript template
- wrangler.toml unchanged (RYE_WEBHOOK_SECRET NOT in file)
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-payment-automation/05-04-SUMMARY.md`
</output>
