---
phase: 11-discount-system
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/components/payment/PaymentsView.jsx
  - src/components/admin/AdminTools.jsx
autonomous: true
requirements:
  - DIS-03
  - DIS-04

must_haves:
  truths:
    - "PaymentsView에 '할인 관리' 5번째 탭이 표시된다 (admin/manager만, teacher 미노출)"
    - "할인 관리 탭에서 할인 타입 목록을 볼 수 있다"
    - "각 행에서 활성/비활성 토글, 수정(인라인), 삭제(인라인 확인) 가 동작한다"
    - "삭제 시 window.confirm 없이 인라인 '정말 삭제?' + [삭제][취소] 버튼으로 확인"
    - "목록 하단 '+' 버튼으로 새 할인 타입을 인라인 폼으로 추가할 수 있다"
    - "할인 타입이 0개일 때 '초기 할인 타입 7개 생성' 버튼이 표시된다"
    - "AdminTools.jsx에 DEFAULT_DISCOUNT_TYPES 상수가 export된다"
  artifacts:
    - path: "src/components/payment/PaymentsView.jsx"
      provides: "5번째 탭 + DiscountTypeManager 컴포넌트"
      contains: "discounts"
    - path: "src/components/admin/AdminTools.jsx"
      provides: "DEFAULT_DISCOUNT_TYPES 상수 export"
      exports: ["DEFAULT_DISCOUNT_TYPES"]
  key_links:
    - from: "PaymentsView DiscountTypeManager"
      to: "onSaveDiscountTypes prop"
      via: "handleSave 계열 함수"
      pattern: "onSaveDiscountTypes"
    - from: "PaymentsView.jsx"
      to: "AdminTools.jsx"
      via: "import { DEFAULT_DISCOUNT_TYPES }"
      pattern: "DEFAULT_DISCOUNT_TYPES"
---

<objective>
PaymentsView에 '할인 관리' 5번째 탭을 추가하고, DiscountTypeManager 컴포넌트를 구현한다.
AdminTools.jsx에 초기 할인 타입 7개 seed 상수를 export한다.

Purpose: 관리자가 할인 타입을 CRUD할 수 있어야 한다 (DIS-03). 초기 설정을 위한 7개 씨드 데이터 제공 (DIS-04).
Output: PaymentsView 5번째 탭 + DiscountTypeManager 컴포넌트 + AdminTools DEFAULT_DISCOUNT_TYPES
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/GIGABYTE/Coding/rye-k/.planning/phases/11-discount-system/11-CONTEXT.md
@C:/Users/GIGABYTE/Coding/rye-k/.planning/phases/11-discount-system/11-01-SUMMARY.md

CRITICAL:
- window.confirm / window.alert 절대 금지 — 삭제 확인은 반드시 인라인 UI
- 할인 관리 탭은 canManageAll(currentUser.role)인 경우에만 탭 버튼 표시
- teacher 역할 → 탭 미노출 (기존 canManageAll 조건 구조 그대로 따름)

PaymentsView 현재 탭 구조 (라인 351~380):
- "수납 관리" (payments)
- "미매칭 입금" (unmatched)
- "입금 내역" (log)
- "즉시청구" (instantCharges)
→ 5번째로 "할인 관리" (discounts) 추가

PaymentsView 현재 props (라인 9~25):
  discountTypes = [], onSaveDiscountTypes → Plan 01이 App.jsx에서 배선 완료

DiscountTypeManager는 PaymentsView.jsx 파일 안에 별도 함수 컴포넌트로 정의한다 (같은 파일 상단부).

DEFAULT_DISCOUNT_TYPES (D-05) — 7개:
  { name: "광고 프로모션",     type: "percent", value: 10, burden: "academy", active: true, notes: "" }
  { name: "지인 소개",         type: "percent", value: 10, burden: "split",   splitRatio: { academy: 0.5, teacher: 0.5 }, active: true, notes: "" }
  { name: "제휴 업체/기관",    type: "percent", value: 10, burden: "academy", active: true, notes: "" }
  { name: "다자녀 등록",       type: "percent", value: 10, burden: "split",   splitRatio: { academy: 0.5, teacher: 0.5 }, active: true, notes: "" }
  { name: "다과목 수강",       type: "percent", value: 10, burden: "split",   splitRatio: { academy: 0.5, teacher: 0.5 }, active: true, notes: "" }
  { name: "특별 프로모션",     type: "percent", value: 25, burden: "academy", active: true, notes: "" }
  { name: "3개월 선납 할인",   type: "percent", value:  5, burden: "academy", active: true, notes: "" }
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminTools.jsx — DEFAULT_DISCOUNT_TYPES 상수 export 추가</name>
  <files>src/components/admin/AdminTools.jsx</files>

  <read_first>
    - src/components/admin/AdminTools.jsx 라인 1~10 (import 목록 및 첫 번째 컴포넌트 선언 전)
  </read_first>

  <action>
AdminTools.jsx의 모든 import 구문 아래, 첫 번째 함수/컴포넌트 선언 위에 아래 상수를 추가한다:

```js
// ── 초기 할인 타입 씨드 데이터 (D-05) ────────────────────────────────────────
export const DEFAULT_DISCOUNT_TYPES = [
  { name: "광고 프로모션",   type: "percent", value: 10, burden: "academy", active: true, notes: "" },
  { name: "지인 소개",       type: "percent", value: 10, burden: "split",   splitRatio: { academy: 0.5, teacher: 0.5 }, active: true, notes: "" },
  { name: "제휴 업체/기관",  type: "percent", value: 10, burden: "academy", active: true, notes: "" },
  { name: "다자녀 등록",     type: "percent", value: 10, burden: "split",   splitRatio: { academy: 0.5, teacher: 0.5 }, active: true, notes: "" },
  { name: "다과목 수강",     type: "percent", value: 10, burden: "split",   splitRatio: { academy: 0.5, teacher: 0.5 }, active: true, notes: "" },
  { name: "특별 프로모션",   type: "percent", value: 25, burden: "academy", active: true, notes: "" },
  { name: "3개월 선납 할인", type: "percent", value:  5, burden: "academy", active: true, notes: "" },
];
```

id와 createdAt은 사용 시(버튼 클릭 시) uid()로 생성한다 — 상수에는 포함하지 않는다.
  </action>

  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "export const DEFAULT_DISCOUNT_TYPES" src/components/admin/AdminTools.jsx 결과 == 1
    - grep -c "광고 프로모션" src/components/admin/AdminTools.jsx 결과 == 1
    - npm run build 통과
  </acceptance_criteria>

  <done>AdminTools.jsx에 DEFAULT_DISCOUNT_TYPES가 export됨. 7개 항목 포함.</done>
</task>

<task type="auto">
  <name>Task 2: PaymentsView.jsx — 5번째 탭 + DiscountTypeManager 컴포넌트 추가</name>
  <files>src/components/payment/PaymentsView.jsx</files>

  <read_first>
    - src/components/payment/PaymentsView.jsx 라인 1~10 (import 목록)
    - src/components/payment/PaymentsView.jsx 라인 9~25 (props 선언)
    - src/components/payment/PaymentsView.jsx 라인 69~76 (useState 선언부 — activeTab 포함)
    - src/components/payment/PaymentsView.jsx 라인 351~380 (탭 버튼 렌더링)
    - src/components/payment/PaymentsView.jsx 라인 381 이후 (탭 내용 렌더링 시작부)
  </read_first>

  <action>
**[A] import에 DEFAULT_DISCOUNT_TYPES 추가**

라인 1~8의 import 목록 끝에 아래 줄을 추가한다:
```js
import { DEFAULT_DISCOUNT_TYPES } from "../admin/AdminTools.jsx";
```

**[B] PaymentsView props에 discountTypes, onSaveDiscountTypes 추가**

함수 선언부의 props destructuring (라인 9~25):
`})`  닫는 괄호 바로 위, `onConfirmInstantPayment` 다음 줄에:
```
  discountTypes = [],
  onSaveDiscountTypes,
```
를 추가한다.

**[C] DiscountTypeManager 컴포넌트를 PaymentsView.jsx 파일 내 최상단(export default 전)에 정의한다**

파일의 `export default function PaymentsView` 선언 바로 위에 아래 컴포넌트를 추가한다:

```jsx
function DiscountTypeManager({ discountTypes, onSaveDiscountTypes, uid: uidFn }) {
  const [editId, setEditId] = useState(null);
  const [editForm, setEditForm] = useState({});
  const [deleteConfirmId, setDeleteConfirmId] = useState(null);
  const [adding, setAdding] = useState(false);
  const [newForm, setNewForm] = useState({ name: "", type: "percent", value: 10, burden: "academy", notes: "", active: true });
  const [saving, setSaving] = useState(false);
  const [err, setErr] = useState("");

  const handleToggleActive = async (id) => {
    const upd = discountTypes.map(d => d.id === id ? { ...d, active: d.active === false ? true : false } : d);
    setSaving(true);
    try { await onSaveDiscountTypes(upd); setErr(""); }
    catch (e) { setErr("저장 실패: " + e.message); }
    finally { setSaving(false); }
  };

  const handleSaveEdit = async () => {
    if (!editForm.name?.trim()) { setErr("이름을 입력하세요."); return; }
    const upd = discountTypes.map(d => d.id === editId ? { ...d, ...editForm } : d);
    setSaving(true);
    try { await onSaveDiscountTypes(upd); setEditId(null); setErr(""); }
    catch (e) { setErr("저장 실패: " + e.message); }
    finally { setSaving(false); }
  };

  const handleDelete = async (id) => {
    const upd = discountTypes.filter(d => d.id !== id);
    setSaving(true);
    try { await onSaveDiscountTypes(upd); setDeleteConfirmId(null); setErr(""); }
    catch (e) { setErr("삭제 실패: " + e.message); }
    finally { setSaving(false); }
  };

  const handleAddNew = async () => {
    if (!newForm.name?.trim()) { setErr("이름을 입력하세요."); return; }
    const entry = { ...newForm, id: uidFn(), value: Number(newForm.value), createdAt: Date.now() };
    if (entry.burden === "split" && !entry.splitRatio) entry.splitRatio = { academy: 0.5, teacher: 0.5 };
    const upd = [...discountTypes, entry];
    setSaving(true);
    try {
      await onSaveDiscountTypes(upd);
      setAdding(false);
      setNewForm({ name: "", type: "percent", value: 10, burden: "academy", notes: "", active: true });
      setErr("");
    }
    catch (e) { setErr("저장 실패: " + e.message); }
    finally { setSaving(false); }
  };

  const handleSeedDefaults = async () => {
    const upd = DEFAULT_DISCOUNT_TYPES.map(d => ({ ...d, id: uidFn(), createdAt: Date.now() }));
    setSaving(true);
    try { await onSaveDiscountTypes(upd); setErr(""); }
    catch (e) { setErr("초기화 실패: " + e.message); }
    finally { setSaving(false); }
  };

  const burdenLabel = (d) => {
    if (d.burden === "teacher") return "강사";
    if (d.burden === "split") return `분담 (학원 ${Math.round((d.splitRatio?.academy ?? 0.5) * 100)}%)`;
    return "학원";
  };

  return (
    <div>
      <div className="ph">
        <div>
          <h1>할인 관리</h1>
          <div className="ph-sub">등록된 할인 타입 {discountTypes.length}개</div>
        </div>
      </div>
      {err && <div className="form-err" style={{ marginBottom: 8 }}>⚠ {err}</div>}
      <div className="card" style={{ padding: 0, overflow: "hidden", marginBottom: 10 }}>
        {discountTypes.length === 0 && !adding && (
          <div style={{ padding: 28, textAlign: "center", color: "var(--ink-30)" }}>
            <div style={{ marginBottom: 12 }}>등록된 할인 타입이 없습니다.</div>
            <button
              className="btn btn-secondary btn-sm"
              onClick={handleSeedDefaults}
              disabled={saving}
            >
              {saving ? "생성 중…" : "초기 할인 타입 7개 생성"}
            </button>
          </div>
        )}
        {discountTypes.map((d, idx) => (
          <div
            key={d.id}
            style={{
              borderBottom: idx < discountTypes.length - 1 ? "1px solid var(--border)" : "none",
              padding: "12px 16px",
            }}
          >
            {editId === d.id ? (
              <div>
                <div className="fg-row" style={{ marginBottom: 8 }}>
                  <div className="fg">
                    <label className="fg-label">이름</label>
                    <input className="inp" value={editForm.name || ""} onChange={e => setEditForm(f => ({ ...f, name: e.target.value }))} />
                  </div>
                  <div className="fg">
                    <label className="fg-label">타입</label>
                    <select className="sel" value={editForm.type || "percent"} onChange={e => setEditForm(f => ({ ...f, type: e.target.value }))}>
                      <option value="percent">% 할인</option>
                      <option value="fixed">금액 할인(원)</option>
                    </select>
                  </div>
                  <div className="fg">
                    <label className="fg-label">값</label>
                    <input className="inp" type="number" min="0" value={editForm.value ?? ""} onChange={e => setEditForm(f => ({ ...f, value: Number(e.target.value) }))} />
                  </div>
                </div>
                <div className="fg-row" style={{ marginBottom: 8 }}>
                  <div className="fg">
                    <label className="fg-label">부담 주체</label>
                    <select className="sel" value={editForm.burden || "academy"} onChange={e => setEditForm(f => ({ ...f, burden: e.target.value }))}>
                      <option value="academy">학원</option>
                      <option value="teacher">강사</option>
                      <option value="split">분담</option>
                    </select>
                  </div>
                  {editForm.burden === "split" && (
                    <div className="fg">
                      <label className="fg-label">학원 부담 비율 (%)</label>
                      <input
                        className="inp"
                        type="number"
                        min="0"
                        max="100"
                        value={Math.round((editForm.splitRatio?.academy ?? 0.5) * 100)}
                        onChange={e => {
                          const v = Number(e.target.value) / 100;
                          setEditForm(f => ({ ...f, splitRatio: { academy: v, teacher: 1 - v } }));
                        }}
                      />
                    </div>
                  )}
                </div>
                <div className="fg" style={{ marginBottom: 8 }}>
                  <label className="fg-label">메모</label>
                  <input className="inp" value={editForm.notes || ""} onChange={e => setEditForm(f => ({ ...f, notes: e.target.value }))} />
                </div>
                <div style={{ display: "flex", gap: 8 }}>
                  <button className="btn btn-primary btn-sm" onClick={handleSaveEdit} disabled={saving}>{saving ? "저장 중…" : "저장"}</button>
                  <button className="btn btn-ghost btn-sm" onClick={() => { setEditId(null); setErr(""); }}>취소</button>
                </div>
              </div>
            ) : (
              <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                <div style={{ flex: 1 }}>
                  <div style={{ fontWeight: 600, fontSize: 13.5 }}>{d.name}</div>
                  <div style={{ fontSize: 11, color: "var(--ink-60)", marginTop: 2 }}>
                    {d.type === "percent" ? `${d.value}%` : `${Number(d.value).toLocaleString("ko-KR")}원`} 할인 · {burdenLabel(d)}
                  </div>
                </div>
                <button
                  onClick={() => handleToggleActive(d.id)}
                  disabled={saving}
                  style={{
                    fontSize: 11, padding: "2px 8px", borderRadius: 99,
                    border: "1px solid var(--border)",
                    background: d.active !== false ? "var(--green)" : "var(--ink-10)",
                    color: d.active !== false ? "#fff" : "var(--ink-30)",
                    cursor: "pointer", fontWeight: 600, whiteSpace: "nowrap",
                  }}
                >
                  {d.active !== false ? "활성" : "비활성"}
                </button>
                {deleteConfirmId === d.id ? (
                  <div style={{ display: "flex", alignItems: "center", gap: 6 }}>
                    <span style={{ fontSize: 11, color: "var(--red)", whiteSpace: "nowrap" }}>정말 삭제?</span>
                    <button
                      style={{ fontSize: 11, padding: "2px 8px", borderRadius: 6, background: "var(--red)", color: "#fff", border: "none", cursor: "pointer", fontWeight: 600 }}
                      onClick={() => handleDelete(d.id)}
                      disabled={saving}
                    >
                      삭제
                    </button>
                    <button className="btn btn-ghost btn-sm" onClick={() => setDeleteConfirmId(null)}>취소</button>
                  </div>
                ) : (
                  <div style={{ display: "flex", gap: 4 }}>
                    <button className="btn btn-ghost btn-sm" onClick={() => { setEditId(d.id); setEditForm({ ...d }); setErr(""); }}>수정</button>
                    <button className="btn btn-ghost btn-sm" style={{ color: "var(--red)" }} onClick={() => setDeleteConfirmId(d.id)}>삭제</button>
                  </div>
                )}
              </div>
            )}
          </div>
        ))}
      </div>
      {!adding ? (
        <button className="btn btn-secondary btn-sm" onClick={() => { setAdding(true); setErr(""); }}>
          + 할인 타입 추가
        </button>
      ) : (
        <div className="card" style={{ padding: 16, marginTop: 4 }}>
          <div className="fg-row" style={{ marginBottom: 8 }}>
            <div className="fg">
              <label className="fg-label">이름 <span className="req">*</span></label>
              <input className="inp" value={newForm.name} onChange={e => setNewForm(f => ({ ...f, name: e.target.value }))} placeholder="예: 지인 소개" />
            </div>
            <div className="fg">
              <label className="fg-label">타입</label>
              <select className="sel" value={newForm.type} onChange={e => setNewForm(f => ({ ...f, type: e.target.value }))}>
                <option value="percent">% 할인</option>
                <option value="fixed">금액 할인(원)</option>
              </select>
            </div>
            <div className="fg">
              <label className="fg-label">값</label>
              <input className="inp" type="number" min="0" value={newForm.value} onChange={e => setNewForm(f => ({ ...f, value: e.target.value }))} />
            </div>
          </div>
          <div className="fg-row" style={{ marginBottom: 8 }}>
            <div className="fg">
              <label className="fg-label">부담 주체</label>
              <select className="sel" value={newForm.burden} onChange={e => setNewForm(f => ({ ...f, burden: e.target.value }))}>
                <option value="academy">학원</option>
                <option value="teacher">강사</option>
                <option value="split">분담</option>
              </select>
            </div>
            {newForm.burden === "split" && (
              <div className="fg">
                <label className="fg-label">학원 부담 비율 (%)</label>
                <input
                  className="inp"
                  type="number"
                  min="0"
                  max="100"
                  defaultValue={50}
                  onChange={e => {
                    const v = Number(e.target.value) / 100;
                    setNewForm(f => ({ ...f, splitRatio: { academy: v, teacher: 1 - v } }));
                  }}
                />
              </div>
            )}
          </div>
          <div className="fg" style={{ marginBottom: 8 }}>
            <label className="fg-label">메모</label>
            <input className="inp" value={newForm.notes} onChange={e => setNewForm(f => ({ ...f, notes: e.target.value }))} />
          </div>
          <div style={{ display: "flex", gap: 8 }}>
            <button className="btn btn-primary btn-sm" onClick={handleAddNew} disabled={saving}>{saving ? "추가 중…" : "추가"}</button>
            <button className="btn btn-ghost btn-sm" onClick={() => { setAdding(false); setErr(""); }}>취소</button>
          </div>
        </div>
      )}
    </div>
  );
}
```

**[D] 5번째 탭 버튼 추가**

기존 탭 버튼 렌더링 블록 (canManageAll 조건 내 ftabs div):
"즉시청구" 버튼 (instantCharges 탭) 다음에 아래 버튼을 추가한다:

```jsx
          <button className={`ftab${activeTab === "discounts" ? " active" : ""}`}
            onClick={() => setActiveTab("discounts")}>
            할인 관리
          </button>
```

**[E] 탭 내용 렌더링 — discounts 탭 추가**

파일 내 `{activeTab === "instantCharges" && <>` 블록이 끝나는 위치 다음에 아래를 추가한다:

```jsx
      {activeTab === "discounts" && (
        <DiscountTypeManager
          discountTypes={discountTypes}
          onSaveDiscountTypes={onSaveDiscountTypes}
          uid={uid}
        />
      )}
```

주의: DiscountTypeManager는 `uid` prop으로 utils.js의 `uid` 함수를 받는다.
PaymentsView.jsx는 이미 `uid`를 utils.js에서 import하고 있다 (라인 4 확인).
  </action>

  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "DEFAULT_DISCOUNT_TYPES" src/components/payment/PaymentsView.jsx 결과 >= 2 (import + 사용)
    - grep -c "DiscountTypeManager" src/components/payment/PaymentsView.jsx 결과 >= 2 (정의 + 사용)
    - grep -c "할인 관리" src/components/payment/PaymentsView.jsx 결과 >= 1
    - grep -c "deleteConfirmId" src/components/payment/PaymentsView.jsx 결과 >= 1 (인라인 삭제 확인)
    - grep -v "window.confirm" src/components/payment/PaymentsView.jsx | grep -c "confirm" 결과 >= 0
      (window.confirm이 없음을 확인: grep -c "window.confirm" src/components/payment/PaymentsView.jsx == 0)
    - npm run build 통과
  </acceptance_criteria>

  <done>
    PaymentsView에 5번째 '할인 관리' 탭이 추가됨 (admin/manager 전용).
    DiscountTypeManager 컴포넌트가 CRUD + 인라인 삭제 확인 + 씨드 버튼으로 동작함.
    npm run build 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → onSaveDiscountTypes | 배열 전체 교체 — 소수 항목이므로 안전 (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Elevation of Privilege | 할인 관리 탭 | mitigate | canManageAll 조건부로 탭 버튼 자체를 렌더링하지 않음 |
| T-11-06 | Denial of Service | 씨드 버튼 중복 클릭 | mitigate | saving 상태로 버튼 disabled 처리 |
</threat_model>

<verification>
```bash
cd C:/Users/GIGABYTE/Coding/rye-k

# AdminTools 씨드 상수 확인
grep -n "DEFAULT_DISCOUNT_TYPES" src/components/admin/AdminTools.jsx

# PaymentsView import + 탭 확인
grep -n "DEFAULT_DISCOUNT_TYPES\|DiscountTypeManager\|할인 관리\|discounts" src/components/payment/PaymentsView.jsx

# window.confirm 없음 확인
grep -c "window.confirm" src/components/payment/PaymentsView.jsx

# 빌드
npm run build 2>&1 | tail -3
```
</verification>

<success_criteria>
- AdminTools.jsx에 DEFAULT_DISCOUNT_TYPES (7개) export됨
- PaymentsView에 '할인 관리' 5번째 탭 버튼 + 탭 내용 존재
- DiscountTypeManager에 활성 토글, 수정(인라인), 삭제(인라인 확인), 추가 폼, 씨드 버튼 모두 포함
- window.confirm / window.alert 사용 없음
- npm run build 통과
</success_criteria>

<output>
완료 후 `.planning/phases/11-discount-system/11-03-SUMMARY.md` 생성:
- DiscountTypeManager 컴포넌트 정의 위치 (라인 번호)
- 5번째 탭 추가 위치 (라인 번호)
- DEFAULT_DISCOUNT_TYPES 위치
</output>
