---
phase: 11-discount-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils.js
  - src/firebase.js
  - src/App.jsx
autonomous: true
requirements:
  - DIS-01
  - DIS-02

must_haves:
  truths:
    - "calcTotalFee(student, feePresets, discountTypes)가 { total, original, discountAmount, discountName } 객체를 반환한다"
    - "discountTypes 미전달 시 역호환 — total === original, discountAmount === 0"
    - "App.jsx에 discountTypes state가 존재하고 rye-discounts Firestore 리스너가 연결된다"
    - "PaymentsView·StudentFormModal·Dashboard·SettlementView에 discountTypes props가 전달된다"
  artifacts:
    - path: "src/utils.js"
      provides: "calcTotalFee 확장 — 3번째 인수 discountTypes=[]"
      contains: "return { total, original, discountAmount, discountName }"
    - path: "src/firebase.js"
      provides: "saveDiscountTypes 함수"
      exports: ["saveDiscountTypes"]
    - path: "src/App.jsx"
      provides: "discountTypes state + KEYS 리스너 + 전체 prop 배선"
      contains: "rye-discounts"
  key_links:
    - from: "src/App.jsx"
      to: "src/firebase.js"
      via: "saveDiscountTypes import"
      pattern: "saveDiscountTypes"
    - from: "src/App.jsx KEYS"
      to: "Firestore appData/rye-discounts"
      via: "onSnapshot"
      pattern: "rye-discounts"
---

<objective>
할인 시스템의 핵심 토대를 구축한다. calcTotalFee를 확장하여 할인을 반영한 객체를 반환하고,
rye-discounts Firestore 리스너·state·저장 함수를 추가하며, 모든 하위 컴포넌트에 props를 배선한다.

Purpose: Wave 2·3 Plan이 discountTypes를 받아 동작하기 위해 이 Plan이 선행되어야 한다.
Output: 확장된 calcTotalFee, saveDiscountTypes 함수, App.jsx 배선 완료
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/GIGABYTE/Coding/rye-k/.planning/phases/11-discount-system/11-CONTEXT.md

CRITICAL — 절대 금지:
- saveStudents([...]) 금지 (배열 전체 덮어쓰기 → 77명 손실 전례)
- window.confirm / window.alert 금지
- 채팅창 전체 코드 출력 금지 — Edit/str_replace 정밀 수정만
- git push 자동 금지

할인 데이터 모델 (D-03):
  DiscountType: { id, name, type: "percent"|"fixed", value, burden: "academy"|"teacher"|"split",
                  splitRatio: { academy, teacher }, active, notes, createdAt }
  student.discount: { discountId, lessonInstrument?, startDate, endDate|null, appliedBy, notes }

calcTotalFee 변경 핵심 (D-04):
  기존: calcTotalFee(student, feePresets) → number
  변경: calcTotalFee(student, feePresets, discountTypes = []) → { total, original, discountAmount, discountName }
  역호환: discountTypes 미전달 → discountAmount=0, discountName=null, total===original
</context>

<tasks>

<task type="auto">
  <name>Task 1: utils.js — calcTotalFee 시그니처 변경 및 할인 로직 추가</name>
  <files>src/utils.js</files>

  <read_first>
    - src/utils.js 라인 250~278 (calcLessonFeeWithFallback + calcTotalFee 현재 구현 전체)
  </read_first>

  <action>
src/utils.js 라인 268~278의 calcTotalFee 함수 전체를 아래 구현으로 교체한다.
주석 포함 라인 267 "// student 전체 월 수강료 계산..."도 함께 교체한다.

교체할 코드 (정확히 이 텍스트로 교체):

```
// student 전체 월 수강료 계산 (레슨별 fee 합산 + 대여료 + 할인 적용)
// - discountTypes: DiscountType[] (미전달 시 [] → 할인 없음)
// - 반환: { total, original, discountAmount, discountName }
// - 역호환: discountTypes 미전달 또는 student.discount 없을 시 total === original, discountAmount === 0
export function calcTotalFee(student, feePresets, discountTypes = []) {
  const lessons = student.lessons || [];
  const rental = student.instrumentRental ? (student.rentalFee || 0) : 0;
  let original;
  if (lessons.length === 0) {
    original = (student.monthlyFee || 0) + rental;
  } else {
    const legacyPerLesson = Math.round((student.monthlyFee || 0) / lessons.length);
    const lessonSum = lessons.reduce(
      (sum, l) => sum + calcLessonFeeWithFallback(l, feePresets, legacyPerLesson),
      0
    );
    original = lessonSum + rental;
  }
  // 할인 적용 (D-02: 학생 단위 단수 할인, D-03: student.discount 단수 객체)
  let discountAmount = 0;
  let discountName = null;
  const disc = student.discount;
  if (disc?.discountId && discountTypes.length > 0) {
    const dtype = discountTypes.find(d => d.id === disc.discountId && d.active !== false);
    if (dtype) {
      const today = new Date().toISOString().slice(0, 10);
      const started = !disc.startDate || disc.startDate <= today;
      const notEnded = !disc.endDate || disc.endDate >= today;
      if (started && notEnded) {
        if (disc.lessonInstrument && lessons.length > 0) {
          // 특정 과목에만 적용 (D-02: lessonInstrument 옵션 필드)
          const target = lessons.find(l => l.instrument === disc.lessonInstrument);
          if (target) {
            const fallback = Math.round((student.monthlyFee || 0) / lessons.length);
            const lFee = calcLessonFeeWithFallback(target, feePresets, fallback);
            discountAmount = dtype.type === "percent"
              ? Math.round(lFee * dtype.value / 100)
              : Math.min(dtype.value, lFee);
          }
        } else {
          // 전 수강료에 적용
          discountAmount = dtype.type === "percent"
            ? Math.round(original * dtype.value / 100)
            : Math.min(dtype.value, original);
        }
        discountName = dtype.name;
      }
    }
  }
  return {
    total: Math.max(0, original - discountAmount),
    original,
    discountAmount,
    discountName,
  };
}
```
  </action>

  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/utils.js에 `export function calcTotalFee(student, feePresets, discountTypes = [])` 시그니처가 존재한다
    - grep -c "return { total:" src/utils.js 결과가 1 이상이다
    - grep -c "discountAmount" src/utils.js 결과가 3 이상이다
    - npm run build 통과 (타입 에러 없음, Vite 번들 성공)
    - ⚠ 주의: 이 변경 후 calcTotalFee 기존 호출부(Dashboard, SettlementView, StudentManagement, PaymentsView)는
      모두 객체를 반환받으므로 직접 산술 연산 시 NaN이 된다.
      이는 후속 Plan 02·03·04에서 각각 .total 접근으로 수정된다.
      이 Plan의 build 통과는 정상 — JavaScript는 동적 타입이므로 빌드 단계에서 오류가 나지 않는다.
  </acceptance_criteria>

  <done>utils.js calcTotalFee가 { total, original, discountAmount, discountName } 객체를 반환한다. npm run build 통과.</done>
</task>

<task type="auto">
  <name>Task 2: firebase.js — saveDiscountTypes 추가 + App.jsx 전체 배선</name>
  <files>src/firebase.js, src/App.jsx</files>

  <read_first>
    - src/firebase.js 전체 (특히 라인 83~121: addInstantCharge 등 기존 함수 패턴)
    - src/App.jsx 라인 1~5 (import 목록)
    - src/App.jsx 라인 240~260 (state 선언부: shopItems, lessonSlots 등)
    - src/App.jsx 라인 371~392 (KEYS 배열)
    - src/App.jsx 라인 1439~1470 (PaymentsView JSX)
    - src/App.jsx 라인 1545~1558 (SettlementView JSX)
    - src/App.jsx 라인 1426~1436 (Dashboard JSX)
    - src/App.jsx 라인 1580 (StudentFormModal JSX)
  </read_first>

  <action>
**[A] firebase.js 수정**

firebase.js 라인 121 (export { db, auth, ... } 줄) 바로 위에 아래 함수를 추가한다:

```
// ── rye-discounts (appData 컬렉션, 배열 전체 교체 — 소수 항목이므로 안전, D-07) ─
export async function saveDiscountTypes(types) {
  try {
    await setDoc(doc(db, "appData", "rye-discounts"), { value: types, updatedAt: Date.now() });
  } catch (e) {
    if (e?.code === "permission-denied")
      throw Object.assign(new Error("쓰기 권한이 없습니다. 재로그인 후 다시 시도하세요."), { code: "permission-denied" });
    throw e;
  }
}
```

**[B] App.jsx — import 추가**

라인 1 또는 2의 firebase.js import 줄에 `saveDiscountTypes`를 추가한다.
기존: `import { db, auth, doc, setDoc, onSnapshot, runTransaction, collection, getDoc, addInstantCharge, updateInstantCharge, addLessonSlot, updateLessonSlot, deleteLessonSlot, firebaseSignIn, firebaseSignInAnon, firebaseLogout, onAuthStateChanged } from "./firebase.js";`
변경: 위 목록 끝에 `, saveDiscountTypes` 추가

**[C] App.jsx — discountTypes state 선언 추가**

라인 248 (`const [shopItems, setShopItems] = ...`) 다음 줄에 추가:
```
  const [discountTypes, setDiscountTypes] = useState([]);
```

**[D] App.jsx — KEYS 배열에 rye-discounts 항목 추가**

라인 389 (`{ key: "rye-shop-items", setter: setShopItems, ... }`) 다음 줄에 추가:
```
      { key: "rye-discounts", setter: setDiscountTypes, default: [] },
```

**[E] App.jsx — PaymentsView JSX에 props 추가**

PaymentsView가 렌더링되는 JSX 블록 (라인 1439~1475 근처)에 아래 두 props를 추가한다.
기존 `feePresets={feePresets}` 다음 줄에:
```
              discountTypes={discountTypes}
              onSaveDiscountTypes={async (upd) => {
                await saveDiscountTypes(upd);
                addLog("할인 타입 수정");
                showToast("할인 타입이 저장되었습니다.");
              }}
```

**[F] App.jsx — StudentFormModal JSX에 prop 추가**

라인 1580 근처 StudentFormModal JSX에서 `feePresets={feePresets}` 다음에:
```
 discountTypes={discountTypes}
```
를 추가한다.

**[G] App.jsx — Dashboard JSX에 prop 추가**

Dashboard가 렌더링되는 JSX (라인 1426~1436 근처) 에서 `feePresets={feePresets}` 다음에:
```
              discountTypes={discountTypes}
```
를 추가한다.

**[H] App.jsx — SettlementView JSX에 prop 추가**

SettlementView JSX (라인 1545~1556 근처)에서 `feePresets={feePresets}` 다음에:
```
              discountTypes={discountTypes}
```
를 추가한다.
  </action>

  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "saveDiscountTypes" src/firebase.js 결과 >= 1
    - grep -c "rye-discounts" src/App.jsx 결과 >= 2 (KEYS 항목 + saveDiscountTypes 호출)
    - grep -c "discountTypes" src/App.jsx 결과 >= 6 (state + KEYS + 4개 컴포넌트 prop)
    - grep -c "const \[discountTypes" src/App.jsx 결과 == 1
    - npm run build 통과
  </acceptance_criteria>

  <done>
    firebase.js에 saveDiscountTypes 함수가 추가되었다.
    App.jsx에 discountTypes state, KEYS 리스너, PaymentsView·StudentFormModal·Dashboard·SettlementView props가 모두 배선되었다.
    npm run build 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → Firestore | 할인 타입 배열 전체를 appData/rye-discounts에 저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | saveDiscountTypes | mitigate | Firestore Security Rules로 admin/manager role만 rye-discounts 쓰기 허용 (기존 규칙 적용) |
| T-11-02 | Information Disclosure | calcTotalFee 반환값 | accept | discountName/Amount는 UI 표시용 — PII 없음, 낮은 위험 |
</threat_model>

<verification>
```bash
cd C:/Users/GIGABYTE/Coding/rye-k

# calcTotalFee 시그니처 확인
grep -n "export function calcTotalFee" src/utils.js

# 반환 객체 확인
grep -n "return { total:" src/utils.js

# firebase.js 함수 확인
grep -n "saveDiscountTypes" src/firebase.js

# App.jsx 배선 확인
grep -c "discountTypes" src/App.jsx

# 빌드 통과
npm run build 2>&1 | tail -3
```
</verification>

<success_criteria>
- calcTotalFee(student, feePresets)가 { total, original, discountAmount: 0, discountName: null } 반환 (역호환)
- calcTotalFee(student, feePresets, [{id: "x", type:"percent", value:10, active:true}]) + student.discount={discountId:"x",...} 시 discountAmount > 0
- saveDiscountTypes가 firebase.js에 export됨
- App.jsx에 discountTypes state, KEYS, 4개 컴포넌트 props 모두 존재
- npm run build 통과
</success_criteria>

<output>
완료 후 `.planning/phases/11-discount-system/11-01-SUMMARY.md` 생성:
- 변경된 함수 시그니처
- App.jsx에 추가된 props 목록
- 다음 Plan들이 받는 props 확인 사항
</output>
