---
phase: 02-portal-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/portal/PublicPortal.jsx
autonomous: true
requirements:
  - POR-01
  - POR-02
  - POR-03
  - POR-07

must_haves:
  truths:
    - "로그인 시 localStorage['ryekPortal']에 loginAt 필드가 저장된다"
    - "자동로그인 복원 시 30일 경과 여부를 체크하고 만료되면 localStorage를 삭제한다"
    - "D-3일(27일 경과) 후 홈 탭 최상단에 세션 만료 배너가 표시된다"
    - "'30일 연장' 버튼 클릭 시 loginAt이 현재 시각으로 갱신되고 배너가 사라진다"
    - "'로그아웃' 버튼 클릭 시 window.confirm 없이 즉시 로그아웃된다"
    - "홈 탭에 '다음 수업' 카드와 '이번 주 수업' 칩이 렌더된다"
    - "lessons[]가 비어있으면 시간표 위젯이 렌더되지 않는다"
    - "홈 탭 최하단에 '수강 신청하기 →' 버튼이 있고 클릭 시 /register로 이동한다"
    - "notes 탭이 정상적으로 lessonNote를 렌더한다 (기존 기능 검증)"
  artifacts:
    - path: "src/components/portal/PublicPortal.jsx"
      provides: "세션 만료 정책 + 시간표 위젯 + 수강 신청 CTA"
      contains: "showExpiryBanner"
    - path: "src/components/portal/PublicPortal.jsx"
      provides: "30일 만료 체크 useEffect"
      contains: "loginAt"
  key_links:
    - from: "useEffect (line ~607)"
      to: "localStorage['ryekPortal']"
      via: "JSON.parse / removeItem"
      pattern: "saved.loginAt && Date.now\\(\\) - saved.loginAt"
    - from: "doLogin() (line ~652)"
      to: "localStorage['ryekPortal']"
      via: "JSON.stringify with loginAt"
      pattern: "loginAt: Date.now()"
    - from: "portal-next-lesson card"
      to: "nextLesson variable"
      via: "직접 참조 (중복 계산 금지)"
      pattern: "nextLesson\\.lessons\\.map"
    - from: "수강 신청하기 button"
      to: "/register"
      via: "window.location.href"
      pattern: "window\\.location\\.href.*register"
---

<objective>
PublicPortal.jsx에 세 가지 주요 기능을 추가한다: (1) 30일 세션 만료 정책 + D-3일 배너(POR-01), (2) 홈 탭 시간표 위젯(POR-02), (3) 수강 신청 CTA 버튼(POR-07). 레슨노트 탭 기능은 이미 완성되어 있어 코드 검증만 수행(POR-03).

Purpose: 브라우저 재방문 시 자동 로그인 유지(30일)와 포털 홈 탭의 핵심 위젯 두 개를 완성한다.
Output: PublicPortal.jsx 수정본 — doLogin + autoLogin useEffect + showExpiryBanner state + 배너 JSX + 시간표 위젯 JSX + CTA 버튼 JSX
</objective>

<execution_context>
@C:\Users\GIGABYTE\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GIGABYTE\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-CONTEXT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-RESEARCH.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-PATTERNS.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-UI-SPEC.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\02-portal-completion\02-01-SUMMARY.md

<interfaces>
<!-- PublicPortal.jsx 핵심 수정 지점 요약 -->
<!-- (RESEARCH.md + PATTERNS.md에서 추출됨 — 파일 전체 탐색 불필요) -->

수정 지점 1 — State 블록 (line ~514, switchErr 선언 다음):
```jsx
// 현재: const [switchErr, setSwitchErr] = useState("");
// 추가: const [showExpiryBanner, setShowExpiryBanner] = useState(false);
```

수정 지점 2 — doLogin() localStorage 저장 (line ~666-669):
```jsx
// 현재:
localStorage.setItem("ryekPortal", JSON.stringify({
  code: found.studentCode,
  pw: getBirthPassword(found.birthDate)
}));
// 수정 후: loginAt: Date.now() 필드 추가 (D-05)
```

수정 지점 3 — 자동로그인 useEffect (line ~607-621):
```jsx
// 현재: if (saved?.code && saved?.pw) { ... 바로 복원 }
// 수정 후: 복원 전 D-06 만료 체크 + D-07 배너 체크 삽입
// 만료 체크: if (saved.loginAt && Date.now() - saved.loginAt > 30*24*60*60*1000)
// 배너 체크: if (saved.loginAt && Date.now() - saved.loginAt > 27*24*60*60*1000)
```

수정 지점 4 — 홈 탭 블록 (line ~1094 `{tab === "home" && (`):
```
삽입 순서:
[0] {showExpiryBanner && 배너 JSX}  ← 첫 번째 자식
[1] {lessons.length > 0 && 시간표 위젯}  ← MonthlyAttendanceHeatmap 위에
[기존] 이달 출석 heatmap (line ~1097)
[기존] 공지사항 (line ~1100)
...
[기존] 기본 정보 섹션 (line ~1230)
[추가] 수강 신청하기 버튼 (기본 정보 섹션 닫힘 직후)
```

기존 재사용 변수:
```jsx
// nextLesson — line ~965에서 이미 getNextLessonDate()로 계산됨
// teachers state — PublicParentView 스코프에서 사용 가능
// student.lessons — student state에서 직접 접근
```

getThisWeekSchedule helper (PATTERNS.md에서 복사):
```jsx
const getThisWeekSchedule = () => {
  const result = [];
  for (let i = 0; i <= 6; i++) {
    const d = new Date();
    d.setDate(d.getDate() + i);
    const dayName = ["일","월","화","수","목","금","토"][d.getDay()];
    (student.lessons || []).forEach(l => {
      (l.schedule || []).filter(sc => sc.day === dayName).forEach(sc => {
        result.push({
          dayName, time: sc.time || "", instrument: l.instrument || "",
          teacherName: teachers.find(t => t.id === l.teacherId)?.name || "강사 미배정",
          daysFromNow: i
        });
      });
    });
  }
  return result.sort((a, b) => {
    if (a.daysFromNow !== b.daysFromNow) return a.daysFromNow - b.daysFromNow;
    return (a.time || "").localeCompare(b.time || "");
  });
};
const thisWeekSchedule = getThisWeekSchedule();
```
이 helper를 nextLesson const (line ~965) 바로 아래에 추가.

섹션 헤더 패턴 (PATTERNS.md line 181):
```jsx
<div style={{display:"flex",alignItems:"center",gap:8,marginBottom:10}}>
  <div style={{width:3,height:14,background:"linear-gradient(180deg,var(--blue),var(--dancheong-blue))",borderRadius:2,flexShrink:0}}/>
  <div style={{fontFamily:"'Noto Serif KR',serif",fontSize:14,fontWeight:500,color:"var(--ink)"}}>섹션명</div>
</div>
```

배너 로그아웃 — setStudent(null) 포함 (PATTERNS.md line 228):
```jsx
onClick={() => {
  localStorage.removeItem("ryekPortal");
  setLoggedIn(false);
  setStudent(null);
  setShowExpiryBanner(false);
}}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: showExpiryBanner state + doLogin loginAt 추가</name>
  <files>src/components/portal/PublicPortal.jsx</files>
  <read_first>
    - src/components/portal/PublicPortal.jsx (line 484-530 — state 선언 블록)
    - src/components/portal/PublicPortal.jsx (line 650-680 — doLogin 함수)
  </read_first>
  <action>
두 곳을 str_replace로 수정한다.

**수정 1: showExpiryBanner state 추가**

str_replace 대상:
```jsx
const [switchErr, setSwitchErr] = useState("");
```

교체 후:
```jsx
const [switchErr, setSwitchErr] = useState("");
const [showExpiryBanner, setShowExpiryBanner] = useState(false);
```

**수정 2: doLogin() — loginAt 필드 추가 (D-05)**

str_replace 대상 (localStorage.setItem 블록, ryekPortal 저장):
```jsx
localStorage.setItem("ryekPortal", JSON.stringify({
  code: found.studentCode,
  pw: getBirthPassword(found.birthDate)
}));
```

교체 후:
```jsx
localStorage.setItem("ryekPortal", JSON.stringify({
  code: found.studentCode,
  pw: getBirthPassword(found.birthDate),
  loginAt: Date.now()
}));
```

주의: 파일 내 `ryekPortal`을 저장하는 지점이 여러 개 있을 수 있다. doLogin 함수 내의 것만 수정. 배너 "30일 연장"의 read-modify-write는 Task 2에서 처리.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "showExpiryBanner" src/components/portal/PublicPortal.jsx` → useState 선언 줄 포함하여 1개 이상 출력
    - `grep -n "loginAt: Date.now()" src/components/portal/PublicPortal.jsx` → doLogin 함수 내 1개 출력
    - `npm run build` 0 exit code
  </acceptance_criteria>
  <done>
    PublicPortal.jsx에 showExpiryBanner state가 선언되고, doLogin()이 loginAt을 localStorage에 저장한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 자동로그인 useEffect 만료 체크 + 홈 탭 배너/위젯/CTA 삽입</name>
  <files>src/components/portal/PublicPortal.jsx</files>
  <read_first>
    - src/components/portal/PublicPortal.jsx (line 600-630 — 자동로그인 useEffect)
    - src/components/portal/PublicPortal.jsx (line 930-975 — getNextLessonDate 함수 + nextLesson 선언)
    - src/components/portal/PublicPortal.jsx (line 1090-1110 — 홈 탭 블록 시작 + 이달 출석 섹션)
    - src/components/portal/PublicPortal.jsx (line 1230-1245 — 기본 정보 섹션 끝 + 홈 탭 닫힘)
  </read_first>
  <action>
4개의 str_replace로 수정한다.

**수정 1: 자동로그인 useEffect 내 만료 체크 추가 (D-06, D-07)**

str_replace 대상 (useEffect 내부 — saved 체크 직후):
```jsx
      if (saved?.code && saved?.pw) {
        const found = students.find(s => s.studentCode === saved.code);
        if (found && getBirthPassword(found.birthDate) === saved.pw
            && (found.status || "active") === "active") {
          setStudent(found);
          setLoggedIn(true);
          initReadState(found.id);
        }
      }
```

교체 후:
```jsx
      if (saved?.code && saved?.pw) {
        // D-06: 30일 만료 체크 (loginAt 없는 기존 세션은 skip — backward compatible)
        if (saved.loginAt && Date.now() - saved.loginAt > 30 * 24 * 60 * 60 * 1000) {
          localStorage.removeItem("ryekPortal");
          return;
        }
        const found = students.find(s => s.studentCode === saved.code);
        if (found && getBirthPassword(found.birthDate) === saved.pw
            && (found.status || "active") === "active") {
          setStudent(found);
          setLoggedIn(true);
          initReadState(found.id);
          // D-07: D-3일 배너 체크
          if (saved.loginAt && Date.now() - saved.loginAt > 27 * 24 * 60 * 60 * 1000) {
            setShowExpiryBanner(true);
          }
        }
      }
```

**수정 2: getThisWeekSchedule helper 추가**

nextLesson const (line ~965) 다음에 삽입. str_replace 대상:
```jsx
const nextLesson = getNextLessonDate(student, DAYS);
```

교체 후 (nextLesson 선언 + helper 추가):
```jsx
const nextLesson = getNextLessonDate(student, DAYS);
const getThisWeekSchedule = () => {
  const result = [];
  for (let i = 0; i <= 6; i++) {
    const d = new Date();
    d.setDate(d.getDate() + i);
    const dayName = ["일","월","화","수","목","금","토"][d.getDay()];
    (student.lessons || []).forEach(l => {
      (l.schedule || []).filter(sc => sc.day === dayName).forEach(sc => {
        result.push({
          dayName, time: sc.time || "", instrument: l.instrument || "",
          teacherName: teachers.find(t => t.id === l.teacherId)?.name || "강사 미배정",
          daysFromNow: i
        });
      });
    });
  }
  return result.sort((a, b) => {
    if (a.daysFromNow !== b.daysFromNow) return a.daysFromNow - b.daysFromNow;
    return (a.time || "").localeCompare(b.time || "");
  });
};
const thisWeekSchedule = getThisWeekSchedule();
```

주의: getNextLessonDate(student, DAYS) 호출 형식이 실제 코드와 다를 수 있다. Task 2 read_first에서 line 930-975를 먼저 읽고 실제 nextLesson 선언 형식을 확인한 후 정확히 매칭하여 str_replace.

**수정 3: 홈 탭 블록 — 배너 + 시간표 위젯 삽입**

홈 탭 블록 첫 번째 자식 `<div>` 여는 태그 바로 다음에 삽입. str_replace 대상 — 홈 탭 첫 번째 `<div style={{marginBottom:16}}>` 직전 (`{tab === "home" && (` 바로 다음):

실제 파일을 읽어 `{tab === "home" && (` 다음 첫 번째 렌더 요소를 찾아 그 앞에 배너와 위젯 JSX를 삽입.

삽입할 배너 JSX (홈 탭 최상단 — 모든 기존 콘텐츠 위):
```jsx
    {/* D-07: 세션 만료 배너 */}
    {showExpiryBanner && (
      <div className="portal-expiry-banner fade-up">
        <span className="portal-expiry-text">로그인이 3일 후 만료됩니다.</span>
        <button
          type="button"
          className="portal-expiry-extend"
          onClick={() => {
            const saved = JSON.parse(localStorage.getItem("ryekPortal") || "{}");
            localStorage.setItem("ryekPortal", JSON.stringify({ ...saved, loginAt: Date.now() }));
            setShowExpiryBanner(false);
          }}
        >
          30일 연장
        </button>
        <button
          type="button"
          className="portal-expiry-logout"
          onClick={() => {
            localStorage.removeItem("ryekPortal");
            setLoggedIn(false);
            setStudent(null);
            setShowExpiryBanner(false);
          }}
        >
          로그아웃
        </button>
      </div>
    )}
    {/* POR-02: 시간표 위젯 — 이달 출석 위에 */}
    {(student.lessons || []).length > 0 && (
      <div style={{marginBottom:16}}>
        <div style={{display:"flex",alignItems:"center",gap:8,marginBottom:10}}>
          <div style={{width:3,height:14,background:"linear-gradient(180deg,var(--blue),var(--dancheong-blue))",borderRadius:2,flexShrink:0}}/>
          <div style={{fontFamily:"'Noto Serif KR',serif",fontSize:14,fontWeight:500,color:"var(--ink)"}}>다음 수업</div>
        </div>
        {nextLesson
          ? nextLesson.lessons.map((l, i) => {
              const tName = teachers.find(t => t.id === l.teacherId)?.name || "강사 미배정";
              const sc = (l.schedule || []).find(s => s.day === nextLesson.dayName);
              return (
                <div key={i} className="portal-next-lesson hero-card" style={{marginBottom:8}}>
                  <div className="portal-next-lesson-inst">{l.instrument}</div>
                  <div className="portal-next-lesson-time">{nextLesson.dayName}요일 {sc?.time || nextLesson.time}</div>
                  <div className="portal-next-lesson-teacher">담당: {tName} 강사님</div>
                </div>
              );
            })
          : null
        }
        {thisWeekSchedule.length > 0 && (
          <div>
            <div style={{display:"flex",alignItems:"center",gap:8,marginBottom:8,marginTop:12}}>
              <div style={{width:3,height:14,background:"linear-gradient(180deg,var(--blue),var(--dancheong-blue))",borderRadius:2,flexShrink:0}}/>
              <div style={{fontFamily:"'Noto Serif KR',serif",fontSize:14,fontWeight:500,color:"var(--ink)"}}>이번 주 수업</div>
            </div>
            <div className="portal-week-chips">
              {thisWeekSchedule.map((s, i) => (
                <span key={i} className="portal-week-chip">{s.dayName} {s.time}</span>
              ))}
            </div>
          </div>
        )}
      </div>
    )}
```

**수정 4: 수강 신청하기 CTA 버튼 삽입 (POR-07)**

"기본 정보" 섹션 닫힘 div 다음, 홈 탭 닫힘 div 전에 삽입. 실제 파일을 읽어 "기본 정보" 마지막 content 이후를 찾아 삽입.

삽입할 CTA JSX:
```jsx
    {/* POR-07: 수강 신청 진입점 */}
    <div style={{marginTop:16,marginBottom:8}}>
      <button
        className="btn btn-secondary btn-full"
        onClick={() => { window.location.href = "/register"; }}
      >
        수강 신청하기 →
      </button>
    </div>
```

window.confirm 절대 없음. 즉시 이동.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "30 \* 24 \* 60 \* 60 \* 1000" src/components/portal/PublicPortal.jsx` → 만료 체크 줄 출력
    - `grep -n "27 \* 24 \* 60 \* 60 \* 1000" src/components/portal/PublicPortal.jsx` → 배너 체크 줄 출력
    - `grep -n "portal-expiry-banner" src/components/portal/PublicPortal.jsx` → 배너 div className 줄 출력
    - `grep -n "portal-next-lesson" src/components/portal/PublicPortal.jsx` → 위젯 div className 줄 출력
    - `grep -n "portal-week-chip" src/components/portal/PublicPortal.jsx` → chip className 줄 출력
    - `grep -n "수강 신청하기" src/components/portal/PublicPortal.jsx` → CTA 버튼 줄 출력
    - `grep -n "window.confirm" src/components/portal/PublicPortal.jsx` → 출력 없음 (0라인)
    - `npm run build` 0 exit code
  </acceptance_criteria>
  <done>
    - 자동로그인 useEffect가 30일 만료 체크와 27일 배너 체크를 포함한다
    - 홈 탭 최상단에 showExpiryBanner 조건부 배너가 렌더된다
    - 배너 버튼 2개(30일 연장, 로그아웃)가 type="button"이고 window.confirm 없이 즉시 실행된다
    - 홈 탭에 시간표 위젯(다음 수업 카드 + 이번 주 수업 칩)이 lessons[] 기반으로 렌더된다
    - 홈 탭 하단에 '수강 신청하기 →' 버튼이 /register로 이동한다
    - npm run build가 통과한다
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - 세션 만료 정책 (D-05 loginAt 저장, D-06 30일 체크, D-07 배너)
    - 홈 탭 시간표 위젯 (다음 수업 카드 + 이번 주 수업 칩)
    - 수강 신청하기 CTA 버튼
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 브라우저에서 `/myryk` 접속
    2. 포털 로그인 후 DevTools → Application → Local Storage → "ryekPortal" 키 확인 → `loginAt` 숫자 필드 있는지 확인
    3. 홈 탭에서 "다음 수업" 카드와 "이번 주 수업" 칩 확인 (수업이 있는 학생으로 로그인)
    4. 홈 탭 하단 스크롤 → "수강 신청하기 →" 버튼 확인 → 클릭 시 /register 이동 확인
    5. POR-03 확인: notes 탭 클릭 → 레슨노트 목록 정상 표시 확인
    6. 배너 테스트: DevTools Console에서 `const s = JSON.parse(localStorage.getItem('ryekPortal')); s.loginAt = Date.now() - 28*24*60*60*1000; localStorage.setItem('ryekPortal', JSON.stringify(s));` 실행 후 페이지 새로고침 → 홈 탭 최상단에 만료 배너 표시 확인
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → React state | ryekPortal JSON 파싱 — 브라우저 로컬 스토리지에서 읽음 |
| user input (code, pw) → student lookup | 포털 로그인 입력이 students 배열과 비교됨 |
| 배너 "로그아웃" 버튼 → localStorage 삭제 | 즉시 실행, 확인 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | localStorage['ryekPortal'] | accept | HTTPS (Cloudflare Pages 기본 적용). XSS는 React JSX 자동 이스케이프로 방어. loginAt 조작은 자신의 세션만 연장 — 타 학생 접근 불가. |
| T-02-02 | Information Disclosure | getThisWeekSchedule() | accept | 이미 로그인된 student의 데이터만 사용. Firestore 규칙(Phase 1)이 데이터 범위 통제. |
| T-02-03 | Spoofing | doLogin() 비밀번호 검증 | accept | 생일 4자리 MMDD — 낮은 엔트로피지만 기존 방식 유지 결정 (RESEARCH.md V6 항목). HTTPS + Firestore 규칙이 1차 방어. |
| T-02-04 | Tampering | 배너 "30일 연장" localStorage write | accept | 자신의 loginAt만 갱신. spread 패턴으로 기존 필드 보존 — 데이터 손실 없음. |
| T-02-05 | Information Disclosure | student.notes 렌더 금지 (line 1024 주석) | mitigate | 신규 JSX에서 student.notes 렌더링 없음 확인. 기존 금지 주석(line 1024) 유지. |
</threat_model>

<verification>
```bash
# 1. 만료 체크 로직 존재
grep -n "30 \* 24 \* 60 \* 60 \* 1000" src/components/portal/PublicPortal.jsx
grep -n "27 \* 24 \* 60 \* 60 \* 1000" src/components/portal/PublicPortal.jsx

# 2. loginAt 저장 (doLogin 내)
grep -n "loginAt: Date.now()" src/components/portal/PublicPortal.jsx

# 3. 배너 + 위젯 클래스 존재
grep -n "portal-expiry-banner\|portal-next-lesson\|portal-week-chip" src/components/portal/PublicPortal.jsx

# 4. window.confirm 없음 (CRITICAL)
grep -n "window.confirm\|window.alert" src/components/portal/PublicPortal.jsx
# → 출력 없어야 PASS

# 5. 수강 신청 CTA + /register
grep -n "수강 신청하기\|/register" src/components/portal/PublicPortal.jsx

# 6. 빌드
npm run build
```
</verification>

<success_criteria>
- localStorage['ryekPortal']에 loginAt 필드가 저장된다 (doLogin 수정)
- 30일 만료 시 localStorage 삭제 후 로그인 화면 (useEffect 체크)
- 27일 경과 시 showExpiryBanner가 true로 설정 (useEffect 체크)
- 홈 탭 최상단에 배너 JSX가 showExpiryBanner 조건부로 렌더된다
- 배너 버튼이 type="button"이고 window.confirm 없이 즉시 동작한다
- 시간표 위젯이 lessons[] 기반으로 "다음 수업" 카드와 "이번 주 수업" 칩을 렌더한다
- lessons[]가 빈 경우 위젯 섹션이 렌더되지 않는다
- "수강 신청하기 →" 버튼이 홈 탭 하단에 있고 /register로 이동한다
- npm run build 0 exit code
</success_criteria>

<output>
After completion, create `.planning/phases/02-portal-completion/02-02-SUMMARY.md`
</output>
