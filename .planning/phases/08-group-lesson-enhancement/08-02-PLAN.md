---
phase: 08-group-lesson-enhancement
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/components/admin/AdminTools.jsx
  - src/App.jsx
autonomous: true
requirements:
  - GRP-02
must_haves:
  truths:
    - "관리자가 AdminTools에서 '레슨 슬롯 초기화' 버튼을 볼 수 있다"
    - "버튼 실행 후 모든 학생의 lessons[].slotId가 채워지고 rye-lesson-slots 문서가 생성된다"
    - "기관 가상회원(isInstitution=true)은 마이그레이션에서 제외된다"
    - "이미 slotId가 있는 lesson은 스킵된다 (idempotent)"
    - "결과 카운트(생성된 슬롯 수, 업데이트된 학생 수)가 UI에 표시된다"
  artifacts:
    - path: src/components/admin/AdminTools.jsx
      provides: "LessonSlotsView 컴포넌트 (export)"
      exports: ["LessonSlotsView"]
    - path: src/App.jsx
      provides: "runLessonSlotMigration 함수 + view 렌더 + nav 진입점"
      contains: "runLessonSlotMigration"
  key_links:
    - from: LessonSlotsView
      to: src/App.jsx runLessonSlotMigration
      via: "onRunMigration prop"
      pattern: "onRunMigration"
    - from: runLessonSlotMigration
      to: "Firestore rye-lesson-slots + rye-students"
      via: "addLessonSlot(per D-09) + updateStudentDoc(per D-10)"
      pattern: "updateStudentDoc"
---

<objective>
AdminTools에 LessonSlotsView 컴포넌트를 추가하고, App.jsx에 마이그레이션 함수를 구현한다. 버튼 클릭 → 인라인 확인 UI → 마이그레이션 실행 → 결과 표시 흐름을 구현한다.

Purpose: 기존 77명의 학생 데이터에서 그룹/개인 슬롯을 자동 생성하고 slotId를 연결해 Phase 8의 핵심 데이터 기반을 구축한다.
Output: LessonSlotsView 컴포넌트 + runLessonSlotMigration 함수
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-group-lesson-enhancement/08-CONTEXT.md
@.planning/phases/08-group-lesson-enhancement/08-01-SUMMARY.md

<interfaces>
<!-- D-03: 마이그레이션 로직 (idempotent) -->
<!--
  알고리즘:
  Step 1: 그룹 맵 구성
    - 대상: isInstitution !== true AND status !== "withdrawn" 인 모든 학생
    - 각 학생의 lessons[] 순회
    - lesson.slotId가 이미 있으면 → 스킵 (idempotent)
    - 슬롯 키(groupKey): `${tid}::${instrument}::${schedFp}`
      - tid = lesson.teacherId || student.teacherId
      - schedFp = JSON.stringify([...lesson.schedule].sort((a,b) =>
          `${a.day}${a.time}`.localeCompare(`${b.day}${b.time}`)))
    - groupMap[groupKey].students.push({ studentId: s.id, lessonIdx: i })

  Step 2: 슬롯 생성 (addLessonSlot — D-09, no transaction)
    - students.length >= 2 → group 슬롯 (name: `${instrument} 그룹`, type: "group")
    - students.length === 1 → individual 슬롯 (name: 학생 이름, type: "individual")
    - slotIdMap[groupKey] = docRef.id

  Step 3: 학생 업데이트 (updateStudentDoc — D-10, per-op transaction)
    - studentUpdateMap: { studentId → updatedStudentObj }
    - groupMap 전체 순회: 각 {studentId, lessonIdx} → lessons[lessonIdx].slotId = slotId
    - studentUpdateMap[studentId]에 누적 (단일 학생 여러 lesson 모두 처리 후 1회 updateStudentDoc)
    - 마지막으로 studentUpdateMap의 각 학생에 updateStudentDoc 1회 호출
-->

<!-- D-10: 마이그레이션 안전 장치 -->
<!--
  - window.confirm 절대 금지 → 인라인 확인 UI (버튼 클릭 → 확인 버튼 노출 → 재클릭)
  - window.alert 절대 금지 → 결과 메시지를 컴포넌트 state로 표시
  - saveStudents() 절대 금지 → updateStudentDoc per-op 트랜잭션만
  - 에러 발생 시 부분 완료 상태 허용 (롤백 없음) — 에러 메시지 표시
-->

<!-- App.jsx updateStudentDoc 시그니처 (line 533) -->
```js
const updateStudentDoc = async (student) => {
  // student.id 필수. runTransaction으로 rye-students 문서 내 배열 교체
};
```

<!-- App.jsx ScheduleView 렌더 줄 주변 패턴 (line ~1286-1295) -->
```jsx
{view === "categories" && user.role === "admin" && <CategoriesView ... />}
{view === "activity" && canManageAll(user.role) && <ActivityView ... />}
{view === "shop" && user.role === "admin" && <ShopView ... />}
// → view === "lessonSlots" 케이스 추가
```

<!-- App.jsx MoreMenu/nav 구조 — AdminTools 진입점 추가 위치 확인 필요 -->
<!-- AdminTools 뷰들은 MoreMenu 또는 admin 탭에서 접근 — 기존 "shop" view 패턴 동일 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminTools.jsx — LessonSlotsView 컴포넌트</name>
  <read_first>
    - src/components/admin/AdminTools.jsx (전체 파일 — 기존 export 함수 패턴, ShopView 구조 참고)
    - src/App.jsx (line 1278-1300 — 기존 AdminTools 뷰 렌더 패턴, nav 구조)
  </read_first>
  <files>src/components/admin/AdminTools.jsx</files>
  <action>
파일 끝(TrashView export 아래)에 LessonSlotsView를 추가한다.

```jsx
// ── LESSON SLOTS MIGRATION VIEW ───────────────────────────────────────────────
export function LessonSlotsView({ students, teachers, lessonSlots, onRunMigration, showToast }) {
  const [confirm, setConfirm] = useState(false);   // 인라인 확인 상태
  const [running, setRunning] = useState(false);
  const [result, setResult] = useState(null);      // { slotsCreated, studentsUpdated } | null
  const [error, setError] = useState("");

  const totalStudents = (students || []).filter(s => !s.isInstitution && s.status !== "withdrawn").length;
  const migratedCount = (students || []).filter(s =>
    !s.isInstitution && (s.lessons || []).some(l => l.slotId)
  ).length;
  const existingSlots = (lessonSlots || []).length;

  const handleRun = async () => {
    setRunning(true);
    setError("");
    setResult(null);
    try {
      const res = await onRunMigration();
      setResult(res);
      showToast && showToast(`마이그레이션 완료: 슬롯 ${res.slotsCreated}개, 학생 ${res.studentsUpdated}명`);
    } catch (e) {
      setError(e.message || "마이그레이션 중 오류 발생");
    } finally {
      setRunning(false);
      setConfirm(false);
    }
  };

  return (
    <div>
      <div className="ph">
        <div><h1>레슨 슬롯 관리</h1><div className="ph-sub">그룹·개인 레슨 슬롯 초기화 및 현황</div></div>
      </div>

      {/* 현황 카드 */}
      <div className="card" style={{ padding: 16, marginBottom: 16 }}>
        <div style={{ display: "flex", gap: 24, flexWrap: "wrap" }}>
          <div><div style={{ fontSize: 11, color: "var(--ink-30)", marginBottom: 2 }}>생성된 슬롯</div><div style={{ fontSize: 22, fontWeight: 700, color: "var(--blue)" }}>{existingSlots}</div></div>
          <div><div style={{ fontSize: 11, color: "var(--ink-30)", marginBottom: 2 }}>slotId 연결된 학생</div><div style={{ fontSize: 22, fontWeight: 700, color: "var(--green)" }}>{migratedCount}</div></div>
          <div><div style={{ fontSize: 11, color: "var(--ink-30)", marginBottom: 2 }}>전체 대상 학생</div><div style={{ fontSize: 22, fontWeight: 700 }}>{totalStudents}</div></div>
        </div>
      </div>

      {/* 결과 표시 */}
      {result && (
        <div className="card" style={{ padding: 14, marginBottom: 16, background: "var(--green-lt)", border: "1px solid var(--green)" }}>
          <div style={{ fontWeight: 600, color: "var(--green)" }}>마이그레이션 완료</div>
          <div style={{ fontSize: 13, color: "var(--ink-60)", marginTop: 4 }}>
            슬롯 {result.slotsCreated}개 생성 · 학생 {result.studentsUpdated}명 업데이트
          </div>
        </div>
      )}
      {error && (
        <div className="card" style={{ padding: 14, marginBottom: 16, background: "var(--red-lt)", border: "1px solid var(--red)" }}>
          <div style={{ fontWeight: 600, color: "var(--red)" }}>오류</div>
          <div style={{ fontSize: 13, color: "var(--ink-60)", marginTop: 4 }}>{error}</div>
        </div>
      )}

      {/* 마이그레이션 버튼 */}
      <div className="card" style={{ padding: 16 }}>
        <div style={{ fontSize: 14, fontWeight: 600, marginBottom: 6 }}>레슨 슬롯 초기화</div>
        <div style={{ fontSize: 13, color: "var(--ink-60)", marginBottom: 14, lineHeight: 1.6 }}>
          기존 학생 데이터에서 그룹/개인 슬롯을 자동 생성하고 lessons[].slotId를 연결합니다.<br />
          이미 slotId가 있는 레슨은 스킵됩니다 (idempotent). 기관 가상회원은 제외됩니다.
        </div>
        {!confirm ? (
          <button
            className="btn btn-secondary"
            onClick={() => setConfirm(true)}
            disabled={running}
          >
            🔄 레슨 슬롯 초기화 실행
          </button>
        ) : (
          <div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" }}>
            <span style={{ fontSize: 13, color: "var(--gold-dk)", fontWeight: 600 }}>실행하시겠습니까?</span>
            <button className="btn btn-primary" onClick={handleRun} disabled={running} style={{ minWidth: 80 }}>
              {running ? "실행 중..." : "확인"}
            </button>
            <button className="btn btn-secondary" onClick={() => setConfirm(false)} disabled={running}>취소</button>
          </div>
        )}
      </div>
    </div>
  );
}
```

규칙: window.confirm / window.alert 절대 금지. 인라인 confirm state 사용.
  </action>
  <verify>
    <automated>grep -c "export function LessonSlotsView" src/components/admin/AdminTools.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function LessonSlotsView" src/components/admin/AdminTools.jsx` → 1줄 출력
    - `grep "window.confirm\|window.alert" src/components/admin/AdminTools.jsx` → 0줄 (없어야 함)
    - `grep "onRunMigration" src/components/admin/AdminTools.jsx` → prop 사용 줄 출력
  </acceptance_criteria>
  <done>LessonSlotsView가 export되고 인라인 confirm UI로 마이그레이션 버튼을 제공한다</done>
</task>

<task type="auto">
  <name>Task 2: App.jsx — runLessonSlotMigration 함수 + view 렌더 + nav</name>
  <read_first>
    - src/App.jsx (line 1-15 imports, line 520-545 updateStudentDoc/addStudentDoc 구현, line 1278-1300 뷰 렌더 영역)
    - src/components/admin/AdminTools.jsx (방금 추가된 LessonSlotsView export 확인)
  </read_first>
  <files>src/App.jsx</files>
  <action>
1. AdminTools import 줄(line 12) 수정 — LessonSlotsView 추가:
   ```js
   import { ActivityView, PendingView, TrashView, CategoriesView, AiSettingsView, ShopView, LessonSlotsView } from "./components/admin/AdminTools.jsx";
   ```

2. updateStudentDoc 함수 아래에 runLessonSlotMigration 함수 추가:
   ```js
   const runLessonSlotMigration = async () => {
     const realStudents = students.filter(s => !s.isInstitution && s.status !== "withdrawn");
     
     // Step 1: 그룹 맵 구성 (slotId 없는 lesson만)
     const groupMap = {};
     realStudents.forEach(s => {
       (s.lessons || []).forEach((l, li) => {
         if (l.slotId) return; // idempotent: 이미 slotId 있으면 스킵
         const tid = l.teacherId || s.teacherId;
         const schedFp = JSON.stringify(
           [...(l.schedule || [])].sort((a, b) => `${a.day}${a.time}`.localeCompare(`${b.day}${b.time}`))
         );
         const key = `${tid}::${l.instrument}::${schedFp}`;
         if (!groupMap[key]) {
           groupMap[key] = { teacherId: tid, instrument: l.instrument, schedule: l.schedule || [], students: [] };
         }
         groupMap[key].students.push({ studentId: s.id, lessonIdx: li });
       });
     });
     
     // Step 2: 슬롯 생성 (addLessonSlot, no transaction)
     const slotIdMap = {};
     let slotsCreated = 0;
     for (const [key, group] of Object.entries(groupMap)) {
       const isGroup = group.students.length > 1;
       const slotName = isGroup
         ? `${group.instrument} 그룹`
         : (students.find(s => s.id === group.students[0].studentId)?.name || "개인");
       const docRef = await addLessonSlot({
         teacherId: group.teacherId,
         instrument: group.instrument,
         type: isGroup ? "group" : "individual",
         name: slotName,
         capacity: isGroup ? group.students.length : 1,
         schedule: group.schedule,
         status: "active",
         notes: "",
       });
       slotIdMap[key] = docRef.id;
       slotsCreated++;
     }
     
     // Step 3: 학생 업데이트 (updateStudentDoc per-op, 학생당 1회)
     const studentUpdateMap = {};
     for (const [key, group] of Object.entries(groupMap)) {
       const slotId = slotIdMap[key];
       group.students.forEach(({ studentId, lessonIdx }) => {
         const base = studentUpdateMap[studentId] || students.find(s => s.id === studentId);
         const lessons = [...(base.lessons || [])];
         lessons[lessonIdx] = { ...lessons[lessonIdx], slotId };
         studentUpdateMap[studentId] = { ...base, lessons };
       });
     }
     let studentsUpdated = 0;
     for (const updatedStudent of Object.values(studentUpdateMap)) {
       await updateStudentDoc(updatedStudent);
       studentsUpdated++;
     }
     
     return { slotsCreated, studentsUpdated };
   };
   ```

3. AdminTools 뷰 렌더 영역에 다음 추가 (ShopView 렌더 줄 아래):
   ```jsx
   {view === "lessonSlots" && user.role === "admin" && (
     <LessonSlotsView
       students={students}
       teachers={teachers}
       lessonSlots={lessonSlots}
       onRunMigration={runLessonSlotMigration}
       showToast={showToast}
     />
   )}
   ```

4. App.jsx에서 "shop" nav 항목 패턴을 찾아 그 바로 아래에 "lessonSlots" 항목 추가:
   ```js
   // 기존 패턴 (App.jsx에서 "shop" 항목이 있는 줄 아래):
   { view: "lessonSlots", label: "레슨 슬롯", icon: IC.calendar, roles: ["admin"] }
   // 정확한 추가 위치: grep -n '"shop"' src/App.jsx 로 확인 후 바로 아래 줄에 삽입
   ```

규칙: saveStudents() 절대 금지. updateStudentDoc per-op만 사용 (이미 위 코드에서 준수함). `addLessonSlot`은 08-01에서 firebase.js에 추가되어 App.jsx import에 포함됨.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "runLessonSlotMigration" src/App.jsx` → 함수 선언 + LessonSlotsView prop 전달 2줄 출력
    - `grep "LessonSlotsView" src/App.jsx` → import 줄 + 렌더 줄 2줄 이상
    - `grep "lessonSlots.*user.role.*admin" src/App.jsx` → view 렌더 가드 확인
    - `grep "saveStudents" src/App.jsx` → throw 선언 1줄만 (호출 줄 없어야 함)
    - `grep "lessonSlots.*label\|label.*레슨 슬롯" src/App.jsx` → nav 항목 줄 출력
    - npm run build 통과
  </acceptance_criteria>
  <done>관리자가 lessonSlots 뷰에 접근하여 마이그레이션을 실행할 수 있으며, 결과가 UI에 표시된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → App.jsx runLessonSlotMigration | 마이그레이션 실행 권한 |
| App.jsx → Firestore rye-lesson-slots | 슬롯 문서 생성 |
| App.jsx → Firestore rye-students | 학생 lessons 필드 업데이트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Elevation of Privilege | runLessonSlotMigration / LessonSlotsView | mitigate | `view === "lessonSlots" && user.role === "admin"` 가드로 admin 전용 렌더. teacher/manager 는 뷰 접근 불가 |
| T-08-02-02 | Denial of Service | runLessonSlotMigration (77명 순차 업데이트) | accept | 소규모 데이터셋(77명). 오류 시 부분 완료 허용, 재실행 시 idempotent로 복구 가능 |
| T-08-02-03 | Tampering | updateStudentDoc 호출 | mitigate | runTransaction 기반 updateStudentDoc 사용. 배열 전체 덮어쓰기(saveStudents) 금지 |
</threat_model>

<verification>
1. npm run build 통과
2. `grep "export function LessonSlotsView" src/components/admin/AdminTools.jsx` → 존재
3. `grep "runLessonSlotMigration" src/App.jsx` → 2줄 이상
4. `grep "window.confirm\|window.alert" src/components/admin/AdminTools.jsx` → 0줄
5. `grep "saveStudents" src/App.jsx` → throw 선언 1줄만
</verification>

<success_criteria>
- LessonSlotsView가 AdminTools.jsx에 export됨
- runLessonSlotMigration이 App.jsx에 구현됨 (addLessonSlot + updateStudentDoc per-op)
- admin만 "lessonSlots" 뷰에 접근 가능
- 마이그레이션은 idempotent (이미 slotId 있는 lesson 스킵)
- window.confirm/alert 없음
</success_criteria>

<output>
완료 후 `.planning/phases/08-group-lesson-enhancement/08-02-SUMMARY.md` 생성
</output>
