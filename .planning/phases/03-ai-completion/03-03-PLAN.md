---
phase: 03-ai-completion
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/components/ai/AiAssistant.jsx
  - src/App.jsx
  - src/constants.jsx
autonomous: true
requirements:
  - AI-04
  - AI-05

must_haves:
  truths:
    - "AiAssistant 자연어 쿼리 결과의 학생 이름을 클릭하면 StudentDetailModal이 열린다"
    - "학생 카드에 hover 시 시각적 피드백이 있다"
    - "rate limit(429) 에러 시 '분당 제한' 안내 메시지가 사용자에게 표시된다"
    - "AiAssistant에서 재시도 1회 로직이 동작한다"
  artifacts:
    - path: "src/components/ai/AiAssistant.jsx"
      provides: "onOpenStudent prop, ListResult 카드 클릭, 에러 재시도 로직"
      contains: "onOpenStudent"
    - path: "src/App.jsx"
      provides: "AiAssistant에 onOpenStudent prop 전달"
      contains: "onOpenStudent"
    - path: "src/constants.jsx"
      provides: "ai-result-row hover 스타일"
      contains: "ai-result-row"
  key_links:
    - from: "src/components/ai/AiAssistant.jsx ListResult"
      to: "App.jsx StudentDetailModal"
      via: "onOpenStudent(sid) → setSelected + setModal('sDetail')"
      pattern: "onOpenStudent"
    - from: "src/App.jsx"
      to: "src/components/ai/AiAssistant.jsx"
      via: "onOpenStudent={(sid) => { setSelected(...); setModal('sDetail'); }}"
      pattern: "setModal.*sDetail"
---

<objective>
AI-04: AiAssistant `ListResult` 카드 클릭 → StudentDetailModal 연결 (onOpenStudent prop 추가)
AI-05: rate limit 429 재시도 1회 로직 + 에러 메시지 개선

Purpose: 자연어 쿼리 결과가 단순 텍스트 목록에서 클릭 가능한 카드로 업그레이드되어
AI 비서에서 학생 상세 화면으로 바로 이동할 수 있게 한다.
rate limit 에러 시 자동 재시도로 일시적 실패를 투명하게 처리한다.

Output:
- `src/components/ai/AiAssistant.jsx` — onOpenStudent prop, ListResult 클릭 핸들러, 재시도 로직
- `src/App.jsx` — AiAssistant에 onOpenStudent prop 전달
- `src/constants.jsx` — .ai-result-row hover 스타일 추가
</objective>

<execution_context>
@C:\Users\GIGABYTE\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GIGABYTE\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-CONTEXT.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-RESEARCH.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-PATTERNS.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\phases\03-ai-completion\03-02-SUMMARY.md

<interfaces>
<!-- AiAssistant와 App.jsx의 실제 인터페이스 -->

App.jsx에서 StudentDetailModal을 여는 패턴 (line 899, 952):
```javascript
// StudentsView에서 학생 클릭
onSelect={s => { setSelected(s); setModal("sDetail"); }}

// modal === "sDetail" && selected && <StudentDetailModal ... />
// setModal(null) 으로 닫기

// App.jsx state (line 241-242):
const [modal, setModal] = useState(null);
const [selected, setSelected] = useState(null);

// students 배열에서 id로 찾는 방법:
students.find(s => s.id === sid)
```

App.jsx에서 AiAssistant 현재 호출 (line 930):
```jsx
{canManageAll(user.role) && (
  <AiAssistant students={students} attendance={attendance} payments={payments} teachers={teachers} />
)}
```

AiAssistant.jsx 현재 컴포넌트 시그니처 (line 194):
```javascript
export default function AiAssistant({ students, attendance, payments, teachers })
// → onOpenStudent prop 추가 필요
```

AiAssistant.jsx ListResult 현재 구조 (lines 31-57):
```jsx
function ListResult({ data }) {  // → data + onOpenStudent 필요
  const [expanded, setExpanded] = useState(false);
  const shown = expanded ? data : data.slice(0, 5);
  return (
    <div className="ai-result-list">
      {shown.map(s => (
        <div key={s.id} className="ai-result-row">  {/* 클릭 핸들러 + cursor 추가 */}
          <Av photo={s.photo} name={s.name} size="av-sm"/>
          <div style={{flex:1,minWidth:0}}>
            <span className="ai-result-name">{s.name}</span>
            {s._absenceCount != null && <span className="ai-result-sub">결석 {s._absenceCount}회</span>}
            {s._attRate != null && <span className="ai-result-sub">출석률 {s._attRate}%</span>}
          </div>
        </div>
      ))}
    </div>
  );
}
```

AiAssistant.jsx MessageBubble (lines 164-181):
```jsx
function MessageBubble({ msg }) {
  // ...
  {msg.type === "list" && <ListResult data={msg.content}/>}
  // → ListResult에 onOpenStudent 전달 필요
}
```

AiAssistant.jsx send 함수의 에러 처리 (lines 257-262):
```javascript
} catch {
  setMessages(m => [...m, {
    id: Date.now() + "e", role: "assistant", type: "text",
    content: "오류가 발생했어요. 잠시 후 다시 시도해주세요."
  }]);
}
```

aiClient.js의 callAi 에러 코드:
```javascript
if (resp.status === 429) throw new Error("rate_limited");
// → AiAssistant send 함수에서 이 에러 코드로 분기 처리
```

src/constants.jsx 내 CSS 문자열 위치:
- `.ai-result-row` 클래스가 이미 CSS 문자열 내에 정의되어 있음
- `grep -n "ai-result-row" src/constants.jsx` 로 위치 확인 후 hover 스타일 추가
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AiAssistant onOpenStudent prop + ListResult 클릭 + AI-05 재시도 에러 처리</name>
  <files>
    src/components/ai/AiAssistant.jsx
  </files>
  <read_first>
    - src/components/ai/AiAssistant.jsx — 전체 파일 (ListResult, MessageBubble, AiAssistant, send 함수)
    - src/aiClient.js — callAi 에러 코드 목록 (rate_limited, auth_required 등)
  </read_first>
  <action>
**1. ListResult 함수 수정 (lines 31-57 근방):**

```jsx
function ListResult({ data, onOpenStudent }) {   // onOpenStudent prop 추가
  const [expanded, setExpanded] = useState(false);
  const shown = expanded ? data : data.slice(0, 5);
  if (data.length === 0) {
    return <div style={{fontSize:12,color:"var(--ink-30)",marginTop:4}}>해당 조건의 회원이 없습니다.</div>;
  }
  return (
    <div className="ai-result-list">
      <div style={{fontSize:11,color:"var(--ink-30)",marginBottom:4}}>{data.length}명</div>
      {shown.map(s => (
        <div
          key={s.id}
          className="ai-result-row"
          onClick={onOpenStudent ? () => onOpenStudent(s.id) : undefined}
          style={onOpenStudent ? {cursor:"pointer"} : undefined}
        >
          <Av photo={s.photo} name={s.name} size="av-sm"/>
          <div style={{flex:1,minWidth:0}}>
            <span className="ai-result-name">{s.name}</span>
            {s._absenceCount != null && <span className="ai-result-sub">결석 {s._absenceCount}회</span>}
            {s._attRate != null && <span className="ai-result-sub">출석률 {s._attRate}%</span>}
          </div>
        </div>
      ))}
      {data.length > 5 && (
        <button className="ai-result-more" onClick={() => setExpanded(v => !v)}>
          {expanded ? "접기 ▴" : `전체 보기 (${data.length - 5}명 더) ▾`}
        </button>
      )}
    </div>
  );
}
```

**2. MessageBubble 함수 수정 (lines 164-181 근방):**

```jsx
function MessageBubble({ msg, onOpenStudent }) {    // onOpenStudent prop 추가
  if (msg.role === "user") {
    return <div className="ai-msg-user">{msg.content}</div>;
  }
  return (
    <div className="ai-msg-bot">
      <div className="ai-msg-av"><RobotSvg size={14}/></div>
      <div className="ai-msg-body">
        {msg.type === "list" && <ListResult data={msg.content} onOpenStudent={onOpenStudent}/>}
        {msg.type === "stats" && <StatsResult data={msg.content}/>}
        {msg.type === "churn-list" && <ChurnListResult data={msg.content}/>}
        {msg.type === "churn-pending" && <ChurnPending/>}
        {msg.type === "churn-ai" && <ChurnAiResult data={msg.content}/>}
        {msg.type === "text" && msg.content}
      </div>
    </div>
  );
}
```

**3. AiAssistant 컴포넌트 시그니처 수정 (line 194 근방):**

```javascript
export default function AiAssistant({ students, attendance, payments, teachers, onOpenStudent })
```

**4. 메시지 렌더 부분에서 MessageBubble에 onOpenStudent 전달 (messages.map 근방):**

```jsx
{messages.map(msg => (
  <MessageBubble key={msg.id} msg={msg} onOpenStudent={onOpenStudent}/>
))}
```

기존 코드에서 `<MessageBubble key={msg.id} msg={msg}/>` → `onOpenStudent={onOpenStudent}` 추가.

**5. send 함수의 에러 처리 개선 (AI-05) — 재시도 1회 포함:**

send 함수 내부의 catch 블록과 aiQuery 호출을 수정:

```javascript
async function send(text) {
  const q = (text || input).trim();
  if (!q || loading) return;
  setInput("");
  setMessages(m => [...m, { id: Date.now() + "u", role: "user", type: "text", content: q }]);
  setLoading(true);
  let churnResult = null;

  // AI-05: 재시도 헬퍼 (429 rate limit 시 1회 재시도)
  async function queryWithRetry(question) {
    try {
      return await aiQuery(question);
    } catch (e) {
      if (e.message === "rate_limited") {
        // 재시도 1회: 3초 대기 후 재시도
        await new Promise(r => setTimeout(r, 3000));
        return await aiQuery(question);  // 두 번째 시도 — 실패 시 에러 전파
      }
      throw e;
    }
  }

  try {
    const res = await queryWithRetry(q);
    // 기존 응답 처리 로직 그대로 유지
    let aiMsg;
    if (res.type === "no_match") {
      aiMsg = { id: Date.now() + "a", role: "assistant", type: "text", content: "아직 그 질문에 답하기 어려워요. 회원·출석·수납 관련 질문을 해보세요." };
    } else if (res.type === "tool") {
      const fn = QUERY_FUNCTIONS[res.tool];
      if (fn) {
        const out = fn({ students, attendance, payments, teachers }, res.args || {});
        if (res.tool === "getChurnRiskStudents") {
          aiMsg = { id: Date.now() + "a", role: "assistant", type: "churn-list", content: out };
          if (out.length > 0) churnResult = out;
        } else {
          const type = res.tool === "getMonthlyStats" ? "stats" : "list";
          aiMsg = { id: Date.now() + "a", role: "assistant", type, content: out };
        }
      } else {
        aiMsg = { id: Date.now() + "a", role: "assistant", type: "text", content: "조회 기능을 찾을 수 없어요." };
      }
    }
    if (aiMsg) setMessages(m => [...m, aiMsg]);
  } catch (e) {
    // AI-05: 에러 유형별 메시지
    const errMsg = e.message === "rate_limited"
      ? "요청이 많아 잠시 후 다시 시도해주세요. (분당 제한)"
      : e.message === "ai_disabled"
      ? "AI 기능이 현재 비활성화되어 있습니다."
      : "오류가 발생했어요. 잠시 후 다시 시도해주세요.";
    setMessages(m => [...m, { id: Date.now() + "e", role: "assistant", type: "text", content: errMsg }]);
  } finally {
    setLoading(false);
  }

  if (churnResult) {
    const pendingId = String(Date.now()) + "p";
    setMessages(m => [...m, { id: pendingId, role: "assistant", type: "churn-pending", content: null }]);
    fetchChurnAnalysis(churnResult, pendingId);
  }
}
```

주의: 기존 send 함수 구조를 유지하되, queryWithRetry 헬퍼를 send 내부 함수로 추가하고
catch 블록의 에러 메시지를 개선하는 두 가지 변경만 적용.
기존 `fetchChurnAnalysis` 비동기 호출 로직은 변경 없음.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onOpenStudent" src/components/ai/AiAssistant.jsx` → 4 이상 (prop 선언, ListResult 전달, MessageBubble prop, 사용부)
    - `grep "queryWithRetry\|재시도\|rate_limited" src/components/ai/AiAssistant.jsx` → 1건 이상
    - `grep "분당 제한" src/components/ai/AiAssistant.jsx` → 1건
    - `npm run build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>
    AiAssistant: onOpenStudent prop 추가 + ListResult 클릭 핸들러 + MessageBubble prop 전달 + send 재시도 로직 + 에러 메시지 개선.
    빌드 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: App.jsx onOpenStudent 연결 + constants.jsx hover 스타일</name>
  <files>
    src/App.jsx,
    src/constants.jsx
  </files>
  <read_first>
    - src/App.jsx — AiAssistant 호출 라인 (line 930 근방) + modal/selected state (line 241-242) + students 배열 확인
    - src/constants.jsx — ".ai-result-row" 클래스가 정의된 위치 (grep으로 먼저 찾을 것)
  </read_first>
  <action>
**1. App.jsx 수정 — AiAssistant에 onOpenStudent prop 전달:**

현재 (line 930):
```jsx
{canManageAll(user.role) && (
  <AiAssistant students={students} attendance={attendance} payments={payments} teachers={teachers} />
)}
```

변경 후:
```jsx
{canManageAll(user.role) && (
  <AiAssistant
    students={students}
    attendance={attendance}
    payments={payments}
    teachers={teachers}
    onOpenStudent={(sid) => {
      const s = students.find(st => st.id === sid);
      if (s) { setSelected(s); setModal("sDetail"); }
    }}
  />
)}
```

설명:
- `setSelected`와 `setModal`은 App.jsx의 기존 state setter (line 241-242)
- `students.find(st => st.id === sid)` — `s`가 loop 변수와 충돌하지 않도록 내부 변수명 `st` 사용
- `if (s)` guard — id가 없는 경우 모달 열지 않음

**2. src/constants.jsx 수정 — ai-result-row hover 스타일 추가:**

먼저 `.ai-result-row` 클래스 정의 위치를 확인:
```
grep -n "ai-result-row" src/constants.jsx
```

해당 위치에서 기존 `.ai-result-row { ... }` 클래스 블록을 찾아 아래와 같이 hover 규칙 추가.
기존 클래스 블록 바로 다음에 삽입 (기존 클래스 내부 수정 최소화):

```css
.ai-result-row { cursor: default; transition: background 0.1s; }
.ai-result-row:hover { background: rgba(43,58,159,0.04); }
```

주의:
- 기존 `.ai-result-row` 정의가 이미 있다면 중복 선언하지 말고 기존 블록에 `transition` 추가 + `:hover` 규칙만 신규 추가
- CSS 문자열 내에서 backtick 템플릿 리터럴로 감싸진 형태 유지 (기존 패턴 준수)
- 외부 CSS 파일 생성 금지 (CLAUDE.md 규칙)
- `cursor: pointer`는 AiAssistant.jsx의 인라인 스타일로 조건부 적용 중이므로, constants.jsx에서는 `cursor: default` (기본값)으로 설정 — onOpenStudent 없는 경우에도 이상 없음
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onOpenStudent" src/App.jsx` → 1 이상
    - `grep "setModal.*sDetail\|sDetail.*setModal" src/App.jsx` → AiAssistant onOpenStudent 내부에 1건 포함
    - `grep "ai-result-row.*hover\|hover.*ai-result-row" src/constants.jsx` → 0건 (hover는 별도 선택자 `.ai-result-row:hover`로 작성)
    - `grep "ai-result-row:hover" src/constants.jsx` → 1건
    - `npm run build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>
    App.jsx: AiAssistant에 onOpenStudent prop 전달. 클릭 시 students에서 찾아 sDetail 모달 오픈.
    constants.jsx: .ai-result-row:hover 스타일 추가.
    빌드 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AiAssistant 카드 클릭 → App.jsx state | 클라이언트 내부 이벤트 — 신뢰 경계 없음 |
| aiQuery 재시도 | 재시도 시 동일 Firebase ID Token 재사용 — 토큰 만료(1시간) 전이면 유효 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | send 함수 queryWithRetry | accept | 재시도 1회 + 3초 대기로 제한. 무한 루프 없음 (두 번째 시도 실패 시 에러 전파) |
| T-03-08 | Information Disclosure | ListResult 카드 클릭 | accept | onOpenStudent는 id만 전달 — students 배열은 이미 클라이언트에 로드됨, 추가 노출 없음 |
</threat_model>

<verification>
Phase 3 전체 완료 조건 (Wave 3 포함):
1. `grep "onOpenStudent" src/App.jsx` → 1건 (AiAssistant prop 전달)
2. `grep "onOpenStudent" src/components/ai/AiAssistant.jsx` → 4건 이상
3. `grep "ai-result-row:hover" src/constants.jsx` → 1건
4. `grep "queryWithRetry\|재시도" src/components/ai/AiAssistant.jsx` → 1건
5. `npm run build` exit code 0

Phase 3 전체 요건 커버리지:
- AI-01: gemini.js + callGemini (Plan 01 Task 1)
- SEC-08: monthly-report.js + churn.js 익명화 (Plan 01 Task 2)
- AI-02: MonthlyReportsView 발송 준비 버튼 (Plan 02 Task 2)
- AI-03: care-message.js + ChurnWidget 버튼 (Plan 02 Task 1 + Task 2)
- AI-04: AiAssistant ListResult 클릭 + onOpenStudent (Plan 03 Task 1 + Task 2)
- AI-05: 재시도 로직 + 에러 메시지 (Plan 03 Task 1)
</verification>

<success_criteria>
- AiAssistant 자연어 쿼리 결과 학생 이름 클릭 시 StudentDetailModal이 열린다
- 학생 카드에 hover 시 배경색 변화가 보인다
- 429 rate limit 에러 시 자동 재시도 1회 후 에러 메시지 표시
- 빌드가 통과한다
</success_criteria>

<output>
완료 후 `.planning/phases/03-ai-completion/03-03-SUMMARY.md` 생성
</output>
