---
phase: 11-discount-system
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/components/student/StudentManagement.jsx
autonomous: true
requirements:
  - DIS-05
  - DIS-08

must_haves:
  truths:
    - "StudentFormModal에 할인 섹션이 표시된다 (수강료 섹션 아래, 메모 섹션 위)"
    - "할인 드롭다운에서 활성 DiscountType을 선택할 수 있다 (첫 항목은 '할인 없음')"
    - "할인 선택 시 시작일·종료일·메모 입력 필드가 나타난다"
    - "다과목(2개 이상) 수강 학생에게만 특정 과목 선택 드롭다운이 표시된다"
    - "저장 시 student.discount 필드가 updateStudentDoc으로 업데이트된다"
    - "수강료 합계 표시가 calcTotalFee().total을 사용한다 (할인 적용 시 할인가 표시)"
  artifacts:
    - path: "src/components/student/StudentManagement.jsx"
      provides: "StudentFormModal 할인 섹션 + calcTotalFee .total 접근"
      contains: "form.discount"
  key_links:
    - from: "StudentFormModal"
      to: "updateStudentDoc"
      via: "onSave 콜백 (student.discount 포함)"
      pattern: "discount"
---

<objective>
StudentFormModal에 학생별 할인 배정 섹션을 추가한다.
calcTotalFee의 두 호출부(라인 132, 234)를 객체 반환에 맞게 업데이트한다.

Purpose: 학생 등록/수정 폼에서 할인 타입을 선택·저장할 수 있어야 한다 (DIS-05).
         calcTotalFee가 이제 객체를 반환하므로 기존 호출부가 NaN이 되는 것을 방지한다 (DIS-08).
Output: StudentFormModal 할인 섹션 UI + .total 접근 수정 완료
</objective>

<execution_context>
@C:/Users/GIGABYTE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/GIGABYTE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/GIGABYTE/Coding/rye-k/.planning/phases/11-discount-system/11-CONTEXT.md
@C:/Users/GIGABYTE/Coding/rye-k/.planning/phases/11-discount-system/11-01-SUMMARY.md

CRITICAL:
- saveStudents([...]) 절대 금지. 학생 discount 필드 저장은 onSave 콜백 → updateStudentDoc 경로로만
- window.confirm / window.alert 절대 금지
- 할인 UI는 canManageAll(currentUser.role)인 경우(admin/manager)에만 표시

Plan 01 결과로 calcTotalFee는 이제 { total, original, discountAmount, discountName } 객체를 반환한다.
- 라인 132: `const totalFee = calcTotalFee(form, feePresets);` → monthlyFee에 저장
  → `const { original: totalFee } = calcTotalFee(form, feePresets, discountTypes);`
  (monthlyFee는 원가(할인 전) 기준으로 저장 — 할인은 student.discount에서 별도 관리)
- 라인 234: `.toLocaleString` 직접 호출 → `.total.toLocaleString` 으로 변경 + 할인 표시

StudentFormModal 위치: StudentManagement.jsx 내부, export function StudentFormModal({ ... }) 정의부
현재 props: { student, teachers, currentUser, categories, feePresets, onClose, onSave }
추가 prop: discountTypes = []

할인 섹션 위치: 수강료 섹션(라인 212~245 근처) 아래, 악기 대여료 섹션 다음, 메모 섹션 전

student.discount 데이터 구조:
{ discountId: string, lessonInstrument?: string, startDate: string, endDate: string|null, appliedBy: string, notes: string }
할인 없음 상태: null
</context>

<tasks>

<task type="auto">
  <name>Task 1: StudentFormModal — discountTypes prop + 할인 배정 섹션 UI 추가</name>
  <files>src/components/student/StudentManagement.jsx</files>

  <read_first>
    - src/components/student/StudentManagement.jsx 라인 55~115 (StudentFormModal 함수 선언 + 초기 form state 설정부)
    - src/components/student/StudentManagement.jsx 라인 156~260 (JSX return 내 수강료 섹션, 악기 대여료 섹션, 메모 섹션)
  </read_first>

  <action>
**[A] StudentFormModal 함수 파라미터에 discountTypes 추가**

현재:
`export function StudentFormModal({ student, teachers, currentUser, categories, feePresets, onClose, onSave })`
변경:
`export function StudentFormModal({ student, teachers, currentUser, categories, feePresets, discountTypes = [], onClose, onSave })`

**[B] form 초기 상태에 discount 필드 추가**

StudentFormModal 내부에서 `useState`로 form을 초기화하는 부분을 찾는다 (보통 `const [form, setForm] = useState({` 라인).
초기 상태 객체에 `discount: student?.discount || null,` 를 추가한다.

**[C] 할인 섹션 UI 추가**

악기 대여료 섹션 (instrumentRental 관련 코드 블록) 다음이자 메모(notes) 섹션 이전에 아래 JSX를 삽입한다.
`canManageAll(currentUser.role)` 조건으로 감싸야 한다:

```jsx
{canManageAll(currentUser.role) && (
  <div className="fg">
    <label className="fg-label">할인 적용</label>
    <select
      className="sel"
      value={form.discount?.discountId || ""}
      onChange={e => {
        const id = e.target.value;
        if (!id) { set("discount", null); return; }
        const today = new Date().toISOString().slice(0, 10);
        set("discount", {
          discountId: id,
          startDate: form.discount?.startDate || today,
          endDate: form.discount?.endDate || null,
          lessonInstrument: form.discount?.lessonInstrument || "",
          appliedBy: currentUser.id,
          notes: form.discount?.notes || "",
        });
      }}
    >
      <option value="">할인 없음</option>
      {(discountTypes.filter(d => d.active !== false)).map(d => (
        <option key={d.id} value={d.id}>
          {d.name} ({d.type === "percent" ? `${d.value}%` : `${Number(d.value).toLocaleString("ko-KR")}원`} 할인)
        </option>
      ))}
    </select>
    {form.discount?.discountId && (
      <>
        <div className="fg-row" style={{ marginTop: 6 }}>
          <div className="fg">
            <label className="fg-label" style={{ fontWeight: 400, fontSize: 11 }}>시작일</label>
            <input
              className="inp"
              type="date"
              value={form.discount.startDate || ""}
              onChange={e => set("discount", { ...form.discount, startDate: e.target.value })}
            />
          </div>
          <div className="fg">
            <label className="fg-label" style={{ fontWeight: 400, fontSize: 11 }}>종료일 (미입력 시 무기한)</label>
            <input
              className="inp"
              type="date"
              value={form.discount.endDate || ""}
              onChange={e => set("discount", { ...form.discount, endDate: e.target.value || null })}
            />
          </div>
        </div>
        {(form.lessons || []).length > 1 && (
          <div className="fg" style={{ marginTop: 6 }}>
            <label className="fg-label" style={{ fontWeight: 400, fontSize: 11 }}>특정 과목만 적용 (미선택 시 전 과목)</label>
            <select
              className="sel"
              value={form.discount.lessonInstrument || ""}
              onChange={e => set("discount", { ...form.discount, lessonInstrument: e.target.value })}
            >
              <option value="">전 과목 적용</option>
              {(form.lessons || []).map(l => (
                <option key={l.instrument} value={l.instrument}>{l.instrument}</option>
              ))}
            </select>
          </div>
        )}
        <div className="fg" style={{ marginTop: 6 }}>
          <label className="fg-label" style={{ fontWeight: 400, fontSize: 11 }}>메모</label>
          <input
            className="inp"
            value={form.discount.notes || ""}
            onChange={e => set("discount", { ...form.discount, notes: e.target.value })}
            placeholder="예: 지인 소개 — 박지연님"
          />
        </div>
      </>
    )}
  </div>
)}
```
  </action>

  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "discountTypes = \[\]" src/components/student/StudentManagement.jsx 결과 >= 1 (props 기본값)
    - grep -c "form.discount" src/components/student/StudentManagement.jsx 결과 >= 3
    - grep -c "할인 없음" src/components/student/StudentManagement.jsx 결과 >= 1
    - grep -c "lessonInstrument" src/components/student/StudentManagement.jsx 결과 >= 1
    - npm run build 통과
  </acceptance_criteria>

  <done>
    StudentFormModal에 discountTypes prop이 추가되고, 할인 배정 섹션(드롭다운 + 날짜 + 과목 선택 + 메모)이 표시된다.
    저장 시 form.discount가 onSave(...form) 스프레드를 통해 updateStudentDoc으로 전달된다.
  </done>
</task>

<task type="auto">
  <name>Task 2: StudentManagement.jsx — calcTotalFee 호출부 .total 업데이트 (DIS-08)</name>
  <files>src/components/student/StudentManagement.jsx</files>

  <read_first>
    - src/components/student/StudentManagement.jsx 라인 128~148 (handleConfirm, totalFee 사용부)
    - src/components/student/StudentManagement.jsx 라인 230~238 (수강료 합계 표시 JSX)
  </read_first>

  <action>
**[A] 라인 132 수정 — monthlyFee 저장 시 원가(할인 전) 사용**

현재:
```
const totalFee = calcTotalFee(form, feePresets);
```
변경:
```
const { original: totalFee } = calcTotalFee(form, feePresets, discountTypes);
```
이유: monthlyFee는 원가(할인 전)를 저장한다. 할인은 student.discount에서 별도 관리하므로
     discounted total을 monthlyFee에 저장하면 추후 할인 제거 시 원가가 사라지는 문제 발생.

**[B] 라인 234 수정 — 수강료 합계 표시: 할인 적용 시 취소선 + 할인가 표시**

현재:
```
<span>{calcTotalFee(form, feePresets).toLocaleString("ko-KR")}원</span>
```
변경:
```
{(() => {
  const fee = calcTotalFee(form, feePresets, discountTypes);
  if (fee.discountAmount > 0) {
    return (
      <span>
        <span style={{ textDecoration: "line-through", color: "var(--ink-30)", fontSize: 12, marginRight: 4 }}>
          {fee.original.toLocaleString("ko-KR")}원
        </span>
        {fee.total.toLocaleString("ko-KR")}원
        <span style={{ marginLeft: 6, fontSize: 10, background: "var(--blue-lt,#EFF6FF)", color: "var(--blue,#3B82F6)", borderRadius: 99, padding: "1px 6px", fontWeight: 600 }}>
          {fee.discountName}
        </span>
      </span>
    );
  }
  return <span>{fee.total.toLocaleString("ko-KR")}원</span>;
})()}
```
  </action>

  <verify>
    <automated>cd C:/Users/GIGABYTE/Coding/rye-k && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "original: totalFee" src/components/student/StudentManagement.jsx 결과 >= 1
    - grep -c "fee.total.toLocaleString" src/components/student/StudentManagement.jsx 결과 >= 1
    - grep -c "fee.discountAmount > 0" src/components/student/StudentManagement.jsx 결과 >= 1
    - grep -c "calcTotalFee(form, feePresets)" src/components/student/StudentManagement.jsx 결과 == 0
      (이전 패턴이 완전히 제거됨 — 모두 discountTypes 3번째 인수 포함으로 교체됨)
    - npm run build 통과
  </acceptance_criteria>

  <done>
    StudentManagement.jsx의 calcTotalFee 두 호출부가 모두 업데이트되었다.
    monthlyFee에는 원가(original)가 저장된다.
    수강료 합계 표시는 할인 적용 시 원가 취소선 + 할인가 + 할인명 뱃지를 보여준다.
    npm run build 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StudentFormModal form → onSave | 할인 정보 포함 student 객체가 updateStudentDoc으로 전달됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | student.discount 저장 | mitigate | canManageAll 조건부 렌더링으로 teacher 역할에서 할인 UI 숨김 |
| T-11-04 | Elevation of Privilege | discountTypes 필터링 | accept | active !== false 필터는 UI 표시용 — 저장 단계에서 검증은 Firestore Rules로 처리 |
</threat_model>

<verification>
```bash
cd C:/Users/GIGABYTE/Coding/rye-k

# props 추가 확인
grep -n "discountTypes" src/components/student/StudentManagement.jsx

# 할인 섹션 UI 확인
grep -n "할인 없음" src/components/student/StudentManagement.jsx

# calcTotalFee 호출부 업데이트 확인
grep -n "calcTotalFee" src/components/student/StudentManagement.jsx

# 빌드
npm run build 2>&1 | tail -3
```
</verification>

<success_criteria>
- StudentFormModal에 discountTypes prop이 추가됨
- 할인 섹션 UI가 canManageAll 조건 내에 렌더링됨
- calcTotalFee 두 호출부가 모두 .total/.original 접근으로 업데이트됨
- npm run build 통과
</success_criteria>

<output>
완료 후 `.planning/phases/11-discount-system/11-02-SUMMARY.md` 생성:
- 추가된 props 목록
- 할인 섹션 JSX 삽입 위치 (라인 번호)
- calcTotalFee 호출부 변경 내역 (라인 번호)
</output>
