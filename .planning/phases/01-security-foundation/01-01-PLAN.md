---
phase: 01-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aiClient.js
  - src/App.jsx
  - src/utils.js
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "프로덕션 빌드의 DevTools Console에서 Firebase UID, 토큰 길이, 알림톡 발송 대상 전화번호가 출력되지 않는다"
    - "resetSeed() 함수 본체가 import.meta.env.DEV guard로 감싸져 있어 프로덕션 빌드에서 generateSeedData()가 실행되지 않는다"
    - "src/ 전체에서 saveStudents([...]) 를 직접 호출하는 활성 코드 경로가 없다 (throw-guard 단 한 줄만 존재)"
  artifacts:
    - path: "src/aiClient.js"
      provides: "console.log 제거된 AI 토큰 취득 함수"
      contains: "async function getToken"
    - path: "src/App.jsx"
      provides: "DEV-guard된 resetSeed + console.log 없는 마이그레이션 코드"
      contains: "import.meta.env.DEV"
    - path: "src/utils.js"
      provides: "알림톡 목업 로그 제거된 sendAligoMessage"
      contains: "sendAligoMessage"
  key_links:
    - from: "src/App.jsx"
      to: "import.meta.env.DEV"
      via: "if (import.meta.env.DEV) { resetSeed = ... }"
      pattern: "import\\.meta\\.env\\.DEV"
---

<objective>
콘솔 PII 로그 제거(SEC-01), 샘플 데이터 초기화 함수를 개발 전용으로 제한(SEC-02), saveStudents 잔여 참조 감사(SEC-03)를 수행한다.

Purpose: 프로덕션 빌드에서 Firebase UID, 토큰 정보, 전화번호가 브라우저 DevTools에 노출되지 않도록 하고, resetSeed / generateSeedData가 프로덕션 번들에서 실행 불가능하게 만든다. SEC-03은 이미 throw-guard가 적용된 상태이므로 grep 확인 후 완료 처리한다.

Output: 수정된 src/aiClient.js, src/App.jsx, src/utils.js + npm run build 통과
</objective>

<execution_context>
@C:\Users\GIGABYTE\Coding\rye-k\CLAUDE.md
</execution_context>

<context>
@C:\Users\GIGABYTE\Coding\rye-k\.planning\ROADMAP.md
@C:\Users\GIGABYTE\Coding\rye-k\.planning\REQUIREMENTS.md

<interfaces>
<!-- src/aiClient.js 현재 getToken() 구조 — 제거 대상 라인 표시 -->
<!-- Line 10: console.log("[ai] currentUser:", user?.uid, user?.isAnonymous);   ← REMOVE -->
<!-- Line 14: console.log("[ai] anon signin:", user?.uid);                       ← REMOVE -->
<!-- Line 20: console.warn("[ai] no user after anon"); return null;              ← REMOVE warn, keep return null -->
<!-- Line 23: console.log("[ai] token len:", t?.length);                         ← REMOVE -->
<!-- console.error 호출 2개(lines 16, 26)는 유지 — 실제 오류 경로 -->

<!-- src/App.jsx 제거 대상 라인 -->
<!-- Line 367: console.log("Migrated studentCodes for", ...)                     ← REMOVE -->
<!-- Line 381: console.log(`[Recovery] ${final.length}명 복구 완료 ...`)         ← REMOVE -->
<!-- Line 779: console.warn("Firebase Auth failed, proceeding with local auth only") ← REMOVE -->

<!-- src/App.jsx resetSeed 현재 구조 (line 684~715) -->
<!-- const resetSeed = async () => { ... };  →  DEV guard로 감싸야 함 -->

<!-- src/utils.js 제거 대상 -->
<!-- Line 118: console.log(`[알림톡 목업] 발송 대상(${targetType}): ${targets.length}명`, targets); ← REMOVE -->

<!-- 유지할 console 호출 -->
<!-- firebase.js의 console.error (Firebase SDK 오류) -->
<!-- CommonUI.jsx MicButton의 console.warn (하드웨어 오류) -->
<!-- functions/api/ai/_utils/anthropic.js의 console.warn (MAX_TOKENS) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: aiClient.js — console.log 4개 제거</name>
  <files>src/aiClient.js</files>
  <action>
src/aiClient.js의 getToken() 함수에서 아래 4개 라인을 정확히 제거한다. 함수 로직(return, throw, catch)은 건드리지 않는다.

제거 대상:
- Line 10: `console.log("[ai] currentUser:", user?.uid, user?.isAnonymous);`
- Line 14: `console.log("[ai] anon signin:", user?.uid);`
- Line 20: `console.warn("[ai] no user after anon");` — 이 줄에서 warn만 제거하고 `return null;`은 유지. 즉 `if (!user) { console.warn("[ai] no user after anon"); return null; }` 를 `if (!user) return null;` 로 교체.
- Line 23: `console.log("[ai] token len:", t?.length);`

유지 대상 (절대 삭제 금지):
- Line 16: `console.error("[ai] anon failed:", e);`
- Line 26: `console.error("[ai] getIdToken failed:", e);`
  </action>
  <verify>
    <automated>grep -n "console.log\|console.warn" src/aiClient.js</automated>
  </verify>
  <done>grep 결과가 0줄 — aiClient.js에 console.log 또는 console.warn 없음. console.error는 2개 존재해야 정상.</done>
</task>

<task type="auto">
  <name>Task 2: App.jsx — console.log 제거 + resetSeed DEV guard 적용 + SEC-03 확인</name>
  <files>src/App.jsx</files>
  <action>
**Part A: console 제거 (3곳)**

1. Line 367 — 마이그레이션 완료 로그 제거:
   ```
   console.log("Migrated studentCodes for", migrated.filter((s,i) => s.studentCode !== studentsArr[i]?.studentCode).length, "students");
   ```
   → 해당 줄 전체 삭제

2. Line 381 — 복구 완료 로그 제거:
   ```
   console.log(`[Recovery] ${final.length}명 복구 완료 (기존 ${curStudents.length}명 보존)`);
   ```
   → 해당 줄 전체 삭제

3. Line 779 — Firebase Auth 실패 warn 제거:
   ```
   console.warn("Firebase Auth failed, proceeding with local auth only");
   ```
   → 해당 줄 전체 삭제. `if (!fbUser) { ... }` 블록이 빈 블록이 되면 블록 자체도 제거한다.

**Part B: resetSeed DEV guard (SEC-02)**

현재 코드 (line 684):
```javascript
const resetSeed = async () => {
  const seed = generateSeedData();
  // ... 전체 Firestore 초기화 ...
  showToast("DB 초기화 완료 — 실제 운영 데이터가 로드되었습니다.");
  setView("dashboard");
};
```

아래와 같이 DEV guard로 전체 resetSeed 함수 본체를 감싼다:
```javascript
let resetSeed;
if (import.meta.env.DEV) {
  resetSeed = async () => {
    const seed = generateSeedData();
    await Promise.all([
      sSet("rye-teachers",          seed.seedTeachers),
      sSet("rye-students",          seed.seedStudents),
      sSet("rye-notices",           seed.seedNotices),
      sSet("rye-attendance",        seed.seedAttendance),
      sSet("rye-payments",          seed.seedPayments),
      sSet("rye-activity",          seed.seedActivity),
      sSet("rye-pending",           []),
      sSet("rye-trash",             []),
      sSet("rye-schedule-overrides",[]),
      sSet("rye-student-notices",   []),
      sSet("rye-fee-presets",       {}),
      sSet("rye-institutions",      []),
    ]);
    setTeachers(seed.seedTeachers);
    setStudents(seed.seedStudents);
    setNotices(seed.seedNotices);
    setAttendance(seed.seedAttendance);
    setPayments(seed.seedPayments);
    setActivity(seed.seedActivity);
    setPending([]);
    setTrash([]);
    setScheduleOverrides([]);
    setStudentNotices([]);
    setFeePresets({});
    setInstitutions([]);
    showToast("DB 초기화 완료 — 실제 운영 데이터가 로드되었습니다.");
    setView("dashboard");
  };
} else {
  resetSeed = () => { throw new Error("resetSeed은 개발 환경 전용입니다."); };
}
```

중요: `generateSeedData()` 는 App.jsx line 34에 정의된 함수이다. generateSeedData 자체는 이 plan에서 별도 파일로 이동하지 않는다 — resetSeed 함수 본체만 DEV guard한다. (generateSeedData의 시작 코드인 checkAllLoaded의 seed 경로(line 344)와 recovery 경로(line 373)는 그대로 유지한다. 이 두 경로는 운영 DB 초기 시드 및 1회 복구 목적으로, DEV guard 대상이 아니다.)

**Part C: SEC-03 감사 확인**

saveStudents 그렙 결과를 확인한다. src/ 내에 saveStudents 참조가 throw-guard 정의 한 줄만 남아 있어야 한다:
```
src/App.jsx:  const saveStudents = () => { throw new Error(...) };
```
다른 활성 호출 경로가 없으면 SEC-03 완료 처리 (코드 변경 불필요).
  </action>
  <verify>
    <automated>grep -n "console.log\|console.warn" src/App.jsx</automated>
  </verify>
  <done>
1. grep 결과 0줄 — App.jsx에 console.log / console.warn 없음 (console.error는 유지)
2. `grep -n "import.meta.env.DEV" src/App.jsx` 결과에 resetSeed 관련 DEV guard가 포함됨
3. `npm run build` 통과
4. `grep -n "saveStudents" src/App.jsx` 결과가 throw-guard 정의 1줄만 출력됨
  </done>
</task>

<task type="auto">
  <name>Task 3: utils.js — 알림톡 목업 console.log 제거</name>
  <files>src/utils.js</files>
  <action>
src/utils.js의 sendAligoMessage 함수(line 116~120)에서 console.log 1줄을 제거한다.

현재:
```javascript
export async function sendAligoMessage(targetType, students) {
  const targets = students.map(s => s.parentPhone || s.phone).filter(Boolean);
  console.log(`[알림톡 목업] 발송 대상(${targetType}): ${targets.length}명`, targets);
  await new Promise(resolve => setTimeout(resolve, 1000));
}
```

변경 후:
```javascript
export async function sendAligoMessage(targetType, students) {
  const targets = students.map(s => s.parentPhone || s.phone).filter(Boolean);
  await new Promise(resolve => setTimeout(resolve, 1000));
}
```

`targets` 변수는 함수 내부에서 더 이상 사용되지 않으므로 해당 줄도 함께 제거한다:
```javascript
export async function sendAligoMessage(_targetType, _students) {
  await new Promise(resolve => setTimeout(resolve, 1000));
}
```

단, targetType과 students 매개변수는 Phase 4에서 AlimTalk 연동 시 사용될 예정이므로 파라미터 시그니처는 유지하되 미사용 lint 경고를 피하기 위해 `_` 접두사로 명명한다.
  </action>
  <verify>
    <automated>grep -n "console.log\|console.warn" src/utils.js</automated>
  </verify>
  <done>grep 결과 0줄 — utils.js에 console.log / console.warn 없음. npm run build 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser bundle → DevTools | 프로덕션 JS 번들에 포함된 console 호출이 브라우저 DevTools에서 PII 노출 |
| src/App.jsx → Firestore | resetSeed()가 프로덕션에서 호출될 경우 운영 DB 전체 덮어씌움 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | aiClient.js getToken() | mitigate | console.log 제거: uid, isAnonymous, token length 노출 차단 |
| T-01-02 | Information Disclosure | App.jsx 마이그레이션 로그 | mitigate | console.log 2개 제거: studentCode 마이그레이션, Recovery 로그 |
| T-01-03 | Information Disclosure | utils.js sendAligoMessage | mitigate | console.log 제거: 전화번호 배열 노출 차단 |
| T-01-04 | Tampering | App.jsx resetSeed() | mitigate | import.meta.env.DEV guard: 프로덕션 번들에서 resetSeed 실행 불가 |
| T-01-05 | Tampering | src/ saveStudents() | accept | 이미 throw-guard 적용됨 — 코드 변경 불필요, 감사로 확인 |
</threat_model>

<verification>
이 plan의 모든 변경이 완료된 후 아래를 순서대로 실행한다:

1. `grep -rn "console.log\|console.warn" src/aiClient.js src/App.jsx src/utils.js`
   → 결과: 0줄 (console.error는 별도 파일에 존재 — 정상)

2. `grep -n "import.meta.env.DEV" src/App.jsx`
   → 결과: resetSeed guard가 포함된 1~2줄 출력

3. `grep -n "saveStudents" src/App.jsx`
   → 결과: throw-guard 정의 1줄만 출력, 다른 호출 없음

4. `npm run build`
   → 빌드 통과. 오류 없음.
</verification>

<success_criteria>
- aiClient.js: console.log 0개, console.error 2개 (anon failed, getIdToken failed)
- App.jsx: console.log 0개, console.warn 0개
- App.jsx: `import.meta.env.DEV` guard가 resetSeed 함수를 감싸고 있음
- utils.js: console.log 0개
- src/ 전체에서 saveStudents 직접 호출 없음 (throw-guard 정의만 존재)
- `npm run build` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/01-security-foundation/01-01-SUMMARY.md` 생성
</output>
